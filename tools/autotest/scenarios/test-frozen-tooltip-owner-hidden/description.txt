ASSERTS that when a building is captured while its owner cannot see it, the owner's frozen ghost stops short of naming the captor -- while still treating the building as hostile. Explored Map OFF + Fog ON (both locked). THE VIEWER IS THE OLD OWNER, which is the whole difference from test-frozen-owner-snapshot: that scenario deliberately makes its viewer a third party, so FrozenUnderFog.OnOwnerChanged's body (which touches frozenStates[oldOwnerIndex] and nobody else's) never runs for the player it asserts on. Until this scenario existed, that branch had never been executed under assertion anywhere in the suite -- the case the fix is FOR had never been observed. NO SCOUT IS NEEDED here and that is not an oversight: Box belongs to USA from tick 0 and ^BasicBuilding mounts its own Vision ladder (structures.yaml:14-23, strength 3 out to 1c0), so a building reveals its own footprint to its owner permanently, USA's ghost reads 'live' with no help, and the CAPTURE is what removes that sight. The transition under test is produced by the event under test rather than staged around it, and the explored bit at 8,16 is guaranteed set because USA held real vision there every tick up to the capture. THE OWNERSHIP CHANGE IS A LUA ASSIGNMENT ON PURPOSE, NOT FOR CONVENIENCE: Box.Owner = russia routes through Actor.ChangeOwnerSync (GeneralProperties.cs:63, Actor.cs:577-592), which removes and re-adds the actor and therefore rebuilds every player's vision sources. An engineer capture does NOT -- CaptureActor.cs:140-143 branches on BuildingInfo to ChangeOwnerInPlaceSync to skip exactly that cascade, and ProximityCapturable.cs:222-227 branches the same way. SUPERSEDED 2026-09-02, AND THE CORRECTION INVERTS WHAT THIS PARAGRAPH ORIGINALLY CONCLUDED. When this file was written, AffectsMapLayer (Vision's base) implemented no INotifyOwnerChanged, so a captured building's vision never left its previous owner, and this paragraph went on to say the scenario was a faithful model of the CODE PATH but not of an engineer capture as the game behaves -- because the old owner kept seeing the cell and their ghost never froze at all -- and warned against swapping in a real capture. That bug was fixed hours later at 49afe9e9, merged at 0d862eeb, and gated RED-then-GREEN by test-capture-vision-handover; the [HIGH] entry in WORKSPACE/bugs/discovered.md is closed. Vision now follows the building on the in-place path too, so the old owner DOES lose sight of the cell on a real capture, their ghost DOES freeze, and the leak this scenario asserts against is reachable through an engineer exactly as it is through the Lua assignment. The Lua assignment remains deliberate for a different and still-valid reason: ChangeOwnerSync and ChangeOwnerInPlaceSync are separate code paths, and this scenario pins the one FrozenUnderFog.OnOwnerChanged actually runs on. But the old warning was advice about a bug that no longer exists. THREE ARMS, NOT ONE, because there were two candidate fixes and a one-field assertion cannot tell them apart: TooltipOwner must read USA (the captor is not named), FrozenActor.Owner must read Russia (the capture IS reflected where relationship predicates read it), and Test.FrozenClickCursor must still resolve a non-empty cursor for a selected USA Abrams (a real consumer still resolves through that Owner). The rejected fix froze Owner, which hides the captor too but makes an enemy ghost read as friendly to AutoTarget, cursors, weapon validity and bot perception, because Player.RelationshipWith(null) returns Ally (Player.cs:254-255). Drop any one arm and the scenario passes on the rejected fix as happily as on the shipped one. NOT ASSERTED: that the capture is hidden -- it is not and cannot be, since USA's units must keep treating the building as an enemy, and only the captor's identity is separable. Also not asserted: SightingIntelOverlay, a second identity channel that rebuilds the GPS-dot palette from fa.Owner.InternalName every frame, left standing on purpose as a separate decidable item. NEEDS A NEW LUA BINDING, Test.FrozenActorTooltipOwner, added alongside the existing Test.FrozenActorOwner -- the two are meant to disagree after an unwitnessed capture and reading both in one assertion is the mechanism above. Does not need Test.KeepRenderPlayer=true: every binding used reads the viewer's own MapLayers or frozen layer, never world.RenderPlayer. RUN HISTORY: written 2026-09-02 and handed up UNRUN -- neither the RED nor the GREEN has been observed. RED-ARM.md in this directory carries the one-token sabotage (pass refreshTooltipOwner: true in FrozenUnderFog.OnOwnerChanged, which is exactly the pre-fix behaviour), the exact failure text to require, and the argument that no setup control can read the sabotaged field so the RED must land on verdict arm 1. Run the RED before trusting the green. Single run: ./tools/autotest/run-test.sh test-frozen-tooltip-owner-hidden
