ASSERTS that a helicopter held only on radar can be right-clicked. Two airborne Halos over fogged, explored ground: one 45.7c from a Stryker SHORAD whose radar is widened to 56c0 (inside radar, 13.7c outside the 32c vision edge), one 65.4c away (outside both). Expects: radar contact detected AND mouse-targetable; dark contact neither. Asserts Test.IsMouseTargetable, the predicate the right-click actually runs -- NOT Test.IsDetectedBy, which returns true for a radar contact whether or not the bug is present, and NOT a Lua Attack order, which bypasses UnitOrderGenerator entirely and fires on the broken build. Both would be false greens. The dark helicopter is load-bearing: the positive rung also passes if everything became targetable, and it catches a nil RenderPlayer. Cell visibility is asserted <= 1 on both first, so a run where vision reached a helicopter reports as SETUP FAULT rather than as a verdict. The end-to-end proof -- a human seeing the attack cursor -- is not automatable and remains a PLAYTEST. Single run: ./tools/autotest/run-test.sh test-radar-only-targetable
