Stage-3 stationary ambush, DETECTION spring (trigger 1, OBS-D coverage): an opted-in AT specialist holds fire on an UNSEEN enemy, then springs the instant that enemy's vision reaches it (becomes detected). Score triggers are disabled (unreachable thresholds), the detector is HoldFire (no damage), and it stops short of overrun — so only detection can spring it. RED = comment the detector-advance line (see the Lua header).
