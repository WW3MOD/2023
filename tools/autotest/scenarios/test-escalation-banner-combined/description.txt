Auto: an Escalation match opens at DEFCON 3 on a 150-tick clock; when the border opens the abrams is clicked onto an 8000-hp t90 and kills it with the first round, ~20 ticks later. Both edges land inside the 66-tick banner hold, so the player must get ONE banner naming both, plus a first-casualty record naming USA's abrams as who fired. Its twin test-escalation-banner-separate asserts the other branch and also PASSES.
