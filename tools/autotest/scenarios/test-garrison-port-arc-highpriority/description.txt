A soldier at a garrison firing port may only be shot from inside that port's arc. Asserts the gate holds for MT (mortar) infantry, which defeated it until 2026-09-15 by carrying an ungated Targetable@HighPriority alongside GarrisonPortOccupant - Actor.IsTargetableBy ORs across targetables, so the permissive one won and the arc trait was never consulted. Controls: a plain rifleman in the same geometry (never affected), and an MT in the open (must stay targetable).
