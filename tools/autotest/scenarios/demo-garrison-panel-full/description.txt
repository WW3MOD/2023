Two captures of GARRISON_PANEL, one per fix. Frame garrison-panel-shelter-overflow: a civilian house holding all ten men in its shelter at once, which is the state four declared reserve rows cannot show — the last row must read "[S] +7 more (10/10 in shelter)" instead of naming a fourth man. Frame garrison-panel-narrow-gap: a PBOX, which declares two firing ports against the chrome's eight port rows and four shelter slots — its shelter rows must sit directly under its second port row rather than at the eight-port position with six rows of dead space between. No verdict; this is a demo.
