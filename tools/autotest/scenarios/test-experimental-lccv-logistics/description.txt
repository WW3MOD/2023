experimental LCCV -> forward Logistics Center: asserts the @experimental bot's demand gate buys an LCCV and LogisticsCenterBotModule sites it FORWARD of the Supply Route (>= 8 cells) before transforming. Two-part pass — a Centre deployed at the SR is a fail, since cutting the resupply round-trip is the whole point. Emits [logistics] request/move-to-site/deploy-refused for diagnosis.
