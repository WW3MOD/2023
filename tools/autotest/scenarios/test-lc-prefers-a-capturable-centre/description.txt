LC capture-over-purchase: with a NEUTRAL logisticscenter 14 cells from the @experimental bot's Supply Route (inside CaptureConsiderCells 40), and a genuine forward platoon ~25 cells out in contact so the demand model WOULD otherwise say buy, the bot must never field an lccv within 3600 ticks. Cash is 25000 so affordability cannot be what refuses. A second neutral Centre sits at 46 cells (outside the bound) to reproduce the shipped maps' one-per-side pairing and guard the distance bound. Capture COMPLETING is watched and reported as a NOTE, not asserted — that is CaptureCoordinatorBotModule's job, not this change's. Pins user ruling 2026-09-03 ("there are already a neutral LC on each side, so the bots should instead capture the exisiting one"). VERIFY ON THE RUN: debug.log must show [logistics] refuse-buy with capturable=1 and non-zero fwd-value, otherwise the gate was never tempted and the pass is not evidence.
