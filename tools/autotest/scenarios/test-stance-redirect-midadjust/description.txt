Residual B1 (mid-adjust redirect): the executor starts relocating a human AR to zone-A cover, and WHILE that adjustment move is in flight (State == Adjusting) the player redirects the AR ~9 cells east. The fixed ITick catches the mid-adjust redirect once the unit leaves the leash+margin band, clears the cohesion slot, and re-anchors at the redirect target — so the AR never gets dragged back to the abandoned cover cell. On the pre-fix build the Adjusting-time guard skips, CohesionSlotMemory walks the AR back to the old cover cell once, and the test fails (AR comes within 6 cells of the zone-A cover cell). Short redirect keeps the trip inside ForgetAfterTicks so the slot stays fresh (the non-self-healing window).
