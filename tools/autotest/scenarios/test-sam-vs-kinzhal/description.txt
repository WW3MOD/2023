Auto: Russia fires the Kinzhal support power at an Abrams 61 cells away, with a USA SAM site sited ON the flight line at mid-map — the defence's absolute ceiling on this playfield, since the missile is then inside its 35c0 ring for the whole ~29-tick flight against a 20-28 tick acquire-aim-fire chain. Asserts the BAND the user asked for, which has two opposite failure modes: the SAM must actually launch an interceptor (zero launches FAILS; silence reads as a broken defence rather than an outmatched one) AND the Kinzhal must still reach its aim point and kill the Abrams (a kill FAILS). Launch is observed via a GrantConditionOnAttack probe, because interceptors are projectiles and never appear in any Lua actor query. The 1-9 tick firing margin is narrow by construction and is itself the result.
