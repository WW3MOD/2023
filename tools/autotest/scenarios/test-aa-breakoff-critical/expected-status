skip
MEASUREMENT HARNESS, no graded answer: every Test.Fail in the file is a STAGING fault, not a
finding. The four are SETUP INVALID (:210), player not found (:221), AA actor missing (:230)
and could not spawn halo (:240). The terminal verdict carries the per-lane report for the
critical-damage three-way split and asserts nothing about it -- the split this scenario is
named for could invert and the run would still be green.

THE STAGING FAILURES STAY FAILURES. "The lanes could not be built" is a genuine red and must
remain one; only the terminal verdict moved from Test.Pass to Test.Skip (2026-09-01). If this
scenario reports FAIL against this declaration, the staging broke -- read the note, do not
delete this file. Its staging is unusually fragile by design: it pins a helicopter at 15% HP
and had to delete -HeliEmergencyLanding to hold it there, and conventions.md:394 records that
the underlying drain is universal rather than a helicopter quirk.

WHY SKIP AND NOT PASS. `Test.Skip` is the harness's own word for "ran, produced no graded
answer" (TestGlobal.cs) and run-batch.sh maps it to the SKIP bucket. The scenario is not
hidden and still runs; the verdict just stops claiming an answer it never computed.

REMOVE THIS FILE in the same commit as whatever bounds the split. test-aa-battery-volleys
:268-297 is the worked example of how a sibling AA scenario turned a measurement into a rung.

NOT EXPOSED TO expected-status.sh's KNOWN GAP. That gap is specific to `fail` declarations,
where a watchdog TIMEOUT-FAIL also exits 1 and therefore grades green. A timeout reaches
expected_status_grade as FAIL, and declared-`skip` + FAIL is RED.
