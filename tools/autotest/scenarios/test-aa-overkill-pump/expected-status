skip
MEASUREMENT HARNESS, no graded answer, and it is the sharpest instance in the suite. The file
computes the decisive quantity outright at :169 --

    local suppressedThroughPump = (obs == nil) or (obs > PumpStopTick)

-- concatenates it into the summary string at :177 as "Y" or "N", and then reaches its
terminal verdict at :186 WHICHEVER WAY IT CAME OUT. It measures the exact thing it exists to
test and never asserts on it: a regression flipping Y to N changed one character in a pass
note. It is one `if` away from being a guard, and that `if` has never been written because
the bound is a judgement call nobody has owned.

Every Test.Fail in the file is a STAGING fault, not a finding: SETUP INVALID (:182), player
not found (:197), lane R actor missing (:203), lane S actors missing (:210), could not spawn
aircraft (:227).

ITS FINDING IS ALREADY GUARDED ELSEWHERE. test-aa-battery-volleys:288-297 asserts the
serialisation this pair only measures, control battery and all. So the mechanic is not
unguarded -- this scenario is the un-bounded instrument, not the guard.

THE STAGING FAILURES STAY FAILURES. "The lanes could not be built" is a genuine red and must
remain one; only the terminal verdict moved from Test.Pass to Test.Skip (2026-09-01). If this
scenario reports FAIL against this declaration, the staging broke -- read the note, do not
delete this file.

WHY SKIP AND NOT PASS. `Test.Skip` is the harness's own word for "ran, produced no graded
answer" (TestGlobal.cs) and run-batch.sh maps it to the SKIP bucket. The scenario is not
hidden and still runs; the verdict just stops claiming an answer it never computed.

REMOVE THIS FILE in the same commit as whatever wraps :169 in an `if` -- that is the highest-
value single edit available in this folder, and a bound set from one run is how flaky tests
are born, so take more than one measurement first.

NOT EXPOSED TO expected-status.sh's KNOWN GAP. That gap is specific to `fail` declarations,
where a watchdog TIMEOUT-FAIL also exits 1 and therefore grades green. A timeout reaches
expected_status_grade as FAIL, and declared-`skip` + FAIL is RED.
