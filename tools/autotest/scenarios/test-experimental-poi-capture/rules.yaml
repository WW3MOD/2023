# Focused capture test. Low cash so the experimental bot produces little — the pre-placed
# TECN + 2 escorts are essentially the whole USA force, isolating capture
# execution from any army-pool interference.

Player:
	PlayerResources:
		DefaultCash: 1000

World:
	-StartGameNotification:
	MusicPlaylist:
		DisableWorldSounds: false
	LuaScript:
		Scripts: test-helpers.lua, test-experimental-poi-capture.lua
