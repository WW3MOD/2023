Auto: proves releasing a banked reservation returns the WHOLE price and stops the bill. Buys a Kinzhal reservation, hands it back through the same order a right-click on its bin icon issues, and asserts refunded == spent exactly, the upkeep entry cleared by name, the proxy left the world, the icon left the bin, and a re-fire attempt is refused. This is NOT the production queue's cancel: it happens entirely after EndProduction, where the queue has no record of the item.
