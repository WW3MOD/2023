# The shell this scenario shells the wood with. Hand-built rather than borrowed from a shipped
# unit, for two reasons that both come down to "the fixture must not drift":
#
#   1. ONLY INCENDIARY AND THERMOBARIC WEAPONS CAN DAMAGE A TREE, and all of them are actively
#      balance-tuned. Warhead.ValidTargets defaults to `Ground, Water` (Warhead.cs:30) and ^Tree is
#      Targetable ONLY as `Trees`, so a warhead that does not name `Trees` cannot touch one.
#      Weapon-level ValidTargets gates AIMING, not impact: ^ArtilleryRound declares
#      `Ground, Trees, Water` at weapon level (weapons-ballistics.yaml:874) so a Paladin aims at a
#      tree quite happily, while neither of its damage warheads names Trees -- the shell lands and
#      does nothing. Enumerated across mods/ww3mod/rules/weapons, the weapons with a Trees-valid
#      DAMAGE warhead are exactly: the four armaments TosRockets (vehicles-russia.yaml:747),
#      FireballLauncher + Flamespray.heavy (structures-defenses.yaml:944, :950) and Flamespray
#      (infantry.yaml:2075), plus the Explodes payloads Iskander/HIMARS/Napalm/CrateNuke/MiniNuke/
#      Atomic/VolatileLoad1-8. Flame and thermobaric, which is exactly why burnt trees exist.
#      Borrowing any of them ties this scenario's arithmetic to the next balance pass on that
#      weapon -- and TosRockets in particular already has two scenarios devoted to it being retuned.
#   2. THE NUMBER HAS TO BE ARITHMETIC, not "enough". The scenario reports total damage delivered at
#      the guarded tree's cell as a multiple of the tree's 2500 HP, and that only works if damage
#      per shell is a known constant.
#
# EVERYTHING BELOW IS CHOSEN TO MAKE PER-SHELL DAMAGE EXACTLY 12000 AT ALL THREE TREE CELLS:
#   Falloff `100, 100` over Spread 2c0 interpolates between two 100s, so every point within 2048
#   units of the impact takes the full 12000 -- and all three trees sit at edge-distance 0 or 512.
#   That is what lets the sponge tree's HP loss be divided by 12000 to get an exact shell count.
#   Beyond 2c0 it interpolates 100 -> 0 and is zero past 4c0, so the Abrams 7 cells south takes none.
#   Inaccuracy is unset (0) so the impact point is the cell centre every time, not a scatter.
#   Blockable: false so ^Tree's own BlocksProjectiles (MaxBypass 4 / BypassChance 80) cannot
#   intercept a shell aimed at the tree it is attached to. That would not change the verdict, but it
#   would make the shell count non-deterministic, and the shell count is the evidence.
#
# NO Versus BLOCK, DELIBERATELY: DamageWarhead skips DamageVersus entirely when Versus is empty
# (DamageWarhead.cs:101-103), so Armor `Wood` neither helps nor hurts. And ^Tree's Armor sets no
# Thickness, so it defaults to 0 and the penetration path short-circuits (DamageWarhead.cs:237-250).
# Damage arrives unmodified except by the victim's own IDamageModifier traits -- which is precisely
# the one thing this scenario is here to measure.
#
# VALIDATED AGAINST ALL 59 ARMAMENT-REFERENCED WEAPONS IN THE MOD, not eyeballed from a couple of
# them -- the first draft was written from the SHAPE of other weapons and cost two game slots to
# two separate missing pieces. Resolving Inherits and taking the weapon-level field of every weapon
# any Armament points at: `BurstWait` appears in 59/59 and is the ONLY universal field that was
# absent here. Everything else missing is genuinely optional -- `Report` (51/59) is a sound,
# `Warhead@Target`/`@Effect`/`@Suppression_*` are deliberate omissions, `Burst`/`BurstDelays` (30/59)
# default to 1 and to unused.
#
# BurstWait is not cosmetic and not merely required: with Burst at its default of 1,
# BurstStep.Advance decrements to 0 on the first shot and returns `wait = burstWait`
# (Armament.cs:74-81), so BURSTWAIT IS THE INTER-SHOT CADENCE for this weapon and ReloadDelay is
# inoperative (no Magazine). 10 ticks over the ~475-tick fire window offers ~47 shots, so the run
# stays bounded by the Abrams' 40-round AmmoPool rather than by the clock. The engine requires it
# non-zero at Armament.cs:184-185 -- a fork-added check, since WeaponInfo.BurstWait defaults to 0
# and no [FieldLoader.Require] marks it.
#
# ClearSightThreshold: 255 copies ^ArtilleryRound (weapons-ballistics.yaml:874) and disables the
# shadow/density line-of-sight gate. Strictly it is belt-and-braces: the firing line runs straight
# north from 20,24 to 20,17 and crosses only the guard tree's own cell, whose density 10 scores 2
# against the default threshold of 5 (WeaponInfo.cs:151) and would pass. But "would pass" is the
# reasoning that has already cost two slots on this file, and the field is a targeting gate with no
# effect on the damage arithmetic, so it costs nothing to remove the question entirely.
TreeGuardShell:
	ValidTargets: Ground, Trees, Water
	Range: 12c0
	BurstWait: 10
	ClearSightThreshold: 255
	Projectile: Bullet
		Speed: 1000
		Blockable: false
	Warhead@Spread: SpreadDamage
		Spread: 2c0
		Falloff: 100, 100
		Damage: 12000
		ValidTargets: Ground, Trees, Water
		DamageTypes: ExplosionDeath
