Auto: Ctrl+Alt+LMB on a build-menu icon selects the player's units of that type. Asserts on-screen scope first (4 Bradleys), escalation to map-wide on the repeat click (7), that Abrams are never swept in, and that a type owned none of leaves the selection intact. Uses Bradleys because ww3mod infantry set SelectionDecorations ShowNever and the Abrams' always-on bracket decoration masks its selection box.
