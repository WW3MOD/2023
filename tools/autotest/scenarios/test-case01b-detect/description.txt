CASE-01B forest ambush — DETECT variant (fire-lane measurability). Clones case-01's kill-clearing geometry + 5x e3.america vs 5x e3.russia (100cr parity). ONE difference: a map-local rules override drops the DEFENDERS' Detectable.Vision 3->1 so the attackers acquire them and the fight RESOLVES (the calibration batch never resolved — defenders were undetectable, so it measured concealment not fighting). Only defender DETECTABILITY is raised; defender detection-of-attacker and firing-at-attacker stay governed by the direction-symmetric ground-shadow byte, so a max-density (item-21) seat that blocks the defender's own DMR fire (5.56mm.DMR, engine-default ClearSightThreshold 5) shows up in the metrics. Verdict note carries per-side fire-lane metrics: defFired=k/5, ttfShot(min/mean/max), defShots, attShots, kills+cost-weighted losses, refined=r/5; per-defender everFired/ttfShot/shots go to debug.log. Capture-mode Pass on resolution or 90s deadline (value is the metrics). Single run: ./tools/autotest/run-test.sh test-case01b-detect
