Pins DroneJammer's range after the 2026-09-15 cut from 20c0 to 12c0. One operator, two enemy drones due east at 11 and 13 cells; PASS needs the near one damaged AND the far one untouched, because either half alone is satisfied by a range that is badly wrong the other way. Observable is Health < MaxHealth (Lua cannot read the 5-tick `dronedisable` condition, and damage is monotone so a sampling gap cannot lose it). No bot plays this map — ^AutoTarget is a trait and needs none — and distances are re-measured every sample, so a drone that drifts across the 12-cell line voids the run as SETUP INVALID rather than answering about a range it never sat at.
