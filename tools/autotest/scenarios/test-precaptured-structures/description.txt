ASSERTS the positive arm of the "Pre-captured Structures" lobby option: with it ON, a neutral capturable structure starts owned by the player on ITS OWN side of the DefconWall border, and one standing inside the border band starts Neutral. rules.yaml authors that border as a two-cell vertical band at x=31,32 spanning the full height of Bounds (1,1,64,32) -- 64 cells, verified statically to leave exactly 2 components under DefconWallRegion's own Map.Contains flood. Two hand-placed Supply Routes (USA at 3,15, Russia at 59,15 -- 3x3, so their CenterPositions land on the centres of cells 4,16 and 60,16, one WEST of the band and one EAST, 56 cells apart on one row) and three 2x2 OILB derricks at 11,15 / 31,15 / 51,15, whose footprints are cells 11-12 / 31-32 / 51-52 across rows 15-16: the first entirely west, the second entirely INSIDE the band, the third entirely east. Expected owners after the transfer lands: USA / Neutral / Russia. Middle is Neutral because all four of its footprint cells are band, which is the footprint test this scenario exists to exercise; MiddleBandPercent is not consulted on this map at all, so a retune of it cannot red this scenario. Each outer derrick has exactly ONE contender on its side, so neither the tie rule nor the distances decide anything -- the distances (7.52 vs 48.50 cells for NearUSA, 8.51 vs 47.50 for NearRussia) survive only as what the anchor setup guard below reads. The assertion waits 30 ticks because Actor.ChangeOwner queues a FrameEndTask (Actor.cs:519-522) rather than reassigning in place -- ownership has not moved on tick 0 and lands at the end of the first tick, so reading earlier would red the feature for working as designed. Two setup controls guard against a green or a red that means nothing: all-three-Neutral is reported as "the option was read as OFF", distinctly from the verdict, because that is the SIBLING scenario's expected result and the two are one keystroke apart in a diff; and NearUSA=Russia with NearRussia=USA is reported as "the anchors are not the Supply Routes", the likely cause being AnchorFor falling through to its HomeLocation fallback. rules.yaml carries -SpawnStartingUnits:, which is why the Supply Routes are authored by hand -- this arm is the one that exercises the SR anchor path rather than the fallback. Pair with test-precaptured-structures-off, which is identical except that its rules.yaml declares no PreCapturedStructures block at all and asserts the shipped default leaves all three Neutral. RUN HISTORY: first run 2026-09-19 (260919_204932_p11001) FAILED at the setup guard with "SETUP: could not resolve players USA / Russia" 15 s in and a zero-byte lua.log. Cause: BOTH sides carried `Playable: True`, which makes each a lobby SLOT, and run-test.sh seats a single local client (`Launch.Map=<scenario>`, run-test.sh:745) -- CreateMapPlayers.cs:110-112 skips an unoccupied slot outright, so no Russia Player object was ever constructed. Russia is now a MAP PLAYER (no `Playable: True`, keeping `Enemies: USA`), which is how every passing two-sided scenario in this tree is built. The same run also exposed a real defect in the trait, fixed alongside: its contender filter was `Playable && !NonCombatant`, which would have dropped the map-player Russia too and made this arm vacuously green with all three derricks going to USA as a walkover. It now takes every non-NonCombatant player that HAS AN ANCHOR, and the trait writes one `PreCapturedStructures: contenders = ...` line to debug.log at world load so the enumerated set and its order are readable from a run dir. Expect `contenders = Russia@4,16, USA@60,16`-shaped output -- map players precede slot players in World.Players, so Russia is index 0. UPDATED 2026-09-20 -- THIS BECAME THE BORDER ARM. It previously had no DefconWall block, so its geometry was degenerate and the trait fell back to the distance-ratio rule. The derivation cannot save it either: `-MapStartingLocations:` means no IAssignSpawnPoints trait exists, so Player.cs:213 gives BOTH sides HomeLocation CPos.Zero and two coincident homes derive no line. Without the authored region this arm would silently stop being evidence about the border rule. TWO THINGS TO READ ON A GREEN RUN, because both rules reach USA / Neutral / Russia on this map on purpose and a silent revert to the ratio would therefore NOT red it: the trait writes `PreCapturedStructures: border = Russia:side1, USA:side0` to debug.log when the border path ran, and `border = (none -- falling back to the 10% ratio rule)` when the region was rejected as degenerate or the RegionCells list was lost. Confirmed on run 260920_134947_p16325, which PASSED with the border line present. Single run: ./tools/autotest/run-test.sh --hidden test-precaptured-structures
