ASSERTS the positive arm of the "Pre-captured Structures" lobby option: with it ON, a neutral capturable structure starts owned by the nearer player, and one nobody is meaningfully nearer to starts Neutral. Two hand-placed Supply Routes (USA at 3,15, Russia at 59,15 -- 3x3, so their CenterPositions land on the centres of cells 4,16 and 60,16, 56 cells apart on one row) and three 2x2 OILB derricks at 11,15 / 31,15 / 51,15, whose centres sit at margins of 545% / 3.6% / 458%. Expected owners after the transfer lands: USA / Neutral / Russia. The half-cell y offset on the derricks is deliberate -- Middle is NOT an exact tie, so a green run is evidence about the ratio band itself rather than about tie handling. 3.6% is well inside the shipped MiddleBandPercent of 10 and well clear of both ends of the empty 8.4%..15.2% calibration band, so a retune inside that band does not red this scenario; a retune BELOW 3.6 does, for a legitimate reason. The assertion waits 30 ticks because Actor.ChangeOwner queues a FrameEndTask (Actor.cs:519-522) rather than reassigning in place -- ownership has not moved on tick 0 and lands at the end of the first tick, so reading earlier would red the feature for working as designed. Two setup controls guard against a green or a red that means nothing: all-three-Neutral is reported as "the option was read as OFF", distinctly from the verdict, because that is the SIBLING scenario's expected result and the two are one keystroke apart in a diff; and NearUSA=Russia with NearRussia=USA is reported as "the anchors are not the Supply Routes", the likely cause being AnchorFor falling through to its HomeLocation fallback. rules.yaml carries -SpawnStartingUnits:, which is why the Supply Routes are authored by hand -- this arm is the one that exercises the SR anchor path rather than the fallback. Pair with test-precaptured-structures-off, which is identical except that its rules.yaml declares no PreCapturedStructures block at all and asserts the shipped default leaves all three Neutral. RUN HISTORY: first run 2026-09-19 (260919_204932_p11001) FAILED at the setup guard with "SETUP: could not resolve players USA / Russia" 15 s in and a zero-byte lua.log. Cause: BOTH sides carried `Playable: True`, which makes each a lobby SLOT, and run-test.sh seats a single local client (`Launch.Map=<scenario>`, run-test.sh:745) -- CreateMapPlayers.cs:110-112 skips an unoccupied slot outright, so no Russia Player object was ever constructed. Russia is now a MAP PLAYER (no `Playable: True`, keeping `Enemies: USA`), which is how every passing two-sided scenario in this tree is built. The same run also exposed a real defect in the trait, fixed alongside: its contender filter was `Playable && !NonCombatant`, which would have dropped the map-player Russia too and made this arm vacuously green with all three derricks going to USA as a walkover. It now takes every non-NonCombatant player that HAS AN ANCHOR, and the trait writes one `PreCapturedStructures: contenders = ...` line to debug.log at world load so the enumerated set and its order are readable from a run dir. Expect `contenders = Russia@4,16, USA@60,16`-shaped output -- map players precede slot players in World.Players, so Russia is index 0. UPDATED 2026-09-20 -- THIS IS NOW THE BORDER ARM, NOT THE RATIO ARM. PreCapturedStructures no longer decides by a distance ratio where a DefconWall border resolves; it asks the border. rules.yaml here now authors `DefconWall: RegionCells:` as a two-cell vertical band at x=31,32 spanning the full height of Bounds (1,1,64,32), verified statically to produce exactly 2 components under DefconWallRegion's own Map.Contains flood. Middle at 31,15 is a 2x2 whose whole footprint (31-32 x 15-16) is band, so it stays Neutral by the footprint test rather than by its 3.6% margin; NearUSA at 11,15 is west with USA's Supply Route (centre cell 4,16, side 0) and NearRussia at 51,15 is east with Russia's (centre cell 60,16, side 1). The expected verdict is UNCHANGED at USA / Neutral / Russia. Without the DefconWall block the derivation produces nothing on this scenario -- `-MapStartingLocations:` means no IAssignSpawnPoints trait exists, so Player.cs:213 gives BOTH sides HomeLocation CPos.Zero and two coincident homes derive no line -- so the trait would fall back to the ratio and this arm would stop being evidence about the border rule. Because both rules reach the same verdict here on purpose, a silent revert to the ratio would NOT red this scenario; the tell is the trait's debug.log line, which reads `PreCapturedStructures: border = Russia:side1, USA:side0` on a working run and `border = (none -- falling back to the 10% ratio rule)` on a reverted one. Read it on every green run. Single run: ./tools/autotest/run-test.sh --hidden test-precaptured-structures
