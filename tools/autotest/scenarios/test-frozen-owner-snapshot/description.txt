ASSERTS that a frozen ghost's recorded owner does NOT follow an ownership change the viewing player did not witness. Explored Map OFF + Fog ON (both locked). A USA Abrams "Scout" observes a Russia pillbox "Box" 3 cells away; the scenario then kills the Scout so the cell falls to explored-fog (visibility 1) and USA holds a frozen ghost; then Box.Owner is set to Neutral while USA cannot see it. USA's snapshot must still read "Russia". This pins the exact width of the one upstream exception to snapshot purity: FrozenUnderFog.OnOwnerChanged (FrozenUnderFog.cs:217-223) refreshes ONLY frozenStates[oldOwnerIndex], the ghost of the player who LOST the actor, so a third-party observer is untouched. Widening that loop to all players reads like a consistency fix and would be a fog leak; this scenario is what catches it, and the failure text names both candidate causes (that loop, and FrozenUnderFogUpdatedByGps.OnOwnerChanged which does refresh every player but only under an active GPS, held by nobody here). SECOND THING PINNED: phase 3 calls Test.FrozenClickCursor, the first behavioural read of a frozen-actor cursor in this suite -- Test.ClickCursor builds Target.FromActor and can never reach the CanTargetFrozenActor arm, and UnitOrderGenerator.TargetForInput needs a live mouse plus a non-null RenderPlayer, which is why the 2579ca0a enter-cursor fix had to be defended by an IL byte-scan instead. DOES NOT NEED Test.KeepRenderPlayer=true: every binding used reads the viewer's own MapLayers or frozen layer, never world.RenderPlayer. If passing the flag changes the verdict, that is itself a finding. The frozen STATE is asserted as a setup control at every phase before any verdict is read from it, because "the owner did not change" is trivially true when no ghost exists -- a run that never observed the building, or never lost sight of it, reports SETUP FAULT rather than green. NEVER RUN: written 2026-09-01, handed up unrun. Single run: ./tools/autotest/run-test.sh test-frozen-owner-snapshot
