ASSERTS that a frozen ghost's recorded owner does NOT follow an ownership change the viewing player did not witness. Explored Map OFF + Fog ON (both locked). A USA Abrams "Scout" observes a Russia pillbox "Box" 3 cells away; the scenario then kills the Scout so the cell falls to explored-fog (visibility 1) and USA holds a frozen ghost; then Box.Owner is set to Neutral while USA cannot see it. USA's snapshot must still read "Russia". This pins the exact width of the one upstream exception to snapshot purity: FrozenUnderFog.OnOwnerChanged (FrozenUnderFog.cs:217-223) refreshes ONLY frozenStates[oldOwnerIndex], the ghost of the player who LOST the actor, so a third-party observer is untouched. Widening that loop to all players reads like a consistency fix and would be a fog leak; this scenario is what catches it, and the failure text names both candidate causes (that loop, and FrozenUnderFogUpdatedByGps.OnOwnerChanged which does refresh every player but only under an active GPS, held by nobody here). SECOND THING PINNED: phase 3 calls Test.FrozenClickCursor, the first behavioural read of a frozen-actor cursor in this suite -- Test.ClickCursor builds Target.FromActor and can never reach the CanTargetFrozenActor arm, and UnitOrderGenerator.TargetForInput needs a live mouse plus a non-null RenderPlayer, which is why the 2579ca0a enter-cursor fix had to be defended by an IL byte-scan instead. DOES NOT NEED Test.KeepRenderPlayer=true: every binding used reads the viewer's own MapLayers or frozen layer, never world.RenderPlayer. If passing the flag changes the verdict, that is itself a finding. The frozen STATE is asserted as a setup control at every phase before any verdict is read from it, because "the owner did not change" is trivially true when no ghost exists -- a run that never observed the building, or never lost sight of it, reports SETUP FAULT rather than green. RUN HISTORY: written 2026-09-01 and handed up unrun; first run the same day FAILED in SETUP at phase 2 -- the Observer sat 18.4 cells from Box, which the map comment called "far outside any sight band" but is in fact inside ^StandardVision's strength-5 rung, because that ladder reaches 32 CELLS and not the ~10 the author assumed (defaults.yaml:95-133). The cell held at visibility 5 to the deadline and no ghost ever formed. Observer moved to 52,16 -- same row as Box, 44 cells, past the outermost 32c0 rung by 12. RE-RUN THE SAME DAY AND PASSED (run 260901_082819_p3797, status pass), which the two commits either side of it did not record: phases 3 and 4 DID execute, and the pass is structurally non-vacuous because the lone `return true` at :213 sits inside phase 4 and every phase transition is gated on the state readout ahead of it. That run is therefore also the first behavioural confirmation that Test.FrozenClickCursor resolves a non-empty cursor over a frozen enemy structure -- phase 3 fails outright at :170 if it does not. What the scenario still lacked as of 2026-09-02 is a demonstrated RED: the verdict arm at :200-210 has never fired, so the guard has only ever been seen passing. RED-ARM.md in this directory carries the one-hunk sabotage that makes it fire (widen FrozenUnderFog.OnOwnerChanged to all players), the exact failure text to require, and the argument for why that sabotage lands on the verdict rather than on a setup control. Run the RED before trusting the green. The author's OTHER flagged risk is resolved statically and does not apply: FrozenUnderFog.TickRender fills HasRenderables for an off-screen ghost just fine, because World.TickRender (World.cs:527) dispatches ITickRender across the whole TraitDict with no viewport cull -- and Box is on-screen regardless, the camera focusing between Scout and Box. Single run: ./tools/autotest/run-test.sh test-frozen-owner-snapshot
