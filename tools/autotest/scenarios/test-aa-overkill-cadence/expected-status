skip
MEASUREMENT HARNESS, no graded answer: every Test.Fail in the file is a STAGING fault, not a
finding. The four are SETUP INVALID (:170), Russia player not found (:180), AA actor missing
(:188) and could not spawn halo (:206). The terminal verdict carries firedOf4,
gapFirstToSecond, the shooterGaps list and haloDeath, and asserts nothing about any of them --
the serialisation this scenario is named for is measured and then discarded into a note.

ITS FINDING IS ALREADY GUARDED ELSEWHERE. test-aa-battery-volleys:288-297 turns exactly this
serialisation into a failing rung ("the stock battery fired one at a time instead of
together"), with an OverkillThreshold: -1 control battery at :44-50 and a count rung at
:268-273 for the degenerate one-shooter case. So the mechanic is not unguarded -- this
scenario is the un-bounded instrument, not the guard.

THE STAGING FAILURES STAY FAILURES. "The battery could not be built" is a genuine red and must
remain one; only the terminal verdict moved from Test.Pass to Test.Skip (2026-09-01). If this
scenario reports FAIL against this declaration, the staging broke -- read the note, do not
delete this file. Staging is the live risk here specifically: bugs/discovered.md:307 records
that this scenario had been measuring nothing for eight days after an unrelated over-count fix
silently disarmed the thing it exploited, and records the repair as FIXED-but-unrun.

WHY SKIP AND NOT PASS. `Test.Skip` is the harness's own word for "ran, produced no graded
answer" (TestGlobal.cs) and run-batch.sh maps it to the SKIP bucket. The scenario is not
hidden and still runs; the verdict just stops claiming an answer it never computed.

REMOVE THIS FILE in the same commit as whatever bounds the cadence -- or delete the scenario
into test-aa-battery-volleys' shadow, which is the other honest resting state.

NOT EXPOSED TO expected-status.sh's KNOWN GAP. That gap is specific to `fail` declarations,
where a watchdog TIMEOUT-FAIL also exits 1 and therefore grades green. A timeout reaches
expected_status_grade as FAIL, and declared-`skip` + FAIL is RED.
