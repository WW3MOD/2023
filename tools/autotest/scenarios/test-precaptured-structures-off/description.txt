ASSERTS the negative arm of the "Pre-captured Structures" lobby option: left at its shipped default, every neutral capturable structure is still Neutral once the match is running. Geometry is IDENTICAL to test-precaptured-structures -- same two hand-placed Supply Routes, same three OILB derricks at 545% / 3.6% / 458% margins -- so the two runs differ in exactly one input, whether rules.yaml carries the option, which is what makes the pair evidence about the option rather than about the map. The default being OFF is load-bearing rather than cosmetic: a Skirmish nobody touched must be the match it was before the option existed, and a feature that fires when switched off is a silent change to every existing game and every saved lobby preset. The positive arm cannot see that, because it turns the option on; this is the only place the shipped default is observed. THE ABSENCE OF THE BLOCK IS THE TEST -- this rules.yaml deliberately does not write `PreCapturedStructures: CheckboxEnabled: false`, because that would pin the override path and say nothing about what a host who never opens the lobby gets. Do not "make the two scenarios symmetrical" by adding it. What this does NOT directly observe: PreCapturedStructures.WorldLoaded returns before touching the actor list when the option is off, and no Lua binding can tell "returned early" from "ran and found nobody near enough". Three Neutral derricks at 545% margins rules out the second reading on this map -- the trait cannot both have run and left a derrick 7.5 cells from a Supply Route alone -- so the early return is observed, indirectly. Both Supply Routes are asserted present and correctly owned before the verdict is read, because without anchors "nothing was captured" is true of a broken map as well as of a working default. Single run: ./tools/autotest/run-test.sh --hidden test-precaptured-structures-off
