Auto: a technician ordered to capture a structure it can never walk to must GIVE THE UNIT BACK. Before the fix the Enter approach loop re-queued a move forever, so the technician was never idle and CaptureDispatchManager.CommittedTarget — which reads the activity queue — counted him busy for the rest of the match. A caged technician must report itself free again; a control technician on open ground must still capture normally.
