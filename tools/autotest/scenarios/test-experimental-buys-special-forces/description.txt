Auto: each @experimental twin (america and russia, both bots experimental) must buy at least one Special Forces of its own faction within 9000 ticks, unaided, and never own more than UnitLimits (2) — no sf is pre-placed and no module can route budget to it, so the floor lane is the only path in. Long run by design (the shipped UnitDelays gate is 3000 ticks and is deliberately not stubbed out, and the ceiling arm holds for a further 750 ticks after both are seen): run with --speed 4 --timeout 660.
