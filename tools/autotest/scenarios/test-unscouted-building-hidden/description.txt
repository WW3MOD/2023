ASSERTS that an enemy building on never-scouted ground cannot be right-clicked. Explored Map OFF + Fog ON (both locked), so ground nobody has looked at sits at ResolvedVisibility 0 -- raw shroud, not the "explored once, now fogged" state FrozenUnderFog legitimately reveals. Two Russia pillboxes: NearBox 3 cells from a USA Abrams (in sight, positive control, must be clickable), FarBox 53 cells away with no vision or radar reaching it (must NOT be clickable). Asserts Test.IsMouseTargetable, the predicate the right-click actually runs -- NOT Test.IsDetectedBy, which is the very CanBeViewedByPlayer call the QUICK FIX 260503 short-circuit overwrites and so cannot discriminate, and NOT a Lua Attack order, which CombatProperties.cs:97 exempts FrozenUnderFog actors from outright. NearBox is load-bearing: the negative rung also passes if buildings were vetoed wholesale. Cell visibility is asserted (far == 0, near > 1) BEFORE the verdict, so a run where sight reached the far box, or where a null RenderPlayer flattened every reading, reports as SETUP FAULT instead of a verdict. MUST be run with AUTOTEST_EXTRA_ARGS="Test.KeepRenderPlayer=true" or TestModeLogic:30 nulls RenderPlayer and every actor reads as clickable. Single run: AUTOTEST_EXTRA_ARGS="Test.KeepRenderPlayer=true" ./tools/autotest/run-test.sh test-unscouted-building-hidden
