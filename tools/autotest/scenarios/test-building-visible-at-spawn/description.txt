REGRESSION GUARD for the strict-FrozenUnderFog restoration, run under the SHIPPED DEFAULT lobby config (Explored Map ON + Fog ON, both locked) -- the opposite configuration to its sibling test-unscouted-building-hidden. Two map-placed Russia pillboxes: NearBox 3 cells from a USA Abrams (actively watched, cell visibility >1) and FarBox 53 cells away (explored but unwatched, cell visibility exactly 1). Rung A samples EVERY tick from tick 1 and records the first tick NearBox reports visible, because the defect 12a9b91b described is transient ("hide on first render before any sight pass runs") and a scenario that settles for 40 ticks before looking would pass against it; budget is 8 ticks and the measured flip tick is printed either way. Rung B asserts FarBox does NOT report the live actor -- under fog the player is entitled to a remembered image, not live damage and instantly-appearing new structures -- and is the standing guard against the QUICK FIX 260503 short-circuit being reintroduced. Each rung is the other's control: A alone passes if everything became visible (the bug), B alone passes if buildings were vetoed wholesale (the regression). Asserts Test.IsDetectedBy, which for a building IS FrozenUnderFog.IsVisible, the same call ModifyRender gates the sprite on. NOT assertable from Lua: that the remembered image is actually drawn in FarBox's place -- there is no frozen-actor hook in TestGlobal, so rung B pins only that the live actor is hidden. That remains a PLAYTEST. Single run: ./tools/autotest/run-test.sh test-building-visible-at-spawn
