Eyeball whether a HELD command-bar mode marks its glyph as well as its panel: a no-mode control then all six modes engaged in turn (Attack Move, Force Move, Force Attack, Guard, Patrol, Waypoint). Shot 06 is the discriminating one — Patrol and Auto-Enter draw the same glyph side by side, and only Patrol should be amber. Requires the Sync.RunUnsynced fix in Test.PressHotkey; without it the run dies on shot 02.
