Demo: the DEFCON 3 dividing line standing on a DIAGONAL, on the shipped Woodland Warfare map, with the line derived from the two spawns exactly as a real match derives it. A diagonal is the case that matters: at the shipped HalfWidth of 512 a one-cell band sealed only axis-aligned lines, and on a diagonal the banded cells touched at their corners so an 8-connected step walked straight between them - it leaked on 131 map/locomotor combinations and nothing caught it, because the feature's one worked example authors a vertical line. HalfWidth is 1024 now and this capture is what confirms the fix in the engine rather than in an audit script. The clocks are pinned at 30000 ticks so the level cannot fall to 2 and take the wall down under the frames. One Abrams is ordered across the line; correct is a continuous band with no staircase hole, and a vehicle that will not cross it. Nothing is asserted.
