Bot restocks its own Logistics Center: a pre-placed, bot-owned logisticscenter at (20,16) is drained to 200/2250 by Test.SetSupply (under CenterRestockThresholdPerMille 500), with two bot-owned trucks at different distances. PASS is two-part — the Centre's stock must RISE above the drain within 1800 ticks, AND the NEAR truck (4 cells) must be the one that moved, since the dispatch ranks distance-dominant and a far-truck delivery is a ranking regression that a refill alone would hide. The drain is asserted before anything else, so a no-op SetSupply reports as its own failure rather than as a dispatch failure. Pins user ruling 2026-09-03 ("Bots needs to learn how to resupply the LC"). NOTE: the transfer itself already worked for humans and is unchanged; the bot now issues the same DeliverSupply order, so this exercises the shared path. Emits [logistics] deliver truck=/center= and, on a failed arrival, [supply] deliver-refused reason=never-arrived.
