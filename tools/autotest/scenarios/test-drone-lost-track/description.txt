Does an @experimental drone operator aim its drone at a lost-track contact instead of the stalest ground it could explore? A scout sees an enemy 30 cells out (outside the operator's own 28-cell verifying bubble), then the SCOUT is killed so the area goes dark and the contact decays rather than being verified-clear. Verdict is a sampled position trace of the drone, not a launch count. RED control: run the sibling scenario test-drone-lost-track-control, which overrides IntelSampleInterval to 999999 in its own rules.yaml. No ai.yaml edit, no revert.
