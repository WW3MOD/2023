fail
The negative arm is BY MERIT: the @experimental drone operator genuinely does not
yet prefer a lost-track contact over the stalest ground it could explore. The
scenario and the code are both correct.

MEASURED, run 260901_084100_p5243_test-drone-lost-track, seed 119467278, on main
at 3dd67e07 (build clean, NUnit 2104/2104):

    samples=86  near(<=18c of 47:54)=0 (0%)  mindist=25

The drone flew 18:45 -> 35:31, parked there for ~60 of 86 samples, and returned;
it never approached the vanish cell. A late intruder (tecn.america at 21:46, t1221,
27c from the vanish cell) is correctly ignored by the scenario as post-decision,
and 22 of the 86 samples follow it. Both execution markers present.

THE OUTCOME ABOVE STANDS. TWO CLAIMS THAT SAT HERE ARE WITHDRAWN (2026-09-02).

1. "25 against an 18-cell bar is a 1.389x shortfall, inside the pre-registered
   <=1.5x band" COMPARED TWO DIFFERENT QUANTITIES. 1.389 is mindist/NearVanishCells
   -- a ratio of CELL DISTANCES. The <=1.5x / >=1.8x band at CONTROL-ARM.md:135 is
   pre-registered for the LostTrackIntelSquares MULTIPLIER, which CONTROL-ARM.md:130
   defines in SCORE space as
       (reveal - bestintelreveal) / (bestintel - intel_at_reveal_argmax)
   How far the drone parked from the vanish cell does not convert into how much
   larger the intel constant would have to be, so the band was never entered and
   "change nothing" was never earned. The accompanying "previously-reported
   1.19-1.47x bounds" are recorded nowhere in this repo -- the same objection a
   worker already raised against the 1.247x figure this file replaced.

   THE BOUND THAT IS ACTUALLY DERIVABLE, and it needs no further run: reveal_hunt
   >= 0 and the hunt cell lost, so the multiplier lies strictly between 1.0x and
   2.10x (CONTROL-ARM.md:118-127). That straddles BOTH thresholds. The sizing
   question is OPEN, not settled.

2. THE MEASUREMENT IS STALE. It predates 1e0226b9, which replaced the drone's
   rectangular revealed-area query with the vision DISC and removed up to 228
   squares of corner credit per candidate -- ~19x MinRevealedSquares (12). Every
   `reveal` in the run above was computed by the box. Both the hunt cell and the
   reveal argmax lose corners, so the direction of the change is NOT predictable
   from the old numbers and must be re-measured.

WHAT THE NEXT RUN MUST CAPTURE, BECAUSE THE LAST ONE DID NOT. The multiplier needs
`bestintelreveal=` off the `[drone] ... launch` line (DroneOperatorBotModule.cs:734).
That line is engine-side and the Lua verdict string cannot carry it. The 260901 run
dir held only PNGs and result.json, and the global debug.log had been truncated by a
later launch before anyone read it -- so a run that reached a real verdict still
could not answer the question it was spent on. run-test.sh now archives debug.log
into the run dir for every outcome; read RUN_DIR/debug.log, never the global file.


PAIRED LAUNCH LINES, BOTH ARMS, 2026-09-15 — THE FIRST TIME THE TWO ARMS HAVE BEEN RUN
BACK TO BACK POST-1e0226b9 WITH ALL FOUR SIZING TERMS CAPTURED.

  treatment  260915_152821_p59962_test-drone-lost-track          fail, both markers
  control    260915_153322_p61098_test-drone-lost-track-control  fail, both markers,
                                                                 records=0 intel=0 (override merged)

  tick  TREATMENT                                                          CONTROL
  200   cell=29,25 reveal=248 intel=0  bestintel=178 @39,51 bir=14         cell=29,25 reveal=248 bestintel=0 bestintelcell=none
  1600  cell=37,57 reveal=180 intel=39 bestintel=43  @39,51 bir=158        cell=37,57 bestintel=0 bestintelcell=none
  1800  cell=39,53 reveal=122 intel=43 bestintel=43  @39,51 bir=107        cell=39,53 bestintel=0 bestintelcell=none

IDENTICAL CELLS AT ALL THREE LAUNCHES. The lost-track term changed NO launch decision on
this geometry. The t1600/t1800 drift toward the contact is NOT the term working -- it is the
revealed-area argmax moving as the nearer ground is consumed, and the control, which has no
intel at all, moves to exactly the same cells. Two further tells inside the treatment alone,
needing no control: at t1600 the chosen cell carried LESS intel than the cell it beat (39 vs
43 at 39,51), and at both retasks the winner was not bestintelcell. A term that decided the
target would have picked 39,51.

"HOW SHORT IS CURRENTLY UNKNOWN" IS ANSWERED. The pre-registered formula (CONTROL-ARM.md,
recorded before the measurement and not moved after it) is

    multiplier = (reveal - bestintelreveal) / (bestintel - intel_at_reveal_argmax)

and the t200 line supplies every term. The control is what makes the denominator legitimate:
with intel 0 everywhere it chose 29,25, so 29,25 IS the reveal argmax, and the treatment
printed intel=0 there.

    (248 - 14) / (178 - 0) = 234 / 178 = 1.315x

PRE-REGISTERED RULING AT THAT VALUE: "<=1.5x means the term is roughly right and this
scenario is simply a hard case -- change nothing." 1.315x is inside that band. This is the
re-measurement ai.yaml:1201-1216 said was the only thing that could close the sizing
question, and it closes it as CHANGE NOTHING.

Two things not to do with this number. It is an UPPER BOUND on the general case, not a
general shortfall: ai.yaml:1219-1223 records that this map is close to a worst case for the
term by construction -- the contact sits 30 cells out against a 22-cell leash, so the closest
reachable hover cell is 8 cells away and falloff has already cut 247 squares to 178. A
contact the drone can park directly on keeps the full 247 and wins comfortably. And setting
LostTrackIntelSquares to the value that flips THIS scenario (>=326, a 1.30x bump) is
tuning-to-pass by CONTROL-ARM.md's own definition, the same move as lowering
FreshSightingTicks would have been.

REMOVE THIS FILE in the same commit as whatever makes the operator prefer the
lost-track contact. A pass against a live `fail` declaration is RED on purpose:
it means the premise moved and this note is stale.

The harness distinguishes "did not prefer the contact" from "never reached a
verdict" by itself: run-batch.sh grades on run-test.sh's OUTCOME NAME rather than
its exit code, so a hang, a crash or a lost outcome is RED under any declaration and
is listed under "NEVER REACHED A VERDICT". An OK(fail) here means the scenario
reached a verdict under its own power. Keep the evidence habit regardless -- 86
samples and a position trace are why the measurement above is believable, not the
tally.
