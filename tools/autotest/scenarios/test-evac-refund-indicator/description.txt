CAPTURE INSTRUMENT, GRADES NOTHING -- terminal verdict is Test.Skip by design, exactly as test-fog-darkness-ruler's and test-screenshot-smoke's are, because no clause in it is a pass/fail statement about the mod. Produces ONE PNG carrying two evacuation refund ticks side by side so a human can answer the question adfb0f2f has never been asked in a running game: does the "+$N" a player is promised for evacuating a unit actually appear on screen, in a place they can see, and does a zero refund read as "+$0" rather than as nothing. THE BUG IT PHOTOGRAPHS THE FIX FOR: the tick was spawned at the actor's CenterPosition at the moment of sale, and a successful evacuation ends OUT OF BOUNDS by construction -- RotateToEdge drags a ground unit GroundOffMapCells = 2 cells past the boundary before selling -- while both of FloatingText's gates resolve position through MapLayers and both answer "hidden" for anything out of bounds (IsExplored returns false when !map.Contains, MapLayers.cs:504-505; IsVisible returns map.Contains outright with fog off, :576-577). So the indicator was suppressed for exactly the evacuations that SUCCEEDED and survived only on the fallback paths that sell in place, which is the user's reported "usually there is a $1000, but sometimes I get nothing" with the two cases the other way round from how it reads. The fix clamps the position into Map.Bounds, passes FloatingText's ignoreVisibility, and lengthens the rise from 30 ticks to 75 (1.8s to 4.5s at Timestep 60).

MUST be run with AUTOTEST_EXTRA_ARGS="Test.KeepRenderPlayer=true", AND THE REASON IS NOT THE USUAL ONE. The usual reason is that TestModeLogic.cs:30-31 nulls World.RenderPlayer for every autotest holding a real player slot, so fog disappears and the frame stops being a picture of what a player sees. That applies here but is the weaker half. The stronger half is that a null RenderPlayer makes ONE OF THE TWO THINGS THE FIX CHANGED UNFALSIFIABLE: FloatingText's own gate is `!ignoreVisibility && (FogObscures || ShroudObscures)` (FloatingText.cs:66) and World.FogObscures/ShroudObscures short-circuit to false the instant RenderPlayer is null (World.cs:109-115), so the text draws whether or not the ignoreVisibility bypass is passed at all -- and DoSell's own `self.Owner.IsAlliedWith(self.World.RenderPlayer)` gate is vacuous too, because Player.RelationshipWith returns Ally for a null argument outright (Player.cs:254-255). A run without the flag therefore photographs a code path THAT CANNOT FAIL and comes back with a perfectly plausible picture of two refund ticks. That is the one failure mode here that would be read as evidence.

SO THE RUN REFUSES TO PRODUCE A VERDICT WHEN THE FLAG IS MISSING. FarBox is a Russia pillbox at 60,30 with all ten Vision layers stripped (it inherits PBOX, which inherits ^Defense -> ^StandardVision, structures-defenses.yaml:5), ~55 cells from either subject and well past that ladder's 32-cell outermost rung, on a map with ExploredMapCheckboxEnabled FALSE and locked -- so USA has never looked at it and its cell reads visibility 0. Test.IsMouseTargetable asks MouseTargetVisibility.IsRevealed, whose actorIsVisible term is !World.FogObscures(a) and which ANDs the whole expression with it: false under a live RenderPlayer, true under a null one. Targetable therefore means null RenderPlayer and nothing else. Both preconditions are asserted before anything is pressed -- the cell must read 0 AND the box must be untargetable -- so a stray vision source invalidating the probe reports as PREMISE BROKEN rather than being silently absorbed. This is the same probe test-unscouted-building-hidden already relies on.

GEOMETRY, AND WHY THE FRAME CAN SEPARATE THREE OUTCOMES RATHER THAN TWO. Bounds are 1,1,64,32, so Map.Bounds is Left=1 Top=1 Right=65 Bottom=33 with Right and Bottom EXCLUSIVE and the last legal west column is x=1. Both subjects exit WEST, and that is derived rather than hoped for: RotateToEdge.ChooseEdgeCell takes the ground branch, whose searchOrigin is self.Location (no SpawnArea actor exists on this map, so FindClosestSpawnAreaForOwner returns null and the fallback applies), and Map.ChooseClosestMatchingEdgeCell sorts Map.AllEdgeCells -- the perimeter of BOUNDS, built by UpdateEdgeCells -- by straight-line distance. From Payer at 6,14 the nearest perimeter cell is 1,14 at 5 cells against 6,1 at 13; from Pauper at 6,19 it is 1,19 at 5 against 6,32 at 13. West wins by more than 2x in both cases, so no pathing wobble can flip it, and `make nav-guard --scenarios` reports 2048 reachable cells in ONE region for every ground locomotor including heavytracked, so ChooseEdgeCell's CanReach predicate cannot reject the west perimeter. The camera is centred on cell 8 at pinned zoom 2.0, putting roughly ten cells of off-map black to the LEFT of column x=1 in frame. That is what buys the three-way read: a tick ON column x=1 just inside the boundary means the clamp works; a tick roughly two cells out IN THE BLACK VOID means the clamp is gone but the visibility bypass is holding; NO tick at all means suppression, the original bug. "Absent" and "drawn where nobody can see it" are different failures with different fixes and a capture that could not separate them would be worth very little.

THE VERDICT SEPARATES "no text" FROM "no sale", which the picture alone cannot. The run records the USA cash delta at the tick each subject is observed dead, so if the PNG shows nothing while the verdict reports money credited, the sale happened and the INDICATOR is what is broken; if no credit was recorded either, nothing evacuated and the frame is not about the indicator at all. It also carries the last in-world cell of each subject and the cell EvacRefundTextMath.ClampToBounds would map it to, so the reader has the predicted text position in result.json without opening a log. The clamp is mirrored in Lua purely to PRINT that prediction and never feeds back into the engine; doing it in CPos space is legitimate because this is a rectangular TEMPERAT map where the MPos the engine clamps in and the CPos read here are the same numbers.

THE ZERO ARM, AND ITS DECLARED GAP. Pauper is `pauperabrams`, an inherit of the shipped abrams (vehicles-america.yaml:456) whose only difference is `DeliversCash@Rotation: Payload: 0`. It exists because a refund that renders as ABSENCE is indistinguishable from the out-of-bounds suppression bug, and because DoSell's draw was made UNCONDITIONAL on the amount -- it used to be gated on `refund > 0`, which swallowed the indicator for exactly the wreck whose owner most wants telling that evacuating it bought nothing. Payload 0 reaches that identical branch deterministically: GoDonateCash sets `amount = info.Payload` whenever Payload is not the -1 sentinel (DeliversCash.cs:96), the handicap adjustment leaves 0 at 0, and DoSell's HP scaling of 0 is 0 at any health. IT IS NOT THE SAME ROUTE A REAL MATCH TAKES, and that is stated rather than hidden: the natural route is HP scaling rounding down, but staging it needs a vehicle under 1/1000th health and ^Vehicle carries ChangesHealth@CriticalDamage with StartIfBelow: 50 (vehicles.yaml:184), so the subject would race its own bleed-out against a seven-cell drive and be flaky for a reason unconnected to the indicator. This arm catches a reintroduced `refund > 0` gate; it does NOT cover the hp/maxHP rounding. The run also checks that the override took -- a non-zero delta for Pauper is reported as suspect setup, because then there is no zero in the frame to read.

TIMING. The two evacuations are ordered 20 ticks apart, not together, for two load-bearing reasons: per-tick cash deltas would merge into one unattributable number if both sales landed on the same tick, and two texts spawned simultaneously would be indistinguishable in frame if either failed to draw. Twenty ticks is comfortably inside EvacRefundTextMath.TickLifetime = 75, so the older text is still alive at the shot; the run measures the actual gap and flags it if it reaches 45 (the older text will have drifted noticeably) or 75 (it had already expired, and its absence is the instrument's fault and not evidence). The shot is armed 15 ticks after the second sale and the verdict 25 ticks after that, because the capture is ONE FRAME LATE -- Test.Screenshot arms a capture and the pixels are sampled at the end of the NEXT RenderTick -- so nothing may touch the world in the same closure.

READ THE PNG AT FULL RESOLUTION. SCREENSHOT.md's standing advice to shrink a capture to ~1280px before Read is WRONG for this one: the entire payload is four to six glyphs of the TinyBold font, and "+$0" versus "+$1000" versus nothing is exactly the small-text discrimination that survives a resize least well. Zoom is pinned at 2.0 to help and the APPLIED value is logged rather than assumed, because Test.SetZoom returns what it actually applied and a clamp there invalidates the pixel geometry above. Two secondary tells in the frame: most of the map should be BLACK unexplored shroud with a lit corridor along each subject's westward path (a uniformly lit map means RenderPlayer was null, and the verdict will have said so), and the row-14 text sitting slightly higher than the row-19 one is the 34-units-per-tick rise working over its 20-tick head start, not a fault.

Single run: AUTOTEST_EXTRA_ARGS="Test.KeepRenderPlayer=true" ./tools/autotest/run-test.sh --size 1600x900 test-evac-refund-indicator
