CONTROL ARM for test-drone-lost-track. Byte-identical to the treatment except one value in its own rules.yaml — IntelSampleInterval: 999999 on DroneOperatorBotModule@experimental — which leaves the belief-contact table empty, so every candidate scores intelSquares 0 and tasking falls back to pure revealed-area staleness: the pre-change behaviour. EXPECTED TO FAIL; that failure is the RED the treatment is read against, and it is not a bug to fix. Proof the arm really is a control: `records=0` and `intel=0` on the `[drone] ... launch` line in RUN_DIR/debug.log. records>0 means the YAML override did not merge and the run is a second copy of the treatment — discard it.
