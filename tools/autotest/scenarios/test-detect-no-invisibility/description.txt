ASSERTS the invisibility fix end to end, through the real engine detection path. A rifleman with Detectable.Vision pinned to 12 (above anything the modifier stack can compose, so it lands exactly on the clamp ceiling) is walked past by a Russian observer at 30, 9, 5 and 2 cells. Expects: NOT detected at 30c (band 1) and 9c (band 8, one strength short), DETECTED at 5c (band 9, strength MATCHES the tier -- the non-strict compare) and 2c (band 10, standing on top of it). Before the fix a unit at the ceiling was tier 10, equal to the strongest vision band, and reveal was strict, so it was invisible at every range to everything. Catches a revert of EITHER half and says which: tier 10 at the premise check = the ceiling is gone; detected only inside 4 cells = reveal went strict again. The negative rungs are load-bearing -- "detected at 2 cells" also passes if the change made everything permanently visible, which is exactly what a careless non-strict comparison does. Asserts the sightline density is 0 first, because ground shadow is subtracted from the OBSERVER and would move every band distance. Single run: ./tools/autotest/run-test.sh test-detect-no-invisibility
