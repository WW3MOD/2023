TEST: the Escalation retaliation window at its TOP rung must hand the player a game-ender it can
actually see and fire — the bug a user hit in a real match against a bot: "it reached the 'End'
phase, saying that 200kt+ weapons are available, I could see the countdown for how long it was
active, but I never got those weapons available."

USA (human slot, america) and Russia (map-authored combatant, russia) climb the ladder in the five
launches NuclearExchangeState.ReportLaunch's rules make the shortest possible chain:

  L1  USA  B61Low      300 t   band 1  ->  Russia window 2
  L2  RU   RuIskander 10000 t  band 2  ->  USA    window 3
  L3  USA  B61Max     50000 t  band 3  ->  Russia window 4
  L4  RU   RuKalibr  100000 t  band 4  ->  USA    window 5 = GameEnder
  L5  USA  W76       100000 t  band 4  ->  Russia window 5 = GameEnder
       (fires USA's PERMANENT band 4, so USA keeps its own END window)

Then it asserts, off the support power bin (SupportPowerInstance.Disabled — the predicate
SupportPowersWidget filters its icon list on):

  * USA's B83Strike reads `ready` inside the END window, and can be ACTIVATED through the real
    order path. Both halves of "I should be able to choose the game ender and place the targets".
  * Russia's SarmatStrike reads `ready` inside its own END window.
  * The faction lock of 2026-09-14 (c8cadc8a) holds: USA never sees the Sarmat, Russia never sees
    the B83.
  * The unowned 6 Mt HighYieldNukeStrike reaches both, on the same terms DoomsdayStrike's final
    exchange already gives it.
  * Negative controls: no game-ender is readable at release, and the band-2 window never lights
    TacNukeStrike, whose host checkbox is off.

RUNS AT SHIPPED LOBBY CONFIGURATION AND THAT IS THE POINT. It does NOT set
PowersSandboxCheckboxEnabled, which every other nuclear scenario does — that option provides
`powers.event`, the prerequisite the three game-enders declare and no faction grants, so with it on
this defect is invisible. Do not add it here.

The match does not actually end: DoomsdayStrikeInfo.RunInTestMode stays false, so the final
exchange is inert and the verdict is not racing an apocalypse. demo-doomsday-deadhand owns that.
