A click no selected unit can carry out must still show and issue the default order, while a click some of them can carry out still leaves the rest alone. Also: force-attack-ground must reach the cell under an enemy the firing unit cannot itself target -- the Iskander hovering a helicopter it can never engage. Since RequiresForceFire came off the launcher armaments (2026-08-22 ruling), this also pins that a plain right-click on an enemy makes the Iskander ATTACK with the attack cursor, including on a target inside its 16-cell minimum range, where the order is accepted at click time and the minimum is enforced by the attack activity backing out instead.
