ASSERTS that an enemy SUPPLYROUTE on never-scouted ground is visible and right-clickable, and that an ordinary enemy pillbox four cells away in the same corner is NOT. Explored Map OFF + Fog ON (both locked) so unscouted ground genuinely exists at ResolvedVisibility 0. Guards the AlwaysVisibleRelationships override on SUPPLYROUTE (ingame/structures.yaml) that holds the Supply Route at its pre-2026-08-27 behaviour, so that removing the six-month FrozenUnderFog.IsVisible regression ships as a pure bug fix with no balance change riding along. The pillbox is the load-bearing rung: "enemy SR visible" also passes if the fog fix silently reverted, or if the exemption landed on a shared template (^Building/^BasicBuilding) rather than on SUPPLYROUTE alone -- both visible means one of those, neither visible means the exemption did not take. The player's own SR is asserted clickable as a wholesale-veto control. Asserting on an ENEMY-owned SR is what makes a partial relationship edit fail: `Ally` alone is the trait default and a no-op, `Neutral` alone does not match an enemy. UNTESTED GAP, deliberate: no rung covers a Neutral-owned SR, because SR capture is designed-but-not-wired (no Capturable, no CaptureManager) so that state is unreachable in a real match -- add the rung when capture lands. RULED 2026-08-27 by the user: keep it visible. IF THAT IS EVER REVISITED TO "GO DARK": invert the rungs and add a scouted enemy SR as the positive control; do NOT delete this scenario. MUST run with AUTOTEST_EXTRA_ARGS="Test.KeepRenderPlayer=true" or a null RenderPlayer reports the whole map clickable. Single run: AUTOTEST_EXTRA_ARGS="Test.KeepRenderPlayer=true" ./tools/autotest/run-test.sh test-supplyroute-exempt-from-fog
