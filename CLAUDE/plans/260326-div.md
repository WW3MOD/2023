
Generally about this file: Lines are added with todos, you mark them with brackets when [DONE] or write what is up with it in the brackets. I test and remove when they are verified to work in game. When you as an agent start a tast you mark it [IN PROGRESS] so that no other agent tries to do the same one in paralell. 


Issues noticed after the upstream merge with openra latest version:

- [DONE] Shift-G to spread/scatter orders doesnt work anymore — GroupScatterHotkeyLogic was stripped from ingame.yaml during upstream merge, restored

- [DONE] Range circles not working anymore — RangeCircleAnnotationRenderable constructor assigned to Pos auto-property but never set the centerPosition field used by all methods, fixed



Not related to openra version upgrade

- [DONE] There is a gap in the bottom bar between "Patrol" and "Take cover" — TAKE_COVER X:376→358, AUTO_ENTER X:410→392

- [DONE] Tooltips from hovering units in the build bar should open like the income tooltip — ProductionPaletteWidget now sets AnchorBounds per-icon with AnchorAbove=false for side positioning

- [DONE] Evacuating units deprioritized from box-select — New ISelectionPriorityModifier interface + SelectionPriorityModifier trait. RotateToEdge grants "evacuating" condition, which triggers -20 priority modifier and amber "EVA" text decoration (top-right). Target line rendered toward exit edge cell

- [DONE] Infantry visual scaling option — Added ScaleControlledBySettings property on RenderSprites, ScaleInfantry game setting (default true), checkbox in Display settings panel. ^Infantry template sets ScaleControlledBySettings: true. When disabled, infantry render at original sprite size

- [DONE] The pause button in the sidebar that we show in game in the top right doesnt show an icon — replaced "[]" text with music/pause glyph icon, swaps to play when paused. Also added shellmap pause/play button on main menu

- [DONE] We should use the electric death animation for infantry caught in the nuke fireball radius — Changed ThermalVaporize warhead DamageTypes from FireDeath to ElectricityDeath, expanded spread to 3c0. Infantry at ground zero now play die6 (electro) animation



- Tested! [NEEDS PLAYTEST] Pilots evacuate helicopters after they are downed with heavy damage, as they should, but when they are ordered to enter the helicopter again it remains neutral. The pilots needs to capture it when entering, and that should work for any neutral helicopter, even a former enemy one. — Code review shows EnterAsCrew.OnEnterComplete correctly calls ChangeOwner for non-allied targets. Targeting logic allows neutral targets. Could not reproduce from code alone. ... TESTED and the pilots can enter and they capure the helicopter but only for the HIND, not for apache, the only difference I can think of is that hind is a transport so maybe that effects it somehow. When the hind was downed other units could still issue enter cargo orders and that should not be possible if it is heavy damaged and cant fly. This also btw caused engineers to not be able to target it so we need to make engineers able to repair cargo actors by using the default right click and force move (ctrl) to enter. Fix all issues and think proactively about the whole issue and different scenarios and adapt the solution to all of the possible scenarios.


- [DONE] In the main menu, the menu itself has a background now but we recently removed that and in the main branch it isnt like that, so remove it — Set Background: (empty) on SINGLEPLAYER_MENU, EXTRAS_MENU, MAP_EDITOR_MENU

- [DONE] Shellmap button should be transparent, no borders, auto-width — Background/Separators empty, dynamic width via MainMenuLogic

- [DONE] Main menu, the "i" info button sits outside the screen now, to the right — Shifted all top-right buttons 30px left

- [DONE] Lobby options panel should fill down to chat box — TOP_PANELS_ROOT Height: PARENT_HEIGHT - 346 (dynamic), tabs repositioned

- [DONE] Change scenario button in lobby — CHANGESCENARIO_BUTTON above CHANGEMAP_BUTTON, opens map chooser pre-filtered to "Scenario" category

- [DONE] Game speed "Normal" → "Real Time" — LobbyOptionsLogic now resolves FluentProvider keys for dropdown values

- [DONE] Frontline scenario team enforcement — Locked Multi0 (NATO, team 1, spawn 5 west) and Multi1 (Russia, team 2, spawn 6 east) with LockFaction/LockColor/LockTeam/LockSpawn. Disabled Multi2-5 (2-player only). Set NonCombatant: False on garrison players.

- [DONE] Helicopter critical crash ejecting pilots — Two fixes: (1) VehicleCrew.INotifyKilled now checks SuppressEjection flag, set by HeliEmergencyLanding during crash descent. (2) EjectOnDeath YAML conditions on ^Aircraft now include !suppress-eject. Also suppress tick-based ejection during crash.

- [DONE] Consolidate pilot infantry — crew.pilot.*/crew.copilot.* actors now use pilot_class icon/pip from old PILOT. Old PILOT/PILOTRn kept because still used by fixed-wing EjectOnDeath (can be removed once fixed-wing migrates to VehicleCrew).


