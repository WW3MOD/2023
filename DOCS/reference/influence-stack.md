# The @experimental influence stack (Stages 0 + A–F)

> Verified against code 2026-07-24 at `main @ b93eda58` (stack code-complete; final merge `36bd3b9e`). Every claim below cites the file:line it was checked against. The design docs (`WORKSPACE/plans/260722_influence_stack_design.md`, `260721_terr_offense_bias.md`) are secondary — where a plan and the code disagree, the code wins.
>
> Curation update 2026-07-29 at `main @ e5b7bbcc`: the §Known gaps capture/garrison item was re-verified and rewritten after item 24 shipped (merge `646515bd` — capture/garrison threat migrated to the belief-side danger field). Other sections retain the 2026-07-24 verification.
>
> Curation update 2026-07-31 at `main @ d0735433`: added §@experimental consumers beyond D/E/F (escort right-sizing, contest-aware support, threat-reactive SR purchasing, transport standoff drop — all verified against code this pass); added the Stage-C `IsFrontlineEdge` frontline-contour primitive + the `MapStartingLocations`/`HomeLocation` seed-collapse trap; refreshed the `MountedTransportBotModule` line refs in §Known gaps (now a hybrid: omniscient candidate + fog-legal danger post-adjust).
>
> Curation update 2026-08-03 at `main @ 62ad3908`: **corrected the participation rule — `@stable` now PARTICIPATES** (2026-08-02 parity promotion; `InfluenceStack.cs:47-48`), so the old "`@stable` excluded / never builds a field" claim was wrong; added §Frontline reachability & staging (Phases 0–4: CrossingMap, through-crossing distance, forward staging, retreat damper, frontline strength profile) and the per-player-opt-in gating recipe it forces.
>
> Curation update 2026-08-03 (follow-up, `main @ 16affd44`): extended §Frontline reachability & staging with **Phase 5** (man-the-line allocation + weakest-point bias + sector posture-hold — the posture-hold-safety-is-positional discipline) and **Phase 6** (`EngineerRouteOpenBotModule` — ledger-committed engineer bridge-repair route-opening); added the P5/P6 consumer rows. The `EnterBehaviour.Dispose` engineer-consumed-at-hut-entry gotcha went to [conventions.md §Engine behaviors that surprise](conventions.md). Verified `FrontlineAllocationMath.cs`, `PoiOffensiveBotModule.cs:1244/:1622/:1639/:1653/:1671`, `EngineerRouteOpenBotModule.cs`, `RepairsBridges.cs:31/:66-72`, `LegacyBridgeHut.cs:32`, `BridgeHut.cs:240`, `CrossingMap.cs:744-745`, `ControlField.cs:829/:977-979`.
>
> Curation update 2026-08-04 at `main @ 26ebe206`: **corrected the P5 sector-posture-hold claim** — posture is evaluated at the axis's own CONTACT sector with an own-strength floor, NOT its target sector (the target-sector form vetoed every offensive axis on every map; fixed `7baa3885`); added **Phase 7** (`LateralSpreadMath` SR-Pressure share cap + opportunity-weighted spread, `ForwardMuster` per-axis muster anchor) with the allocator mechanism that made the funnel possible; **restated the §Known-gaps item-24 line to code truth** (A/B run + KEEP-OFF recommendation vs gates committed ON at HEAD, disposition user-owned) and corrected the now-false "@stable is item-24-off / byte-identical" claim; refreshed stale `ai.yaml` line refs (the file lives at `mods/ww3mod/rules/ai/ai.yaml`). Verified `FrontlineAllocationMath.cs:179-203`, `LateralSpreadMath.cs`, `PoiOffensiveBotModule.cs:464/:469/:480/:492/:505/:521/:1398/:1619-1632/:1793/:1810/:1824/:1841/:2622-2637/:2773-2791`, `rules/ai/ai.yaml:159/:315/:377/:454-463/:498/:1216/:1262/:1294`.
>
> Curation update 2026-08-09 at `main @ 25a8aebd` (doc-reconciliation pass): **corrected sixteen stale code anchors** flagged by [`DOCS/bots/04` §9](../bots/04-perception-and-fields.md) and [`06` rank 21](../bots/06-inherited-misfits.md) — twelve `ControlField.cs` anchors (`CellSize` `:177`→`:389`, `GrayBand` `:205`→`:417`, `ScoreAt` `:531`→`:879`, `IsSiteAnchor` `:493-501`→`:841-849`, `ApplyAnchors`, `StampAnchor`, `AnchorRadiusCells`, `IsVerifiedFresh`, `IsFrontlineEdge`, the stagger offset ×2, `ControlFieldMath`'s span), the Stage-F believed-danger thresholds (`PoiOffensiveBotModule.cs:188/:192`→`:228/:232`), `GarrisonBotModule:102`→`.cs:219`, `StrategicCaptureRepointEnabled` `:155`→`:184`, and `sizeThreatThreshold` `:289`→`:297`. **All the underlying values were, and remain, correct — only the coordinates had drifted.** No claim changed.
>
> See also [`DOCS/bots/04` §8.4](../bots/04-perception-and-fields.md) for a scoping qualification on the §Invariants headline *"do NOT gate on `Participates`"*: that rule is correctly scoped to a lever that must not move the benchmark control, and CLAUDE.md settles that `@stable` otherwise **inherits improvements**, so for an ordinary fix `Participates` is the right gate.

> Curation update 2026-08-09, **landed on `main`** as `6fc1cfff` → `1092573d` → `c69835eb` (reconciled `5642d931`; verified at `main @ af36e686`) — this is merged work, not a pending branch: **Stage-B intensity is no longer what this doc described, and every threshold consuming it changed units.** `DangerKernelMath` had modelled a weapon's fire cycle as `damage x Burst / ReloadDelay`, which mis-states WW3MOD by ~130x for the ~90% of weapons paced by `BurstWait` and ~5x the other way for the ~14 paced by `ReloadDelay`+`Magazine`; `WeaponThroughput` now derives the real cycle from `Armament` (`CanFire` blocks on `IsReloading || IsWaitingBurst`, both counters tick in parallel, so the cycle is the MAX of the two, and `Magazine` counts SHOTS). Consequently **every level threshold on the danger field is now expressed in DANGER UNITS** (100 = the core intensity of the median ground-threatening actor type at full confidence — `DangerKernelMath.ReferenceIntensity`), converted at each call site by `DangerFieldLayer.GroundDangerUnitsToField` / `AirDangerUnitsToField`, and the fields were renamed to carry the unit (`*Threshold` → `*Units`). Two thresholds stay on the RAW scale on purpose and are documented in place: `GarrisonBotModule.MinBelievedDanger: 1` (a scale-free presence test) and `HelicopterSquadBotModule.AirDangerSafeThreshold: 0` (the air channel carries no baseline, so literal 0 is meaningful at any scale). The threshold VALUES are provisional — they were derived from kernel geometry, not measured, because the only recorded distribution predates the cadence fix; `[danger] reference` and `[danger] dist` are unconditional so a single play session can validate them. Line numbers elsewhere in this doc predate this work and were not re-verified. **The split to hold on to:** the field's own side (formula, per-type intensities, per-cell steps, the weapon-class RANKING) is settled and statically re-derivable — worked at [`DOCS/bots/04` §3.2](../bots/04-perception-and-fields.md). Any conversion BETWEEN a configured threshold and a raw field value is not, because `ReferenceIntensity` is a ruleset-wide median resolved at world load.

The influence stack is the fog-respecting bots' **fog-legal strategic substrate**: a per-player memory of where the enemy has been seen, projected into danger and territory fields, then consumed by the offense/heli/truck layers to route around threats and repoint attacks — all without reading ground truth through the fog. It is built in layers that are each **inert data** until a later stage consumes them. Byte-identity is guaranteed for every **non-participating** actor by construction; the participants are the two fog-respecting bots (`@experimental` + `@stable`) plus humans (fields for the overlay only, no behavioural consumer). `@stable` is a **frozen validated snapshot** of `@experimental` — since the 2026-08-02 parity promotion it participates and sets the same fog-legal consumer flags, and the tournament A/Bs live `@experimental` against it.

Stage naming (as in the source file headers): **0** heli standoff (engine-provided seam), **A** belief store, **B** danger fields, **C** control field + overlay, **D** heli danger nav, **E** ground danger nav, **F** strategic repoint.

## Participation & staggering — the shared gate

`InfluenceStack` (static helper, `InfluenceStack.cs`) is the single place that decides *who* gets a field and *when* it recomputes, so all three world layers narrow to the same participant set.

- **`Participates(player)`** (`InfluenceStack.cs:42-52`): a player reads the stack iff it is a combatant AND either a **fog-respecting bot** — `@experimental` OR `@stable` (`player.BotType == "experimental" || "stable"`, `:47-48`) — or a **human combatant** (`player.Playable`, `:51`). **`@stable` participates since the 2026-08-02 parity promotion** (`ExperimentalBotType`/`StableBotType`, `:35-36`, with the rationale inline at `:30-34`): it is a frozen validated snapshot of `@experimental`, so it builds the same fields and sets the same fog-legal consumer flags — the tournament A/Bs live `@experimental` against it. Only legacy/other bot profiles and non-combatants are excluded → they never build a field → byte-identical. Humans participate because the overlay is a human-facing read; the choice is a **sim-legal proxy for "the overlay viewer"** — it deliberately never reads `world.RenderPlayer` (that would make simulation depend on the render path and desync; see the header comment `:1-21`).
- **Staggering** (`GatherParticipants` `:52-58`, `SubInterval` `:62-69`): each layer round-robins **one participant per sub-slot** so no single tick rebuilds every player's field. Over `UpdateInterval` ticks each participant is refreshed exactly once.

## Stage A — belief store (`BeliefStore.cs`)

Per-player table of **believed enemy contacts** — the commander's memory of where the enemy was seen, not ground truth. Generalises the engine's `FrozenActorLayer` (which persists *structures* under fog) to mobile units, with confidence on top.

- **Contact lifecycle** lives in the pure, world-free `PlayerBeliefContacts` (`:61-114`, split out for NUnit like `UnitRoleResolver`'s facts/Classify split). Each recompute: `InjectLive` → `InjectFrozenStatics` → `ResolveUnobserved` (`:199-201`).
- **Fog discipline (no cheating, by construction):** live sightings come only from enemy actors the player can legally see right now — `Actor.CanBeViewedByPlayer(player)` + a `HealthInfo` filter to skip crates/props (`InjectLive`, `:216/:219`). Remembered statics come only from the player's own `FrozenActorLayer.FrozenActorsInRegion(…, onlyVisible: true)` — the fog-frozen last-seen set (`InjectFrozenStatics`, `:232`).
- **Static vs mobile decay is the load-bearing split.** Structures/defences are `IsStatic` (no `MobileInfo`, no `AircraftInfo`, `:273-278`) and are **exempt from decay** — they persist until verified gone, refreshed at the `FrozenConfidence` ceiling (60, `:132`) while their frozen ghost shows. Mobiles are NOT refreshed from ghosts; they **decay** by `MobileDecayPercent` (75%, `:136`) each recompute and drop below `MinConfidence` (15, `:139`) — "assumed still there, fading" (`DecayUnrefreshed`, `:98-113`).
- **Verified-clear:** a contact whose cell is currently visible (`player.MapLayers.IsVisible(c.Cell, 1)` — the `1` = "currently unfogged" threshold) with nothing live there is removed immediately (`ResolveUnobserved`, `:263`). A commander does not claim a cell he can see is empty.
- Fresh sighting confidence = 100 (`:126`); `UpdateInterval` = 25 ticks (`:123`); deterministic first-tick stagger offset = **0** (`:168`, NOT a `SharedRandom` draw — see Invariants).
- **Inert in Stage A:** pure data; the query seam is `Contacts(player)` (`:289`). Only Stage B reads it.

## Stage B — per-domain danger fields (`DangerFieldLayer.cs`)

Two per-player threat-projection channels stamped from the belief store: **ANTI-GROUND** (`DangerChannel.Ground`) and **ANTI-AIR** (`DangerChannel.Air`, `:51`). A contact contributes to a channel only if it carries a weapon that can hit that domain; each contact stamps a radial kernel whose **radius ← weapon range**, **intensity ← damage throughput × durability/cost weight × confidence**.

- **Air-channel discriminator** (`DangerKernelMath.WeaponThreatensAir`, `:135-138`): keys on `ValidTargets.Contains("Helicopter") || Contains("Air")` — a ground MG that lists `Helicopter` genuinely threatens our helis. This is **deliberately broader** than `UnitRoleResolver`'s `ShortRangeAD`, which keys on `"Air"` alone to find dedicated SAMs. *Danger = "what can shoot me down"; AD role = "what is a SAM".*
- **Ground discriminator** (`WeaponThreatensGround`, `:142-149`): any `ValidTarget` that is not an air-domain type. Air-domain types are `{Air, Helicopter, ICBM}` (`:130`) — the `ICBM` exclusion stops SAM/CRAM/interceptors (which carry `"Air, ICBM"`) stamping a spurious anti-ground aura at full AA range.
- **Kernel math** (`Compute`, `:153-175`): `radius = range/1024 + RangeBufferCells` capped at `MaxRadiusCells` (32); `intensity = throughput × durabilityWeight/DurabilityBase × confidence/100`, where `durabilityWeight = DurabilityBase(100) + HP/HealthDivisor(10) + Cost/CostDivisor(50)` — a tank's aura is denser than a rifleman's at equal throughput. Facts are read from **actual armament data at map load** (`ExtractKernelFacts`, `:481-517`; `WeaponInfo` resolved by `WorldLoaded`) — never hard-coded per unit.
- **Stamp** (`:350-379`): linear falloff `intensity × (r−d+1)/(r+1)` (`ISqrt` radial distance), **additive** across contacts so stamp order does not matter.
- **The territory baseline is GROUND-ONLY** — the single most load-bearing Stage-B fact. `DangerKernelMath.BaselineChannels(contribution) => (contribution, 0)` (`:182`): the baseline derives from believed enemy *ground* weapon reach, so it must not paint the air channel. `StampBaseline` writes both channels but the air term is 0 (`:453-457`). This is what lets Stage D use `AirDangerSafeThreshold = 0` as a literal "outside every believed AA envelope" test — a non-zero `AirDanger` reading means a believed weapon can *actually* shoot a heli there, not merely "near enemy ground" (the invariant is flagged in the code at `:177-181`).
- **Territory baseline projection** (`ProjectTerritoryBaseline`, `:390-420`, a Stage-C seam so it needs the control field): wherever the player believes the enemy *holds* ground (`ControlOwner.Enemy`), project a low-intensity danger (`BaselineIntensity` = 5, `:217`) outward to the believed enemy ground-weapon envelope — the "a spotter/drone could arrive" danger. It is **data-driven** — the envelope is the longest ground range among current believed contacts (`BelievedEnemyGroundEnvelopeCells`, `:466-477`), never a hard-coded "arty = 40 cells" (the fallback knob `BaselineFallbackEnvelopeCells` defaults **0 = off**, `:223`). Stamped only from the **frontier** of enemy territory (`IsBelievedEnemyFrontier`, `:424-430`), capped at `BaselineMaxProjectionCells` (24). Because it stamps **additively** across every frontier cell, a dense sector's baseline **stacks** and can exceed 40 easily — do not assume it is a small constant (this is why Stages E/F need thresholds *above* it and a strict-improvement rule, below).
- `UpdateInterval` = 25 (`:192`); deterministic stagger offset = **`UpdateInterval/3`** (`:292`). Query seam: `GroundDanger` / `AirDanger` / `Danger(channel)` (`:543-560`), `ActiveCells` (`:574`).

## Stage 0 — heli standoff (engine seam, `HelicopterStates` / `HelicopterSquadBotModule`)

Not part of the field pipeline, but the seam Stage D rides on. The engine *already* stands a hovering/sliding attack heli off at weapon range (`FlyAttack.cs:183-198`, `Fly.cs:187-190` — see [`architecture.md` §Attack standoff](architecture.md)); heli **overflight** was a bot-order artifact (a bare `Order("Attack", …)` at a single distant target). Stage 0 fixes it with a default-off `StandoffEngagement` flag (`HelicopterSquadBotModule.cs:64`): when on, the squad FSM issues `AttackMove` toward the target cell so `AutoTarget` engages the nearest in-range threat at standoff and only advances when clear (`HelicopterStates.cs:152-155/:395`). Guard: `BusyAttackMove` (`HelicopterStates.cs:213`) detects the nested `AttackMoveActivity` so re-issuing the order each tick doesn't cancel an in-progress `FlyAttack`. Off ⇒ `@stable`/controls/human byte-identical.

## Stage C — control field (`ControlField.cs`) + danger overlay (`DangerOverlay.cs`)

### Control field: believed territory

Per participating player, every coarse grid cell (`CellSize` = 2 map cells, `:389`) carries a believed-ownership **score**: `> 0` ours, `< 0` enemy, `|score| ≤ GrayBand` contested. Pure math in `ControlFieldMath` (`:93-238`, split for NUnit).

- **`ScoreAt` sign convention** (`:879`): **positive = ours, negative = enemy, 0 = no field**. `OwnerAt` buckets it via `Classify` (`:146-154`): `> GrayBand` → `Own`, `< −GrayBand` → `Enemy`, else `Contested`. `GrayBand` = 150 (`:417`).
- **Seed:** Voronoi by proximity to each player's public home beachhead (`SeedVoronoi` from `HomeLocation`, `:347-367`; `SeedScore` `:98-109`) — nearest home owns the cell at `±SeedStrength` (500, `:184`), a tie reads the contested midline (0). Home positions are public map facts, so seeding is not a fog leak. **Synthetic-scenario trap:** a map/rules block that strips `MapStartingLocations` leaves the spawn assigner null, so `Player.HomeLocation` falls back to `PlayerReference.HomeLocation` = `CPos.Zero` for BOTH sides (`Player.cs:182`); every cell is then equidistant from (0,0) ⇒ seeds Contested everywhere and the whole-map Voronoi wash vanishes, leaving only presence + site-anchor cells painted. Real play always assigns spawns, so this only bites demos — set `HomeLocation: x,y` explicitly per `PlayerReference` in the demo `map.yaml`.
- **Presence update** (`GatherPresence` `:386-420`, `UpdateScore` `:112-132`): own/allied **armed** units (`AttackBaseInfo` or `AutoTargetInfo`, `:402` — trucks/civilians don't hold ground) paint toward + (`+PresenceGain` = 250, `:190`); believed enemy contacts (from the belief store *only*, `:411-419`) erode toward − and eventually flip the sign (capture). Both present → actively contested, bled toward gray at `ContestErodePercent` (40%). Verified-clear → relax to gray immediately (`VerifiedClearErodePercent` = 100). No evidence → lingers, decaying at `PersistDecayPercent` (8%) so the front doesn't flicker. Clamped to `±MaxScore` (1000, `:187`).
- **Site anchors — the anchor-floor semantics (load-bearing for Stage F).** `IsSiteAnchor` = a non-mobile structure carrying `SupplyProviderInfo` OR `CaptureManagerInfo` (`:841-849`) — Supply Routes and capturable income/utility (derricks). Each recompute, `ApplyAnchors` (`:717-760`) re-asserts: home beachheads + own/allied site structures as **self** anchors, and **believed-enemy** site structures (from the belief store, fog-legal) as **enemy** anchors. `StampAnchor` (`:762-786`) floors a disc out to `AnchorRadiusCells` (4, `:423`): self anchors `Math.Max(score, +strength)`, enemy anchors `Math.Min(score, −strength)` (`ApplyAnchor`, `:137-143`), tapering `AnchorStrength(800) × (r−d+1)/(r+1)`. At grid distance 4 the taper still reads ≈ **−160** — below `−GrayBand` (150) — so **the whole radius-4 disc around any enemy site reads Enemy regardless of who surrounds it.** This is *why* Stage F must read the surrounding ring, not the target cell (below).
- **Verification memory:** `LastVerified[gx,gy]` records the tick a cell was last observed; `IsVerifiedFresh` (`:902-910`) returns true only within `StalenessWindow` (500 ticks) — the overlay's perishable green→gray relax.
- **Frontline contour = a binary half-plane split, not a sign flip** (`ControlFieldMath.IsFrontlineEdge`, `:223`). An edge between two grid cells is a frontline edge iff exactly one side classifies `Enemy`: `scoreA < −GrayBand != scoreB < −GrayBand`. Keyed on enemy-vs-not-enemy (believed-ours AND the contested no-man's-land both count as "our side") on purpose — the verified-clear rule relaxes every observed-empty cell to 0, opening a wide neutral buffer between the armies, and a strict ± sign-flip contour vanishes in that buffer (0 is neither sign), whereas the half-plane boundary is always a continuous closed contour around the believed-enemy region. Consumed **render-only** by `TerritoryOverlay` (hold-`T`) and `DangerOverlay` Control mode (`TerritoryOverlay.cs:241/:248`, `DangerOverlay.cs:178/:184`); no sim consumer.
- `UpdateInterval` = 25; deterministic stagger offset = **`UpdateInterval/2 + 1`** (`:542`).

### Danger overlay (`DangerOverlay.cs`) — dev, render-only

`/danger` chat command (`CommandName` = "danger", `:46`) cycles **Off → DangerGround → DangerAir → Control → Off** (`:75/:104`). Primary read is **safety, tri-state** (`DangerColor`, `:152-162`): danger > 0 → **red** (own units in a danger zone read red too); else recently observed (`IsVerifiedFresh`) → **green** (verified safe); else → **gray** (unobserved). `Control` mode demotes the control field to a secondary owner-colour/margin wash (`ControlColor`, `:165-181`). Render-only: `world.RenderPlayer ?? world.LocalPlayer`, reads the viewer's own layers (`:115`) — `RenderPlayer` is legal here because this is NOT sim code; OFF by default, never ships on hold-Space.

## Stage D — heli danger nav (`HeliDangerNav.cs`)

Pure-function navigation math over an **anti-air** danger sampler (`Func<CPos,int>`), split from `HelicopterStates` like the math classes above. Rides on Stage 0: `avoid = standoff && DangerFieldAvoidanceEnabled(owner)` (`HelicopterStates.cs:372`) — with standoff off there is no attack-move destination to steer, so avoidance is inert rather than conflicting. Gated by default-off `DangerFieldAvoidance` (`HelicopterSquadBotModule.cs:71`).

- **AA-safe leash** (`LeashedEngageCell`, `:71-95`): the cell nearest the target (expanding Chebyshev rings, fixed scan order) whose air-danger ≤ `AirDangerSafeThreshold` — fire from the AA-safe edge instead of diving onto a target buried in AA. The threshold defaults **0** (`HelicopterSquadBotModule.cs:75`), meaningful *only because* the air channel carries no baseline (Stage B).
- **Lateral detour** (`DetourWaypoint`, `:102-142`): when the straight approach's max air-danger (`PathMaxAirDanger`, `:47`) exceeds the threshold, pick the perpendicular midpoint offset (±`lateralCells` × {1,2}) whose two-leg worst-case is strictly lower — route around the SAM, don't fly through it.
- **Spike withdraw** (`SafestAirCellOnRing`, `:148-173`): the lowest-air-danger cell on a Chebyshev ring — a deterministic retreat toward the least-covered heading when a new AA lights up.
- Off-map/invalid cells are fed as `Impassable = int.MaxValue` (`:32`) so no search steers off the playable area. **Zero random draws** (see Invariants).

## Stage E — ground danger nav (`GroundDangerNav.cs`)

The Stage-D pattern for **ground** movers, deliberately kept a **separate** pure class (HeliDangerNav stays byte-identical). Two perceived behaviours reduce to ONE decision — `DetourWaypoint` (`:91-141`):

- **Flow around strongpoints** and **high-value movers pull back / go lateral / re-enter** are the same function: when the straight route's max ground-danger (`PathMaxGroundDanger`, `:58`) exceeds `safeThreshold`, insert the perpendicular waypoint (±`lateralCells` × {1..`maxSteps`}) whose two-leg worst-case is **strictly less** than direct (`worst < best`, seeded at `best = direct`, `:110/:132`).
- **The rear-lateral route EMERGES from the Stage-B/C gradient — it is not scripted.** Because the ground field carries the territory baseline (deep believed-enemy ground reads expensive, the friendly rear ~0), the exposure-minimising side of the detour *is* the rear; a larger `maxSteps` just pushes the safe waypoint deeper. There is no rear-seeking code.
- **Correctness against the ambient baseline is the strict-improvement rule, not the threshold.** The baseline stacks additively and will exceed any fixed threshold in a contested approach, so the threshold is only a cheap early-out. Where danger is roughly *uniform* every lateral candidate reads the same worst-case as direct, nothing beats it, and the function returns null → go direct. A dogleg is emitted only where a genuine local peak makes a lateral lane measurably cheaper.
- **Waypoint passability is guarded, the line-walk is not.** A rear water/cliff cell reads `GroundDanger 0` (maximally "safe") and would be actively preferred, then the Move no-ops; so the chosen waypoint cell is filtered through a `waypointPassable` predicate (`:125`, a locomotor terrain check on the WAYPOINT ONLY). The line-walk sampler is deliberately left terrain-blind (feeding impassable line cells as Impassable would make a route merely clipping a water edge read max-exposed — the declared v2 terrain-flow problem), so a route whose straight legs cross unreachable ground is a documented v1 limitation left to the pathfinder.
- **Implementation is a two-leg queued order, not A* cost-field integration.** The consumer emits `AttackMove(waypoint)` then `AttackMove(target, queued)` (Move for trucks), so the A* layer is untouched; cost per request is O(steps·pathLen) sampler reads at the module's slow re-eval cadence (PoiOffensive 100 ticks, SupplyFollower 150). Tradeoff: a coarse single dogleg, not a true least-cost path (v1).

## Stage F — strategic repoint (`PoiMap.cs` + `PoiOffensiveBotModule.cs`)

Completes the `@experimental` fog migration for **attack-axis selection and expansion**: drops the omniscient threat term from the base offense score and re-derives it from the **believed** control + danger fields.

- **The omniscient read that was migrated is the THREAT term, not POI discovery.** The single omniscient grid feeding attack-axis selection is `PoiMap.SampleThreat` → `InfluenceMap.GetEnemyInfluence` (`InfluenceMap.Recompute` scans `world.Actors` with no fog check). POI *discovery* (`PoiMap.Discover`) also scans `world.Actors` but only finds **static structures** whose locations are public map facts — so it is left as-is; the belief store re-derives mobile-unit threat.
- **Suppress-in-PoiMap, reshape-in-module.** `GetOffensiveTargets(perspective, bool suppressOmniscientThreat = false)` (`PoiMap.cs:297`): when true it sets `enemyInfluence = 0` → threat-neutral safe bucket AND skips the `GetEnemyInfluence` snapshot entirely (`:296/:351`), so the repoint path never touches the omniscient grid. **Default false ⇒ byte-identical** for every caller that doesn't opt in (control-bot `MountedTransportBotModule` ×2); since `stable-0802` **both** offense twins opt in (`StrategicRepointEnabled: true` — `@experimental` `rules/ai/ai.yaml:315`, `@stable` `:1262`). The base `ScoredPoi` score is a product `value × distFactor × threatFactor × ownershipMul` (`PoiScoring.Score`, `:596-597`) then `× bias/100` (`ApplyBias`, `:602-603`, the lossy truncation) — so suppressing at source and re-multiplying in the module beats dividing the omniscient factor back out.
- **Consumer** (`PoiOffensiveBotModule.RescaleByBelievedFields`, `:699-759`): gated `repoint = StrategicRepointEnabled && controlField != null` (`:504`, inert if no control field). Two pure factors from `PoiOffenseMath`, then `mul = bop × dangerMul / 100` (`:726`), re-sorted with the same `PoiScoring.CompareForOrder` comparator PoiMap uses.
- **Balance-of-power reads the SURROUNDING RING, not the target's own cell — and this is not optional.** Every Attack/Pressure target is a site-anchor structure whose own cell (and a disc out to `AnchorRadiusCells`) is floored to ≈ −800 by the enemy anchor (Stage C) — so `ScoreAt(targetCell)` is *always* deeply enemy and the boost could never fire. `NeighborhoodControlScore(scoreAt, gx, gy, radius)` (`PoiOffenseMath` `:1556`) averages the 8 cardinal+diagonal cells at grid `radius = AnchorRadiusCells + 1` (`:708`) — one cell past the anchor footprint, so the floored disc is **excluded entirely** (the target's own cell is never sampled). `BalanceOfPowerFactor(neighborhoodScore, grayBand, boost, damp)` (`:1575`) then buckets on the **same `GrayBand` the control field classifies by** (`:701`): ring `> +GrayBand` (we hold the surroundings → encircled → PRESS, boost) / `< −GrayBand` (deep enemy → damp) / contested → neutral 100. An off-grid ring sample reads 0 (biasing edge targets toward neutral — a safe direction).
- **Believed danger is on a DIFFERENT scale than InfluenceMap.** `BelievedDangerFactor(groundDanger, mild, hostile, safeMul, mildMul, hostileMul)` (`:1591`) reads `DangerFieldLayer.GroundDanger` at the target cell and buckets safe/mild/hostile. Its thresholds (`BelievedDangerMildUnits` = 30, `BelievedDangerHostileUnits` = 100, `PoiOffensiveBotModule.cs:236/:247`) are **IN DANGER UNITS since 2026-08-09** — 100 units = the core intensity of the median ground-threatening actor type at full confidence (`DangerKernelMath.ReferenceIntensity`), converted at the call site by `DangerFieldLayer.GroundDangerUnitsToField`. They are NOT raw field readings: expressed raw at 40/120 against a field whose stamped cells read in the thousands and up, the mild bucket was unreachable and the three-way split degenerated to binary. They still sit *above* the territory-baseline intensity so ambient "deep enemy ground" danger doesn't damp every axis — only a genuine believed weapon envelope does. Reusing the InfluenceMap `ThreatMildThreshold` (20) here would class almost everything hostile.
- **All sub-multipliers default 100 (inert)** (`BopBoostMultiplier`/`BopDampMultiplier`/`BelievedDanger*Multiplier`, `:177-204`), so a bare `StrategicRepointEnabled: true` changes only the threat *source*, not the ranking, until the YAML supplies active values. Current `@experimental` tuning (`rules/ai/ai.yaml:316-322`, 2026-07-24): boost ×150, damp ×60, believed-danger safe ×100 / mild ×60 / hostile ×20 (`DangerFieldRouting: true` at `:304`, `StandoffEngagement`/`DangerFieldAvoidance` at `:1059/:1063`).

## Invariants (load-bearing — do not break)

- **Zero `SharedRandom`/`LocalRandom` draws in the entire stack.** The always-on world layers (BeliefStore/DangerFieldLayer/ControlField tick for *every* profile) must not draw from the synced `SharedRandom` at load to self-stagger — that would shift the RNG stream for `@stable`/control games and break replay/benchmark byte-identity even though the trait is behaviour-inert. Instead each uses a **distinct deterministic offset**: BeliefStore **0** (`BeliefStore.cs:168`), DangerFieldLayer **Interval/3** (`DangerFieldLayer.cs:292`), ControlField **Interval/2+1** (`ControlField.cs:542`). The nav/scoring math is integer walks over fixed candidate orders with iteration-order tie-breaks — zero draws (`HeliDangerNav.cs:18-21`, `GroundDangerNav.cs`, `PoiOffenseMath` fixed `NeighborhoodDirections` `:928-931`).
- **Flags off ⇒ the pre-feature path — but that is a claim about the FLAGS, not about `@stable`.** Every consumer flag defaults off/inert and every `PoiMap`/nav seam has a default that reproduces the pre-feature path. **NOTE (`b8d2e601`, 2026-08-02): the `@stable` twin now SETS most of these flags**, so it is *not* byte-identical — only a profile that leaves a given flag unset takes that flag's default path. Check the `@stable` block in `rules/ai/ai.yaml` before asserting byte-identity for any specific flag; see the standing correction in `WORKSPACE/DISCOVERIES.md` (2026-08-11).
- **Two gating patterns — know which applies, and do NOT gate on `Participates`.** For a consumer on a **per-profile** trait instance (e.g. `PoiOffensiveBotModule@experimental` is already `RequiresCondition: enable-ai-experimental`, and `@stable` is a separate instance), a default-off flag set only on the experimental block is sufficient — the instance topology does the work. For a consumer bolted onto a **shared** instance (`SupplyFollowerBotModule@supply` on `enable-ai-any`), the flag alone is not enough, but **`InfluenceStack.Participates` is no longer the right second term**: since the 0802 parity promotion it returns true for `experimental` *and* `stable` (and every human combatant) — `InfluenceStack.cs:43-52` — so a `Info.Flag && Participates(player)` double-gate reaches `@stable` and does *not* preserve byte-identity. The house answer is an explicit bot-type conjunct: `CommitOnOrderMath.ShouldCommitShared(flag, resource, isExperimentalBot)` (`PoiGoalGuard.cs:300`), with `isExperimentalBot` from `player.BotType == InfluenceStack.ExperimentalBotType` (as `GarrisonBotModule.cs:219` does). **Any in-file comment that still cites the `participates` double-gate as a byte-identity guarantee is stale and load-bearing-false** — several survive, and three older `SupplyFollowerBotModule` flags (`SectorSpread` / `SmallSquadCoverage` / `DangerEvac`) genuinely do reach `@stable` today as a result. When a comment asserts byte-identity, re-derive the gate rather than trusting the sentence: a gate whose meaning is defined elsewhere can be widened out from under every comment that describes it.
- **The territory baseline feeds the GROUND channel only** (`DangerKernelMath.BaselineChannels`, `DangerFieldLayer.cs:182`) — the Stage-D air consumer depends on `AirDanger == 0` meaning "AA-clear".

## @experimental consumers beyond D/E/F

The nav/repoint stages (D/E/F) are not the only fog-legal consumers. Several later `@experimental` levers read the belief/danger/control fields directly. Each is a **default-off flag on a per-profile trait instance** (the first gating pattern above — `@stable`/normal never touch the field), all enabled in current `@experimental` tuning (`ai.yaml`, as of 2026-07-31):

- **Capture escort right-sizing** (`CaptureCoordinatorBotModule`, `EscortTierSizingEnabled`, `:188`). `ResolveEscortTier` (`:1018`) buckets a capture target None/Light/Full from the ring-averaged `ControlField` score + `DangerFieldLayer.GroundDanger` + distance-from-own-SR, via the pure `EscortSizingMath.Resolve`. It is **reduction-only** — applied only to targets NOT already flagged contested, it can only *lower* the escort (None ⇒ the technician goes alone, no combat units reserved) — so it composes with, never fights, the contest bump below. Freed units need no redirection system: escorts are issued as a tick-local grouped `AttackMove` and are never committed to the `PoiGoalGuard` ledger, so an idle escort is immediately re-eligible for the offense. Reads the **ring** at grid `AnchorRadiusCells + 1`, not the target cell (the site anchor floors its own disc — same geometry as Stage F).
- **Contest-aware capture support** (`CaptureCoordinatorBotModule`, `ContestAwareSupportEnabled`, `:163`). `IsContestedNeighbourhood` (`:944`) bumps the escort 2→4 and pre-summons defenders when a target's surroundings read contested. Its predicate is `GroundDanger > threshold` **OR** the control ring reads specifically `Enemy` — deliberately NOT "`OwnerAt != Own`": `ControlFieldMath.Classify` buckets score-0 (unobserved / no-man's-land) as `Contested`, so a "gray = contested" predicate would fire on nearly every open-ground derrick and collapse the very distinction the lever draws. (The believed-danger channel has no such problem — it reads above baseline only where a believed weapon envelope actually reaches.)
- **Threat-reactive SR-defense purchasing** (`AdaptiveProductionBotModule`, `SupplyRouteDefenseEnabled`, `:56`). Classifies believed contacts near any owned SR from `BeliefStore.Contacts` by their own actor type (Air / Armor / Infantry), accumulates believed value per class, and pre-buys the matched counter pool — bypassing the `MinEnemySightings` gate a 2-tank rush never trips. The belief store is the correct seam because the scalar `DangerFieldLayer.GroundDanger` deliberately discards attacker identity (a lone rifleman saturates any "armor" threshold; `WeaponThreatensGround` keys on the *target* domain, so an attack heli stamps the ground channel) — "what should I build to counter this" needs identity, which only the belief store retains.
- **Frontier-distance standoff** (`PoiOffensiveBotModule` + `HelicopterSquadBotModule`, `MinFrontierDistanceCells`, default 0). `ControlField.RecomputePlayer` computes a `DistanceToEnemyFrontier` multi-source BFS (seeded row-major from every `Classify==Enemy` cell, 4-connectivity, capped at `MaxFrontierDistanceCells`) at the tail of the score-field recompute — so it rides the same per-player `Participates` gate + `UpdateInterval` cadence with no new timer, and `@stable`/normal never build the array (they get no `PlayerControl`). Two consumers hold behind that front: the fires echelon anchor (`PoiOffensiveBotModule.cs:1028`) and the heli engage cell both reduce to one pure decision — `FrontierStandoffMath.RearwardSteps` (`FrontierStandoffMath.cs:59`) walks rearward along the away-from-target axis one coarse cell per hop until frontier distance ≥ min, bounded by a small step budget so it is never a free search. The step is **Chebyshev-scaled** (max-norm) so a diagonal hop still crosses a whole coarse cell instead of under-advancing ~0.7 of one. Off-grid cells read the same "far" sentinel a populated-but-enemy-free field gives, so `RearwardSteps` takes an explicit `onGrid` predicate and hard-stops at the boundary rather than trusting the sampler; `FrontierDistanceAt` returning that sentinel for no-field/off-grid ⇒ zero push ⇒ byte-identical until the field is populated.
- **Transport standoff drop** (`MountedTransportBotModule`, `BelievedDangerStandoff`, `:102`). The drop-off picker still selects its candidate from the omniscient `GetFrontline`/`GetFriendlyInfluence` (see Known gaps), but `ApplyStandoff` (`:621`) then backs the chosen cell out of the believed anti-ground danger envelope (`DangerFieldLayer.GroundDanger`) — a fog-legal *post-adjust*, an identity pass-through when the field is absent (`@poi`/frozen twin). Paired with a pickup corridor (`PickupCorridorCells`).
- **Heli transport drop-site reshaping** (`HelicopterSquadBotModule`, `RiskWeightedDropSite`, `:263`). The heli transport drop was the one transport drop still committing to a *raw* omniscient pick: `threatMap.FindWeakestEnemyCell` (`ThreatMapManager.cs:232`) scans every cell for the lowest total enemy Cost, so a lone cheap unit sitting deep behind the enemy SR reads as the globally "weakest" cell and the infantry drop lands unreachable/lethal (the opposite discipline from the `MountedTransportBotModule` post-adjust above). The fix keeps the omniscient weak cell — plus top `PoiMap` offensive POIs — as *candidates* but ranks them by a fog-legal `TransportDropSiteMath.ScoreDrop` (`:1022`), a **weight not a filter** (nothing hard-banned), over believed-enemy control DEPTH (`ControlField.ScoreAt` — the enemy-SR anchor floors a radius-4 disc, a strong "behind the enemy SR" negative), believed danger (`GroundDanger`/`AirDanger`), and Chebyshev distance from own SR. Candidate-source omniscience is precedented; only the reshaping term is belief-side, so the fog-legality invariant holds. A separate default-off `FlightPathHysteresis` (`:246`) damps the attack-heli approach/withdraw **order cadence** — it holds the committed leg until the recomputed destination shifts ≥ `FlightPathHysteresisCells` — without changing the destination logic (standoff/danger-nav/frontier), so the first-contact AA gate, strategic pin, and evacuation are structurally untouched.

## Frontline reachability & staging (Phases 0–7)

The radial danger/control fields are terrain-blind by construction, so a river-split front, crossings-as-chokepoints, and "where do idle units muster" were all unrepresented (the old §Known-gaps "terrain-aware v2"). Phases 0–7 add a terrain/frontier layer that the `@experimental` offense module reads. **Every lever is a default-off Info flag on `PoiOffensiveBotModule` and reproduces the frozen path when off** — so `@stable`/legacy stay byte-identical even though `@stable` now participates in the shared world layers.

- **CrossingMap** (`Traits/World/CrossingMap.cs`) — a map-static geometry layer built once at load. `CrossesGroundBarrier(a, b)` (`:459`) walks the straight SR→POI line and flags any cell whose terrain type is in `WaterTerrainTypes = {Water, River}` (`:336`) — keyed on **water terrain type, not foot-impassability**, so cliffs/walls are not misread as a barrier (`:373-377`). It exposes two crossing sources that avenue-mapping must union: **`CrossingCells`** (`:453`) = intact land spans across water, and **`Crossings`** (`:427`) = `GroundCrossing` structs for *repairable/destroyed-bridge* huts (their cells are water, so the span detector never adds them — they live only here, carrying the hut's `CellX/CellY`). `CrossingMapInfo.GroundLocomotor` (`:324`, default `"tracked"`) picks the reference locomotor for component classification.
- **Locomotor water table is the load-bearing subtlety** (`mods/ww3mod/rules/world.yaml`). "The river as a barrier" is NOT a component-count of the army locomotor, because the common ground locomotors treat water differently: `Locomotor@TRACKED` (`:153`) has `River: 10` (nonzero ⇒ **fords rivers**), `Locomotor@LIGHTTRACKED` (`:126`) has `RiverShallow: 75` but **no `River`** (river-blocked — this is the bulk of the offense free pool: bradley/m109/m270), and `Locomotor@FOOT` (`:28`) has **no `River`/`Water`** (deep water impassable) but `Rock: 50` (crosses mountains). So the `tracked` graph over-estimates reachability for the real army, and mountains — not the river — split the `tracked` graph. Barrier detection therefore keys on the water TERRAIN TYPE directly rather than any locomotor's passability. On a river map where all POIs sit in one fordable `tracked` component, the *binary reachability* factor is inert and the active lever is DISTANCE (next bullet).
- **Through-crossing distance** (`PoiReachabilityMath.ThroughCrossingDistanceCells`, `Traits/BotModules/PoiReachability.cs:71`) replaces a far-bank POI's crow-flies distance with the SR→crossing→POI detour (`sum of two legs, clamped ≥ direct`). Wired into scoring as a gated `Func<CPos,int?>` provider built at `PoiOffensiveBotModule.cs:768-774` (guarded `ReachabilityGatingEnabled && crossingMap != null`, default false `:214`) and passed into `PoiMap.GetOffensiveTargets` (`:778`); **null ⇒ exact Euclidean ⇒ byte-identical off**. This de-concentrates the axis distribution: far-bank central-crossing POIs stop scoring as if adjacent, so the bot secures its own bank + pressures the enemy SR instead of lunging across the river.
- **Forward staging** (`ForwardStagingMath.StagingCell`, `Traits/BotModules/ForwardStagingMath.cs:88-138`; gate `ForwardStagingEnabled`, default false `:363`). Musters the idle free pool forward off the SR road by **steepest descent on `ControlField.FrontierDistanceAt`** (the frontier-distance BFS gradient — the SR sits deep in the rear at large distance; each descent step goes toward the nearest front), halting `StagingStandoffCells` short of the line and never stepping into a danger cell above threshold. A **flat field** (no believed enemy) has no improving neighbour ⇒ returns the SR ⇒ inert. This is NOT `FrontierStandoffMath.RearwardSteps` (which walks *away* from a known target); the free pool is target-less, so the descent gradient is the natural primitive. **Correction to earlier research:** the free pool is not a standing reservoir — `AllocateProportional` drains it to 0 most evals (`free==0` logged every eval in the small-army case), so staging only bites when units are WITHHELD (a losing axis skipped by `NoReinforceLostFights`, an under-min release, a retreating batch no axis reclaims) — e.g. large late-game armies or a sustained losing axis.
- **Retreat-oscillation damper** (`RetreatDamperMath`, `Traits/BotModules/RetreatDamperMath.cs`; gate `RetreatDamperEnabled`, default false `:399`). The base retreat FSM (`CombatRetreatMath.Step`) already had ENTRY hysteresis (a sustained losing streak + asymmetric re-engage band), so the damper does NOT touch entry. It adds the missing anti-oscillation pieces: a post-retreat **re-advance DWELL** and an **advance-strength floor**. The load-bearing safety property: `StepReadvanceHold` returns 0 whenever the axis is Retreating (`:55-56`) — the damper can only delay a RE-advance or filter massing, **never delay a genuine withdrawal**. Two shapes the strength floor taught, both worth carrying to any future hold: **(1) a gate comparing a per-axis quantity against a global constant needs a proof the quantity can ever reach it.** `MinAdvanceStrength: 1200` is on the health-weighted build-value scale while `AllocateProportional` may only ever fund an axis to 2–3 hulls (~900), so the bar stayed true all match and the axis parked in the rally bubble — a permanent veto wearing the costume of a delay (measured: 34 damper-hold fall-backs with **zero** genuine-retreat events). It is now read as **fill completion** — hold only while the force the allocator actually allocated has not all arrived — plus a `MaxAdvanceHoldEvals` backstop, both failing OPEN toward advancing. **(2) "bounded per occurrence" is not "bounded" — check what resets the counter.** `StepFillHold` originally zeroed the budget on the release eval, refunding it instantly, so a still-massing axis ping-ponged hold×N / advance×1 forever; the budget is now spent once per massing EPISODE (persisting across the release while `nearRally && FillIncomplete`). Note also that the hold's own trigger, `NearRally`, is a bare rally-proximity test, so this lever is in practice a **spawn-flow gate** whatever its design intent — when auditing "does anything delay fresh reinforcements", grep hold predicates for rally/SR-proximity terms rather than for intent words like *muster* or *staging*.
- **Frontline strength profile** (`FrontlineProfileMath`, `ControlField.cs:280`; gate `FrontlineProfileEnabled`, default false `PoiOffensiveBotModule.cs:420`). An explicit per-sector OWN/ENEMY strength profile + weakest-enemy-sector query built on top of the control field. **This is the case that forces the gating recipe below** — see next section's note.
- **Man-the-line + weakest-point + sector posture-hold** (`FrontlineAllocationMath`; three default-off gates on `PoiOffensiveBotModule` / `LayeredDefenceBotModule`). Three numbers-only decisions over the Phase-4 profile: (a) **man-the-line** (`AllocateAcrossAvenues`, consumed at `LayeredDefenceBotModule.cs:585`) spreads a reserve budget across crossing avenues — COVERAGE first (every avenue whose believed enemy strength ≥ `ManTheLineMinThreat` gets one picket, enemy-desc/index-asc order) then largest-remainder (Hamilton) MASS weighted by `max(0, enemy−own)`, so surplus concentrates where we are outnumbered while a sector we hold keeps only its picket; (b) **weakest-point bias** (`FrontlineAllocationMath.WeakestSectorBiasFactor`, gate `WeakestPointBiasEnabled` `:464`, strength `WeakestPointBiasMultiplier` `:469`) a ×100 score MULTIPLIER (inert at 100) boosting an axis whose target sits in the believed-thinnest enemy sector; (c) **sector posture-hold** (gate `SectorPostureHoldEnabled` `:480`) HOLDS an axis standing in a frontier sector whose believed enemy force is ≥ `SectorPostureHoldRatioPct`% (default 200) of our own believed strength **there**. **Posture is evaluated at the axis's own CONTACT sector — its unit centroid (`PostureShouldHold`, `:2622-2637`) — never its target sector, and is floored by `SectorPostureHoldOwnFloor` (default 3, `:492`).** Both guards exist for one reason: the ratio test `enemy ≥ own × pct` degenerates to `enemy ≥ 0` when believed `own ≈ 0`, and our believed presence in the enemy REAR — exactly where an enemy-SR Pressure axis points — is always ~0 under fog. Evaluating the *target* sector therefore vetoed every offensive axis on every eval and on every map (not a map-specific quirk), marching the whole army home; the own-strength floor is the backstop that stops a sector we do not occupy from ever reading as a hold (`FrontlineAllocationMath.cs:179-203`). Diagnosed live and fixed in `7baa3885`. **The posture-hold "never blocks a genuine retreat" guarantee is POSITIONAL, not conditional:** `CommitAndOrder` runs three fall-back gates *in order* — genuine retreat (`:2483`, RETURNS for a `Retreating` axis) → retreat damper (`:2519`) → posture-hold (`:2533`) — so placing posture-hold AFTER the retreat gate that already returned means it can only ever fire on a NON-retreating axis, with no re-check. All three reuse the SAME `OrderRetreat(ResolveMusterAnchor(axis) ?? stagingAnchor ?? rallyCell)` grouped fall-back (one order stream that composes with the retreat FSM instead of fighting it) and must RETURN before the mission-commitment snapshot (`:2547`), or a held axis marked `Committed` would let `PartitionHeldAxes` freeze it holding forever. **These gates are early `return`s, so removing one is a RE-ROUTE, not a subtraction** — the axis falls through to the next gate, and to `ApplyMissionCommitment`, which can stamp it `Committed` at partial fill. Audit the successor gate before claiming a gate removal only takes behaviour away. A frontier sector is a pure column-bucket of a map-cell X (`SectorOfCell` → `FrontlineProfileMath.SectorOfMapCellX`, `:1689`), needing nothing from the ControlField internal grid — weakest-point buckets the axis TARGET cell, posture-hold the axis CENTROID. `ControlField.AvenuesForSector` ALLOCATES a fresh `List` per call (`:977-979`), so the man-the-line consumer reads `ControlField.Avenues` (map-static, deterministically ordered) ONCE per scan and buckets in one pass (`TryBuildAvenueData`), never per-unit.
- **Engineer route-opening** (`EngineerRouteOpenBotModule`, gate `RouteOpenEnabled`, default false; declared only in the `@experimental` block). The one Phase that MANUFACTURES a new land axis rather than spending the force it has: when the weakest enemy sector's avenue is a REPAIRABLE destroyed bridge (`CrossingStatus.Repairable`), dispatch an `e6` (`RepairsBridges`) with the `RepairBridge` order plus a small free-pool combat screen, stake both in the shared `PoiGoalGuard` ledger (engineer under `bridge-repair:<hutId>`, each screen unit under `bridge-screen:<hutId>` — the same `CommitOnOrderMath.ShouldCommit` gate every executor uses), and on success release everyone so the now-intact avenue folds back into Phase-5 man-the-line for free (no separate attack path). Opts THIS player into the frontline profile via `RequestFrontlineProfile` (the per-player recipe below). Two load-bearing facts: **(1)** a repairable avenue's `Cell` IS the bridge hut's `Location` — `CrossingMap.RevalidateCrossings` records the repairable `GroundCrossing` with `CellX/CellY = hut.Location` (`CrossingMap.cs:744-745`) and `ControlField.AddAvenue` (`:829`) carries it into `FrontlineAvenue.Cell`, so the hut is resolved by exact `a.Location == avenue.Cell` over `world.Actors` filtered on `LegacyBridgeHutInfo`/`BridgeHutInfo`, no hut→avenue map; the engineer is identified by TRAIT (`HasTraitInfo<RepairsBridgesInfo>()`), never a name list. **(2)** the `e6` uses `EnterBehaviour.Dispose`, so it is CONSUMED at hut-entry while the bridge repairs — the mission must gate "repair in progress" BEFORE its engineer-died failure check (same ordering discipline as posture-hold); see [conventions.md §Engine behaviors that surprise](conventions.md).
- **Lateral spread + forward muster** (`LateralSpreadMath`, gates `LateralSpreadEnabled` `:505` / `ForwardMusterEnabled` `:521`). Fixes the *other* half of the mid-map pooling: the axis SIZE allocator is min-fund-then-remainder-by-score (`PoiOffenseMath.AllocateProportional`, `PoiOffensiveBotModule.cs:2773-2791` — every axis gets `MinAxisSize`, then the WHOLE remainder is apportioned by score with a largest-remainder rule), so any large score multiplier collects the bulk of the army onto one axis. With `SrPressureScoreMultiplier: 260` (`RescaleSrPressure`, `:1133-1140`) the enemy-Supply-Route **Pressure** axis is that axis — and because a Pressure axis holds at fires standoff rather than closing, ~90% of the force stands off mid-map on a single axis that never resolves. **`LateralSpreadMath.Rebalance` is a thin post-transform on the size vector, not a new apportionment:** it caps the SR-Pressure axis at `SrPoolSharePct`% of the pool (floored at `MinAxisSize`) and hands the freed units to the other axes by REUSING `FrontlineAllocationMath.AllocateAcrossAvenues` with per-axis *opportunity* (believed enemy thinness) as the weight — so coverage-first mans every non-SR axis and the mass phase concentrates surplus where the enemy is weakest. Total unit count is conserved exactly; `SrPoolSharePct <= 0` or `>= 100` ⇒ inert; **if there is no non-SR axis to receive the excess the cap is NOT applied** (funnelling onto the enemy SR is correct when it is the only viable target). `ForwardMuster` (`ResolveMusterAnchor`, `:2070`) fixes the fall-back *destination*: it seeds the same pinned `ForwardStagingMath.StagingCell` descent from the axis's own unit CENTROID instead of the module-wide SR anchor, so a damper/posture-held axis regroups at the line in its own sector rather than marching to the rear rally. **Two traps live in that resolver and generalize.** (a) A resolver feeding a `??` chain must return **null**, never a useless value: `OrderRetreat(ResolveMusterAnchor(axis) ?? stagingAnchor ?? rallyCell)` means a non-null self-seed both no-ops the order *and* disables every fall-back behind it. (b) But `StagingCell` returns its seed in **three** distinct situations — already at the standoff (`ForwardStagingMath.cs:112-113`), every forward neighbour danger-blocked so the walk breaks at step 0 (`:131-132`, `:150-151`), or a flat/unpopulated field — and only the third, near the SR, is the pooling pathology; the first two ARE the designed "hold in place at the line" contract. Nulling all three inverted the lever for forward axes into a rearward march. The response is therefore gated on `axis.NearRally`: near ⇒ null (pull forward), forward ⇒ return the axis's own cell so `OrderRetreat`'s `moved` guard suppresses the order. **When a helper returns "the input" as a sentinel, enumerate every path that produces it before attaching one meaning to it.** The comparison is also made in **grid** space on purpose: `ControlField.GridCellToMapCell` yields the grid cell CENTRE (`gx * CellSize + CellSize / 2`), so at `CellSize: 2` a map-space round-trip reproduces the input only for odd coordinates and would miss the no-move case on even ones — a parity dependence `ResolveStagingAnchor`'s map-space comparison still carries. The score multiplier itself is deliberately untouched — retuning `SrPressureScoreMultiplier` is parked behind a measured A/B because it also carries SR-denial doctrine (`ai.yaml:454-463`).

**Gating recipe — a NEW per-`@experimental` read on a SHARED world layer must be a per-player OPT-IN, not a world-trait/`world.yaml` global.** Because `@stable` (and humans) now `Participate`, every one of them gets a `PlayerControl` and runs `RecomputePlayer` — so gating new work by a flag on `ControlFieldInfo` set in `world.yaml` would build it for `@stable` and humans too, breaking the frozen-snapshot control and the byte-identity of the overlay read. The fix (`FrontlineProfileMath`): `ControlField.RequestFrontlineProfile(player)` (`:933`) adds the player to a `HashSet<Player> profileEnabled` (`:500`); `RecomputePlayer` builds the profile only for members (`:579`). The master flag is the per-profile `PoiOffensiveBotModule.FrontlineProfileEnabled` (default false), and only the `@experimental` module calls `RequestFrontlineProfile` — `@stable`/human never opt in ⇒ zero profile work ⇒ byte-identical. This is a third gating pattern on top of the two in §Invariants: **per-player opt-in for reads bolted onto an always-on shared world layer that `@stable` now shares.**

## Known gaps / deferred

- **Capture + garrison threat is MIGRATED (item 24, merge `646515bd`); the omniscient callers that remain are deliberate.** Capture ordering (`CaptureCoordinatorBotModule`, gate `StrategicCaptureRepointEnabled`, default false `:184`) and garrison ordering+sizing (`PoiGarrisonBotModule`, gate `DefendRepointEnabled`, default false `:106`) now re-derive threat from the **believed** `DangerFieldLayer.GroundDanger` instead of the omniscient `InfluenceMap`. Each asks PoiMap for a threat-neutral base via the `suppressOmniscientThreat` seam (`GetCaptureTargets`/`GetDefendTargets`, `PoiMap.cs:266/:391`), which **skips `SampleThreat` entirely** (`PoiMap.cs:427/:495`) — that method's `FindActorsInCircle` fallback (`:531-535`) is itself omniscient, so nulling the layer is not enough; the sampler must be skipped. Then a fog-legal factor is re-applied: capture DAMPS (`RescaleCaptureByBelievedDanger` → `PoiScoring.BelievedThreatFactor`, `CaptureCoordinatorBotModule.cs:578-597`), garrison RAISES (the mirror; `RescaleDefendByBelievedDanger` → `PoiScoring.BelievedDefendFactor`, `PoiGarrisonBotModule.cs:374-393`). **Garrison is TWO reads, not one:** ordering (`:238`) AND size — `TargetThreat` (`:360-366`) reads the believed danger carried back in the repurposed `EnemyInfluence` field, and the size bump keys on `BelievedDangerHostileUnits`, converted to raw field units at the call site (not the InfluenceMap `ThreatMildThreshold`) via `sizeThreatThreshold` (`:297`). Both gates default off on the **engine class** ⇒ any profile that omits them is byte-identical; but since the `stable-0802` parity promotion BOTH twins set them — `@experimental` (`rules/ai/ai.yaml:159` capture, `:498` garrison) and `@stable` (`:1216`/`:1294`) — so `@stable` is no longer an item-24-off control (only `normal`/legacy profiles are). See the A/B disposition note below. **Still deliberately omniscient (not scoped for migration):** `LayeredDefenceBotModule` (`GetFriendlyInfluence`/`GetEnemyInfluence` `:248-249`, `GetFrontline` `:234`), `MountedTransportBotModule` (`GetFrontline` `:550`, `GetFriendlyInfluence` `:554` — but note its drop cell is now *post-adjusted* off the believed danger field when `BelievedDangerStandoff` is on, see [§consumers beyond D/E/F](#experimental-consumers-beyond-def); the omniscient read only picks the candidate cell, not the final drop), and the render-only debug `FrontlineOverlay` (`GetFrontline` `:77/:79` — not sim code, and distinct from the `ControlField`-based `IsFrontlineEdge` contour in Stage C).
- **The Stage-F benchmark re-baseline is RUN** (2026-07-29 — `WORKSPACE/ai-bench/runs/260728_rebaseline_result.md`, instrument `main @ e5b7bbcc`, post-item-26/28). 40 measured + 20 cal matches. With item-24 belief-repoint held OFF (the clean zero per user decision) and item-25 `StrategicRepointEnabled` ON, `@experimental` **underperforms `@stable`** on both rungs (S2 net-swing median ≈ −$1,425; S1 win 3–7), a regression vs the pre-26/28 `260721` "Exp ≈ Stable" baseline attributed to the global item-26/28 changes. The item-24 gate-enablement A/B was then RUN against that zero (2026-07-29 — `WORKSPACE/ai-bench/runs/260729_item24_ab_result.md`, 40 fresh matches, instrument `2bf335cf`): the belief-repoint gates measured as a **behavioral no-op** (Arm B byte-identical to Arm A on all 20 measured matches) while carrying a per-tick cost, so the benchmark's **recommendation is KEEP OFF** — and, per that card, **no `ai.yaml` change was committed; the disposition is deferred to the user.** **Code truth at HEAD: the gates are committed ON** for `@experimental` (`StrategicCaptureRepointEnabled` `rules/ai/ai.yaml:159`, `DefendRepointEnabled` `:498`) and for the `@stable` twin (`:1216`/`:1294`), so the shipping bot runs them despite the KEEP-OFF recommendation. Treat this as *open, user-owned* — do not "fix" it in either direction without the user's call.
- **Danger kernels are radial v1** (`DangerFieldLayer.cs:24-26`); terrain-aware *flow* (routing the danger field itself around a river) is still v2, and the Stage-E line-walk terrain-blindness (above) is the same boundary. The **reachability/crossing** side of that gap is now partially addressed by the default-off Phases 0–4 layer (see §Frontline reachability & staging) — a river-split front and crossings-as-chokepoints are represented for POI distance/scoring, but the danger *kernels* remain radial.

## Consumers & file map

| Stage | File | Consumer / seam |
|---|---|---|
| gate | `Traits/World/InfluenceStack.cs` | `Participates` / stagger — shared by A/B/C |
| A | `Traits/World/BeliefStore.cs` | belief contacts (read by B, C) |
| B | `Traits/World/DangerFieldLayer.cs` | ground+air danger (read by D, E, F, overlay) |
| 0 | `Traits/BotModules/HelicopterSquadBotModule.cs`, `.../Squads/States/HelicopterStates.cs` | `StandoffEngagement` (seam for D) |
| C | `Traits/World/ControlField.cs`, `Traits/World/DangerOverlay.cs` | control field (read by B baseline, F, overlay) |
| D | `.../Squads/States/HeliDangerNav.cs` | attack-heli AA routing |
| E | `.../Squads/States/GroundDangerNav.cs` | `PoiOffensiveBotModule` axes + `SupplyFollowerBotModule` trucks |
| F | `Traits/World/PoiMap.cs`, `Traits/BotModules/PoiOffensiveBotModule.cs` (`PoiOffenseMath`) | attack-axis / expansion repoint |
| B+C | `Traits/BotModules/CaptureCoordinatorBotModule.cs` (`EscortSizingMath`) | capture escort right-sizing + contest-aware bump |
| A | `Traits/BotModules/AdaptiveProductionBotModule.cs` | threat-reactive SR-defense purchasing |
| B | `Traits/BotModules/MountedTransportBotModule.cs` | standoff drop post-adjust (`ApplyStandoff`) |
| P0 | `Traits/World/CrossingMap.cs` (`CrossingMapMath`) | crossing/barrier geometry (read by reachability) |
| P1 | `Traits/BotModules/PoiReachability.cs` (`PoiReachabilityMath`) | through-crossing distance provider for POI scoring |
| P2 | `Traits/BotModules/ForwardStagingMath.cs` | free-pool forward muster (frontier-distance descent) |
| P3 | `Traits/BotModules/RetreatDamperMath.cs` | retreat re-advance dwell + strength floor |
| P4 | `Traits/World/ControlField.cs` (`FrontlineProfileMath`) | per-sector frontline strength profile (opt-in) |
| P5 | `Traits/BotModules/FrontlineAllocationMath.cs` (+ `PoiOffensiveBotModule` / `LayeredDefenceBotModule`) | man-the-line allocation + weakest-point bias + sector posture-hold |
| P6 | `Traits/BotModules/EngineerRouteOpenBotModule.cs` (`RouteOpenMath`) | engineer bridge-repair route-opening (ledger-committed) |
| P7 | `Traits/BotModules/LateralSpreadMath.cs` (+ `PoiOffensiveBotModule.ApplyLateralSpread` / `ResolveMusterAnchor`) | SR-Pressure share cap + opportunity-weighted lateral spread; per-axis forward muster |

Math classes are nested in their trait files and NUnit-pinned without a game run: `DangerKernelMath` (DangerFieldLayer), `ControlFieldMath` (ControlField), `PoiOffenseMath`/`PoiScoring` (PoiOffensiveBotModule/PoiMap), `HeliDangerNav`/`GroundDangerNav` (own files). Tests: `ControlFieldMathTest.cs`, `HeliDangerNavTest.cs`, `GroundDangerNavTest.cs`, `PoiOffenseTest.cs`.

## When you update this doc

Re-verify against the cited lines before trusting them — this stack was in fast flux through 2026-07. Update the header verification date + SHA. Facts about the omniscient `InfluenceMap`, suppression, aircraft standoff, or `UnitRoleResolver` belong in their home docs ([architecture.md](architecture.md), [conventions.md](conventions.md)) — deep-link, don't restate.
