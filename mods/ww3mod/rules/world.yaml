
^BaseWorld:
	Inherits: ^Palettes
	AlwaysVisible:
	ActorMap:
	ScreenMap:
	Selection:
	ControlGroups:
	MusicPlaylist:
		VictoryMusic: score
		DefeatMusic: map
	TerrainGeometryOverlay:
	DebugVisualizations:
	# LOCAL LIGHTING. Every value here is the trait's own default and is written out on purpose: with
	# Intensity 1 and neutral tints, TintAt returns exactly (1,1,1) and enabling this trait is a visual
	# no-op. SpriteRenderable multiplies its tint by that identity; TerrainSpriteLayer writes
	# `alpha * (1,1,1)` into the vertex colour, which is the same value Util.FastCreateQuad writes when
	# the trait is absent. A map that wants night sets Intensity below 1 here; light events add on top.
	# What enabling it DOES cost is a TintAt call per sprite per frame (SpriteRenderable.cs:116) plus a
	# four-sample terrain refresh per dirtied cell — see WORKSPACE/DISCOVERIES.md 2026-09-06.
	TerrainLighting:
		Intensity: 1
		HeightStep: 0
		RedTint: 1
		GreenTint: 1
		BlueTint: 1
	Locomotor@TerrainSpeedsTemplate:
		Name: template
		Passes: field
		Crushes: mine, crate
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 90
			Rough: 70
			Debris: 50
			Rock: 30
			Beach: 80
			RiverShallow: 30
			Shallow: 20
	Locomotor@FOOT:
		Name: foot
		Passes: field, tree, sandbag, fence
		Crushes: mine, crate
		SharesCell: true
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 90
			Rough: 70
			Debris: 60
			Rock: 50
			Beach: 80
			RiverShallow: 40
			Shallow: 30
	Locomotor@FOOT_AMPHIBIOUS:
		Name: foot-amphibious
		Passes: field, tree, sandbag, fence
		Crushes: mine, crate
		SharesCell: true
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 90
			Rough: 70
			Debris: 60
			Rock: 50
			Beach: 80
			RiverShallow: 50
			Shallow: 50
			River: 40
			Water: 40
	Locomotor@FOOT_MOUNTAINER:
		Name: foot-mountainer
		Passes: field, tree, sandbag, fence
		Crushes: mine, crate
		SharesCell: true
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 90
			Rough: 70
			Debris: 60
			Rock: 50
			Beach: 80
			RiverShallow: 40
			Shallow: 20
			Cliffs: 15
	Locomotor@FOOT_AMPHIBIOUS_MOUNTAINER:
		Name: foot-amphibious-mountainer
		Passes: field, tree, sandbag, fence
		Crushes: mine, crate
		SharesCell: true
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 90
			Rough: 70
			Debris: 60
			Rock: 50
			Beach: 80
			RiverShallow: 50
			Shallow: 50
			River: 40
			Water: 40
			Cliffs: 15
	Locomotor@LIGHTWHEELED:
		Name: lightwheeled
		Passes: field
		Crushes: mine, infantry, crate
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 70
			Beach: 50
			Rough: 35
	Locomotor@WHEELED:
		Name: wheeled
		Passes: field
		Crushes: mine, infantry, fence, crate
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 70
			Beach: 50
			Rough: 35
			Debris: 20
	Locomotor@HEAVYWHEELED:
		Name: heavywheeled
		Passes: field
		Crushes: mine, infantry, sandbag, fence, crate
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 70
			Beach: 50
			Rough: 35
			Debris: 40
	Locomotor@LIGHTTRACKED:
		Name: lighttracked
		Passes: field
		Crushes: mine, infantry, fence, crate
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 70
			Beach: 50
			Rough: 35
			Debris: 40
			RiverShallow: 75
	Locomotor@LIGHTTRACKEDAMPHIBIOUS:
		Name: lighttracked-amphibious
		Passes: field
		Crushes: mine, infantry, fence, crate
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 70
			Beach: 50
			Rough: 35
			Debris: 40
			RiverShallow: 75
			Shallow: 75
			Water: 15
			River: 25
	Locomotor@TRACKED:
		Name: tracked
		Passes: field
		Crushes: mine, infantry, sandbag, fence, crate
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 70
			Beach: 50
			Rough: 35
			Debris: 40
			RiverShallow: 75
			River: 10
	Locomotor@HEAVYTRACKED:
		Name: heavytracked
		Passes: field
		Crushes: mine, infantry, sandbag, fence, barbedwire, crate
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 70
			Rough: 65
			Debris: 60
			Beach: 50
			RiverShallow: 75
			River: 5
	Locomotor@TRACKED_AMPHIBIOUS:
		Name: tracked-amphibious
		Passes: field
		Crushes: mine, infantry, fence, crate
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 70
			Beach: 50
			Rough: 35
			Debris: 40
			RiverShallow: 75
			Water: 10
			River: 10
	Locomotor@Walker:
		Name: walker
		Passes: field
		Crushes: mine, infantry, fence, crate
		TerrainSpeeds:
			Road: 100
			Bridge: 100
			Clear: 90
			Rough: 90
			Debris: 80
			Rock: 50
			RiverShallow: 75
			Beach: 75
			River: 50
	Locomotor@NAVAL:
		Name: naval-deep
		Crushes: mine, crate
		TerrainSpeeds:
			Water: 100
	Locomotor@NAVAL_SHALLOW:
		Name: naval-shallow
		Crushes: mine, crate
		TerrainSpeeds:
			Water: 100
			River: 70
	Locomotor@LANDINGCRAFT:
		Name: lcraft
		Crushes: mine, infantry, crate
		TerrainSpeeds:
			Water: 100
			Beach: 50
			Shallow: 80
	Locomotor@IMMOBILE:
		Name: immobile
		TerrainSpeeds:
	Locomotor@IMMOBILE_SUMMON:
		Name: immobilepara
		TerrainSpeeds:
			Clear: 1
			Road: 1
			Beach: 1
	TerrainRenderer:
	ShroudRenderer:
		# Explored ground with no current observer sits at visibility 1 and so draws all nine fog
		# layers. At the engine baseline of 1 that leaves it at ~26% of its lit brightness; 1.4
		# leaves ~15% (measured on-screen 2026-09-01) — dark enough that terrain reads as shape
		# rather than detail, while separating clearly from the flat black of unexplored shroud.
		# The previous 1.85 measured ~6.4% and was visually indistinguishable from shroud, which
		# defeated the separation goal — verified via Test.KeepRenderPlayer captures at 1 / 1.4 / 1.85.
		# Fully visible ground is visibility 10, draws no fog layer at all, and is unaffected.
		FogDarkness: 1.4
	Faction@randomside:
		Name: Any Side
		InternalName: Random
		RandomFactionMembers: america, russia
		Side: Random
		Description: Random Side\nOne of the playable factions is chosen at random when the game starts.
	Faction@0:
		Name: America
		InternalName: america
		Description: America\nNATO's lead power. Precision airpower, networked armour and air cavalry: fewer units, costlier, striking first and at range.
	# Faction@1:
	# 	Name: Ukraine
	# 	InternalName: ukraine
	# 	Side: NATO
	# 	Description: Ukraine\n
	Faction@1:
		Name: Russia
		InternalName: russia
		Description: Russia\nNATO's principal adversary. Massed armour, layered air defence and rocket artillery: weight of fire and staying power over finesse.
	# JumpjetActorLayer:
	# 	HeightOffset: 1c460

World:
	Inherits: ^BaseWorld
	# AI tournament harness — silently no-op unless launched with both
	# Test.Mode=true AND Test.TournamentConfig=<path>. See
	# WORKSPACE/plans/260511_ai_tournament_harness.md
	BotVsBotMatchWatcher:
	# Behavior-lint per-unit lifecycle logger. Silently no-op unless launched with
	# both Test.Mode=true AND Test.UnitLifecycleLog=<true|path>. Emits a JSONL event
	# stream (meta/spawn/order/idle/death/end) for tools/behavior-lint. Observation-
	# only — never affects the simulation. See WORKSPACE/behavior-lint-spec.md.
	UnitLifecycleLogger:
	# Universal Test.SpeedMultiplier apply for non-tournament test runs.
	# No-op unless Test.Mode=true + Test.SpeedMultiplier>1 + no tournament config.
	TestModeSpeedMultiplier:
	ScenarioLobbyDropdown:
	# Runs time-varying local light events (LightEventWarhead from any weapon, TimedLightSource from any
	# actor). World-only rather than in ^BaseWorld because it is ITick and the editor world does not tick.
	# Idle cost is one early-out per tick; nothing in the mod emits an event yet.
	LightEventManager:
	# Runs local heat-haze events -- a screen-space refraction shimmer over hot air (HeatEventWarhead
	# from any weapon, TimedHeatSource from any actor). Same keyframe-envelope vocabulary as
	# LightEventManager above, with distortion strength in place of intensity.
	#
	# THIS IS THE MOD'S FIRST IRenderPostProcessPass, so it is worth being exact about what it costs
	# when nothing is burning: WorldRenderer.ApplyPostProcessing (WorldRenderer.cs:393-402) tests
	# `pass.Type != type || !pass.Enabled` and CONTINUES -- before Game.Renderer.Flush(), before
	# WorldBufferSnapshot() and before any draw call. Enabled is `Tracker.ActiveDraws.Count > 0`, a
	# list length. So an idle map pays one enum compare and one list-length read, three times a frame,
	# and no fullscreen work whatsoever. HeatEnvelopeTest pins that the list really is empty when
	# nothing is hot -- including mid-event, while an envelope passes through zero strength.
	#
	# World-only rather than in ^BaseWorld because it is ITick and the editor world does not tick.
	HeatHazeRenderer:
	HealerClaimLayer:
	UnitDefaultsManager:
	# Right-click a capturable structure with NOTHING selected and the nearest free technician is
	# sent at it; select several structures and press Deploy (F) to spread the free technicians
	# across them. Only units whose Captures trait does NOT set CaptureToNeutral are dispatched,
	# which is what keeps riflemen — who merely neutralise a building — out of it.
	CaptureDispatchManager:
	# PIPELINE 5 cohesion stance identities (human-facing; bot moves stay byte-identical).
	# DP-2 SpreadHuman* = hand-tuned 'dispersed' interval (NOT weapon-AoE-derived); DP-3
	# LooseHumanCoverSearchRadius = widened cover-first search for Loose. Defaults live in
	# CohesionMoveModifierInfo; surfaced here so they are tunable in one place.
	CohesionMoveModifier:
		SpreadHumanColSpacing: 4096
		SpreadHumanRowSpacing: 3072
		LooseHumanCoverSearchRadius: 4
	ThreatMapManager:
		CellSize: 8
		UpdateInterval: 90
		SpreadFactor: 0.3
	# WW3MOD doctrine layer (Stage A.1): denser per-cell influence grid
	# with frontline derivation. See WORKSPACE/ai/doctrine.md +
	# WORKSPACE/ai/stage_a_frontline_perception.md.
	InfluenceMap:
		CellSize: 2
		UpdateInterval: 25
		# Each unit's influence reaches 5 grid cells (10 map cells) in every direction
		# with linear falloff. Tuned so a defensive line and an attacker that are still
		# 15-20 cells apart already register a contested band — that's when the
		# doctrine considers them "in contact", not when they're shooting at each
		# other. Larger = more sensitive overlay; smaller = sharper band only when
		# units are right on top of each other.
		ContributionRadius: 5
	# Stage A.3: in-game debug overlay for the InfluenceMap frontline.
	# Toggled via chat command "/frontline" — hotkey wiring is Stage A.4.
	FrontlineOverlay:
	# POI-strategy Phase 2: strategic point-of-interest layer. Discovers money
	# capturables + neutral/enemy Supply Routes (deny targets) and scores them
	# per-player by value x distance x threat (threat sampled from InfluenceMap
	# above). Queried only by the Experimental/Stable AI modules (CaptureCoordinator
	# target ordering; offense) — Normal/Rush/Turtle never read it, so this trait's presence
	# doesn't change their behaviour. Income weights mirror the CashTrickler
	# amounts (OILB 50 / FCOM 100 / BIO 150) used by CaptureCoordinatorBotModule.
	# See WORKSPACE/plans/260719_experimental_ai_poi_strategy.md.
	PoiMap:
		# PITFALL: only list structures that actually have CashTrickler — a $0
		# building here outbids real income POIs and wastes the capturer.
		IncomeWeights:
			oilb: 50
			fcom: 100
			bio: 150
			miss: 10
			hosp: 20
		SupplyRouteDenyValue: 120
		DistanceHalfLifeCells: 20
		ThreatMildThreshold: 20
		ThreatSafeMultiplier: 100
		ThreatMildMultiplier: 40
		ThreatHostileMultiplier: 10
		OwnershipNeutralIncomeMultiplier: 100
		OwnershipEnemyIncomeMultiplier: 70
		OwnershipEnemySupplyRouteMultiplier: 100
		OwnershipNeutralSupplyRouteMultiplier: 70
		DiscoveryInterval: 50
	# --- Strategic/tactical split, Phase 1 ---
	# Spec: WORKSPACE/plans/260722_strategic_tactical_split_SPEC.md (§3a/§3b/§3d).
	# Pure data + render: NOTHING consumes these for behavior yet. The overlay is
	# the verification tool — eyeball layer correctness before Phase 2 consumes them.
	# §3a: per-player, FOG-RESPECTING sighting/threat field, built from each player's
	# own vision + FrozenActorLayer (unlike the omniscient InfluenceMap/ThreatMapManager).
	SightingThreatLayer:
	# §3b: static per-cell cover quality + cover-edge orientation, computed once at map
	# load from Map.DensityLayer (same cover substrate CohesionMoveModifier reads).
	TerrainAffordanceLayer:
	# §3d: hold-Space intel overlay — balance-of-power wash (green=friendly, red=enemy,
	# gray=computed grayzone) + last-seen enemy GPS dots. Reads ONLY the viewing player's
	# own §3a layer + frozen actors, so it leaks nothing through fog. Ships as hold-Space;
	# "/intel" chat command toggles a dev always-on switch.
	SightingIntelOverlay:
	# Phase 3 role-resolver (data-only): classifies every actor into a coarse UnitRole
	# once at map load; O(1) queries thereafter. Pure ruleset inspection — no RNG, no
	# orders, no sim state — so registering it is behaviour-inert for every profile.
	# NOTHING consumes it yet (Phase-4 consumers gate behind their own UseUnitRoles flag).
	# Design: WORKSPACE/plans/260722_unit_role_resolver_DESIGN.md.
	UnitRoleResolver:
	# --- Influence stack, Stage A ---
	# Design: WORKSPACE/plans/260722_influence_stack_design.md (§2A).
	# Per-player believed-enemy-contact memory: sight → persist-with-decay → verified-clear.
	# Built from own vision + FrozenActorLayer (same fog-legal substrate as SightingThreatLayer),
	# generalising frozen structures to mobile units. Pure data: NOTHING consumes it for
	# behaviour. Registering it is behaviour-inert for every profile (@stable + control bots
	# never read it) — only Stage-B danger fields and the later human overlay ever will.
	BeliefStore:
	# --- Influence stack, Stage B ---
	# Design: WORKSPACE/plans/260722_influence_stack_design.md (§2B).
	# Per-player anti-ground + anti-air danger fields, stamped from the belief store above.
	# Kernels read from actual armament data (range → radius, damage×durability×confidence →
	# intensity). Air channel keys off "can hit an airborne target" (Helicopter or Air).
	# Baseline territory projection is now wired to the Stage-C control field below. Pure data:
	# NOTHING consumes it for behaviour,
	# so registering it is inert for @stable + control bots — only Stage-C consumers will read.
	DangerFieldLayer:
	# --- Influence stack, Stage C ---
	# Design: WORKSPACE/plans/260722_influence_stack_design.md (§2C/§2D).
	# Per-player believed-territory (control) field: Voronoi-seeded full-map ownership with
	# capture/persistence/grayzone semantics + Supply-Route/derrick anchors. Feeds the danger
	# field's territory baseline and the overlay below. Pure data — @experimental + human-overlay
	# only (narrowed via InfluenceStack); @stable + control bots never read it (byte-identical).
	ControlField:
	# frontline-influence Phase 0: terrain/reachability model (per-locomotor connected components +
	# repairable-bridge crossings + amphibious-crossable component pairs). Computed LAZILY on first
	# request from a participating @experimental/@stable bot; @stable-off/normal/human games never
	# build it, so it is byte-identical (inert data, zero RNG, no per-tick work until first query).
	CrossingMap:
	# Stage-C dev commander's overlay: tri-state danger safety (green/red/gray) + air-channel
	# toggle + control-field secondary wash. Render-only (reads the viewer's own layers, leaks
	# nothing through fog). OFF by default — cycled by the "/danger" dev chat command; never
	# ships on hold-Space. No simulation state depends on this render path.
	DangerOverlay:
	# Stage-C PLAYER-FACING territory overlay: first-class red/green/gray control wash + diagonal
	# staleness stripes (intensity ∝ ticks since a cell was last observed). Render-only (reads the
	# viewer's own ControlField, leaks nothing through fog). Ships as a dedicated HOLD-KEY (T) —
	# separate from hold-Space so it does not z-fight the SightingIntel wash; "/territory" forces it
	# on for dev/screenshots. No simulation state depends on this render path (byte-identical sim).
	TerritoryOverlay:
	ChatCommands:
	DevCommands:
	DebugVisualizationCommands:
	PathFinderOverlay:
	HierarchicalPathFinderOverlay:
	PlayerCommands:
	HelpCommand:
	ScreenShaker:
	BuildingInfluence:
	# ProductionQueueFromSelection:
	# 	ProductionPaletteWidget: PRODUCTION_PALETTE
	LegacyBridgeLayer:
		Bridges: bridge1, bridge2, bridge3, bridge4, br1, br2, br3, sbridge1, sbridge2, sbridge3, sbridge4
	CustomTerrainDebugOverlay:
	CrateSpawner:
		DeliveryAircraft: badr.crate
		QuantizedFacings: 16
		Minimum: 1
		Maximum: 2
		SpawnInterval: 2000
		WaterChance: 0
		InitialSpawnDelay: 3000
		CheckboxDisplayOrder: 1
	SmudgeLayer@SCORCH:
		Type: Scorch
		Sequence: scorches
		SmokeChance: 50
		SmokeImage: smoke_m
		SmokeSequences: idle
	SmudgeLayer@CRATER:
		Type: Crater
		Sequence: craters
		SmokeChance: 25
		SmokeImage: smoke_m
		SmokeSequences: idle

	# ---- blast-scar bands, outermost first ----------------------------------
	# SMUDGE LAYER ORDER IS Z ORDER -- "Order of the layers defines the Z sorting"
	# (SmudgeLayer.cs:30). These five are listed LIGHTEST FIRST so the darkest
	# draws last and wins. Within one blast the bands are annuli and never
	# overlap, but two blasts overlapping is routine, and in the other order a
	# later weapon's faint outer rim would paint over an earlier weapon's blasted
	# centre -- damage would appear to heal.
	#
	# SmokeChance is rolled PER CELL (SmudgeLayer.cs:158), not per warhead, so it
	# is only set on the two innermost bands. AtomicHighYield's rim band alone is
	# ~2600 cells; Scorch's 50 there would be over a thousand SpriteEffects from
	# one shot.
	# SHORE FADE, 2026-09-09. Only these five layers set it. The two stock layers above
	# (SCORCH, CRATER) leave ShoreFadeCells at its default 0 and are byte-identical to before.
	# 2 gives a two-cell ramp, so the disc eases off at a river instead of ending on a hard cell edge.
	# SHORE FADE MIN ALPHA, 2026-09-10, and it is a FIX to the line above rather than a new knob.
	# The ramp used to fall to 1/3 on the cell touching water. A smudge is one flat alpha over a whole
	# cell, so that is a 67% drop across one cell edge -- and because the ramp is 2 cells wide while
	# the rivers on these maps are 2-3 cells wide, EVERY cell of a ford is within reach of some water
	# and the entire crossing was held at 1/3-2/3. Measured on demo-highyield-nuke's ford (cells
	# 71-74 x 50-52): a 6-cell-wide band held below full strength, with the 4 ford cells at 1/3. That
	# reads as exactly the bright unscarred sand block the fade was written to remove. 0.7 keeps the
	# shoreline cell unmistakably scarred and still thins it into the water.
	# (The ramp is also Euclidean now, not Chebyshev -- Chebyshev's contours are literal squares, which
	# is what put hard rectangular corners several cells clear of the water. SmudgeLayer.cs:376-415.)
	# GROUND-COVER OVERLAY, 2026-09-10. Only these five layers set it, for the same reason only these
	# five set ShoreFadeCells: SCORCH and CRATER are the RA smudge types every ordinary weapon in the
	# mod shares, and widening a nuclear-scarring decision onto conventional craters is not what was
	# asked for. Crop fields are ACTORS (^CivField, 3187 of them on river-zeta), and actors draw in a
	# later pass than terrain decals, so the scar on a field cell was always placed and always hidden --
	# farmland read as immune to a nuclear blast. This redraws the band over those cells only, after
	# actors. Ground cover is the exact restriction (Passable.GroundCover): a cell qualifies only if
	# EVERY actor in it is ground cover, so a scar can never land on top of a tank, soldier or building.
	SmudgeLayer@SCARRIM:
		Type: ScarRim
		ShoreFadeCells: 2
		ShoreFadeMinAlpha: 0.7
		GroundCoverOverlay: true
		Sequence: scarband-rim
		SmokeChance: 0
	SmudgeLayer@SCARBURN:
		Type: ScarBurn
		ShoreFadeCells: 2
		ShoreFadeMinAlpha: 0.7
		GroundCoverOverlay: true
		Sequence: scarband-burn
		SmokeChance: 0
	SmudgeLayer@SCARCHAR:
		Type: ScarChar
		ShoreFadeCells: 2
		ShoreFadeMinAlpha: 0.7
		GroundCoverOverlay: true
		Sequence: scarband-char
		SmokeChance: 0
	SmudgeLayer@SCARCRATER:
		Type: ScarCrater
		ShoreFadeCells: 2
		ShoreFadeMinAlpha: 0.7
		GroundCoverOverlay: true
		Sequence: scarband-crater
		SmokeChance: 20
		SmokeImage: smoke_m
		SmokeSequences: idle
	SmudgeLayer@SCARCORE:
		Type: ScarCore
		ShoreFadeCells: 2
		ShoreFadeMinAlpha: 0.7
		GroundCoverOverlay: true
		Sequence: scarband-core
		SmokeChance: 40
		SmokeImage: smoke_m
		SmokeSequences: idle
	WarheadDebugOverlay:
	SpawnMapActors:
	MapBuildRadius:
		AllyBuildRadiusCheckboxVisible: false
		BuildRadiusCheckboxVisible: false
	MapOptions:
		ShortGameCheckboxVisible: false
		GameSpeedDropdownDisplayOrder: 10
		TechLevelDropdownVisible: false
		GameSpeed: default
	SyncReportsOption:
		# Developer block (60-62), between Debug Menu and Sandbox. Both sides pay the cost of
		# sync reports, so it belongs with the other developer toggles rather than among the
		# settings a host actually tunes.
		CheckboxDisplayOrder: 61
	LobbyDummyOptions:
	CreateMapPlayers:

	StartingUnits@none:
		Class: none
		ClassName: None
		Factions: america, russia
		BaseActor: supplyroute

	StartingUnits@squad_america:
		Class: squad
		ClassName: Squad
		Factions: america
		BaseActor: supplyroute
		SupportActors: TL.america,E3.america,E2.america,AR.america, TL.america,E3.america,E2.america,AR.america, AT.america,MEDI.america
		InnerSupportRadius: 5
		OuterSupportRadius: 6
	StartingUnits@squad_russia:
		Class: squad
		ClassName: Squad
		Factions: russia
		BaseActor: supplyroute
		SupportActors: TL.russia,E3.russia,E2.russia,AR.russia, TL.russia,E3.russia,E2.russia,AR.russia, AT.russia,MEDI.russia
		InnerSupportRadius: 5
		OuterSupportRadius: 6

	StartingUnits@platoon_america:
		Class: platoon
		ClassName: Platoon
		Factions: america
		BaseActor: supplyroute
		SupportActors: TL.america,E3.america,E2.america,AR.america,E3.america, TL.america,E3.america,E2.america,AR.america,E3.america, TL.america,E3.america,E2.america,AR.america,E3.america, MT.america,AT.america,DR.america,SN.america,MEDI.america
		InnerSupportRadius: 5
		OuterSupportRadius: 6
	StartingUnits@platoon_russia:
		Class: platoon
		ClassName: Platoon
		Factions: russia
		BaseActor: supplyroute
		SupportActors: TL.russia,E3.russia,E2.russia,AR.russia,E3.russia, TL.russia,E3.russia,E2.russia,AR.russia,E3.russia, TL.russia,E3.russia,E2.russia,AR.russia,E3.russia, MT.russia,AT.russia,DR.russia,SN.russia,MEDI.russia
		InnerSupportRadius: 5
		OuterSupportRadius: 6

	StartingUnits@Motorized_america:
		Class: motorized
		ClassName: Motorized
		Factions: america
		BaseActor: supplyroute
		SupportActors: abrams,bradley,humvee, TL.america,E3.america,E2.america,AR.america, TL.america,E3.america,E2.america,AR.america, TL.america,E3.america,E2.america,AR.america, MT.america,AT.america,DR.america,SN.america,MEDI.america
		InnerSupportRadius: 5
		OuterSupportRadius: 7
	StartingUnits@Motorized_russia:
		Class: motorized
		ClassName: Motorized
		Factions: russia
		BaseActor: supplyroute
		SupportActors: t90,bmp2,bmp2, TL.russia,E3.russia,E2.russia,AR.russia, TL.russia,E3.russia,E2.russia,AR.russia, TL.russia,E3.russia,E2.russia,AR.russia, MT.russia,AT.russia,DR.russia, SN.russia,MEDI.russia
		InnerSupportRadius: 5
		OuterSupportRadius: 7

	StartingUnits@AirSupport_america:
		Class: air
		ClassName: Air support
		Factions: america
		BaseActor: supplyroute
		SupportActors: abrams,bradley,strykershorad, TL.america,E3.america,E2.america,AR.america, TL.america,E3.america,E2.america,AR.america, TL.america,E3.america,E2.america,AR.america, MT.america,AT.america,AA.america,SN.america,MEDI.america, littlebird,heli,tran
		InnerSupportRadius: 5
		OuterSupportRadius: 7
	StartingUnits@AirSupport_russia:
		Class: air
		ClassName: Air support
		Factions: russia
		BaseActor: supplyroute
		SupportActors: t90,bmp2,tunguska, TL.russia,E3.russia,E2.russia,AR.russia, TL.russia,E3.russia,E2.russia,AR.russia, TL.russia,E3.russia,E2.russia,AR.russia, MT.russia,AT.russia,AA.russia,SN.russia,MEDI.russia, HIND,HIND,HALO
		InnerSupportRadius: 5
		OuterSupportRadius: 7

	MapStartingLocations:
		SeparateTeamSpawnsCheckboxDisplayOrder: 54
	SpawnStartingUnits:
		DropdownDisplayOrder: 50
		# Immediately after Starting Units (50): the two dropdowns offer the same package
		# classes and are read together. Default stays `none`, so a match nobody touches is
		# the match it was before this option existed.
		ForwardDeploymentDropdownDisplayOrder: 51
	PathFinder:
	ValidateOrder:
	ControlAllUnitsManager:
	DebugPauseState:
	MiniMapPings:
	StartGameNotification:
		TextNotification: notification-match-start
	ObjectivesPanel:
		PanelName: SKIRMISH_STATS
	LoadWidgetAtGameStart:
	ScriptTriggers:
	CellTriggerOverlay:
	TimeLimitManager:
		# "Dead Hand Clock", not "Doomsday Clock" (user ruling, 2026-09-10). The player-facing name
		# of the whole mechanic is Dead Hand -- the Soviet Perimeter system it is modelled on --
		# and it now reads that way in the lobby, in DoomsdayStrike's checkbox label and in its
		# tooltip. THE CODE DELIBERATELY DISAGREES: the trait is still DoomsdayStrike and the
		# option id is still `doomsday`, because that id is wire-visible and renaming it would
		# silently discard stored settings. See the comment at DoomsdayStrike.cs's label field.
		TimeLimitLabel: Dead Hand Clock
		TimeLimitDescription: Nuclear apocalypse when the clock runs out
		# Match block: Game Speed (10), this (11), then the Dead Hand checkbox (12) which decides
		# what happens when this clock reaches zero. The two were previously in different
		# sections at opposite ends of the panel despite being one feature.
		TimeLimitDisplayOrder: 11
		# The expiry notification is DoomsdayStrike's to give: "Time limit has expired." undersells
		# what is about to happen and lands one line above "DEAD HAND ACTIVATED. Incoming."
		SkipTimerExpiredNotification: true
		TimeLimitWarnings:
			40: FourtyMinutesRemaining
			30: ThirtyMinutesRemaining
			20: TwentyMinutesRemaining
			10: TenMinutesRemaining
			5: WarningFiveMinutesRemaining
			4: WarningFourMinutesRemaining
			3: WarningThreeMinutesRemaining
			2: WarningTwoMinutesRemaining
			1: WarningOneMinuteRemaining
	ColorPickerManager:
		PreviewActor: e3.colorpicker
	# DOOMSDAY. The lobby has said "Doomsday Clock / Nuclear apocalypse when the clock runs out" since
	# long before anything implemented it; this is what makes that true. It REPLACES the plain
	# time-limited game rather than sitting beside it — there is no separate "time limit" concept left
	# in the UI to coexist with, and a mode selected by two dropdowns that must agree is a worse lobby
	# than one. The checkbox exists so a host can get the old score-comparison ending back, and defaults
	# ON so that the shipped label is not lying by default.
	#
	# TIMED AUTOTESTS AND TOURNAMENTS ARE UNAFFECTED: RunInTestMode defaults false, so a TestMode
	# session with a time limit still ends the way it always did. The demo scenario opts in explicitly.
	DoomsdayStrike:
		OutlierMissile: deadhandtacticalmissile
		CityMissile: deadhandstrategicmissile
		# 14c0, just inside `Atomic`'s ShockwaveDamage MaxRadius of 15c0 — the ~2.7 psi contour, the
		# outer edge of serious structural damage (weapons-superweapons.yaml). It used to be 20c0, read
		# off the delivery body's claim of igniting structures to ~28 cells, and it was set high
		# because the whole total-coverage guarantee rested on it. IT NO LONGER DOES: the fill pass
		# that consumed this number is gone (2026-09-07 retune), so all this now governs is the jitter
		# bound and how far a city's warheads may spread from its centroid. Stating it against the
		# blast contour rather than the ignition one is the honest reading now that nothing downstream
		# is generating warheads from it.
		LethalRadius: 14c0
		JitterRadius: 4c0
		# 10c0, DOWN FROM 12c0, and it is not a taste change: it must be <= LethalRadius - JitterRadius
		# (14 - 4 = 10) or the separation filter can drop a derrick and leave it outside every blast.
		# The old 12 was safe only because the lethal radius was 20 and the fill pass covered the rest.
		MinSeparation: 10c0
		CityLinkDistance: 8c0
		CityMinBuildings: 4
		# ONE per city, not two. river-zeta clusters into exactly two cities once crop fields and trees
		# stop being counted as buildings, so one warhead each IS the user's "two city destroyers".
		WarheadsPerCity: 1
		SpawnAltitude: 38c0
		ApproachDistance: 5c0
		WithinWaveTicks: 2
		OutlierToCityPauseTicks: 40
		AnnihilationDelayTicks: 90
		ResolutionDelayTicks: 30
		LeadInTicks: 30

	# LIVE as of the Phase 2 missile powers, for TWO options now: the tactical nuclear strike gate and
	# the high-yield strategic one added on 2026-09-06.
	#   * player.yaml's GrantConditionOnLobbyOption@tacnuke reads `tactical-nuke` from here, and the
	#     power's RequiresCondition makes its icon absent when the host leaves it off (the default).
	#   * @highyieldnuke reads `high-yield-nuke` the same way, but that one DEFAULTS ON -- the user's
	#     explicit temporary choice so both nuclear powers are reachable for testing without ticking
	#     anything, with the release default left to them. Neither default is overridden here; the C#
	#     values in PowersLobbyOptions.cs are the shipped behaviour and the only thing set below is
	#     where the two checkboxes sort.
	#
	# The trait's airstrike options stay disabled for v1 -- so they are HIDDEN rather than deleted.
	# Hiding is the right lever: the fields keep documenting what the airstrike re-enable will
	# need, `airstrikes` keeps registering with its default so GrantConditionOnLobbyOption@airstrikes
	# resolves the moment that block is uncommented, and nothing in the lobby advertises a feature
	# that is not there.
	# Re-enabling airstrikes post-v1 = flip these two Visible flags back to True and uncomment the
	# AirstrikePower@America/@Russia + GrantConditionOnLobbyOption@airstrikes blocks in player.yaml.
	# See CLAUDE/RELEASE_V1.md (Deferred to v1.1).
	#
	# ==== HIDING IS NOT DELETING, AND HERE THE DIFFERENCE IS THE WHOLE GAME ====
	# `nuclear-arsenal` and `powers-sandbox` are hidden below by the SAME lever, and it is the only
	# safe one. Deleting either option -- or stripping this trait -- does NOT leave the setting at
	# its shipped default; it INVERTS it. GrantConditionOnLobbyOption computes
	#     optionEnabled = OptionOrDefault(Option, !GrantWhenOptionDisabled)
	# (GrantConditionOnLobbyOption.cs:47-48), so THE REGISTERED DEFAULT AND THE UNREGISTERED
	# FALLBACK ARE DIFFERENT VALUES. Both gates use GrantWhenOptionDisabled: true, so an option
	# nobody registered falls back to FALSE, which GRANTS the *-disabled condition:
	#   * `nuclear-arsenal` gone -> `nuke-arsenal-disabled` granted -> ALL 13 GATED POWERS VANISH
	#     (10 in rules/ingame/nuclear-arsenal.yaml, plus @B61Mid, @Kinzhal and @GBU57 in
	#     player.yaml -- the last two CONVENTIONAL weapons riding a checkbox labelled "Nuclear
	#     Arsenal").
	#   * `powers-sandbox` gone -> `powers-sandbox-disabled` granted -> the three unfiltered
	#     ProvidesPrerequisite traits switch off -> `powers.event` is provided by no faction, and
	#     the 8 autotest scenarios that set PowersSandboxCheckboxEnabled: true break at their FIRST
	#     assertion, because what they override is the REGISTERED default.
	# Visible: False touches none of that. LobbyCommands.LoadMapSettings (:1382-1410) registers
	# every option ILobbyOptions yields with no visibility test at all; IsVisible is read in exactly
	# one place, LobbyOptionsLogic.cs:400, which decides whether to draw a ROW. Option registered,
	# default intact, behaviour unchanged, row gone. Same pattern as airstrikes/shortgame/techlevel.
	PowersLobbyOptions:
		AirstrikeCheckboxVisible: False
		AirstrikeCheckboxDisplayOrder: 81
		AirstrikeCooldownVisible: False
		AirstrikeCooldownDisplayOrder: 82
		# 30-33 is the Arsenal section in LobbyOptionsLogic: the three weapon gates in ascending
		# yield, then the (unimplemented) powers master switch at 33. The C# defaults are 102-105,
		# which predate the sections and would scatter these down the panel.
		TacticalNukeCheckboxDisplayOrder: 30
		HighYieldNukeCheckboxDisplayOrder: 31
		NuclearArsenalCheckboxDisplayOrder: 32
		# HIDDEN, NOT REMOVED -- read the block comment above before touching this. The arsenal
		# stays ON (NuclearArsenalCheckboxEnabled defaults true) and all 13 powers stay reachable;
		# the host simply no longer gets a row to switch them off with. The DisplayOrder above is
		# retained so un-hiding is a one-line change back to True.
		NuclearArsenalCheckboxVisible: False
		# Sandbox is deliberately NOT an Arsenal option: it does not decide which weapons exist,
		# it suspends the faction rules governing who may buy them. It sorts into Developer (62)
		# with the other testing switches.
		PowersSandboxCheckboxDisplayOrder: 62
		# HIDDEN, NOT REMOVED -- and this one is load-bearing for the test suite. Sandbox stays
		# registered and OFF by default, so a normal match is faction-locked exactly as before,
		# and the scenarios that turn it on in their own rules.yaml keep working. Deleting the
		# option would take `powers.event` away from them; hiding it does nothing to them at all.
		PowersSandboxCheckboxVisible: False

	# DEFCON ESCALATION -- the match-wide alert level, and the three dropdowns that configure it.
	# The player-actor half (DefconCasualtyObserver, GrantConditionOnDefconLevel) is in player.yaml.
	#
	# SPINE ONLY. This trait owns the level and gates nothing; the conditions it drives have no
	# consumers yet. MarkAsPlaceholder therefore defaults TRUE in C#, which renders all three
	# dropdowns dimmed with the lobby's placeholder tooltip -- honest, because picking DEFCON
	# Escalation today changes nothing a player can see. Set MarkAsPlaceholder: False here on the
	# change that lands the first consumer.
	#
	# THE DEFAULT GAME MODE IS Skirmish AND MUST STAY THAT WAY UNTIL THE FEATURE IS COMPLETE.
	# Skirmish is a strict no-op: no level, no conditions, no clock. It is what lets this land in
	# pieces without anything changing for someone testing from main. The default lives in
	# DefconEscalationInfo.ModeDefault and DefconEscalationTest.SkirmishIsAStrictNoOp guards it.
	#
	# Only the 3 -> 2 transition is on a clock, and Escalation Pace scales only that. 2 -> 1 fires
	# the moment enemy action destroys anything, at any pace. The three pace durations are UNTUNED
	# PLACEHOLDERS in C# (9000 / 5000 / 2500 ticks = 9:00 / 5:00 / 2:30 at the default 60 ms
	# timestep -- 16.67 ticks per second, NOT 25) and are deliberately not overridden here, so the
	# first person to tune them changes one place.
	#
	# 20-23 is the Escalation section in LobbyOptionsLogic, and THE CEILING IS ITS FOURTH MEMBER.
	# It was previously left at its C# default of 88 and omitted from the section map, which
	# stranded it alone in the implicit "Other" bucket at the very bottom of the lobby -- three
	# sections away from the three dropdowns it is configured alongside, on the same trait.
	DefconEscalation:
		ModeDisplayOrder: 20
		StartAtDisplayOrder: 21
		PaceDisplayOrder: 22
		CeilingDisplayOrder: 23
	# THE DEFCON 3 DIVIDING WALL, AND IT IS NOW LIVE ON EVERY SHIPPED MAP. This block used to be
	# registered with no configuration at all, which made the wall inert everywhere -- and that
	# became a correctness problem the moment the readout shipped, because at DEFCON 3 it tells the
	# player "The border is closed. Neither side may cross it." while nothing enforced it.
	#
	# DERIVED, NOT AUTHORED. No map draws a line. DeriveFromSpawns splits the match's combatants
	# into their two alliance groups, takes each group's centroid home, and uses the perpendicular
	# bisector -- so the line is equidistant from both sides by construction on any map, including
	# the 4- and 6-spawn ones where which spawns are occupied is not known until the match starts.
	# An explicitly authored Start/End still WINS over the derivation, which is what makes this an
	# override list rather than per-map authoring: the one worked example is
	# tools/autotest/scenarios/test-defcon-wall/rules.yaml, and no shipped map needs an entry.
	#
	# Still inert in Skirmish, which is still the default game mode: the wall stands only at
	# DEFCON 3 and Skirmish holds NoLevel. Nothing here changes a Skirmish match.
	DefconWall:
		DeriveFromSpawns: True
		# THE BORDER HAS TO BE SEEN, and until this landed it could not be. DefconWall writes
		# Map.CustomTerrain, and CustomTerrain is read by Map.GetTerrainIndex (pathfinding,
		# GrantConditionOnTerrain) and by NOTHING in the terrain draw: TerrainRenderer subscribes
		# only to Map.Tiles and Map.Height (TerrainRenderer.cs:92-93) and renders from Map.Tiles.
		# So the wall sealed the map while the map's own grass stayed on screen, and the readout's
		# "The border is closed" pointed at nothing. The trait now draws it itself.
		CrossingRefusedTextNotification: notification-defcon-border-closed
	OrderEffects:
		TerrainFlashImage: moveflsh
		TerrainFlashSequence: idle
		TerrainFlashPalette: moveflash

	# MapStartingLocations:
	# 	SeparateTeamSpawnsCheckboxDisplayOrder: 6
	# 	SeparateTeamSpawnsCheckboxVisible: False
	# 	SeparateTeamSpawnsCheckboxLocked: True
	# SpawnStartingUnits:
	# 	DropdownDisplayOrder: 1
	# PathFinder:
	# ValidateOrder:
	# DebugPauseState:
	# MiniMapPings:
	# StartGameNotification:
	# ObjectivesPanel:
	# 	PanelName: SKIRMISH_STATS
	# LoadWidgetAtGameStart:
	# LuaScript:
	# TimeLimitManager:
	# 	TimeLimitWarnings:
	# 		40: FourtyMinutesRemaining
	# 		30: ThirtyMinutesRemaining
	# 		20: TwentyMinutesRemaining
	# 		10: TenMinutesRemaining
	# 		5: WarningFiveMinutesRemaining
	# 		4: WarningFourMinutesRemaining
	# 		3: WarningThreeMinutesRemaining
	# 		2: WarningTwoMinutesRemaining
	# 		1: WarningOneMinuteRemaining

EditorWorld:
	Inherits: ^BaseWorld
	EditorActorLayer:
	EditorCursorLayer:
	EditorSelectionLayer:
	LoadWidgetAtGameStart:
	EditorActionManager:
	MarkerLayerOverlay:
	BuildableTerrainOverlay:
		AllowedTerrainTypes: Clear, Road
