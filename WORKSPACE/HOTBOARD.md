# Hotboard

> What's actively in motion **right now**. Scope and status live in `RELEASE_V1.md`; the ordered roadmap in [`PIPELINE.md`](PIPELINE.md) (stubs — dossiers and archive under [`pipeline/`](pipeline/README.md)).
> **Cap ~40 lines. Rotate stale entries out** — once something ships, the tracker and `git log` tell the story. **Every line here carries a `file:line` or a SHA, or it does not belong.** History of *why* a thing was hard belongs in the dossier, not here.

**Reconciled 2026-09-10 at `main @ 27a7f23a`.** The previous stamp was `dc899995` (2026-08-13) — **1546 commits back**, and every count in it had drifted. **`main` is AHEAD of `origin/main` by one** (`27a7f23a` unpushed). **`v0.1.0` shipped 2026-09-08** (tags `v0.1.0`/`v0.1.1`/`v0.1.2`), so the scope question is now "what is in 1.0", not "when do we start shipping". **Push policy unchanged:** workers never push; the manager pushes `main` only after a merge verifies (build clean + NUnit green). Two machines push concurrently — append-vs-append conflicts in `DISCOVERIES.md` / `bugs/discovered.md` **resolve keep-both, never pick a side.**

**Worktrees and unmerged branches — re-measured 2026-09-10 at `main @ d7900714`.** The four that were in flight (`defcon-holdfire`, `defcon-ladder`, `defcon-wall`, `scar-over-fields`) are **all merged and their worktrees removed**. **Parked WIP:** `powers-tab` (`7a3d1f14`), `shadow-relight` (`0ed0b50a`, recon only). **`cameo-sourcing`'s branch is merged (`92a81de5`) but the worktree is kept on purpose** — it holds an untracked `work/` of candidate cameo renders; move those before removing it. `--no-merged main`: `exp-terr-bias` (STOP note `ccd12c98`), `wt/powers-tab`, `wt/shadow-relight`, `wt/shellmap-session`.

> ❌ **`wt/shellmap-session` (`2218df8e`) is NOT a pending crash fix — DROP IT.** Triaged 2026-09-10 at `f7b80f17`. The crash was real (`Session.ClientInSlot` is a `SingleOrDefault` that asserts one client per slot; `SetupShellmapBots` appended instead of assigning, so the corruption surfaced one world-load later as an unexplained exit). **It is already fixed on `main` by `e7ca4e1a`** — a better fix by the same author 86 minutes later — in both halves: `LoadShellMapInner` (`Game.cs:551`) resets the session, and `ShellmapBots.SeatBots` (`ShellmapBots.cs:32-34`) is independently idempotent. Pinned by `ShellmapBotsTest.cs`, 5/5 green. This branch is the earlier of two attempts and was never deleted. **It also does NOT merge cleanly** — a real merge gives `UU Game.cs`. The previous wording here ("an unmerged main-menu crash fix") cost one dispatch; that is what this line exists to prevent.

## Working on
- **DEFCON Escalation — spine shipped, three pieces in flight, nothing a player can see yet.** `8a204ed5` / `dfe326dc` landed `DefconEscalation.cs`, `DefconEscalationState.cs`, `DefconCasualtyObserver.cs`, `GrantConditionOnDefconLevel.cs` + `DefconEscalationTest.cs`. **It gates nothing: `MarkAsPlaceholder` defaults TRUE (all three lobby dropdowns render dimmed) and `ModeDefault = Skirmish` is a strict no-op** (`DefconEscalation.cs:50`, contract spelled out at `world.yaml:716-737`). **Skirmish must stay the default until the feature is complete**; `MarkAsPlaceholder: False` is the change that lands with the first consumer. Hold-fire, the nuclear release ladder and the dividing wall are the three branches above. Pace durations are **untuned placeholders** and are deliberately not overridden in YAML, so the first tuner changes one place. **DEFCON appears nowhere in `PIPELINE.md` (0 hits)** — it is manager-decision-driven (`.maestro/.../manager-9d3ca782/decisions/01-07`).
- **Scenarios were RE-SCOPED by the user 2026-09-09, and PIPELINE knows nothing about it.** Decision `08-scenarios-split-in-two-…`: pre-placed units become a plain lobby dropdown that works on **any** map; a "scenario" becomes a purpose-built map with scripts attached; the shellmap becomes a purpose-built map too. **This is mostly a DELETION** (`scenarios.yaml` layer, `ScenarioLobbyDropdown`, `map.ScenarioNames`). **Sequencing is load-bearing: do NOT delete first** — the main menu background runs through the scenario machinery today. Anything describing the old lobby scenario layer as the plan is now wrong. Half of the dropdown half already shipped as **Forward Deployment** (`a0eb48c5`, `ForwardDeploymentGeometry.cs`, `world.yaml:617`, default `none`).
- **Item 56 — supply trucks — is a RUN, not a dispatch, and it is now the ONLY survivor of the 08-13 batch.** The blunt fix the user pre-authorised **exists and is switched ON in shipped content**: `IgnoreDangerForDelivery: true` (`ai.yaml:1689`, shared instance → reaches `@stable`), plus `ClusterStickinessNeedMargin: 1000` (`:1587`). The old "seven sites, no config flag reaches one of them" framing is **false** — six sites, all reached. **The acceptance bar is unchanged and still owed: one bot-vs-bot match on a real map, not a green scenario.** Items **57, 58, 59, 60, 61 are all SHIPPED and archived** (57 at `closed-items.md:236`) — the batch bullet that listed them as live is retired with this reconciliation. Detail: `PIPELINE.md:315-333`.
- **Item 40 — danger-scale stage (c) is still the whole item, but its downstream story has changed.** Stage (a) done (`ddcc5d6c`), stage (b) instrument shipped; **(c) — re-derive 18 ground + 1 air thresholds — remains open** (`PIPELINE.md:304-307`). **What is no longer true: it "gates the benchmark re-baseline".** Item 43 was **TAKEN 2026-09-05/06** — first valid 40-match corpus, card `benchmarks/260905-rebaseline.md`, standing in `ai-bench/LADDER.md`. **Items 25 and 31 are archived** and cannot be gated by a future measurement. `DangerFieldLayer` has no per-profile gate, so **`@stable` has already moved** — as it has again since, knowingly, via item 56's flag and `MinUnitsPerAmbush`/`FreePoolMinAdvanceUnits` (`bb89f9fd`).
- **Saved-game restore is STILL RED on a second, independent leak — located, named, NOT fixed, and NOT re-measured since 08-13.** Net frame 711 / world tick 2130, actor `4712 at.america`: `Mobile.Facing` 256 vs 118, `AttackFrontal.IsAiming` replay-only (`dc8e0abf`). **The cites in the old entry are dead — the file moved and the code was refactored:** it is `Activities/Move/AttackMoveActivity.cs` (289 lines), the halt is now `AmbushTactics.ShouldHaltBeforeContact(...)` called at `:195-196`, and `GroupDetectedBy` is at `:236` → `CanBeViewedByPlayer` at `:247`/`:260`. Still **live on `@stable`**. Next instrument unchanged: `CanBeViewedByPlayer` for 4712 and its ambush-radius allies at ticks 2126–2129 on both lives. **Attack it, do not adopt it — four confident causes have already dissolved.**
- **The 2-human desync is untested against any of it, and the host trap survives with NEW plumbing.** `EnableSyncReports` still reaches clients from the host's lobby globals (`OrderManager.cs:159,162`; server side `Server.cs:349`) — **the user must host** — but the gate is no longer that alone: `OrderManager.cs:142-170` now ANDs a human-client count and a lobby option (`Session.SyncReportsOptionId`) on top. Second trap unchanged: an explicit `EnableSyncReports: False` in either `settings.yaml` survives `Settings.Save()` silently — check both. Fixed and separate: three bot modules writing synced actor state (`91056894`); `wt/desync-guard` is merged, its absence from the branch list is the expected end state. **The `Detectable` sync change is NOT a fix** (`c440906e`) — never cite it as one.
- **`ForwardStaging` executes on `@stable` and has still never been watched in a live match.** `ai.yaml:731 ForwardStagingEnabled: true`, `:2591 ForwardStaging: true` — bare `Move` to 40% of the SR→POI vector, no danger-field consultation on the staging leg. **Watch attack helis at the SR.** Do not confuse it with item 64's `RendezvousWithOffensiveStaging`, which is **false at both sites** (`ai.yaml:2193, 2271`) and has therefore never affected play.
- **Item 22 — case-01 forest ambush: still awaiting one user yes/no, not more measurement.** The bar is live and unratified in code: `parse-case01-bar.py:33-37` (`DEF_MAX_MEAN 50`, `ATT_MIN_MEAN 300`, `MIN_SEEDS 6`, `BAR_B_DEF_MAX 0`). It is `AWAITING-USER.md` §4.

## Gates and counts — re-measured, not quoted
- **`.\make.ps1 test` is now four gates, not one** (`make.ps1:212-225`): `nav-guard` → `lua-gate` → `smudge-gate` (new, `27a7f23a`) → `--check-yaml`. **The lint RED is long fixed; `make test` is a usable merge gate.**
- **Lint floor: `mods/ww3mod/lint-baseline.txt`, 571 lines / 374 entries — `[repo]` 15, `[accepted]` 6, `[requires-ra-content]` 350.** The old "87 local / 437 CI / 62 maps with no cordon" is dead: the cordon entries were pruned (`2453672d` −40, `6c9f89fc` −23, `017410b7` −8). Five real missing files persist in both environments (`lint-baseline.txt:160-164`). **Never compare a local count with a CI one**, and **diff the error LIST, never the count** — the per-map re-run still never deduplicates *(not re-measured here; no lint was run)*.
- **nav-guard still gates exactly 10 maps** (`baseline.json` `states.live` = 10 keys) — but the denominator moved: **320 scenario directories + 10 shipped maps = 330**. A green nav-guard run on a scenario change is real and vacuous.

## Recently shipped
- **Nuclear/FX arc:** vaporize wired to warheads `f83826d4`; scar blending `212751df`; scar reaches trees + waterline `7ff2c186`; **scarring under destroyed actors** `93b94fa0`; **Rock and Cliffs accept scars** `f2962fa5` (`wt/scar-over-fields` is doing crop fields); burnt trees `74b559ed`; **off-map explosions light and draw at full brightness** `ec5539ab`; Oreshnik six-warhead MIRV `c22bbe88`; per-power cameos `92a81de5`.
- **Chrome/release:** v0.1.0 could not start on any real install — one missing `~` `3f859ca2`; Multiplayer Create button `cad69490`; lobby left column scrolls as one `8a7f17d8`; Missions button hidden `b3a2d450`; range-ring dash endpoints `d3647502`.
- Older landed work (08-06 → 08-13 batches: game clock, transport-idle/lift, pips zoom, AA overkill clamp, onboarding panel, LC squad rearm, elimination cascade, autotarget preempt) is in `git log` and `pipeline/archive/shipped-log.md`. **Standing caveat that outlives them:** `f910ac7d` — a green run is not evidence unless something could have made it RED.

## Pending user sign-off
[`AWAITING-USER.md`](AWAITING-USER.md) is down to **five** questions with an answer sheet at the top: version string, public identity, Iskander-vs-HIMARS, the case-01 bar, splash/menu art. The older highlight list (balance proposals 001–003, parity batches, item-24 gate, item-31 aggro sweep) closed at the 08-19/08-20 reconciliations — **do not re-ask it.**

## Quick stats
- Shipped playable maps: **10** (`mods/ww3mod/maps/`). Autotest scenarios: **320** — recount, it grows weekly.
- AI profiles: **2** — `Experimental AI` (`experimental`) and `Standard AI 0902` (`stable`), `ai.yaml:82-90`. There is no Normal/Rush/Turtle ladder.
- Engine C# modified: **not settled here** — `CLAUDE.md` says ~264, a diff against the import commit counts 1293. Two different measures; nobody has reconciled them.
- Suite health: **not re-measured** (no runs in this pass). Last read is `audits/260901-autotest-suite-audit.md` at `97c2fe78`: 189 of 210 `test-*` enter a batch, 64 folders carry no verdict call at all.
