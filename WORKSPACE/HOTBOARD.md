# Hotboard

> What's actively in motion **right now**. Scope and status live in `RELEASE_V1.md`; the ordered roadmap in [`PIPELINE.md`](PIPELINE.md) (stubs — dossiers and archive under [`pipeline/`](pipeline/README.md)).
> **Cap ~40 lines. Rotate stale entries out** — once something ships, the tracker and `git log` tell the story. **Every line here carries a `file:line` or a SHA, or it does not belong.** History of *why* a thing was hard belongs in the dossier, not here.

**Reconciled 2026-09-20 at `main @ 554895ba`; stamp advanced the same evening to `df5a0fb4` for the endgame lines only** — worktrees, branches and the two nuclear-endgame entries below were re-measured at `df5a0fb4`; every other count still stands as measured at `554895ba`. `main` carries `c8534ffb` (`wt/exchange-variants` merged), `03131f83` (`wt/final-exchange` merged) and `df5a0fb4` (launch-line fix) over `origin/main @ 6e80bcb4`; the push follows the full gate at `df5a0fb4`. Push policy unchanged: workers never push; the manager pushes `main` only after a merge verifies (build clean + NUnit green). Append-vs-append conflicts in `DISCOVERIES.md` / `bugs/discovered.md` **resolve keep-both, never pick a side**.

**Worktrees and unmerged branches — re-measured at `df5a0fb4` (`git worktree list`, `git branch --no-merged main`).** Both endgame branches are merged: **`wt/final-exchange`** (`782a9530`, merged at `03131f83`) and **`wt/exchange-variants`** (`67451e28`, merged at `c8534ffb`). Their worktrees `final-exchange`, `fx-pin` (detached pin at `782a9530`) and `exchange-variants` go once the post-push salvo-vs-exchange perf pair has run from `exchange-variants`' frozen Release build, and the two branches are deleted with them. **Parked WIP, both genuinely unmerged:** `wt/powers-tab` (`7a3d1f14`) and `wt/shadow-relight` (`33c5023f`). `--no-merged main` is exactly 4: `exp-terr-bias`, `wt/powers-tab`, `wt/shadow-relight`, `wt/shellmap-session`.

> ❌ **`wt/shellmap-session` (`2218df8e`) is NOT a pending crash fix — DROP IT.** Triaged 2026-09-10 at `f7b80f17`. The crash was real (`Session.ClientInSlot` is a `SingleOrDefault` that asserts one client per slot; `SetupShellmapBots` appended instead of assigning, so the corruption surfaced one world-load later as an unexplained exit). **It is already fixed on `main` by `e7ca4e1a`** — a better fix by the same author 86 minutes later — in both halves: `LoadShellMapInner` (`Game.cs:551`) resets the session, and `ShellmapBots.SeatBots` (`ShellmapBots.cs:32-34`) is independently idempotent. Pinned by `ShellmapBotsTest.cs`, 5/5 green. This branch is the earlier of two attempts and was never deleted. **It also does NOT merge cleanly** — a real merge gives `UU Game.cs`. The previous wording here ("an unmerged main-menu crash fix") cost one dispatch; that is what this line exists to prevent.

## Working on
- **Both nuclear-endgame threads LANDED 2026-09-20 evening** — the final-exchange redesign with the issue-path one-tick correction (`03131f83`; residual 0 on all eight detonations at `782a9530`, run `260920_180111`, `AutoFireIssueLag` in `DoomsdayStrike.cs`) and the exchange-only missile bodies (`c8534ffb`; cascade-keyed `EscalationMissileActor` at `nuclear-arsenal.yaml:272,359`, selected by the same `IsExchangeLaunch` predicate that reserves the cascade slot, `MissileStrikePower.cs:685-688`). **Open for the user's in-game look, not for more measurement:** the post-cascade whiteout scale and the 16-tick relight cadence during a cascade (`Light.TerrainRefreshInterval 16` on the variant weapons). The `FINAL EXCHANGE launch` debug line names the body that flew only from `df5a0fb4` — before it, `MissileStrikePower.cs:809` printed the power's base `MissileActor`, so a cascade that flew the variants still logged `sarmatmissile`; the perf fingerprint (thermal 0, 12 relight hits over eight detonations) is what showed the variants flying.
- **DEFCON Escalation has SHIPPED — the entry that stood here ("spine shipped, gates nothing, three pieces in flight") is retired.** All five pieces landed: the dividing wall `ab2ac8b8`, hold-fire `2a93fa6e`, the nuclear release ladder `e01217c4` then `7f1e97c7` (clock + 50/100 kt as two rungs), the final-exchange redesign `211da87c`, and the Trident II/W88 ender `165642f5` + `aebdbc95` (**the B83 has left Escalation — do not cite it as America's ender**). **`MarkAsPlaceholder` is now FALSE in C#** and the pace dropdown it gated no longer exists; a host sets the no-rush period in minutes (`world.yaml:948-969`). **`ModeDefault = Skirmish` still stands and must** until the feature is complete — Skirmish is a strict no-op, pinned by `DefconEscalationTest.SkirmishIsAStrictNoOp`. Sandbox is off the dropdown since 2026-09-19 and is now a rules-only opt-in.
- **Scenarios were RE-SCOPED by the user 2026-09-09, and PIPELINE still knows nothing about it.** Decision `08-scenarios-split-in-two-…`: pre-placed units become a plain lobby dropdown that works on **any** map; a "scenario" becomes a purpose-built map with scripts attached. **This is mostly a DELETION** (`scenarios.yaml` layer, `ScenarioLobbyDropdown`, `map.ScenarioNames`). **Sequencing is load-bearing: do NOT delete first** — the main menu background runs through the scenario machinery today. Half the dropdown half shipped as **Forward Deployment** (`a0eb48c5`).
- **Item 56 — supply trucks — is a RUN, not a dispatch.** The blunt fix the user pre-authorised is switched ON in shipped content: `IgnoreDangerForDelivery: true` (**`ai.yaml:1754`** — re-derived; the 09-10 cite of `:1689` is dead) plus `ClusterStickinessNeedMargin: 1000` (**`:1652`**). **The acceptance bar is unchanged and still owed: one bot-vs-bot match on a real map, not a green scenario.** Detail: `PIPELINE.md`.
- **Item 40 — danger-scale stage (c), re-derive 18 ground + 1 air thresholds — remains open** (`PIPELINE.md:338-341` — re-derived at `554895ba`; the 09-10 cite of `:304-307` now lands on item 90). It does **not** gate the benchmark re-baseline: item 43 was taken 2026-09-05/06 (`benchmarks/260905-rebaseline.md`). `DangerFieldLayer` has no per-profile gate, so **`@stable` has already moved**, knowingly.
- **Saved-game restore is STILL RED on a second, independent leak — located, named, NOT fixed, and not re-measured since 08-13.** Net frame 711 / world tick 2130, actor `4712 at.america`: `Mobile.Facing` 256 vs 118, `AttackFrontal.IsAiming` replay-only (`dc8e0abf`). Cites re-checked at `554895ba` and still good: `Activities/Move/AttackMoveActivity.cs` (289 lines), halt at `:195-196`, `GroupDetectedBy` at `:236` → `CanBeViewedByPlayer` at `:247`/`:260`. Still live on `@stable`. **Attack it, do not adopt it — four confident causes have already dissolved.**
- **The 2-human desync is untested against any of it, and the host trap survives with NEW plumbing.** `EnableSyncReports` reaches clients from the host's lobby globals (`OrderManager.cs:159,162`; `Server.cs:349`) — **the user must host** — ANDed with a human-client count and a lobby option (`OrderManager.cs:142-170`). Second trap: an explicit `EnableSyncReports: False` in either `settings.yaml` survives `Settings.Save()` silently (`Settings.cs:421`, erase at `:436-438`) — check both. **The `Detectable` sync change is NOT a fix** (`c440906e`).
- **`ForwardStaging` executes on `@stable` and has still never been watched in a live match** — and it is now **two sites each**, not one: `ForwardStagingEnabled: true` at `ai.yaml:752` and `:3144`, `ForwardStaging: true` at `:2678` and `:2749`. Bare `Move` to 40% of the SR→POI vector, no danger-field consultation. **Watch attack helis at the SR.** Not the same as item 64's `RendezvousWithOffensiveStaging`, false at both sites (`ai.yaml:2258`, `:2353`).
- **Item 22 — case-01 forest ambush: still awaiting one user yes/no, not more measurement.** Bar live and unratified at `parse-case01-bar.py:33-37`. It is `AWAITING-USER.md` §4.

## Gates and counts — re-measured, not quoted
- **`.\make.ps1 test` is now FIVE gates, not four** (`make.ps1:325-338`): `nav-guard` → `lua-gate` → `smudge-gate` → **`mount-gate`** (new, wired at `a1953f70`; core landed `9a513da2`) → `--check-yaml`. mount-gate models the **packaged** root, which is the one layout every other gate is blind to — it is why the v0.1.0 no-main-menu bug reached a tag.
- **Lint floor: `mods/ww3mod/lint-baseline.txt`, 574 lines / 372 entries — `[repo]` 16, `[accepted]` 6, `[requires-ra-content]` 350.** `[repo]` is the number that is supposed to fall and it went 15 → 16 since 09-10. **Never compare a local count with a CI one**, and **diff the error LIST, never the count** *(no lint was run in this pass)*.
- **nav-guard still gates exactly 10 maps** (`tools/nav-guard/baseline.json`, `states.live` = 10 keys) against **357 scenario directories + 10 shipped maps = 367**. A green nav-guard run on a scenario change is real and vacuous. The 09-10 stamp said 320 scenarios; **recount, it grows weekly.**

## Recently shipped
- **Nuclear endgame:** final exchange + map-scaled packages + Trident II/W88 `211da87c`; parallel terrain relight, byte-identical, `ParallelSweep` opt-out `74c3f01d`; nuke-perf rig + four invisible fireball-relight fixes + lever 1 `4061796b` (lever 2 reverted as inert); exchange-only missile bodies `c8534ffb`, issue-path one-tick correction `03131f83`, launch-line fix `df5a0fb4` (2026-09-20 evening).
- **DEFCON borders as map data:** pre-captured structures assigned by the dividing line `a8a497ef`; the editor's Zones tool + painted DMZ moved into each map's `map.yaml` `b2602fb4` (all nine shipped map UIDs changed with it).
- Older landed work (nuclear/FX arc, chrome/release, the 08-06 → 08-13 batches) is in `git log` and `pipeline/archive/shipped-log.md`. **Standing caveat that outlives them:** `f910ac7d` — a green run is not evidence unless something could have made it RED.

## Pending user sign-off
[`AWAITING-USER.md`](AWAITING-USER.md) is still **five** questions with an answer sheet at the top: version string, public identity, Iskander-vs-HIMARS, the case-01 bar, splash/menu art. **Two NEW items need a ruling and are not yet in that file:** (1) **the pre-captured band-only rule** — under `a8a497ef` all 90 eligible structures on the nine bordered maps get an owner and **zero stay neutral**, where the old ratio left 19 neutral including woodland-warfare's Nuclear Reactor and x-lake's central `bio`; the fix, if wanted, is to widen those maps' bands, not to change the trait. (2) **the editor Zones tool** (`b2602fb4`) — whether painting a DMZ in the editor is the authoring path going forward, given `RegionCells` in `rules.yaml` is permanently un-editable. The older highlight list closed at the 08-19/08-20 reconciliations — **do not re-ask it.**

## Quick stats
- Shipped playable maps: **10** (`mods/ww3mod/maps/`). Autotest scenarios: **357** — recount, it grows weekly.
- AI profiles: **2** — `Experimental AI` (`experimental`) and `Standard AI 0902` (`stable`), `ai.yaml:82-90`. There is no Normal/Rush/Turtle ladder.
- Engine C# modified: **not settled here** — `CLAUDE.md` says ~264, a diff against the import commit counts 1293. Two different measures; nobody has reconciled them.
- Suite health: **not re-measured** (no runs in this pass). Last read is `audits/260901-autotest-suite-audit.md` at `97c2fe78`: 189 of 210 `test-*` enter a batch, 64 folders carry no verdict call at all.
