# Hotboard

> What's actively in motion **right now**. The v1 release tracker (`RELEASE_V1.md`) is the source of truth for scope and status.
> The ordered roadmap of what's next lives in [`PIPELINE.md`](PIPELINE.md) — the user steers by reordering it.
> Cap ~40 lines. Rotate stale entries out — once shipped or `[T]`, the tracker / commit history tells the story.

**Reconciled 2026-08-13.** Main @ `dc899995`, **in sync with `origin/main`**. **One worktree outstanding:** `wt/missile-trace` (`C:\Users\fredr\worktrees\ww3mod\missile-trace`, based on `dc899995`, no commits yet) — PIPELINE item 45's Phase 0. `exp-terr-bias` remains the only branch `--no-merged main`. **Push policy:** the user tests from a second computer, so the *manager* pushes `main` once a merge is verified (build clean + NUnit green) and never before; workers still never push. Two machines push to `main` concurrently, and the conflicts seen are append-vs-append in `DISCOVERIES.md` / `bugs/discovered.md`: **resolve keep-both, never pick a side.**

**The lint RED that stood here is FIXED — do not repeat the old warning.** `4d3c8f90` (`wt/lint-sellable`) took it **583 → 100** by overriding `Sellable.RequiresCondition` on `gtwr`/`pbox`/`hbox` to drop the `!being-captured` term (`structures-defenses.yaml:86-87`, `:183-184`, `:270-271`), and `4d19a8e4` removed the 15 `EjectionSurvivalRate` nodes (~85 expected residual). **`make test` is usable as a merge gate again.** Two caveats survive: **no file in the repo asserts the current count** (`bugs/discovered.md:644` still says "402 of 496" and is stale), and **the per-map re-run still never deduplicates**, so any branch adding one autotest scenario raises the total by exactly 3 with no code involved — **diff the error LIST, never the count.** Still open underneath it: 65 maps with no cordon, and nav-guard covers only 10 of 167 maps (PIPELINE item 62).

**Landed 2026-08-12 — the three orphaned branches are gone.** `wt/missile-overfly` (`864537c4`, docs only: the Ataka overfly bug does not exist as diagnosed), `wt/aa-los-test` (`09d44040`, seven AA scenarios + the LOS/commitment findings, no engine change), `wt/autotarget-preempt` (`68b627ce`, **behavioural on both profiles**: a unit switches off a low-priority target when a higher-priority one appears; +8 NUnit cases, 1402/1402 green). The preempt merge had a real code conflict in `SmartMoveActivity.cs` — the dry-unit `AmmoPool.CannotFight` early-out and the new `fromProtectedOverride` out-param were independent edits to the same statement and are now combined, with the flag declared before the ternary so the early-out arm still compiles. **The `@stable` targeting change is a knowing one: the next benchmark baseline must be re-taken.**

## Working on
- **NEW TOP OF QUEUE — user batch 2026-08-13, six items (PIPELINE 56–61) from watching a live `@experimental` bot-vs-bot match. These outrank the 08-12 close-out residue.** **Item 56 — supply trucks still do not commit to a delivery — is the highest priority in the whole queue, above item 40.** The user has **pre-authorised the blunt fix** ("even if we need to completely disable their danger awareness"), and the **acceptance bar is binding: a full bot-vs-bot match on a real map showing a truck complete a delivery — NOT a green scenario.** The load-bearing recon finding: **"disable danger awareness" is SEVEN sites in `SupplyFollowerBotModule.cs`, not one seam**, and site 7 (`:2152 FindSafeFollowPosition`) reads `ThreatMapManager` rather than `DangerFieldLayer`, so **no config flag covers it.** Then: 57 build composition (three symptoms — supply-truck floor of 2 at t=0 is *deliberate*; medic starved by a 9‰ argmax, hypothesis not measured; **no standing-population floor exists for any type except `truk`**); 58 staggered passenger bail (**three of four asks already true — only the pacing is missing**); 59 neutralise-don't-capture money structures (**needs a primitive that does not exist**, already documented for the Supply Route); 60 + 61 command-bar Evacuate button and hotkey audit (**chrome only, no simulation — run these in PARALLEL**).
- **Item 40 — danger-scale rework. STAGE (a) IS DONE (`ddcc5d6c` / `f09183e2`, 2026-08-11) — the "in flight on `auto/danger-durability`" line here was stale.** `HealthDivisor` 10 → 1000, `CostDivisor` 50 → 5000 takes the weight band from **1.20×–29.50× to 1.00×–1.28×**. **Stage (b) is partly performed and its instrument shipped:** a `--danger-reference` command re-derives the reference line offline and reproduces the 2026-08-10 play log digit for digit, giving **ground 3412 → 2748 (−19.5%), air 3627 → 2034 (−43.9%)** — **the two channels rescale by DIFFERENT factors, so an air threshold can never be re-derived from a ground measurement.** Both of stage (c)'s blockers are cleared: a `[danger] pct` line (deciles + p95/p99) now ships, and the "phantom weapon" premise was **refuted** (an unlisted armor class takes 100%, so omission is the opposite of a zero). **Stage (c) — re-derive all 18 ground + 1 air thresholds on a percentile-of-live-field model — is now the whole of item 40**, and it still gates the benchmark re-baseline, which gates items 22, 25, 31 and ambush gate (b). `DangerFieldLayer` is a world trait with no per-profile gating: **`@stable` has already moved.**
- **The danger UNIT, not the thirteen thresholds.** The `[danger] reference` read the 08-09 session asked for came back on 08-10 (`084367b0`) and it is **negative**: `evacLevel` 1,706 against live median cells of 27,919 (USA) / 94,010 (Russia), min/max straddling the median by 2.57 orders, only 92 of 424 types contributing. Every one of the thirteen thresholds sits below the median cell of the field it gates — a scale error in the unit, not thirteen tuning errors. **The sequencing prerequisite is now ANSWERED (`f2a31035`): fix the durability weight FIRST.** It only moves the reference −19% (3,412 → 2,748, because the median type is a 1.22× rifleman), but it divides heavy contacts' stamps by ~20×, so thresholds derived before it would be fitted to a field about to shrink under them. Bonus: an offline parser now reproduces the reference line exactly, so **re-deriving the danger unit no longer needs a live game session**. Plan for user review: `plan-260810-post-measurement.html`.
- **Supply trucks: much built, still not demonstrated fixed — and as of 2026-08-13 the user has said so directly.** Six merges plus a direct-to-main run have attacked the loop from every angle (`e79ddd97`, `574a3c08`, `8d0ff18b`, `0eef99d6`, `5642d931`, + `d1f84a30`…`377085db`), and `test-supply-far-front-reached` passes for the first time. **It has been declared fixed to the user three times and there is still no fourth declaration to make** — the evidence gate (`084367b0`) says the evac guard still trips unconditionally once contact exists. **This is now PIPELINE item 56 with a binding acceptance bar that explicitly rejects scenario evidence**, because `closeout/bdedd544.md` §4 recorded both supply scenarios green while the user's own game showed no deliveries: *a test bed that always reaches a state cannot reveal a broken transition into it.*
- **DESYNC — THIS ENTRY WAS WRONG AND IS REWRITTEN 2026-08-13. Three separate things; keep them separate (full detail: PIPELINE item 42).** (1) **`wt/desync-guard` is MERGED and cleaned up**, not unmerged — its absence from the branch list is the expected end state. (2) The old "leading suspect", divergent Red Alert content under `^SupportDir|Content/ra/v2/`, **is outranked and was never the cause.** The restore desync's first real cause was found by instrument (`0e206359`: the engine's own `Debug.SyncCheckBotModuleCode` fired once, at world tick 1536, matching the independently-bisected onset): **three bot modules were writing synced actor state directly** — `crew.QueueActivity(false, new RotateToEdge(…))` in `PoiOffensiveBotModule.SweepEjectedCrew`, `.EvacuateOutOfAmmoUnit`, and `HelicopterSquadBotModule.Evacuate`. Since `Player.cs:224-232` enables bot logic under `IsBot && Game.IsHost`, **that mutated the host and no other client — a live multiplayer desync in any match containing a bot.** **FIXED at `91056894`**; all three now issue the existing `Evacuate` order. (3) **The `Detectable` sync change is NOT a fix** — verified at `c440906e`, the scenario failed identically to its pre-fix baseline with the token hashed 0 times and `CurrentVisibility` 21–22. Keep it as improved sync coverage; never cite it as a fix.
- **The saved-game restore is STILL RED on a second, independent leak — located and named, not fixed.** Net frame 711 / world tick 2130, actor `4712 at.america` (Russia-bot): `Mobile.Facing` 256 recording vs 118 replay, `AttackFrontal.IsAiming` true in the replay only; frames 709–710 byte-identical (`dc8e0abf`). Mechanism named at `476ddf33`: **the march ends itself on a visibility predicate** — `AttackMoveActivity`'s Stage-2 halt-before-contact (`:138-155`, behind `AmbushTacticsCondition`) drops an Ambush unit to idle via `GroupDetectedBy` → `CanBeViewedByPlayer`, and the two lives disagree at tick 2128. **Live on `@stable`** (`AttackMoveActivity.cs:148-151`). Two mechanisms already died by measurement — a missing order and an external cancel — **do not re-run them.** Next instrument: `CanBeViewedByPlayer` for 4712 and its ambush-radius allies at ticks 2126–2129 on both lives. **Attack it, do not adopt it: four confident causes on this bug have already dissolved.**
- **THE 2-HUMAN DESYNC IS UNTESTED AGAINST ANY OF IT**, and the host trap is unchanged: **`EnableSyncReports` is read from the HOST's lobby globals by every client** (`Server.cs:339` → `OrderManager.cs:114`), so if the friend hosts from a build without `b1178706`, **neither machine records anything** and both get "No sync report available" — identical to the symptom under investigation. **The user must host.** Second trap: `Settings.Save()` only strips nodes matching the *compiled* default, so an explicit `EnableSyncReports: False` in either `settings.yaml` overrides it permanently and silently — check both.
- **2026-08-06 user-report batch — THREE fixes landed on main, all PUSHED (main @ `e15e986b`, NUnit 1148/1148)**: (1) **game clock** `1abb62e1` — clock/time-limit/strategic-progress readouts rescaled under the debug speed button; now format elapsed `WorldTick` with the invariant `GameSpeed.Timestep` (never mutable `world.Timestep`); hover tooltip shows real time. Reported-not-fixed: `TimeLimitManager.cs:110` integer division (limits ~4% long), `PlayerStatistics.cs:89` speed-dependent graph sampling; (2) **transport-idle** `c89d20bb` — humvee overbuy (affordability-filtered composition argmax degenerated to cheapest-type pump) + transport helis parked forever (`Actor.IsIdle` never true for hovering aircraft → evac timer unreachable). **that STILL-OPEN item is now CLOSED** — user granted the `@stable`-altering fix and `auto/transport-lift` merged on MSI (`3e59fed1`, reconciled at `38a6d5fa`): transports can fly lifts at all for the first time, `ShouldBuyTransport` included as the churn-risk finding required. Two adversarial review rounds caught three unexercised defects in the woken path, the worst being a pre-existing uncapped `.Take(MaxWeight)` that would have pinned the `Cargo` lock and bricked the first airframe permanently. **`ForwardStaging` now actually executes on `@stable`** after being inert since it shipped — bare `Move` to 40% of the SR→POI vector, no danger-field consultation on the staging leg, never observed live: **watch attack helis at the SR**; (3) **pips zoom** `e15e986b` — spectator-era sub-MinZoom decoration cull dropped; pips now render at every zoom. **Live validation of all three: the user's next game** (clock true under 2x/4x/8x + hover tooltip; no humvee pile-up, transports evac; pips at all zooms).
- **Item 22 — case-01 forest ambush: CALIBRATING, awaiting user bar.** The provisional cost-ratio bar is ill-posed (÷0); reframe awaits user ratify (`AWAITING-USER.md`).

## Recently shipped / landed
- **2026-08-12/13 window — 55 commits, 19 merges, `4d3c8f90`…`dc899995`.** **Bot synced-write desync fixed** `91056894` (**`@stable` affected** — `HelicopterSquadBotModule.Evacuate` is reachable there, so the benchmark control has been mutating host-only all along). **AA overkill clamp** `16eca8e8` — one AA missile stopped claiming five kills' worth; MANPAD-vs-Halo stand-down **172 → 55 ticks**, measured RED→GREEN at one seed, plus a render-only "holding fire" marker (**`@stable` moves twice**: AA cadence *and* rocket-artillery hold-fire economics). **Onboarding panel** `dd6171cd` — a first-run "There is no Construction Yard" screen, singleplayer-only and version-gated. **LC squad rearm** `484eb913` — a soldier can dock from any subcell, so a squad no longer strands everyone but the first man. **Lint usable again** `4d3c8f90` (583 → 100). **Test harness** `d0a23b0d` — stopped destroying and misreporting its own verdicts (per-run result dirs, `AUTOTEST_VERDICT` banner, named CRASH/NO-RESULT/TIMEOUT outcomes). **Curation complete** `dc899995` — 73 of 73 entries tagged for the 08-10/11 scope, plus two test-methodology rules banked into `AUTOTEST.md`. **CLAUDE.md corrected** `23802389` — the Supply Route capture rule asserted a mechanic that is not wired.
- **NEGATIVE RESULT worth more than most greens (`f910ac7d`): `test-autotarget-preempt-air`'s RED control PASSED.** Both required runs were performed at the shipped 110-tick deadline and **both passed**, so the test cannot discriminate the fix it exists to prove — `68b627ce`'s preemption remains shipped on zero behavioural evidence. The pin was verified *applied* (a trace printed `PreemptScanInterval=0` on the control), so this is not a botched run. **"Preemption is inert" is NOT what was demonstrated.** Banked as a standing rule in `AUTOTEST.md`: a green run is not evidence unless something could have made it RED.
- **User gameplay batch, items 1–6 (2026-08-10)** — vehicle occupants `18838dd7` (critical-damage disable, paced dismount, emergency bail, softer passenger damage); FX pass across all 161 weapons `62d8148e`; dry infantry break off to rearm `175a4784`. **Three `@stable` behavioural deltas declared across the batch** — next benchmark baseline must be re-taken knowingly.
- **User-reported bug run (2026-08-10/11)** — elimination cascade `f49b6aca` (**changes multiplayer outcome resolution**: destroying one bot's SR defeated every survivor slotted after it); medic autonomy `cfcc947b`; dry unit drops its attack order `bd7b6bb2`. All three share one root: a unit with a running activity is never idle, so every "on idle" hook was unreachable for it.
- **Live-play batch 2026-08-08, items 36/37/38 SHIPPED** — crew retire + ground units drive off the map `9ab1b2e2`; tank-trap diagonal squeeze closed `dd3430a8` (**also changes `@stable`** — PathFinder stopped skipping the height-discontinuity rule). Items 34 (transport pickup) and 35 (derrick-rush transports) **remain open** — recon only, no implementing commit.
- **Bot attention model Stage 1 (2026-08-08)** — order gate `0eef99d6`: incumbency + dwell at `ModularBot.QueueOrder`. The wiggle was commitment with the wrong lifetime, not missing commitment — which is why seven prior reimplementations had not fixed it.
- **AI plumbing (2026-08-08)** — posture veto `bd3abacf` (the commitment layer existed and was structurally unreachable); unit purpose `09877fd5` (opening technicians garrisoned rear houses with no enemy on the map, unrecoverable for the match).
- **nav-guard (2026-08-09)** `2754f341` — static per-locomotor connectivity gate, now a prerequisite of `make test`. Exists because a blocking-rule change sealed a 335-cell field on river-zeta and only a reviewer's hunch caught it.
- **Release readiness (2026-08-11)** — text reads as WW3MOD `4836ceed`; Ogg enabled + dead `arabs/` voices dropped `2f31404e`; `tools/cameo` `2c110a67`; **`ASSET-LICENSING.md` `17e3ce4c`** — 1,246 redistributed files inventoried with confidence levels and a removal plan. **Ship-as-is is settled; that document is a planning artifact, not a task list.** Credits corrected `54ea7210`. Captured as PIPELINE item 39.
- **08-09 merges** — danger scale `5642d931`, aircraft rearm `af36e686`, plus the six-document `DOCS/bots/` set.

## Pending user sign-off
See [`AWAITING-USER.md`](AWAITING-USER.md) — the full user-gate queue. Nothing there could be proven resolved at the 08-11 reconciliation; **five new items were added** (post-measurement decisions, the widened benchmark debt, aircraft rearm host, asset-licensing acknowledgement, the 8 failing autotests). Standing highlights: balance proposals 001–003 (verified still unapplied in YAML), parity batch runs, case-01 bar, item-24 gate disposition (KEEP OFF recommended, **verified still committed ON in both profiles at HEAD**), item-31 aggro sweep, fires/brain enablement sweep.

## Quick Stats
- Engine files modified: 280+
- Maps: 13
- AI bot types: 3 (Normal, Rush, Turtle)
- Regression tally 2026-08-10: 60 pass / 8 fail (none traceable to the two engine merges)
