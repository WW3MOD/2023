# Close-out reports — nine manager sessions archived 2026-08-12

Each report has the same four sections: **§1** open work with a next concrete step, **§2** uncommitted or unmerged artifacts, **§3** questions put to the user that were never answered, **§4** knowledge that lived only in the session transcript. All nine validated their own claims against **`main` @ `35876332`**, and were committed together at `9d11d72f`.

**Their open work is folded into [`../PIPELINE.md`](../PIPELINE.md) as queue items 42–54, and their unanswered questions into [`../AWAITING-USER.md`](../AWAITING-USER.md).** _(2026-08-19: the pipeline was split. Items 42–46, 48, 49, 53, 54 now have dossiers under [`../pipeline/items/`](../pipeline/items/); 47, 50, 51 and 52 are closed and live in [`../pipeline/archive/closed-items.md`](../pipeline/archive/closed-items.md).)_ Those two files are the working queues; this directory is the evidence behind them. Read a report when you need the reasoning, the file:line specifics, or the caveat that a summary dropped.

**No report left uncommitted or unmerged work.** All nine §2 sections are "none" — every branch merged, every worktree removed. The one branch still `--no-merged main` is `exp-terr-bias`, which belonged to none of them (see `e929eb99`).

| Report | Manager scope | Left open? |
|---|---|---|
| [`54ab3880.md`](54ab3880.md) | Multiplayer desync — diagnosis and instrumentation (`b1178706`) | **Yes.** Root cause still unnamed, but the suspect changed: `Detectable.visionDetectableConditionToken` syncs a handle instead of a state, and the fix is modelled ten lines below it in the same file. → item 42 |
| [`6361c2be.md`](6361c2be.md) | Six-item gameplay batch — crit-damage vehicles, dismount, passenger damage, FX audit, out-of-ammo rearm | **Yes, but only on verdicts.** All six items delivered and merged; two one-line FX edits held on a user verdict that was skipped, plus the bail-order question. → items 49, 50 |
| [`art-6cde8456.md`](art-6cde8456.md) | Release artwork and audio — cameos, icons, load screen, music, licensing inventory | **Yes, mostly user-side.** Tooling and wiring shipped; **not one art or audio file was added to the repo.** Also the GPLv3 packaging gap, which is an obligation and actionable now. → items 46, 47, 48, 49 |
| [`bdedd544.md`](bdedd544.md) | Supply-truck delivery doctrine, danger-scale measurement, autotest harness | **Yes.** The `@stable` re-baseline (calls it "the most consequential item here") and one scenario that passes without asserting why. **Its §3 questions are records, not gates — it says explicitly not to re-ask them.** → items 43, 51 |
| [`e929eb99.md`](e929eb99.md) | Unnamed sub-manager under the WW3MOD lead | **No — nothing at all.** Never ran any work: no tracks, tasks, backlog, charter, log entries, workers or questions. Its value is two observations about the lead's state it deliberately left untouched (`exp-terr-bias`, `TEMPmt.txt`). |
| [`f5b7d1a6.md`](f5b7d1a6.md) | In-game issue reports #1–#4 — dry-unit attack orders, AA stand-down, medics, elimination cascade | **Yes, one track.** #1, #3, #4 merged; **#2 (AA stand-down) is diagnosed and measured but not fixed**, and survived the second machine's merges. Three unanswered questions, all on #2. → item 44 |
| [`fixes-cfcaa2ca.md`](fixes-cfcaa2ca.md) | One track — `retarget-priority`, a unit drops a low-priority target when a higher-priority one appears (`68b627ce`) | **Yes.** The fix **merged with zero behavioural evidence** — its scenario has never been run at the shipped deadline, because the autotest grant it asked for was never answered. → item 44(b), item 52 |
| [`missiles-e2475f8d.md`](missiles-e2475f8d.md) | Missile system — ground-truth audit, agreed spec, staged repair | **Yes — the whole programme, exactly as left.** Blocked on three design answers; Phase 0 and Phase 1 need no autotest run and can start the moment they arrive. Carries a **warning that `WORKSPACE/missile-diagnosis.md` is unreliable from §2 down.** → item 45 |
| [`multi-d1c4aecd.md`](multi-d1c4aecd.md) | NAT, port-forwarding, server advertisement (`bfd683c2`) | **Yes, four items deliberately deferred** — dedicated server, ~38 dead Fluent keys, a `Nat.Status` snapshot, and whether to keep advertising on `master.openra.net`. **No unanswered questions** — its one question was answered and shipped. → item 53 |

## Cross-cutting, and worth knowing before you read any single report

- **Four reports independently raise the `@stable` benchmark re-baseline, and their counts of the debt do not agree.** They count overlapping-but-different merge sets, and no report counted the others'. PIPELINE item 43 records the disagreement rather than resolving it; whoever takes the baseline should enumerate from `git log`.
- **`54ab3880` and `fixes-cfcaa2ca` / `f5b7d1a6` do not contradict each other** where they overlap — `f5b7d1a6` explicitly checked what `fixes-cfcaa2ca` merged and found it narrows the AA stand-down without closing it. Both are folded into item 44 as one item with two halves.
- **`multi-d1c4aecd` §4 corrects an attribution** that is easy to re-derive wrongly: `wt/desync-guard` **was merged and cleaned up, not lost**, and that manager never worked on desync at any point.
- **Several reports flag their own claims as unverified.** Those caveats are carried into the queue entries verbatim rather than laundered — notably the husk-smoke risk (`FallsToEarth` never traced), the asset-licensing origin column (inference, nothing listened to or viewed), and the `EmergencyBailDelay > 0` path (the only uncovered code path on its branch, and the fix round added logic to it).
