# Close-out — manager `6361c2be` (six-item gameplay batch: crit-damage vehicles, dismount, passenger damage, FX audit, out-of-ammo rearm)

All six user items were delivered and merged. Validated against `main @ 35876332`.

## 1. Open work, and the next concrete step

Two FX edits, both **one-line**, both gated on an unanswered user verdict rather than on any remaining work. Both re-checked against `35876332` and **untouched upstream**:

- **FX audit item 10 — a burning vehicle goes *tink*.** `VehicleCookoffTiny Warhead@Effect` is still `Explosions: piff` / `ImpactSounds: gun27.aud` (`mods/ww3mod/rules/weapons/weapons-explosions.yaml:50-53`) — a small-arms dust puff and a pistol report for a vehicle cook-off.
  **Next step:** swap to `Explosions: napalm_small` / `ImpactSounds: firebl3.aud`.

- **FX audit item 12 — a thrown grenade out-explodes a 40mm GL round by four rungs.** `HandGrenade Warhead@Effect` is still `Explosions: explosion_medium` (`mods/ww3mod/rules/weapons/weapons-ballistics.yaml`, `HandGrenade:` block).
  **Next step:** demote to `Explosions: explosion_small`.

Full context for both is in the committed audit `WORKSPACE/fx-audit.md`, "Changes I would make, in order" (items 10 and 12).

Audit item 17 (un-flatten the middle of the explosion ladder) was **explicitly declined** by the user — "we need more effects, but I don't think it is worth it now". Recorded in `DISCOVERIES.md`; do not re-propose without new art.

## 2. Uncommitted or unmerged artifacts

**None.** Working tree clean, `main` in sync with origin.

Three branch refs outlive their (already-removed) worktrees and are all ancestors of `main` — safe to `git branch -d`:

- `wt/vehicle-occupants` — merged at `18838dd7`
- `wt/ammo-rearm` — merged at `175a4784`
- `wt/fx-round1` — merged at `62d8148e`

The untracked `WORKSPACE/*.log` files in this checkout belong to other sessions, not this one.

## 3. Questions asked of the user, never answered

Both still live at `35876332`.

- **Bail order.** Passengers now bail out roughly 45 ticks **before** the crew. The user asked for parity, believing the crew ejects instantly; it does not — `VehicleCrew` waits for the hull to stop (`StopTimeout: 25`) then waits again (`PostStopDelay: 20`). The new passenger path has neither, because "regardless if the vehicle is still moving" is incompatible with a stop-wait.
  The fix that needs **no** `VehicleCrew.cs` edit is `EmergencyBailDelay: 45`. Verified still unresolved: `Cargo.cs:110` has `EmergencyBailDelay = 0` and **no YAML anywhere sets it**.
  **Caveat that must travel with this decision:** `EmergencyBailDelay > 0` is the only uncovered code path on that branch, and the fix round *added* logic to it (the re-check on arrival plus the `bailPending` interlock). If the user sets 45, that is first-run-in-anger code.
  There is a defensible do-nothing: infantry bail as fast as they physically can while the crew spends a moment trying to stop the vehicle.

- **FX edits 10 and 12.** The user's verdict pass worked down the audit list and ruled on every other item — 6/7/8/9/11/13/14 to do, 16 resolved as *remove* the contrails, 18 conditionally, 17 declined — but skipped 10 and 12 without comment. Read as an oversight rather than a rejection, so both were **held rather than assumed** in either direction.

## 4. Knowledge that lived only in the transcript

**None outstanding.** Everything was persisted before this close-out, across two `log_append` entries, one `log_decision`, `WORKSPACE/fx-audit.md`, `DISCOVERIES.md` and `WORKSPACE/bugs/discovered.md`. The load-bearing items, for a future session that would otherwise re-derive them:

- **The rebase trap, and its generalisation.** `wt/fx-round1`'s `MuzzleSequence` insertions landed on the same `Armament` blocks where `wt/vehicle-occupants` had changed `PauseOnCondition … || critical-damage` → `|| heavy-damage-attained`. Resolving "ours" wholesale would have silently reverted a merged, reviewed gameplay fix with build, tests and lint all green — the only symptom being a burning tank still firing in a live match. **When two branches edit the same YAML blocks for unrelated reasons, verify the merged result against the invariant, not against the conflict count.** Verified here by sorted-diffing the entire `PauseOnCondition` line set, not by counting.
- **Both review blockers had one shape: the guard existed, but the state it read was wrong.** A killing blow reached `Cargo.Damaged` reading `Dead`(32), above `Heavy`(8), so the bail fired and `EjectOnDeath` never ran. The ammo break-off read the `Rearmable`-filtered pool subset rather than every pool. Both inverted an explicit user requirement while the test suite stayed green, because the tests covered pure functions and not the wiring.
- **An auditor's confidence about *intent* is worth far less than its evidence about *mechanism*.** Every mechanical finding in `fx-audit.md` held under review; both of its readings of intent were wrong, and the user overruled both (small-arms hits on aircraft draw nothing *deliberately*; piffs are stripped above .50cal rather than added to the IFVs).
- **`SmokeTrailWhenDamaged` does not exist in this engine** — removed upstream in the 20210321 update. The audit's item 13, which the user approved, would have **failed the mod load**. All 27 blocks are migrated to `LeavesTrails`; the 17 husk blocks map to *unconditional* because `^BasicHusk` never grants `critical-damage`.
- **Untraced risk:** a husk trail is unconditional *and* `TrailWhileStationary: true`, so a husk that comes to rest rather than exploding would smoke indefinitely. `FallsToEarth` was never traced to establish whether that state is reachable.
- **`@stable` benchmark re-baseline owed by five deltas** from this session's merges — vehicle firepower gated at `Heavy` not `Critical`; passenger evacuation; `StateBase.CanAttackTarget` no longer tasking paused-armament units; `ReturnWhenEmpty` on `^Soldier` (shared template, no owner-side split); the unconditional mid-resupply withhold. All disclosed in commit messages per `CLAUDE.md`.
- **Zero on-screen verification across this entire session** — the user played throughout, so no autotests ran at all and every behavioural claim across four merges is static analysis reviewed twice. Highest-value live checks, in order: a **Grad or TOS salvo** (the only edits needing a new sequence *and* a new trait to both be right, neither lint-covered end to end); whether **27 new smoke trails** at `MovingInterval: 2` read as plumes or walls (one number in 27 places); whether a **soldier actually rearms at a Logistics Centre** (infantry have never walked that path); whether the **turret lock at 50%** feels too early.

### Closed upstream, no action

`d53779d9` records the user's ruling that burning ejected crew is **intended**. That retroactively explains their revert of `b3591ef5` (which had bounded the inherited burn via `ExternalCondition`) and closes that thread — the unbounded burn is the desired behaviour.
