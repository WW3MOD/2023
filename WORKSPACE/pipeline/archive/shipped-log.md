# Archive — shipped log

_Split out of `WORKSPACE/PIPELINE.md` on 2026-08-19 at `main @ de78a1ed`. **Text is verbatim.**_

Most recent first. Exact wording pulled from git log / HOTBOARD at the time each entry was written; **this is the archive, and the commit history is authoritative.**

Numbered items that shipped keep their full analysis in [`closed-items.md`](closed-items.md) — this file is the chronological ledger.

---

## LANDED — harness merge complete (2026-07-29)
The three reviewed auto-branches were merged into main on 2026-07-29 via clean `--no-ff` merges (all on origin). Branches left intact — disposition is the user's call (open question posted).
- **auto/may-salvage** (`ec757ad4`) → merged `ae7ca6d8` — tunguska AA ammo-pool ownership fix (`primary-air` rejoins the shared 180-round pool, closing a free-AA-ammo exploit) + m113 dead-reference cleanup; quantified ship-as-is (`plans/260729_tunguska_ammo_note.md`).
- **auto/spread-prefix** (`a55c8b6a`) → merged `23398408` — spread preserves per-unit prefix waypoints.
- **auto/b1-walkback** (`864fdb39`) → merged `2bf335cf` — residual-B1 mid-adjust fix.

---

## SHIPPED
_Most recent first. Exact wording pulled from git log / HOTBOARD; this is the archive, the commit history is authoritative._

- **Release readiness — branding text, Ogg audio, cameo tool, asset inventory (2026-08-11)** — four merges plus two docs commits: `4836ceed` (shipped text reads as WW3MOD, credits screen enabled), `2f31404e` (Ogg Vorbis switched on, unreachable `arabs/` voice set deleted, audio pipeline documented), `2c110a67` (`tools/cameo`), `17e3ce4c` + `54ea7210` (`WORKSPACE/ASSET-LICENSING.md` and the credits correction it forced). **Full framing is queue item 39** above — including the audit's own instruction that shipping as-is is settled and none of its removal plan should be executed now.

- **Medic autonomy — medics stop wedging on orders they cannot carry out (user-reported, 2026-08-11)** — the reported bug was a medic on Hunt appearing to refuse a heal order and then standing still. **The order was never refused.** The heal armament pauses under suppression and a wounded man is usually under fire — so is the medic beside him; `ChooseArmamentsForTarget` filters *disabled* armaments but not *paused* ones, so targeting succeeded, the medic approached, aimed, and `CheckFire` declined silently every tick while the activity kept reporting Attacking. It never completed, so he was never idle, which silenced his auto-heal and his auto-follow together. Fixes: `AbandonWhenArmamentsPaused` (default false, `^MEDI` opts in, also covers a paused garrisoned `AttackBase`); explicit `HealerAutoTarget.SearchRange` (8c0) — it had been derived from the 1c0 heal weapon then min'd with `DefensiveRange`, so **critical-first scoring, claim de-confliction and stabilize-and-switch were all dead beyond one cell** (`434b7555`); `FollowStances` (default Defensive only, baseline-preserving; `^MEDI` opts into Defensive + Hunt, HoldPosition still stays put); new `AttendAlly` trait so clicking a friendly soldier is an order at all (`58c544d0` — clicking an *unhurt* soldier previously produced no order whatsoever); and `a76903f1` stops medics crossing the field for a scratch (ignore patients above 90%). Review fixes at `4a23a210`; a stall guard whose trigger condition was also satisfied by success recorded in DISCOVERIES (`cf9e0562`). (merged `cfcc947b`)

- **A unit that runs dry drops its attack order instead of aiming forever (user-reported, 2026-08-11)** — same shape as the medic bug, found independently: the attack activities never consulted ammo, so a dry unit kept reporting Attacking while `CheckFire` silently declined, `Tick` returned not-finished forever, and a unit with a running activity is never idle — so the existing rearm-on-idle hook was never asked. One shared `AmmoPool.CannotFight` predicate (aircraft carved out, matching the existing RTB precedent); guards ending the activity in `Attack.cs`, `AttackFollow` and `AttackMoveActivity` — that is what releases a unit *already holding* the order, and it covers every producer since they converge on `GetAttackActivity`; issue-time refusal in `AttackBase`/`AttackMove` `ResolveOrder` for non-queued orders, plus the force-attack-ground cursor gap that had no dry check; bot duplicate predicates collapsed onto the shared one. `AttacksSupplyRoutes` deliberately exempt — that order fires no weapon and contestation scores on unit value, so a dry unit parked on an SR is still doing its job. Scoped to orders that execute now (`27763a9b`). `06f0605a` corrects the `@stable` byte-identity claims that had expired at the 0802 parity promotion; `c55bcfe1` discloses the change's real determinism cost. (merged `bd7b6bb2`)

- **Vehicle occupants — critical-damage disable, paced dismount, emergency bail, softer passenger damage (user gameplay-batch items 1–4, 2026-08-10)** — a burning hull stops shooting when it starts burning rather than later (`28f8097a`); a squad dismounts in pairs instead of falling out all at once (`eb87decc`); passengers bail on the crew's cue rather than a damage state later (`4e8e29e2`); a hull hit costs the men in the back half what it did (`3f4dffba`). Reviewed per-item over two rounds; **item 3 was rejected in round 1 for two blockers** — a one-shot kill left passengers alive because Dead outranks Heavy in the bail guard, and the bail fallback could place infantry on impassable terrain — both verified closed (`ad041269`), plus a delayed bail now re-checks the state it was scheduled on (`c4245c89`). **`@stable` inherits three behavioural deltas** and says so: firepower gated at Heavy rather than Critical, passenger evacuation, and bot target-selection no longer tasking units whose armaments are paused. Next benchmark baseline must be re-taken knowingly. (merged `18838dd7`)

- **Visual hit feedback across all 161 weapons (user gameplay-batch item 5, 2026-08-10)** — implements the user-approved subset of `WORKSPACE/fx-audit.md` over two review passes. **The audit's headline premise was wrong in a useful way:** there are no dangling effect references, because the `[SequenceReference]` lint refuses to load a bad name. The real causes are lint-invisible — empty `Explosions` lists, target-type mismatches where one unlisted type suppresses the effect entirely rather than degrading it (`8f5dcbc3`), and a 5×4px impact sprite. Two findings beyond the audit: a SAM intercept of a ballistic missile rendered **nothing** (`^ShootableMissile` is `TargetTypes: ICBM` and not Air, and `CreateEffectWarhead.DoImpact` early-returns on `ImpactActorType.Invalid` before the terrain check); and **`SmokeTrailWhenDamaged` does not exist in this engine** — removed upstream in the 20210321 update, so the audit's proposed edit would have failed the mod load (`f2468743`), all 27 blocks migrated to `LeavesTrails` using that update rule's own field mapping, 17 husk blocks mapping to unconditional because `^BasicHusk` never grants critical-damage. User overruled the audit twice. Companion terrain/prop pass on river-zeta (`8343900b`) with the nav-guard baseline re-recorded (`9bade8c5`). (merged `62d8148e`)

- **Dry infantry break off to rearm (user gameplay-batch item 6, 2026-08-10)** — root cause was a **sampling gap, not a missing rule**: `AutoRearmIfAllEmpty` fires only from `INotifyAttack` and `INotifyBecomingIdle`, and `AmmoPool` does not tick, so a soldier under orders was never re-examined after the shot that emptied him (`ed780538` — "a man under orders is never idle, so nothing ever asked him again"). Bot releases them; box-select skips them. Reviewed adversarially over two rounds — its blocker on the ammo-pool set was real for the combat engineer and **its generalisation to the rifleman was refuted**, verified directly against `infantry.yaml`: `^E1` is the Conscript with matching pool sets and the cited line belongs to `^E3`, which lists both pools; only `^E6` diverged, one class of fourteen (`051d790a` — "empty" meant two different sets of pools). **`@stable` inherits both halves** — `ReturnWhenEmpty` sits on `^Soldier`, shared by both profiles, and the mid-resupply withhold is unconditional by necessity; next baseline to be re-taken knowingly. (merged `175a4784`)

- **Elimination cascade — eliminating one player no longer defeats every survivor slotted after them (user-reported, 2026-08-10)** — destroying one bot's Supply Route in a 3-bot skirmish marked the *player* defeated and killed all their units. `ResolveTeamElimination` mutated `WinState` inside a loop whose team-membership test **read** `WinState`: `MarkFailed` sets Lost synchronously, a Lost player is instantly Spectating, and `RelationshipWith` returns Ally for any Spectating player before consulting the alliance masks — so once the victim was marked, every still-Undefined player after it in slot order was marked Lost too. Membership and win state are now snapshotted before the first `MarkFailed`, and `SameTeam` (alliance masks, immune to Spectating) replaces `IsAlliedWith` at all four sites. **Changes multiplayer outcome resolution.** Also closes the 1v1 "both sides Lost, no winner" case that a 2026-07-30 entry had wrongly declared provably correct — and `b1b39c6b` retracts a sibling-call-site reachability claim that was itself wrong. (merged `f49b6aca`)

- **Supply doctrine — forward delivery made to work, and measured (2026-08-10, direct to main, `d1f84a30`…`377085db`)** — a long single-lane run rather than a branch, alternating fix and autotest so each claim had a scenario behind it. Behaviour: the fleet is sized from **starving customers** rather than a share of army value (`d1f84a30`); a truck is on an errand from the moment it has a target, not from dispatch (`63f2ec48`); an errand in flight has a **frozen destination** (`d1134422`) and a truck with a drop in flight does not evacuate (`e0249693`); the crate is anchored to the platoon and starvation lifts the follow leash (`0ed01e0e`); **danger picks the mode** — drop-and-leave under fire, serve-in-place on a quiet front (`94eb30de`) — with an absolute limb added because when everything is dangerous nothing is relatively dangerous (`9aa441a7`); a standoff you cannot have degrades to the doorstep, not to no anchor (`ececabb7`); a platoon too low on ammo to fight is not available for tasking (`3a53169b`, six-site availability census at `980f846d`); crash fixed reading a trait off a sold truck (`6f24138b`). Harness: refuse a second concurrent run instead of corrupting both (`f3c7a29e`), `--all` skips scenarios that can never write a verdict (`8492416d`), batch runs default to 8× (`8a193c41`), Lua comments stripped before deciding a scenario has no verdict (`bc1874d0`), and tests rewritten so a pass requires the platoon to be **fed without leaving the front** (`b632c36b`, `633d6d62`) — the previous outcome-only assertion had been calling "the front walked to the supply line" a success (`e2552f16`). Two method findings worth keeping: **a bug that cannot fire looks exactly like a bug that is not there** (`b02e60e2`), and **correct in isolation, wrong in combination** (`bf54c71d`). `948b6e71` states the forward-delivery model the knowledge bank had stopped describing. **`test-supply-far-front-reached` passes for the first time** (`377085db`) — but see the SESSION STATE: the `[danger] reference` read is still negative and the loop is not demonstrated closed.

- **nav-guard — static per-locomotor map-connectivity gate (2026-08-09)** — a movement or blocking rule change can seal part of a map off and nothing in the build notices. **The reverted first version of the diagonal-squeeze rule closed a 335-cell hedged field on river-zeta to every vehicle, and was caught only because a reviewer thought to look.** `tools/nav-guard` decodes terrain from `map.bin`, places the statically-authored blocking actors from `map.yaml`, builds the 8-connected movement graph per locomotor and measures the largest connected component against a committed baseline; wired as `make nav-guard` / `make.ps1 nav-guard` (no build, no engine) and as a **prerequisite of `test` on both platforms**. The model is edge-level, not cell-level, because that is the whole bug class — the squeeze rule denies a step between two cells that both stay passable, so a per-cell passability map cannot represent it and a flood fill over one cannot find the pocket. Baseline is a committed file rather than an old-vs-new git diff: a parent-commit comparison only ever sees one step, so a drift of five cells per commit never trips it. Validated against the engine's own map previews. (`e537df0d`, merged `2754f341`)

- **Queue item 37 — tank traps can no longer be driven between diagonally (live-play batch 2026-08-08)** — two traps placed corner to corner let a vehicle drive diagonally between them, defeating the barrier; **River Zeta has 25 traps forming 13 diagonal pairs across two authored defensive chains, every one of them porous.** Scoped deliberately after the first version failed: asking a *generic* passability question made every tree, hedge, rock, wall and cliff a solid shoulder and closed a 335-cell hedged field to vehicles. The rule is now keyed to a `BlocksDiagonalSqueeze` tag carried only by TANKTRAP1/2, read through a `CellFlag` so the test stays O(1) in the A\* inner loop; trees and hedges untouched. Two review rounds — **the second found that a denied squeeze never reached `Move`'s only path-clearing escape** (that escape tests `nextCell`, but a squeeze's blockage is in the shoulders and `nextCell` is open ground), so a sealed route spun a full A\* every ~41 ticks forever, reachable from an ordinary player order; the scenario had passed regardless because its squeezer's None path runs through a trap cell and trips the escape by the ordinary route — **a green test proving the path it took rather than the mechanism it was written for** (`be036370`). Narrowing the `ignoreActor` bail to tagged actors also removes the supply-traffic exemption, so supply trucks no longer ignore tank traps path-wide on every restock. **Changes `@stable` behaviour** — PathFinder's adjacent-cell shortcut also stopped skipping the height-discontinuity rule, so the next baseline needs re-taking knowingly. Unit tests 1206/1206 incl. 8 new geometry pins; scenario `test-tanktrap-diagonal` passed all three lanes. (merged `dd3430a8`)

- **Queue items 36 + 38 — evacuation means leaving (live-play batch 2026-08-08)** — item 36 stops `PoiOffensiveBotModule` recruiting ejected crew into armoured pushes; item 38 makes an evacuating ground unit drive the last two cells **past** the boundary under `Drag` instead of vanishing several cells inside it. Adversarial review returned MERGE WITH FIXES and found three defects, two sharing a root: **`Drag` calls `SetCenterPosition`, which never calls `SetLocation`, so the actor's CELL never leaves the map** — which is why the out-of-bounds audit came back clean (every CPos-keyed consumer is safe by construction, not by guard) and equally why the boundary-cleared exit was dead code and the occupancy comment was inverted. Also fixed: cancel during the drive-off left the unit alive off-map with no sell, because `Activity.Cancel` tests `IsInterruptible` on itself, not its child. **Carried residual, logged not fixed:** the change's own claim that no other module recruits crew was disproved — **five do, and none consult `IsEvacuating`**. `EvacDriveOffMath` + 80 lines of pins. (`02fe9eab` + `3b1a3ac6`, merged `9ab1b2e2`)

- **Order gate — bot orders get an incumbent and a dwell (2026-08-08)** — Stage 1 of the attention model, against the user's most visible complaint: units wiggle forward, back, forward to a different spot every few seconds. **A census found the cause is not missing commitment but commitment with the wrong lifetime** — ~28 anti-churn dampers already exist, each private to a module and purged the moment the unit leaves that module's eligibility set, and eligibility is exactly what flickers. **The dedup memory is destroyed by the same event that triggers the re-issue, which is why seven reimplementations had not fixed it.** Adds one record whose lifetime no module can reach plus two predicates at `ModularBot.QueueOrder`: the incumbent module keeps a unit unless a challenger outranks it (replacing an arbitration that was previously whichever module happened to be declared later in `ai.yaml`), and a different-destination order to a busy unit whose standing order is young is suppressed. Equivalence dedup deliberately NOT the instrument — in the top churn sources the destinations genuinely differ. **Three review rounds and the arc is the point:** round 1 found `QueueOrder` returned void, so callers advanced "I already ordered this" caches after orders the gate had silently discarded, **stranding units permanently** (`1767ec8b`); round 2 found the same class recurring, and suppression was made opt-in so a forgotten mark costs damping rather than safety (`96a2b8c3`). Census: `WORKSPACE/recon/260808-order-churn-census.md` (`977210c7`); staging plan `WORKSPACE/plans/260808-bot-brain-staging.md` (`a77cac90`, reconciled `89352a37`). (merged `0eef99d6`)

- **Supply trucks drop and leave (2026-08-08)** — a static destination dissolves the follow/evac limit cycle: the truck places a SUPPLYCACHE near the demand and departs, so abort geometry, scan phase and enemy weapon envelopes stop deciding whether resupply happens. **The mechanic already existed — 750 supply, 4-cell aura, merge-on-drop — and the bot simply never chose to use it.** Two adversarial rounds: round 1 found four plumbing defects and the repair introduced a latch, which the implementer caught itself; round 2 cleared the code but scoped its green to exclude the tests, **and that gate earned its keep — the pin covering the latch repair was a tautology over `ShouldIssueDrop`'s own signature, passing whatever the module did, while its comment claimed to cover a void block it never reached** (now `ErrandStillRunning`, verified by counterfactual, `34c39abe`). Notable in the design: the dispatch record voids off the truck's **observable state** (idle and still loaded) rather than off an acknowledgement, because `ModularBot` silently skips orders whose subject is player-controlled — a timeout would have stranded the truck, state-keyed voiding self-heals on the next scan. (`a86e2fb6` + `d6276da6` + `f027339b`, merged `8d0ff18b`)

- **Truck churn — damp the two fastest oscillators (2026-08-08)** — **the truck's fastest wobble is an activity, not an order, so it never passes the bot's order funnel and no order-level damping can reach it**: a residue latch flipping on a 7-tick scan gating a per-tick `RotateToEdge`. One bad 0.42 s sample was enough to send a loaded truck driving for the map edge to sell itself, until a later bot scan happened to catch the latch clear and dragged it back. The residue verdict has no scalar to band — it is three independent boolean axes (aura membership, need threshold, affordability) — so a threshold band on any one leaves the other two undamped; hence a **dwell**: `ResidueConfirmScans` requires ~2.1 s of agreement before the latch sets, while the clear stays instant. Review verified the asymmetry eliminates the oscillation outright in the observed regime and slows the adversarial one by 3×. **The asymmetry rests on cost, not irreversibility — an earlier draft claimed the set could not be undone, and that was wrong; the reversal it described is precisely the wiggle being fixed** (`670b0a20`). Also gives the follow Move the deadband the drop path in the same module already had. (`122c5f62`, merged `574a3c08`)

- **Unit purpose — an arriving unit gets an owner, and a reason to garrison (2026-08-08)** — a bot's opening technicians walked into a rear civilian house and garrisoned it **with no enemy anywhere on the map**, then were unrecoverable for the match: no bot module can unload a garrison, and the blackboard claim was released once the unit left the world, so the books showed a free unit that no longer existed. Two causes, one not where recon said. `GarrisonBotModule`'s gate contained **no enemy, danger, belief or POI term at all** — its only threat-aware code was a sort comparator, a reordering that never filtered — so with no enemies every building scored equally and it picked an arbitrary house on the bot's first tick. And opening play had no owner: `LayeredDefence` defers to `SquadManager`, which skips ground units without claiming, which defers to `PoiOffensive`, which returns before it builds a free pool. **A to B to C to nothing.** Recon proposed unblocking that return; **the implementer refuted it and was independently confirmed** — `IsEligibleCombatUnit` narrows to MainBattle or IndirectFire and a technician is CaptureSpecialist, so `StageFreePool` structurally cannot stage one in any game state. The real owner was `CaptureCoordinator`, which discarded the undispatched remainder of its scan with no order, no claim and no log, manufacturing exactly the idle unclaimed unit garrison then recruited; it now musters them. `@stable` inherits the garrison gate and the capturer reserve deliberately (`acb471ff`). Census `WORKSPACE/recon/260808-unit-purpose-census.md`; three false claims from the review corrected at `45032e07`. (merged `09877fd5`)

- **Posture veto — bound the sector posture-hold so a flanking axis can commit (live-play batch 2026-08-08)** — the user's report was that the bot sends nearly everything at the centre while the flanks sit undefended and full of capturable POI, and a group that does go flanking is "constantly ordered back, and then forward again, stuck in a loop"; their ask was a layer that decides "I will attack here, with these units" and commits. **The commitment layer already existed and was unreachable.** `ApplyMissionCommitment` is the only writer of `axis.Committed`, and `PartitionHeldAxes` opens with `if (!axis.Committed) continue;` — all three fall-back gates return **above** that snapshot, deliberately, so a held axis is not frozen out of its own release gate. The consequence is structural: a posture-held axis is never stamped, so it is never protected, so it re-enters the full reshuffle every eval **with no memory of what it decided last eval**. The commitment window and abort baseline were fine; they simply never applied. `SectorPostureHoldMaxEvals` (0 = unbounded = legacy; `@experimental` 3) makes them reachable, with a deliberately monotone counter — one that decayed on the press half of a flipping verdict would refund budget as fast as it is spent and bound nothing. **The diagnosis ships as three candidates rather than one**: the first draft asserted a period-2 rearward oscillation and review refuted it for the profile it ships on, so the branch instruments to tell the three apart rather than guessing (`4776e713`). (merged `bd3abacf`)

- **Supply dwell — close the evac control loop, then damp it (2026-08-07)** — user report: supply trucks drive part-way toward the front, get ordered back toward the SR, and repeat, never resupplying anyone; three previous attempts had not stuck. Recon (`WORKSPACE/recon/260807-supply-truck-oscillation.md`) found it is **ONE module fighting itself, not two modules contending**. **The loop was open, which is why a damper was never going to fix it:** `SupplyFollowerBotModule` chose between a follow branch and an evac branch every ~150 ticks on a memoryless threshold, and the danger term it consulted was danger **at the assigned cluster** — a quantity that does not change when the truck moves. A decision that cannot respond to the action it causes has no fixed point, so damping alone converts a continuous retreat into a periodic one. Worse, selection took the highest-`AmmoNeed` cluster, which is the one that has been fighting, which is the one in the most danger — **the criteria for selecting and for refusing were positively correlated, so it was structurally guaranteed to pick a destination it would then refuse to approach.** Also fixed: `GarrisonBotModule` permanently claimed trucks through a `PassengerInfo` fallback and never released them (a *frozen* truck, distinct from the oscillating one, on `enable-ai-any` so it hit every profile); `FindOwnSupplyRoute` took an arbitrary rather than the nearest SR, so a retreat could step toward the front; and **the danger field's territory baseline is written only to cells with both coordinates odd**, so any single-cell read was parity-aliased by up to ~40. Destination-trust flag inverted to fail safe (`f2fb794f`). (merged `e79ddd97`)

- **Vehicle wobble fix — item 28 REGRESSED, disabled by default (live-play bug, 2026-07-30)** — vehicles crabbed side-to-side on near-straight (non-axis-aligned) moves. Root cause is queue item 28 (string-pull, `1f036ecb`), NOT item 27's turn tuning: `Move.cs:233` aims each rendered segment at the string-pulled sightline target while string-pull deliberately leaves *facing* on the cell axis, so the rendered travel gains a large perpendicular component — the unit crabs — and the `MoveSecondHalf` snap back to the exact cell centre (`Move.cs:735` + `SetPosition` `Move.cs:760`) turns it into a per-cell sawtooth. Within OpenRA's snap-to-every-cell-centre model, item 28's first-half-only smoothing makes the sawtooth *sharper* than the honest staircase. Fix (user's call): flip engine default `MobileInfo.StringPullMovement true→false` (one line, reverts `Move.cs` to pre-item-28 for all movers; item 27's turn/speed gains kept). All item-28 code + tests retained for a future correct attempt. **Rework path** (own pipeline item, not this window): ride the sightline across cells — smooth both halves + stop the centre-snap — which touches synced `CenterPosition` (desync risk). New NUnit pin `PathStringPullingTest.SmoothedTravelCrabsOffTheFacingAxis` models the real snap-to-centre re-anchoring and pins the 204-WDist/cell lateral crab (the pre-existing `ShadowDivergenceStaysBounded` mis-modelled re-anchoring as `from = smoothed`; comment corrected). Engine, 2 files.

- **FiringLOS off-map crash fix (live-play bug, 2026-07-30)** — `HasClearLOS` indexed `map.ShadowLayer[lookupFrom]` with no outer bounds check (the existing "Bounds check" comment only guarded the inner layer). Off-map actors are routine (map-edge reinforcements; airborne targets past the edge), and `CellLayer`'s indexer throws `IndexOutOfRangeException` on an out-of-map `MPos` — crashing the game from `AttackFollow`'s tick. Reported from live play on two machines. Fix guards both `HasClearLOS` (off-map → treat as clear, consistent with the other no-data paths) and `GetGroundShadowDensity` (off-map → return 0). Engine, 1 file, +9/−1. (`99a58363`, on main — pushed)

- **Omniscient InfluenceMap migration — capture + garrison repointed to belief (queue item 24, default-off, @experimental gates)** — closes the declared Stage-F gap: `CaptureCoordinatorBotModule` capture ordering and `PoiGarrisonBotModule` garrison ordering AND sizing stop reading the omniscient `InfluenceMap.GetEnemyInfluence` and re-derive threat from the BELIEVED `DangerFieldLayer.GroundDanger` (fog-legal: per-player field, own belief, 0 fallback for never-observed). Same `suppressOmniscientThreat` seam pattern as Stage-F offense: PoiMap grows the default-false seam + `BelievedThreatFactor` (capture: threat lowers score, 100/60/20 decreasing) / `BelievedDefendFactor` (defend: threat raises score, 100/150/250 increasing — the mirror bucket); gated call sites `Info.StrategicCaptureRepointEnabled` / `Info.DefendRepointEnabled`, both `&& dangerField != null`. **Byte-identity off-gate proven at review**: `suppress ? null : ...` / `suppress ? 0 : SampleThreat(...)` collapse verbatim to the old expressions; zero new SharedRandom draws; ActorID total-order sorts. YAML gates live ONLY under @experimental (ai.yaml capture :180-185, garrison :309-314); @stable twins carry NONE; per-profile trait instances with distinct `RequiresCondition` make the single flag sufficient. Two DISCOVERIES surprises: `SampleThreat`'s fallback path is itself omniscient (`FindActorsInCircle`) — suppressed at both call sites with PITFALLs; garrison consumed threat twice (ordering + sizing), both repointed together for scale consistency (`BelievedDangerHostileThreshold: 120` on the GroundDanger scale). Residual omniscient callers (deliberate, out of scope): `LayeredDefence` :248-249, `MountedTransport` GetFrontline/GetFriendlyInfluence, `FrontlineOverlay` :54. Adversarial review: **MERGE** — all 7 brief points attacked at file:line and held, incl. file-level YAML verification (the item-28 failure mode) and byte-identity line trace; NIT: `influence-stack.md` §Known gaps line now stale for @experimental (curation pass); OBS: capture path runs suppress+rescale twice per tick under repoint (deterministic, accepted). **Gate enablement awaits the item-25 re-baseline** before any @experimental default-on claim. 6 NUnit sign-convention pins (`PoiMapTest`; **524 total = new baseline**). Verified post-merge on main: build 0 errors, NUnit 524/524. (4 commits `670d05d5`..`ba387afa`, merged `646515bd`)

- **Vehicle turn feel — snappier hulls, no speed bleed (queue item 27, YAML-only)** — two-lever pass on vehicle cornering: (1) `TurnSpeedLoss: 1 → 0` on the `^Vehicle` template — the engine subtracts this from speed every tick a unit is inside a turning arc (`Move.cs:551`); no per-unit overrides existed, so one template line removes the mid-turn speed bleed uniformly and reverts by deletion. (2) ~+30% uniform hull `Mobile.TurnSpeed` per-unit (every combat vehicle overrides the template, so a template raise alone was insufficient): `^Vehicle` 20→25, `^Walker` 10→13, humvee/t72 15→19, m113/bradley/abrams/strykershorad/btr/bmp2/t90/tos/tunguska 10→13, m109/m270/HIMARS/grad/giatsint 8→10, iskander 5→6. Turret `TurnSpeed` untouched; only hull-fixed shooters (no `Turreted` trait) are giatsint + iskander, both low-rate artillery — negligible aim impact. Recon correction folded to DISCOVERIES: turning **arcs** cover all cornering under ~135°; only the ~180° U-turn band stop-turns. Demo `demo-vehicle-turns` (4 USA lanes, 90° serpentine loops, HoldFire, no enemies; lane 1 abrams×3 clearest; verdict-free per DEMO rules). Verified via lint (baseline 391; 394 with demo = pre-existing per-map gtwr/hbox/pbox being-captured errors ticking 104→105, no new error types) — worker correctly did not launch the game (focus-stealing). Manager diff-inspection (YAML-only lane): full diff read line-for-line against report, verdict-grep on demo lua clean. **Feel sign-off pending user**: `./tools/autotest/run-demo.sh demo-vehicle-turns`; A/B by reverting the tuning commit `2829bc60`. (2 commits, merged `aab56954`)

- **Path string-pulling — visual turn-chain smoothing for full-cell movers (queue item 28, engine, default-on for vehicles)** — visual-WPos string-pulling that replaces the cell-by-cell zigzag of A* paths with straightened segments for full-cell movers: new pure `PathStringPulling` geometry (DDA supercover walk for passability of the straightened chord, divergence clamp bounding visual-vs-logical offset) injected at three `Move.cs` segment sites; the sim path is untouched — logical cell occupancy, triggers, and combat positions are byte-identical, only the rendered trajectory smooths. **Cycle**: first adversarial review returned DO-NOT-MERGE — the gate `!AlwaysTurnInPlace` was a no-op (property commented out mod-wide), meaning infantry would smooth too; fix cycle replaced it with `!LocomotorInfo.SharesCell` (locomotor-structural: `SharesCell: true` on exactly the four `foot*` infantry locomotors in world.yaml; engine default false; `Mobile.cs:316` assigns subcells only when SharesCell — so the gate is "full-cell movers", precisely the vehicles/walkers set). False doc claims corrected in all 4 places including the upstream recon error (`recon/260728-movement-locomotion.md:22`); NIT test reworked into a genuine stateful re-anchor accumulation check (`ShadowDivergenceStaysBounded` threads `from = smoothed` forward). Re-review (adversarial, file-level verification of every YAML claim — the original failure mode) returned **MERGE**: independent rebuild 0 errors, NUnit 508/508 on branch. Carried OBS: transient rendered-sprite overlap with another moving vehicle's cell ≤512 units, cosmetic; two pre-existing float usages in Move.cs acceleration index; zero-length smoothed segments self-correct; naval locomotors would smooth if ever enabled (fine — revisit if naval ships); vehicle-trajectory non-byte-identity is the accepted item-25/26 contract (re-baseline required, run plan `7fa0b046`). 9 NUnit pins (**512 total = new baseline**). Verified post-merge on main: build 0 errors, NUnit 512/512. (merged `1f036ecb`)

- **Territory overlay v2 — player-facing control view + staleness stripes (queue item 23, render-only)** — new `TerritoryOverlay` trait (`IRenderAnnotations`, world actor): whole-map green(own)/red(enemy)/gray(contested) wash from the viewer's own Stage-C `ControlField` (`OwnerAt`/`ScoreAt` — no god-view; identical fog discipline to `DangerOverlay`), alpha ramped by hold margin (owned 95→180 for legibility on dark temperate terrain); diagonal **white** staleness stripes (light hatch reads over both wash and bare ground; dark stripes vanish) with intensity ∝ `ticks-since-verified` via new read-only `ControlField.TicksSinceVerified` accessor (pure reads, mirrors `IsVerifiedFresh`; `int.MaxValue` for never-observed → max stripe immediately) and pure `TerritoryStripeMath.StripeAlpha` (clamp-BEFORE-multiply, overflow-pinned). Toggle: hold-`T` (`ShowAllOrders`/Space pattern — WorldRenderer flag → ViewportControllerWidget → ingame.yaml/hotkeys.yaml; only collision is editor-context `EditorActorsTab`, mutually exclusive); `/territory` dev chat command for headless capture; `/danger` untouched. Sim-pure by construction (render-only; zero writes, zero `[Sync]`, zero RNG — reviewer-grepped whole diff). Demo scenario `demo-territory-overlay` + DISCOVERIES entry (HomeLocation/Voronoi demo trap: stripping `MapStartingLocations` collapses seeds to CPos.Zero for both sides; overlay legibility gotchas). Adversarial review: **MERGE, 0 FIX, 0 NITs** (4 accepted OBS: per-cell render allocation while held matches `DangerOverlay` precedent; faint stripe on in-vision cells by design; environmental MSB3026 copy-retries; `/territory` not dev-gated, same as `/danger`); reviewer independently re-ran build+tests and re-judged both screenshots (gate-off clean / wash+stripes correct). 4 NUnit pins (`TerritoryStripeMathTest`; **503 total = new baseline**). Verified post-merge on main: build 0 errors 0 warnings, NUnit 503/503. (5 commits, merged `b8ead59f`)

- **Sprite pipeline foundation — export → edit → import → see-in-game workbench (queue item 29, tooling only)** — the full loose-sprite workbench with zero engine C#: `tools/sprite/export.sh <SPRITEFILE>` (Utility `--extract` + `--png` into `work/<prefix>/` as indexed PNGs; handles tileset-ext sprites like `t01.tem`, verified on `t01.tem` 10 frames + `e1.shp` 438 frames) and `tools/sprite/import.sh <PREFIX>` (validate → rebuild SHP; `validate.py` hard-fails on non-Indexed8, unequal frame size, or palette drift vs `temperat.pal` — all three fail-paths tested; round-trip byte-clean). Wrappers relocate Utility output out of `engine/`. **Recon open questions answered:** (1) loose-file-over-mix precedence → **LOOSE WINS** — "last-mounted package wins" (`FileSystem.cs:195` `LastOrDefault`; `ww3mod|bits` mounts after the RA mixes), proven empirically with a rebuilt `3tnk.shp`; caveat — the loose filename must match the *resolved* name (`UseTilesetExtension` trees need `t01.tem`, not `.shp`). (2) `PngSheet` enabled in `mod.yaml` SpriteFormats (one additive line, one-line revert) — indexed AND RGBA loose PNGs render in-game (screenshot-verified); auto-sliced frames anchor CENTERED, source SHP offset lost (`PngSheetLoader.cs:142`) — fix via sequence `Offset:` or `--png-sheet-import` metadata; indexed PNGs carry an embedded palette → **no player-colour remap** (use the SHP path for team-coloured units); RGBA = palette-independent truecolour. Findings in DISCOVERIES (2 dated entries). Creative/art use stays BACKLOG per the brief. Manager diff-inspection (tooling tier): footprint exactly 5 new files under `tools/sprite/` + 1 mod.yaml line + DISCOVERIES — clean. (`1d0845a0`+`fac291e9`+`4376d5bc`, on main)

- **Stance-aware cover positioning — one order, good positions (queue item 21, unblocks case-01)** — a concealment-refinement layer on group-order slot resolution in `CohesionMoveModifier` (the `IModifyGroupOrder` seam): gate `isHuman && stance == Ambush && cohesion != Tight` — select an ambush-stance squad, click once at a tree cluster, each unit's formation slot is refined to the best concealed cell within radius 3. Score = `Map.ForestGroundShadow(Σ tree density, 5×5 window)` — the item-26 superlinear curve reused as the concealment currency; per-slot candidates scored `effective = concealment − chebyshev·distancePenalty` with a strict improvement margin (open terrain scores 0 everywhere → **byte-identical to stock formation placement**); deterministic tie-breaks (effective → concealment → chebyshev → (Δy,Δx)); conflict-free by construction — `taken` set seeded with ALL original slots, own slot freed per-unit (reviewer-demanded coverage: pure `ResolveConcealmentSlots` driver + 5 seeding tests). Passability first (`map.Contains` + `CanStayInCell`). Order-time only — `StancePositioningExecutor` opts out of Ambush (`Stance < FireAtWill`), so nothing repositions afterward; cache key extended with the ambush flag symmetrically. **Bots byte-identical by gate** (sole invocation from synced `UnitOrders.cs:410`; bots default FireAtWill — frozen AI benchmark untouched). 4 tunable Info fields; PITFALLs at the stance gate + first-subject Mobile convention. Adversarial review: GREEN-WITH-NITS, 0 blockers — byte-identity traced per-vector (call-site, bot default, open-terrain incl. degenerate params), cell-conflict invariant proven by induction; both nits resolved (test coverage landed `5ab3c36b`, mixed-stance gating documented as PITFALL). 14 NUnit pins (`StanceCoverPositioningTest`; **499 total = new baseline**). Verified post-merge on main: build 0 errors, NUnit 499/499. (`5ab3c36b` et al., merged `5c6cc1f0`)

- **Forest concealment & cover — conservative pass (queue item 26, feeds case-01)** — Phase 1: superlinear ground shadow-attenuation curve via new pure `Map.ForestGroundShadow` — byte-identical to the old linear curve for Σdensity ≤ 20 (thin treelines still barely dent detection), each further density unit counts double above the knee (density-10 cells: 1→1, 2→2, 3→4, 4→6, 5→8, 6→10). Net: 4 dense cells hide stock Vision-3 infantry from moderate-range (13-16c) viewers; 6-deep forest hides until point-blank. Airborne shadow deliberately left linear (heli spotting asymmetry preserved). Both `shadows.bin` regenerated in the same commit; PITFALL at the bake site. Phase 2: new `DensityModifiesDamage` (`IDamageModifier`) — the recon's dormant `TerrainModifiesDamage` keys on painted terrain type and trees sit on Clear tiles, so a density-window sibling was built instead: 3×3 windowed `DensityLayer` sum (adapted from `CohesionMoveModifier.CoverScore`; includes center cell, no own-cell guard — functionally identical for infantry since trunk cells are impassable), ladder on `^Infantry` density 15→94% / 30→88% / 50→80% (max 20% reduction, deep forest only), never dampens healing, integer-only, zero RNG. **Global by design (humans+bots) → item-25 re-baseline required before trusting any bot-improvement claim.** Phase 3 (read-only): **ShadowLayer stays BAKED — tree death does NOT open sightlines** (dynamic pipeline disabled 260503, no live callers: Building.cs:386-398 mutation commented, Map.cs:1013-1082 "CURRENTLY UNUSED", World.cs:505-508 flush commented) — gates the BACKLOG dynamic-forest ideas. Adversarial review: GREEN-WITH-NITS, 0 FIX (byte-identity below knee proven — all authored densities are multiples of 5, float-exact; determinism clean; citations accurate; tests non-tautological); comment-precision nits landed as `27c48099`; accepted residuals: `^CivInfantry` inherits cover (negligible), no dedicated concealment autotest (case-01's scenario is the integration test). 23 NUnit pins (`ForestShadowTest` 12 + `DensityModifiesDamageTest` 11; **485 total = new baseline**). Verified: build green, NUnit 485/485, lint 391 = baseline. (`db9545a1`+`69e6ee86`+`0bc2b73d`+`27c48099`, merged `fc9fe396`)

- **Ambush Stage 4 — bot lane-ambush consumer (queue item 8, final stage)** — `LaneAmbushBotModule`, an @experimental-only `IBotTick` player trait (`RequiresCondition: enable-ai-experimental`, deliberately NO `@stable` twin — byte-identity by absence): derives beachhead→enemy-SR corridors (new public `PoiMap.OwnSupplyRoute` seam; enemy SR is a fog-legal public anchor), posts up to `MaxAmbushes 2` × `UnitsPerAmbush 2` at `PostFractionPct 40`% of the lane (our side of the midline), grants `enable-ambush-tactics` at runtime through the shipped `ExternalCondition@ambushtactics` seam (the first real granter — Stages 1–3 turn ON per-unit while everyone else stays gate-0), ledger-commits `ambush:<enemyAnchorId>` against squad re-issue oscillation (s1+2 OBS-2), single `queued:false` AttackMove + `SetUnitStance` (OBS-3), polls the new read-only `AutoTarget.AmbushSprung` accessor and releases sprung units with a stance reset that clears the terminal latch. `CanHostAmbush` is a STRUCTURAL filter (gate-wired + grantable seam) — excludes the whole `^AutoTargetGround*` family automatically (OBS-1), self-heals if the gate is ever wired wider. Zero RNG (deterministic stagger constant, `OrderBy(ActorID)`, integer WPos math with overflow guard) — stricter than the sibling garrison module. Pre-merge hardening fast-follows (`67071b02`): `TraitDisabled` release-everything cleanup (tokens + ledger + stances — ICBM-leak lesson) and `suppressOmniscientThreat: true` on the offensive-target read (fog-legality parity with offense). Adversarial review: MERGE, 0 FIX, 6 OBS (notable residuals: FFA/2v2 anchor-set oscillation on close-scored SRs; sprung unit re-taskable within the ≤100-tick re-eval window — both bounded). 10 NUnit pins (`AmbushLaneMathTest`: 7 PostPosition + 3 LaneIsViable; 462 total = new baseline). Verified: build green, NUnit 462/462, official lint 391 = NEW accepted baseline — the 382→391 drift predates Stage 4 (present on base `4b28ed4f`, no new maps, benign warning classes; Stage 4 net-zero). Gate (b) benchmark pricing still owed before default-on. (`d0992098` + `67071b02`, merged `2b6c6166`)
- **Ambush Stage 3 — stationary literal-ambush state machine (queue item 8, partial)** — a stationary Ambush unit now runs the §5.2 DORMANT → TRACKING → SPRUNG machine behind `enable-ambush-tactics` (still granted by NOBODY in shipped rules): pre-aims the best fog-visible target, then springs on the FIRST of five triggers — (1) detected, (2) damaged (via the existing `INotifyDamage` latch), (3) best-strike degrading (K-tick exit prediction from range-sample-derived radial speed + `MinSpringThreshold` + 2-sample hysteresis — fires *before the best shot is lost*, the reframed "spring near optimal"), (4) saturation (`HighSpringThreshold` sustained — covers enemy-stops), (5) overrun (`max(weapon MinRange, floor)` breach). Worthwhile score = fog-filtered `FindActorsInCircle(killZoneRadius)` at 25-tick staggered cadence, `OrderBy(ActorID)` before every pick; SPRUNG terminal until stance reset — a bot ~75-tick re-issue re-idles a sprung unit *sprung*, no latch churn (OBS-2 clean); one-way Stage-2→Stage-3 handoff, the machines never run concurrently. **Byte-identity**: gate read first; ungated branch character-identical to stock (reviewer diffed vs pre-change); new fields non-[Sync], gated-path-only; zero RNG (full-diff scan). 14 config knobs on `AutoTargetInfo`; pure trigger helpers in `AmbushTactics.cs`. Fork A shipped as no-new-code (prone-when-stationary already renders; no concealment claim); unused "running peak" dropped. Adversarial review: MERGE, 0 FIX, 9/9 targets pass. Review OBS for Stage 4: kill-zone score vs weapon-range trend are different spatial domains (trigger 3 can be score-blocked on a lone out-of-zone target — defensible, undocumented); trigger-2 enum path NUnit-only at runtime; convoy GREEN can't distinguish trigger 1 from 3/4 — **RED baseline run mandatory before trusting GREEN**. 21 NUnit pins (452 total = new baseline). Verified: build green, NUnit 452/452; lint skipped (zero mod YAML — reviewer-confirmed). 3 authored autotest scenarios (`test-ambush-{convoy,enemy-stops,fast-convoy}`) NOT yet run — RED+GREEN batch awaits user goahead. (`d7549f83`, fast-forward merge)
- **Ambush Stages 1+2 — executor fire-stance opt-out + halt-before-contact (queue item 8, partial)** — Stage 1: `StancePositioningExecutor` relinquishes units whose `AutoTarget.Stance < FireAtWill` (Ambush/HoldFire) — the "executor un-ambushes my units" bug fixed; C# live-[Sync]-property check chosen over the planned `RequiresCondition` wiring because `CheckConditions` lint is per-actor and grant co-location isn't structurally guaranteed (reviewer endorsed the deviation). Stage 2: gated halt-before-contact in `AttackMoveActivity` — an Ambush-stance unit on attack-move/auto-move halts (latch-and-drain, terminate-to-idle so `INotifyDamage` damage-springs still fire) when its group is fog-legally detected (`CanBeViewedByPlayer` OR, 10-cell radius, 10-tick cadence, order-independent, zero RNG); plain Move always obeyed (fork B structurally enforced — Move never constructs AttackMoveActivity). Gate `enable-ambush-tactics` granted by NOBODY in shipped rules; `ExternalCondition@ambushtactics` on ^AutoTarget is a sync-inert grantable seam. **Byte-identity**: AttackMove diff purely additive; FireAtWill units short-circuit before any condition read; executor path trait-disabled for non-experimental cohorts. Adversarial review: MERGE, 0 FIX, 7/7 targets pass. 4 OBS carried as Stage-3/4 constraints: **OBS-1** gate wired only on ^AutoTarget family — AA IFVs (^AutoTargetAAIFV) + one assault-move vehicle never halt even when granted; **OBS-2** bot-owned granted units without a PoiGoalGuard ledger commit oscillate on ~75-tick squad re-issue; **OBS-3** shift-queued follow-up order runs on halt and walks the unit out of ambush (accepted v1 edge); **OBS-4** a Stage-2 behavioral autotest is owed before default-on. 5 NUnit pins (`AmbushTacticsTest`, incl. exhaustive gate-off matrix — 431 total = new baseline). Verified: build green, NUnit 431/431, lint 382 = baseline (defaults.yaml touched). In-game testable only by granting `enable-ambush-tactics` via map rules. (`174075e9` + `3ddd0b40`, fast-forward merge — tip `3ddd0b40`)
- **Formation realism micro-wave — arrival jitter, settle facing + sector fan, rolling halt (queue item 6)** — human squads stop *loosely* instead of on a stamped lattice: each unit settles a hash-derived fraction of a cell off its exact slot (lateral jitter + staggered depth from one shared 2D offset in `ComputeBoxSlots`), then turns once to a common front with a per-unit fan (`(target−centroid).Yaw + FacingFan(ActorID)`, latched `Turn` from `TickIdle`). New pure-static `FormationRealism.cs` (xxHash-finalizer on ActorID — zero sim RNG); caps `LateralCap < MinSlotSpacing/2`, `DepthCap < min(rowSpacing/2, MinSlotSpacing)` guarantee world-space non-crossing (after cell-snap two adjacent slots may rarely share a cell — Move resolves it like a manual double-click; wording softened per review OBS-1/2); `CanStayInCell` fallback to the exact slot, gated like the line path. **Bots byte-identical** (gates on the item-5 seam `Playable && !IsBot`; bot path produces the exact prior `slots[i] = baseCell` expression; settle fields non-[Sync], never set for bots — benchmark-frozen), **Tight = vanilla untouched** (early-return before all of it). Adversarial review: MERGE, 0 FIX; targets 1–4,6,7 pass, OBS-3 accepted (no re-face after a post-settle nudge, cosmetic). 11 NUnit pins (`FormationRealismMathTest`, 426 total = new baseline). Verified: build green, NUnit 426/426; lint skipped (no YAML). 7 further ideas parked in `WORKSPACE/cohesion/260725_formation_realism_ideas.md`. (`319f82f7` + `c17ad0bb`, fast-forward merge — tip `c17ad0bb`)
- **Team win/loss attribution fix (user-reported)** — 1 human vs 2 allied bots: bots won but only one showed "Won", the other "Lost". Root cause: a win/elimination interleave — `SupplyRouteContestation.HasActiveTeamSupplyRoute` skipped allies with `WinState != Undefined`, so once bot1 clinched Won (CVC inference a tick earlier), bot2's `OnDefeatBarFull` saw "no active team SR" and `ResolveTeamElimination` Phase 1 marked the still-Undefined bot2 Lost. Violates the trait's own contract (overrun-with-allies → passive, never eliminated). Fix: (1) an ally with `WinState == Won` counts as an active team SR (a team with a victor is never eliminable); (2) Phase 1 skips players whose team already has a victor (`TeamAlreadyWon`/pure `TeamHasVictor`) — defense-in-depth for a Won ally whose SR actor left the world. The spared teammate flips Won on the next `ConquestVictoryConditions.Tick` (awards independently every tick). 2 NUnit pins (415 total = new baseline). Adversarial review: MERGE, 0 FIX, all 7 attack targets pass (FFA/multi-team safe — Won-check sits inside the allied gate; single caller). Verified: build green, NUnit 415/415; lint skipped (no YAML touched). (`b7fb528b`, merged `37974788`)
- **Helicopter squad tick-0 crash fix (user-reported)** — "Air support" starting units + bot heli squad crashed to desktop the moment the game loaded: `HelicopterStateBase.FindClosestEnemy`'s world.Actors fallback (only reached when the ThreatMap is empty — i.e. tick 0) enumerated every actor incl. positionless enemy **PlayerActors**, which pass every filter clause but have no `IOccupySpace` → `CenterPosition` NRE inside `ClosestToIgnoringPath`. Fix: `a.OccupiesSpace != null` guard in the filter. Provably behavior-neutral for benchmarks/byte-identity: any invocation of that fallback ALWAYS crashed (enemy PlayerActors always exist), so no non-crashing game ever executed the path. The file's other closest-enemy searches source from `FindActorsInCircle` (spatial, never holds positionless actors) and were already safe. Manager diff-inspection (15 insertions). Verified: build green, NUnit 413/413. (`219de60a`, merged `4819766d`)
- **Supply crate lingering-when-low fix (user-reported)** — dropped supply caches (truck deploy) sat around almost-empty forever, same UX problem trucks had pre-item-4: SUPPLYCACHE had no self-removal path except LC absorption. Fix: opt-in `SupplyProvider.RemoveBelowSupply` (default 0 — LC/TRUK untouched); a stationary provider disposes itself via frame-end task when `currentSupply` drops below the threshold. SUPPLYCACHE set to 50 (mirrors TRUK's RestockThreshold). Deterministic — one ITick guard covers both drain paths (ResupplyTarget + DeductSupply). Accepted edge: LC mid-absorption loses the final <50 residue when the cache self-despawns (≤49 of 750 credit value). Bonus: also closes the fully-drained-via-rearm lingering case. Manager diff-inspection (27 insertions). Verified: build green, NUnit 413/413, lint 382 = baseline. (`1bd7b5c4`, merged `a87cc8a8`)
- **Cohesion stance identities — Tight=vanilla / Spread=dispersed / Loose=cover (queue item 5)** — the three cohesion stances become three visibly different behaviors instead of one box at three widths: **Tight = classic** (all cohesion adjustments off — vanilla OpenRA grouping, human orders only per review OBS #2 gate), **Spread = dispersed** (hand-tuned interval 4096 target / 3072 min — DP-2, no AoE derivation), **Loose = combat interval** (bend the line freely for cover — DP-3, cover-bid beats geometry). DP picks 2026-07-25: DP-1 Tight=vanilla (column identity dropped), DP-4 preview ghosts deferred, DP-5 build order agent's choice. CohesionStanceMathTest NUnit pins (413 total = new baseline). Verified: build green, NUnit 413/413, lint 382 = baseline. (merged `52a4395c`)
- **Hidden test windows — SDL_WINDOW_HIDDEN profile (queue item 16)** — hidden test/tournament launches switch from `OPENRA_WINDOW_MINIMIZED` (SDL_MinimizeWindow *after* creation — mapped-then-minimized race surfaces black windows on Windows) to the pre-existing `OPENRA_WINDOW_HIDDEN=1` path (`SDL_WINDOW_HIDDEN` at CreateWindow — the window is never mapped, never focus-steals). Non-obvious catch: a born-hidden window never fires `SDL_WINDOWEVENT_HIDDEN` and `IsSuspended` is event-driven, so it is set `true` explicitly at construction (env-gated, before the render thread spawns) — suspension/framerate-cap semantics identical to minimized; LogicTick is independent of suspension (render-only gate), sim/determinism untouched. `run-test.sh` gains `--hidden` (`--minimized` kept legacy), `run-tournament.sh` Mode-B and `run-batch.sh` (review OBS-1) forward it. Adversarial review: MERGE, all 5 attack targets pass — nothing in the codebase can un-hide the window. Verified: build green, single `--hidden` autotest PASS (sim free-ran, ~8s wall). Caveat (OBS-2): screenshots are blank under `--hidden` (as with minimized) — use `--visible`/`--background` for screenshot verdicts. (`55a840cd` + `52c66d51`, merged `43bbcb2c`)
- **Batch base-seed forwarding (item-15 follow-up)** — `run-batch.sh --seed N` = BASE seed; the k-th test gets a derived seed via a monotonic offset that skips 0 (collision-free incl. negative bases, e.g. base −1 → −1, 1, 2); the no-seed path collapses byte-for-byte unchanged (empty `SEED_ARGS` under word-splitting); validation matches `run-test.sh` (integer, negative ok, 0 rejected as the unset sentinel). Manager diff-inspection in lieu of full review (shell-only). (`7b61e82d`, merged `d37c2571`)
- **Single-test seed reproducibility (queue item 15)** — `run-test.sh --seed N` plumbs Test.RandomSeed into the engine; `World.DeriveLocalSeed` extracted as a pure public static (character-identical arithmetic to the shipped inline transform — adversarially verified, default path byte-identical for all profiles/replays); `TestMode.ResolvedSeed` captured only when TestMode active; additive `"seed"` field in the verdict JSON. Review NOTE-1 taken: `--seed 0` rejected (engine treats 0 as the unset wall-clock sentinel — the harness would have claimed reproducibility falsely); NOTE-2 taken: frozen golden vector `DeriveLocalSeed(1017)==894286612` (independently re-computed by the manager) guards against a both-sites constant rename. Key finding: NO LocalRandom→SharedRandom migration needed — all 42 bot-decision consumers already draw from the seeded `world.LocalRandom` (from `2d3c8fe0`). 7 NUnit pins. Optional follow-up parked: forward `--seed` through `run-batch.sh`. (`d71dd48b` + `b58232b0`, merged `e6ebf9d0`)
- **Fires economics — cluster targeting + ammo-EV gate (queue items 14+19)** — artillery aims at the CLUMP and rockets only fire when the salvo pays for itself: `FiresEconMath.cs` pure integer helpers (cluster weight/score/priority-bonus; `SalvoCost = ceil(Burst/ReloadCount)×SupplyValue` — Grad 680 vs Paladin 60; FireWorthy iff clump value ≥ salvo cost × margin); AutoTarget cluster term (shared trait, humans can opt in later) gated per-unit via `ClusterTargetingCondition: enable-ai-experimental`; `UnitRoleResolver` tube/rocket split on max weapon Burst (floor 8 — tube 1–3 vs rocket 12–40); `PoiOffensiveBotModule.FiresEvGate` flips rocket pieces HoldFire/FireAtWill by clump EV with a self-healing stranding reconciliation. Adversarial-review blocker (8th real catch this window): the reconciliation skipped the no-targets early return, stranding held pieces defenceless in HoldFire during mop-up/fog — fixed by extracting `ReconcileFiresHoldFire` and calling it on BOTH paths. Flags default-off, @experimental only, zero new RNG, byte-identical off. 8 NUnit pins. (`40ae4ee2` + `9673c774`, merged `b25ba2f0`)
- **Early-game tuning — no idle trucks, proportionate AA, faster spread (queue item 12)** — the bot's opening minutes read purposeful: supply-truck call-ins deferred until derrick income exists (no idle queue at the SR), vehicle-AA (SHORAD/Tunguska) gated on a fog-legal observed-enemy-air count (AA infantry stay as the cheap baseline picket), early fan-out in smaller groups for faster territory grab. Implemented by splitting the shared ground UnitBuilder the HelicopterSquad way: shared block gains `&& !enable-ai-experimental`, @experimental twin carries the new default-off flags — Normal/Stable byte-identical (the A/B control is untouched). Verification: build green, NUnit 389/389, lint delta +48 fully attributed to item-13's 12 scenario folders (pre-existing per-map baseline classes: 3× `being-captured` consumes + 1× missing cordon each) — **382 is the new lint baseline**. (`c4012ca4`, merged `267ef4f3`)
- **EXPAND benchmark maps — Polar Disorder + Woodland Warfare (queue item 13)** — 12 new tournament scenario rungs (`tournament-s{1-eco,2-combat}-{polar-disorder,woodland-warfare}{,-mirror,-cal-nn}`) adapted from the two pre-existing canonical map assets (Polar Disorder: SNOW tileset, 12 OILB; Woodland Warfare: TEMPERAT + 1210 trees, 8 OILB). SRs nudged inward to symmetric anchors (Polar `93,16`/`4,81`, Woodland `3,6`/`94,91`) because the 3×3 SR footprint overflows at native corner spawns. Auto-discovered via mod.yaml MapFolder — no registry edit. Smoke-verified in-game via debug.log: SRs placed at the nudged anchors, both bots spawning + running full module stack, zero exceptions. NOTE: bare `run-test.sh` cannot complete a tournament rung (300s wall watchdog, no TimeLimit/speed injection — that's `run-tournament.sh`'s job), so rung smoke checks read the game log, not the harness verdict. Ladder re-baseline over the new rungs remains DECLARED, awaiting user goahead. (`8a0dce0d`, merged `83a6638a`)
- **Fires / artillery doctrine (queue item 11)** — @experimental IndirectFire pieces peel off the grouped axis AttackMove and hold a weapon-range standoff anchor (`maxWeaponRange − FiresStandoffMargin` on the target→piece bearing, recomputed from the live axis target each re-eval): advance-to-range, hold-and-fire via AutoTarget, back off when the target closes. Adversarial-review catch: a raw anchor on impassable ground caused a per-interval re-order loop cancelling in-flight shots — fixed with `NearestPassableCell` (deterministic Chebyshev-ring clamp, budget 4) + a never-re-order-identical-destination gate. Flag `FiresStandoff` default-off (@experimental only); flag-off byte-identical by construction; zero new RNG. 12 NUnit pins (378 total). (`f20d2798` + `3aca99a1`, merged `6a33813d`)
- **Influence Stage F — strategic repoint + territorial balance-of-power revival (queue item 9)** — @experimental attack-axis selection drops the omniscient InfluenceMap threat term (`GetOffensiveTargets(perspective, suppressOmniscientThreat)` — default-false, all other callers byte-identical) and re-derives it from BELIEVED fields: balance-of-power reads the control of the ring AROUND each target at `AnchorRadiusCells+1` (adversarial-review catch: every enemy target is a site-anchor structure whose own cell floors ≈−800 → the boost never fired; ring read restores "press the encircled enemy" ×150 / damp lunging into believed strength ×60 / contested neutral) + believed-danger damp on its own threshold scale. Zero RNG, default-off flags, 14 NUnit pins (366 total). Completes the @experimental fog migration for attack axes; capture/garrison omniscient reads documented as deferred follow-on. DECLARED benchmark re-baseline not yet run (awaits test grant). Terr-bias revival — supersedes parked `exp-terr-bias` (branch kept, worktree removed). (`16e0e673` + `fba22955`, merged `36bd3b9e`)
- **Phase-4b role-migration wave (queue item 10)** — air squads, capture and call-in composition consume `UnitRoleResolver` behind `UseUnitRoles` (@experimental only): air-squad membership = AttackAir + Buildable + non-heli (drops airstrike spawns, keeps helis with HelicopterSquad); CaptureCoordinator routes ALL six pool readers through one `CapturerNames` accessor (adversarial-review catch — five raw sites fixed); AdaptiveProduction filters call-in candidates by role category with zero new RNG draws. Bidirectional set-equality lint over DefaultRules catches roster/list divergence both ways. Flag-off byte-identical. NEW BUG found en route: legacy `AirUnitsTypes` case-mismatch no-op — @stable fixed-wing squads never form (`WORKSPACE/bugs/discovered.md`); role mode incidentally fixes it for @experimental. (`232947ce` + `1f00d361`, merged `3bddab50`)
- **Influence Stage E — danger-weighted ground routing (queue item 7)** — ground attacks flow *around* defended cores and supply trucks pull back-lateral-re-enter: `GroundDangerNav` (pure integer, zero-random) computes a bounded two-leg detour waypoint when the straight route's worst-case ground danger exceeds threshold; rear-lateral routing EMERGES from the Stage-C territory baseline gradient, not script. Review blocker fixed (on-map impassable cells read danger 0 and were preferred as waypoints — locomotor passability check on the waypoint cell only) + truck deadband. Default-off flags; @experimental-only via `InfluenceStack.Participates` double-gate on the shared @supply module; flag-off byte-identical. 8 NUnit pins. (`ab7bd283` + `057ab755`, merged `fcb17a86`)
- **Supply truck counts-as-empty + evacuate (queue item 4)** — unusable residue latches counts-as-empty via live tri-state `SupplyProvider.ResidueVerdict` (MinNeedThreshold-aware, NUnit-pinned); Evacuate-stance trucks keep serving below RestockThreshold down to the last usable batch (no trip home to reserve for — adversarial-review blocker catch), then evacuate via `DropsSupplyCache` with red bar; every refill path clears the latch; bot economy layer stops re-tasking evacuating trucks. Gated behind `EvacuateOnUnusableResidue` (TRUK only). (`6fb952c7` + `5863d76a`, merged `f2602e67`)
- **Influence Stage D — heli danger consumer (queue item 6)** — @experimental attack helis consume the air-danger channel: engage cell leashed to the nearest AA-safe cell, lateral detour waypoint when the approach line crosses air danger, spike-triggered withdraw along the least-AA-covered heading. Pure integer nav math (`HeliDangerNav.cs`), zero-random, byte-identical when disabled; rides on Stage-0 standoff (inert when standoff off, review-hardened). 8 NUnit pins. (`36921468` + `867d9d46`, ff-merged)
- **Win-condition fix (queue item 1)** — SR team victory now explicitly awards Won: two-phase `ResolveTeamElimination` (mark eliminated team Lost, then award per-survivor only when every non-allied combatant is Lost — FFA/2v2v2 safe, adversarial-review catch), `AwardVictory` narrowed to CVC-present + Primary objectives (campaign missions untouched), TestMode guard. 6 SR unit tests. (`4ae664b8` + `86e993a6`, merged `5ab49f18`)
- **Cohesion stabilization (queue item 3)** — large-group line extent capped, greedy nearest-slot matching (kills criss-cross), cover-bid-beats-geometry, treeline detection via density-covariance anisotropy → soldiers line up ALONG the treeline; per-order matching memo for O(n²·log n) dispatch. Adversarially reviewed. (`d1858312` + `46a5021a`, merged `786d4770`)
- **Lobby team-selection column (queue item 2)** — per-slot Team column restored (header + editable dropdown + read-only label for remote/bot rows); the stock controls were hidden, not deleted. Screenshot-verified 2v2. (`95329170`)
- **Ambush / undetected-unit behavior — DESIGN** — critical design doc: existing idle-Ambush mechanics mapped (`AutoTarget.cs:511`), three premises corrected against code (prone ≠ concealment; per-unit scan is cheap; late rear-shot spring conflicts with suppression), staged implementation plan, 4 open forks awaiting user review. (`plans/260722_ambush_undetected_design.md`, `1a3f81f1`)
- **Influence Stage C — control field + tri-state /danger overlay** — per-player believed-territory control field (Voronoi seed + capture/persistence/grayzone/anchors) and the green/red/gray danger overlay; zero-SharedRandom stack, ground-only baseline. (`0833b376`)
- **Phase-4 role-model hardening** — unit-role model hardened with ruleset lint table, SF/DR coverage rows, aligned Cargo/carrier predicate + NUnit pins. (`81f52040` / `55326b3e`)
- **Phase-4a role-based tasking** — bot modules consume `UnitRoleResolver` behind `UseUnitRoles` (@experimental only); artillery / SHORAD kept off the line, IFV carriers stay back. (`acc42ad7`)
- **Influence Stages A + B — belief / danger substrate** — per-player belief store (contact memory + decay) and per-domain danger fields (ground + air), pure-data World traits. (`e1e16b95`)
- **Influence Stage 0 — heli standoff** — attack helis use gated attack-move standoff engagement, stop-and-fire at missile range instead of overflying. (`090ad9d0`)
- **Phase-3 human stance defaults + unit-role resolver** — human stance enablement, data-only role resolver, executor hardening (anchor lifecycle, arrival tolerance). (`7f1138e3`)
- **Phase-2 stance positioning executor** — tactical positioning executor for stance-driven unit placement.
- **Phase-1 threat layer + intel overlay** — sighting-based threat perception layer + hold-Space intel overlay substrate.
- **Phase-0 cohesion over-spread fix + re-baseline** — grouped-unit over-dispersion cured, benchmark re-baselined.
- **Sim-throughput 8× harness** — benchmark runs at 8× speed for faster AI iteration.
- **Observer full vision** — observer/replay watches with full-map vision.
- **Ladder regime change + re-baseline** — benchmark ladder regime updated and re-baselined.
- **Live-play crash fixes** — Passenger NRE + transport unload crash.
- **TECN ferry-to-capture** — technician ferried to capture target.
- **Evac + heli tasking fixes** — crew evacuation + helicopter tasking corrections.
