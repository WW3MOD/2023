### 45. Missile system — ground-truth audit → agreed spec → staged repair **[LARGELY DONE. The Javelin loop sub-item is PARKED BY USER RULING 2026-08-14 pending a video capture — do NOT re-dispatch it.]**

> ✅ **VERDICT 2026-08-19 (`main @ 5890b053`) — BOTH "still open" deliverables are DISCHARGED. The item does not close: the spec names its own successor question.**
>
> **The flag checked one and presumed the other. Both are done.** `DOCS/reference/missiles.md` exists (11,500 bytes, `85d146c8` *"missiles: commit the agreed intent spec (it has been referenced while untracked)"*). **And the per-weapon-class miss-detonation rule — the deliverable this item said was outstanding — is written**: §2 defines the class taxonomy (SACLOS wire-guided / fire-and-forget / top-attack / anti-air / cruise-strategic) and §3 states the rule per class, marked *"Agreed 2026-08-13"*. The three ground classes self-destruct at closest approach; **AA flies on to fuel-out**, which is the user's explicit exemption.
>
> **⚠️ But it is a SPEC, not an implementation.** `85d146c8` commits intent. Nothing here establishes that the shipped code obeys §3 — and the document's own framing says the opposite is expected: *"where the shipped code disagrees with this document, the code is wrong."*
>
> **THE NEW OPEN QUESTION, named by §3 itself and worth carrying as this item's remaining scope.** The miss test now runs on **physical** 3D separation (fixed at `1ec6f17c`, after the shipped code was found testing a *lead-inflated* distance against a *physical* constant — 38 of 44 traced latches fired while the missile was still physically closing). **That fix exposed a latent defect it had been masking:** the *detonation* test still measures to the aim point (`targetPosition + leadTarget + offset`), so the two are no longer commensurable. Nothing bounds `offset` by `CloseEnough`, and **`ATGM` rolls `Inaccuracy: 512` against the default `CloseEnough: 298` — so a missile can sit physically inside the proximity radius without fusing.** The doc marks this *"Open, not yet decided"* and warns it **changes when every missile in the game detonates, so it needs its own measurement.**
>
> **Also recorded in §3 and easy to re-derive wrongly: the anti-air exemption is about DISPOSAL only, never reacquisition.** An earlier draft said an AA missile "may reacquire"; that was an over-reading of the user's wording, which grants continued *flight*, not renewed *homing*. **I2b overrides it and is fleet-wide with no exceptions.**
>
> **The Javelin park is untouched by any of this** — still user-ruled, still needs a video recording. Do not reopen it.

_**RECONCILED 2026-08-14 against `main @ f0f058ea`. THE 2026-08-13 BLOCK BELOW IS SUPERSEDED — read this first and ignore its "nothing has landed" / "get answers to the three design questions" / "`wt/missile-trace` exists" claims, all three of which are now false.**_

_**What landed.** The three design questions were all answered (recorded in `closeout/missiles-e2475f8d.md` §3): audit scope is **every guided missile**; the miss-detonation rule is a **per-weapon-class rule still to be proposed**, not a settled decision; and close-range AA misses are **a bug** under a general **distance-invariance invariant** — *"as long as the weapon can fire, the missile should be able to hit"* — which makes any systematic near-or-far degradation a defect **even where it is physically realistic**, and rules out "make the launcher refuse the shot" as a fix. The audit programme shipped: AA missiles can aim upward, Javelin airbursts are visible, the no-reacquire invariant is enforced, per-tick tracing with a termination taxonomy is in place. `68e7c09f` landed the 559-line terminal-geometry audit; `f0f058ea` landed the measurement run plus four autotest scenarios, a Lua rig, a Python scorer, and two read-only TestMode bindings (`Test.GetLiveMissileRange` / `GetLiveMissileNearestId`) that let a scenario perturb its target at a **measured remaining range** rather than a guessed tick offset. The `wt/missile-trace` worktree is gone; no missile branch is outstanding._

_**THE JAVELIN LOOP IS ANSWERED AND PARKED. Do not spend another diagnosis on it from the code.** 556 shipped-configuration flights across four scenarios: **zero** `flyStraight` latches, **zero** fuel-outs, widest turn **47 facings (66°)** against the **128 (180°)** a loop requires; every missile detonated at or near closest approach within 45 ticks against a 71–74 tick fuel ceiling. It also **refuted the audit's own condition (A)**: nine flights cleared BOTH spheres — missing the tank *and* their own aim point by more than a fuse radius — and all nine still detonated, on `segment_closest`, a clause centred on a **third** point (`targetPosition + leadTarget`) between the other two. Condition (A) is therefore **not sufficient** to reach the latch, and the latch is what both survival and the orbit depend on. **So the loop is not in `Missile.cs`.** Two candidates remain and the run could not distinguish them: a **second missile from the same launcher** read by a player as the first coming back (`AttackFrontal`/`Armament`, never examined), or the sighting being a **frozen heading plus a 5-tick contrail merely looking like a curve** (never ruled out, cheap to rule out). **USER RULING 2026-08-14: park until it can be caught on video** — eight diagnoses have now been spent on this and a single recording settles in seconds what static reading keeps failing to settle. **Reopen only with a recording.** Full write-ups: [`audit/javelin-terminal-geometry.md`](../../audit/javelin-terminal-geometry.md) (static) and [`audit/javelin-terminal-geometry-run.md`](../../audit/javelin-terminal-geometry-run.md) (measured)._

_**Still open under this item**, and unaffected by the park: the **per-weapon-class miss-detonation rule** (a Phase 2 deliverable — the class taxonomy of SACLOS wire-guided / fire-and-forget / top-attack / AA / cruise has to be defined before the rule can be written against it; AA is already settled as exempt and may fly on until fuel-out), and `DOCS/reference/missiles.md`, which still does not exist._

_**Superseded 2026-08-13 block, kept for provenance only:** **RECONCILED 2026-08-13 against `main @ dc899995`. Nothing has landed on the missile system — `engine/OpenRA.Mods.Common/Projectiles/Missile.cs` has ZERO commits in `4d3c8f90..HEAD`, and so do `mods/ww3mod/rules/weapons/`. Every file:line in this item still reads as filed.** The only change is that a manager has since picked the work up: `wt/missile-trace` is the one non-`main` worktree in the repo, which matches this item's Phase 0 (per-tick missile trace instrumentation). **`WORKSPACE/closeout/missiles-e2475f8d.md` had uncommitted edits at the time of this reconcile and was deliberately left untouched** — it belongs to that running session, so treat this item's status as provisional and defer to whatever that manager commits._

**Perceived:** missiles behave the way the user expects them to. Today three AA soldiers fired at a Littlebird from 2–4 cells and all missed — in the user's words, *"it looked like the tracking mechanism didn't arm at all, so they just fired straight and kept flying in a straight line"* — and *"plenty of missiles miss and never explode."* The user's own severity read: the system *"has worked OKAY except the occasional misses… not catastrophic, but it breaks at some points."*
_Source: [`closeout/missiles-e2475f8d.md`](../../closeout/missiles-e2475f8d.md). The whole programme is open exactly as left; plan committed at `.maestro/managers/manager-4d821d1c-93e4-4833-a2b4-327d4728a8a5/plans/01-missile-system-ground-truth-audit-agreed-spec-th.md`. Verified unchanged against current main rather than assumed: `Missile.cs` newest commit `d64a7a68`, `Armament.cs` `5a1a4517`, `weapons-missiles.yaml` `f2468743` — all predate the session; `DOCS/reference/missiles.md` does not exist._
_**Next concrete step:** get answers to the three design questions in [`AWAITING-USER.md`](../../AWAITING-USER.md), then dispatch **Phase 0 (per-tick missile trace instrumentation) plus the four read-only audit workers in parallel**. Neither needs an autotest run, so both can start the moment the questions are answered._
_**Plan shape.** Phase 0 — a per-tick trace under a debug flag: position, target position, state, facings vs desired, `allowPassBy`, `flyStraight`, `lockOn`, distances, `loopRadius`, `distanceCovered` vs `rangeLimit`, and the **exact termination reason** (which detonation clause fired, or removed-without-exploding). Highest-leverage item — that session lost hours to hand-integrated flight geometry that was wrong twice, in two different ways. Phase 1 — four parallel read-only auditors: flight & guidance; detonation & lifetime (incl. warhead + armour facet model); fire control & targeting; weapon data sweep. Phase 1.5 — **one adversarial reviewer whose only mandate is to attack those four reports; after two confidently wrong analyses in one session this is not optional.** Phase 2 — `DOCS/reference/missiles.md`, agreed with the user before any fix. Phases 3–5 — repro scenarios authored but not run, then ONE goahead running the whole batch with tracing on, then staged repair. **The grant-batching is deliberate**: autotest runs are user-gated and the harness is single-instance, so all non-gated work is front-loaded._
_**The competing hypothesis for the close-range miss, and the warning that travels with it:** `HomingActivationDelay` (the missile flies unguided in Freefall until `ticks == delay + 1`, `Missile.cs:911`) and/or the separate `Arm` gate on `Explode()` (`Missile.cs:1147`) may both expire *after* a fast missile has already crossed 2–4 cells. If so the projectile is behaving correctly and the real defect is **fire control taking a shot it cannot make**. **These two readings need opposite fixes — do not assume which.**_
_**User design rule, stated:** a missile that misses should **generally still explode**, EXCEPT anti-air missiles, which may keep flying until fuel-out. Treat silent removal-without-detonation as a defect to hunt in `Missile.cs`'s termination paths._
_**CARRY-FORWARD WARNING — `WORKSPACE/missile-diagnosis.md` (`6dda9e91`) is UNRELIABLE from §2 down.** Its raw fleet field table is usable; **its conclusions are not.** Three known errors: (1) its overfly *consequence* analysis was falsified by the pre-fix baseline — corrected on main at `864537c4`; it traced only one arm of a two-arm branch and missed that the other arm's strict `>` silently corrects the problem. (2) It read `WAngle(24).Facing` as 24 when `.Facing` is `Angle >> 2` = **6**, so every loop-radius and descent-geometry figure in it is off by ~4× — compounded further by a WW3MOD-added terminal turn-rate boost (`Missile.cs:885-894`, up to 20 facings/tick). (3) It called the Javelin's `CruiseAltitude: 10c0` "almost certainly a typo for 1c0"; it is the **deliberate top-attack flight profile** — the weapon already declares `TopAttack: true` (`weapons-missiles.yaml:2-20`), routing damage to the top armour facet via `DamageWarhead.cs:131`. **Acting on that advice would break the weapon.** Only the user's question caught it._
_**Structural finding worth sizing after the audit, not before.** `Missile.cs` is inherited OpenRA code built around **terrain height levels** — cliffs a missile must climb over. WW3MOD's `mod.yaml` declares `MapGrid` with no `MaximumTerrainHeight`, so the height layer is uniformly zero **and always will be**. `InclineLookahead`, the `predClf*` machinery, cliff avoidance and `allowPassBy` can never do anything useful in this mod — and they are not merely dead, they still **latch state**: `allowPassBy` is set true on every ground shot because `lastHt >= targetPosition.Z` evaluates `0 >= 0`. Two commits (`000a3795`, `85374503`) have already patched the fallout, each fixing one half and leaving the other. Strongest candidate for the "general improvement" the user asked about._
_**Solved upstream, no action:** the Mi-28 overfly conclusion (the bug is not real as diagnosed) landed at `864537c4`._
