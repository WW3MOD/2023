### 43. `@stable` benchmark re-baseline — **THE FRAMING BELOW IS WRONG AS OF 2026-08-14: this is not a stale baseline, the instrument was measuring a different game** **[user-gated: multi-test grant]**

_**REFRAMED 2026-08-14 (`wt/econ-gate`, off `main` @ `2c274589`). Read this before any of the accounting below it.** Everything under this header was written on the premise that the control **drifted** — that `@stable` moved N times and the baseline needs re-taking against the new control. That premise is now the smaller half of the problem. **Every bot benchmark number ever taken from the `tournament-*` suite is INVALID, not stale, because both bots in every one of those matches had no economy at all.** `PlayerResources.Tick` gated income *and* upkeep on `Playable`, which map-player bots are not, so the whole suite measured a game in which neither side could earn or spend anything past its opening 7 500 allocation (`DISCOVERIES.md` 2026-08-14, both entries; `bugs/discovered.md` same date). **`@stable` appears 43 times across the suite against `@experimental`'s 19 — the control was the more heavily affected of the two.** The fix is on `wt/econ-gate`; both profiles get a live economy._
_**Practical consequence for this item: the debt is no longer "N control changes to re-measure" but "the entire corpus was taken on a broken instrument and must be re-taken from zero."** It CANNOT be discharged by re-running the old ladder and diffing, because there is no valid prior number to diff against. Treat every pre-2026-08-14 result as **void**, not as a baseline._
_**The MECHANISM, stated precisely, because the obvious reading is wrong and would send someone to fix the scorer.** The scoring math was **not** zeroed and no scorer component read a dead field. All three components bypass `Earned`/`Cash` entirely: `army_value` and `kills_value` come from `PlayerStatistics`, and `capture_income` reads the gross integrator `pr.TotalBuildingIncome` directly (`BotVsBotMatchWatcher.cs:304-319`), which is driven by `CashTrickler` registration **outside** the gated `Tick` and was never affected. **The corpus is void because the SIMULATION being scored was a game in which neither side could reinforce past its opening allocation** — the numbers were faithfully measured from a match that does not resemble a real one. Scoring is fine; the world it scored was not._
_**This SUPERSEDES the "one piece of good news" paragraph near the end of this item** (corrected inline there): the claim that no existing result is contaminated and that the exposure is forward-only is **false**. It reasoned only about `@stable` YAML drift and did not know the economy had been dead for both sides all along._
_**Ordering:** the dependency chain below (item 40 gates this; this gates 22/25/31) is unaffected. What changes is that the economy fix is now a **new prerequisite in front of all of it** — re-baselining before it merges would simply bank a second invalid corpus._

**Perceived:** nothing directly. But until it is taken, **every "did the bot get better?" number is measured against a control that has moved underneath it**, and four other queue items are measurements that cannot be trusted. _(2026-08-14: and, per the reframe above, on an instrument with no economy in it.)_
_Sources, all four raising it independently: [`closeout/bdedd544.md`](../../closeout/bdedd544.md) §1a (calls it "the most consequential item here"), [`closeout/6361c2be.md`](../../closeout/6361c2be.md) §4, [`closeout/f5b7d1a6.md`](../../closeout/f5b7d1a6.md) §1 and §4, [`closeout/fixes-cfcaa2ca.md`](../../closeout/fixes-cfcaa2ca.md) §4a. Merged here into one item; each source's specifics are kept below._
_**CONTRADICTION, recorded rather than resolved — the four sources count four different sizes, and the sets overlap only partly:**_
_- [`AWAITING-USER.md`](../../AWAITING-USER.md) (2026-08-11) says **four** merges declare a knowing `@stable` change._
_- `bdedd544` says **five change sets from its own session** — danger fire-cycle merge, aircraft rearm, supply delivery commitment, demand-driven fleet sizing, the dry-infantry recruitment gate — **plus the second machine's seven merges**, i.e. "at least twelve", and concludes: **"any A/B against `@stable` right now is measuring a control that has shifted at least twelve times underneath it."**_
_- `6361c2be` says **five deltas from its own merges** — and they are a **disjoint set** from `bdedd544`'s five: vehicle firepower gated at `Heavy` not `Critical`; passenger evacuation; `StateBase.CanAttackTarget` no longer tasking paused-armament units; `ReturnWhenEmpty` on `^Soldier` (shared template, no owner-side split); the unconditional mid-resupply withhold._
_- `fixes-cfcaa2ca` adds one nobody else counted, and it is the nastiest: the `OnlyTargets` repair on `wt/autotarget-preempt` does **not** sit behind `PreemptScanInterval`. It fixed a pre-existing inverted clause (live since `8b9f6937`) that made `HasValidTargetPriority` return `false` for **every** target in the game, silently dropping targets on every stance-decrease path — so it changes **twelve** stance-change and target-validity call sites regardless of the flag. **Consequence: `PreemptScanInterval: 0` is NOT a clean revert. Anyone treating it as one will be wrong.**_
_- `f5b7d1a6` adds a further one whose *scope* is easy to under-state: `bd7b6bb2`'s disclosure is not "bots issue the same orders" — dry units now become **idle**, which feeds every idle-triggered trait and `IsIdle`-gated bot census that never saw them before, and `WithInfantryBody.TickIdle` draws `SharedRandom` **twice per idle transition**. The behaviour change is local; **the RNG consequence is global. Do not diff replays across that commit.**_
_**The honest reading: the debt is larger than any single report states, because no report counted the others' merges.** Whoever takes the baseline should enumerate from `git log` rather than from any of these numbers._

_**RECONCILED 2026-08-13 against `main @ dc899995` — the debt GREW again, and the standing advice above is now MORE right, not less. Five further contributors, none of them counted by any of the four sources. The contradiction above is NOT resolved; these are added to it.**_
_- **`HelicopterSquadBotModule.Evacuate` is reachable on `@stable`, and has been all along** (`d5a4a42b`, merged `91056894`). This is the nastiest of the five because it is retroactive: the `@stable` twin at `ai.yaml:1666` omits every Evacuate field and so runs on active engine defaults, and `HeliEmploymentMath.Decide`'s first branch is ungated (`!hasUsableAmmo && !canRearm` returns Evacuate regardless of target, home, window or contact). **So the benchmark control has been mutating synced state host-only for as long as that path has existed** — the desync fix flowing to `@stable` per settled policy is the smaller half of this disclosure. The larger half is that the control was already not what it was assumed to be. Observable delta of the fix itself is small: the evac now lands a few ticks later via the bot's order queue, and the refund picks up the handicap adjustment the player-facing Evacuate already applies (zero at handicap 0)._
_- **`@stable` moves TWICE at `16eca8e8`**, and the second one is easy to miss: (i) AA cadence, from the per-shooter overkill clamp; (ii) **rocket-artillery hold-fire economics**, because `PoiOffensiveBotModule` also calls `EstimatePercentDamage` for its fires valuation and that gate is true on **both** profiles. Disclosed as a desync-class correction rather than tuning._
_- **The danger durability weight has LANDED, not pending** (`f09183e2`, merged `ddcc5d6c`, 2026-08-11). Item 43 previously listed item 40's danger work as an *expected* future contributor; stage (a) is in the tree. `DangerFieldLayer` is a World trait (`world.yaml:368`, bare key, no YAML override) so there is no per-profile gate: **the field has already shifted for `@stable`.** Magnitudes, measured statically rather than predicted: ground reference 3412 → 2748 (−19.5%), air 3627 → 2034 (−43.9%). **The two channels rescale by DIFFERENT factors**, which matters to whoever reads the new baseline._
_- **`153ab8e6` (LC subcell dock) is a behaviour change on both profiles, though a narrow one.** It changes `Resupply`'s ground arrival test everywhere, but is a verified no-op for full-cell units, for `Repairable`/`LayMines`, and for aircraft — it bites only where the tolerance is smaller than a subcell offset, which today is exactly the Logistics Centre dock. Small, but a bot squad that previously stranded all but one man now rearms as a group, which is a real change to `@stable` unit availability._
_- **`AttackMoveActivity`'s Stage-2 ambush halt is live on `@stable`** (`fffad21e`, `AttackMoveActivity.cs:148-151`: `LaneAmbushBotModule@stable` posts `@stable` ambushers, sets Ambush stance and grants the gate). This is not a *change* — it is a correction to the record about what the control already does, and it belongs here because it is the second doc claim in a week that said live `@stable` code was dead._
_**A standing correction to how this item is framed, from `9eb0cb66` (merged `d976a0c4`): `@stable` is the benchmark control — a fixed and VERSIONED configuration, not a frozen one.** `architecture.md` had asserted under "Widened ambush" that byte-identity was "by ABSENCE — there is NO `@stable` twin"; `LaneAmbushBotModule@stable` exists at `ai.yaml:1994`, added by `b8d2e601` (2026-08-02). That is the dangerous shape of stale claim — it declares live code dead, so acting on it would delete a shipping feature. **The durable lesson banked with it: a byte-identity claim is a statement about YAML, not about the C# beside it, which is why such claims survive next to correct-looking code citations. Prefer flag-relative phrasing.**_
_**~~One piece of good news, and it bounds the damage:~~ RETRACTED 2026-08-14 — this paragraph is FALSE and was the most load-bearing wrong claim in the item.** It read: "`9eb0cb66` checked benchmark integrity and **no existing result is contaminated**. The newest artefact in `WORKSPACE/ai-bench/runs/` is dated 2026-07-29, so nothing has been measured since the control started moving… **The exposure is forward-only**." **Every part of that conclusion fails.** It audited only `@stable` **YAML drift** and asked "has anything been measured since the control moved?" — a question that presupposes the instrument was sound before the drift. It was not: the `tournament-*` bots had **no economy in any match, ever**, so the 2026-07-29-and-earlier artefacts it cleared are exactly the contaminated ones. **The exposure runs BOTH ways — prior results are void AND the control keeps moving forward, including by the economy fix itself.** _(Corrected on review: an earlier draft of this retraction said "backward, not forward", which is an overcorrection that would invite a future session to skip disclosing the next `@stable` move. Both halves are live: re-take everything behind you, and keep disclosing everything ahead.)_ The versioning/disclosure half of the original paragraph (Stable AI 0730 → 0802, `ai.yaml:50`, user-authorised and disclosed) still stands — that part was about drift and is unaffected. **The generalisable error: an integrity audit scoped to "what changed recently" cannot detect a defect that was present from the beginning.**_
_**Dependency, stated by the sources and preserved here:** item 40 (danger-scale rework) is the gate **in front** of this, and this in turn gates items **22, 25, 31 and ambush gate (b)**. `bdedd544`: take it **ONCE and last**, and **after** the danger-unit question, not before._
_**Next concrete step — recon before spending an hour of matches.** Last `WORKSPACE/ai-bench` activity is `5dc14934`, which predates everything since. The `ai-bench` worktree was **638 commits behind `main`** when last checked, so settle whether the ladder runs from `main` or from that worktree **before** running anything. `8a193c41` already stopped `SPEC.md` pointing benchmarks at a worktree — confirm that landed as intended, then run the ladder._
_**Also disclosed and inheriting to `@stable`:** item 40's own danger work (`DangerFieldLayer` is a world trait with no per-profile gating) and, per item 44, any change to the autotarget arithmetic. Expect item 40 to be the largest single contributor._
