### 62. Found in the 08-12/08-13 log and represented nowhere in this queue **[filed 2026-08-13 by the reconcile; each line is new open work, none of it owned]**

> ✅ **VERDICT 2026-08-19 (`main @ 5890b053`) — all five lines re-checked. Two are done, one is half wrong as written, two stand.**
>
> **⚠️ THE `halo` LINE IS WRONG IN ITS HEADLINE AND WORSE THAN FILED IN ITS DANGEROUS HALF — correct it, do not just carry it.** It names four conditions "that nothing grants". **Three of them DO have a grantor:** `crash-disabled`, `autorotation` and `crash-landing` are granted by `HeliEmergencyLanding` on `^Helicopter` (`mods/ww3mod/rules/ingame/aircraft.yaml:175-177`) — so for those three the lint error is a checker artefact, exactly as the bug entry's own sub-note at `bugs/discovered.md:629` suspected.
> **`rotor-stopped` is the real one, and it is confirmed: ZERO grantors** anywhere under `mods/` or `engine/` — every single occurrence is a `RequiresCondition` — against **12 consumer lines on 6 helicopters**: TRAN (`aircraft-america.yaml:55,59,67,71`), littlebird (`:267,271`), HELI (`:408,412`), HALO (`aircraft-russia.yaml:64,68`), HIND (`:240,243`), MI28 (`:428,432`).
> **The consequence was never spelled out, and it is a VISIBLE defect rather than a lint curiosity.** The consumers come in pairs. The `RequiresCondition: rotor-stopped` half of each pair — the stopped-rotor idle overlay — is **permanently OFF (6 dead traits)**; the `!airborne && !rotor-stopped` half is **permanently ON whenever grounded**, so **a landed helicopter's rotor never visually stops.** That is something a player can see. Entry has moved to `bugs/discovered.md:621-633` (this item cites `:18-30`).
>
> **HALF DONE — nav-guard / cordons. THE WARNING IN THIS LINE IS SPENT.** The cordon half **landed and came out green**: `67888986` (merge `wt/cordon`) + `097738f4` *"Re-cordon the nine shipped maps: a 1-cell border on all four sides"*, and **zero shipped maps appear among the 63 remaining `does not define a valid cordon` lines** in `lint-baseline.txt:54+` (all are `test-*` / `demo-*` / `tournament-*`). **So "adding cordons will hard-fail nav-guard and needs a deliberate re-bless on played maps" has already happened — do not re-raise it as a risk.** The coverage half is unchanged: nav-guard still checks exactly **10** maps (`tools/nav-guard/baseline.json`, `states.live`), and the 157 scenario maps are still invisible to it. **That is what remains.**
>
> **STILL WRONG, VERBATIM — the `Versus` tables.** `weapons-missiles.yaml:380-396` (`IskanderTargeter`, `Warhead@Target`) still lists `None/Wood/Concrete/Light/Medium/Heavy/Brick` all at `0`; `HIMARSTargeter` (`:399-401`) inherits it unchanged. Counted from `Armor: Type:` across `mods/ww3mod/rules/`, the mod's actual set is Light 38, Heavy 15, Concrete 13, Medium 10, None 7, Wood 4, **Unarmored 3, Kevlar 1, Indestructable 1** — Brick is not among them, and the three real classes are omitted, so each takes the unmodified 50.
>
> **DONE — `ResupplyBehaviorSelectorLogic.DoNow`.** The Evacuate case now reads `world.IssueOrder(new Order("Evacuate", at.Actor, false));` with a PITFALL naming the old direct `RotateToEdge` write. Landed at **`e49ff242`** *"determinism: route Evacuate through its order; sweep Widgets/** for the same shape"*. **The prediction in this line — that item 60 would fix it as a side effect if the button were built on the order path — paid out.**
>
> **STANDS — the lint-count line**, moved to `bugs/discovered.md:1253`, still reading "402 of the mod's 496" with no FIXED annotation. Premise intact; only the line number rotted.

**Perceived:** varies. The map-cordon line is the only one a player could ever feel, and only if it is done wrong.
_These are landings whose **residue** was recorded in `DISCOVERIES.md` / `bugs/discovered.md` but which no PIPELINE item carries. Grouped so they are findable, exactly as item 54 is._
_- **`halo` consumes four conditions that nothing grants — FILED, NOT FIXED.** `c820ccd6` (merged `f068a51a`) touched `WORKSPACE/DISCOVERIES.md` and `WORKSPACE/bugs/discovered.md` **only — zero code.** Entry at `bugs/discovered.md:18-30`, `[medium] UNTRIAGED`: `halo` consumes `crash-disabled`, `autorotation`, `crash-landing`, `rotor-stopped`, and the entry itself states **"NOT ESTABLISHED: whether any of this is behaviourally live"**. **This is the INVERSE class of what item 52 fixed, and the more dangerous direction** — a `RequiresCondition` that can never be true means the trait is permanently OFF. **Two sub-findings inside it are separately open:** `rotor-stopped` has **no grantor anywhere** while **six** helicopters consume it (TRAN, littlebird, HELI, HALO, HIND, MI28, file:line in the entry); and **the lint reports only 1 of those 6**, which is a trustworthiness question about the checker itself — it matters if `make test` ever becomes a merge gate._
_- **nav-guard covers 10 of 167 maps, and fixing that will hard-fail it.** Recorded at `c820ccd6`/`DISCOVERIES.md`: **157 autotest scenario maps are invisible to nav-guard**, and **9 of the 10 shipped maps have no cordon**. So adding cordons is not cleanup — it will hard-fail nav-guard and need a deliberate `nav_guard.py bless`, on **played maps**. That is a reviewed change, and it is roadmap-worthy on its own. `DISCOVERIES.md:8` records that the map half was **held for unrelated sequencing reasons**, so this is a known deferral rather than an oversight._
_- **`bugs/discovered.md:644` still asserts "402 of the mod's 496" lint errors with no FIXED annotation.** Item 52 is closed; that line is not, and it is the statement most likely to mislead the next reader. **No file in the repo states the current count** — `4d3c8f90` claims 583 → 100 residual and `4d19a8e4` should take it to ~85, but nothing asserts it._
_- **A mod-data bug was filed against `IskanderTargeter` / `HIMARSTargeter` `Versus` tables** (`f09183e2`): both zero a class this ruleset does not have (Brick) and omit three it does (Kevlar, Unarmored, Indestructable). **Because an unlisted class takes the unmodified 100%, omission is the OPPOSITE of a zero**, so both deal full 50 to infantry. Harmless for the danger unit (see item 40) but wrong as data._
_- **`ResupplyBehaviorSelectorLogic.DoNow` issues Evacuate as a direct activity write, not an order** (`:134`). Client-local UI, so **not** the host-only desync class fixed at `91056894` — but it is **not order-routed, so `GameSave` never records it**, which is the property that broke saved-game restore for the bot version. Unverified at runtime. Recorded here and cross-referenced from item **60**, which would fix it as a side effect if the new button is built on the order path._

---
