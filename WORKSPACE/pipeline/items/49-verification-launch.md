### 49. One verification launch — three claims, one game start **[OPEN — all three primary claims untouched. One adjacent check is ANSWERED and struck; one new claim added.]**

_**RECONCILED 2026-08-13 against `main @ dc899995`.** Path-scoped `git log 4d3c8f90..HEAD` on `mods/ww3mod/chrome/credits.yaml`, on the NVorbis/Ogg/sound-loader paths and on the cameo / `bits` / `uibits` paths each returns **zero commits**. **The three primary claims (a) credits screen opens, (b) a real 64×48 cameo reads in the 62×46 slot, (c) NVorbis decodes an `.ogg` are all still unobserved.** The item stands._
_**STRIKE one adjacent check: "whether a soldier actually rearms at a Logistics Centre" is ANSWERED, and it was a real bug.** `Resupply`'s ground arrival test compared the host's `CenterPosition` against the unit's **body** position, which for a cell-sharing unit carries a `MapGrid.SubCellOffsets` entry (~393 units) it neither chose nor can shed; `closeEnough` is `WDist.Zero` for any host with no `RearmsUnits` to read a `CloseEnough` off (`AmmoPool.cs:374`), which is every ground rearm host in this mod — the LC has `RepairsUnits` only. So **only the holder of the zero-offset subcell could ever dock**, and two soldiers cannot both hold it: whichever reached the depot second stood there indefinitely re-queueing a no-op approach every tick. Any squad sent to rearm together hit this. Fixed at `153ab8e6` (merged `484eb913`) by measuring from the **cell** — subtracting `OffsetOfSubCell(ToSubCell)` at `Resupply.cs:163-195`. **Verified by autotest, not by code reading:** `tools/autotest/scenarios/test-lc-rearm-partial-order/` was committed RED at `ff63d6e3` with measurements ("PairA ends 412/500 taking one round at a time, errand still running at 40s") and goes **RED→GREEN on all four subjects**, PairA still holding a non-centre subcell (10,−256) and now taking 50-round dock batches, full at 672 ticks. Giving the LC a `RearmsUnits` trait was rejected — it makes the lookup succeed by asserting something untrue about the building._
_**ADD to the launch list, from item 48:** the how-to-play panel's own verification claims a 1600×900 screenshot that is **not committed**. Confirm the first-run panel actually appears on a cold profile — it is gated on `NonBotClients.Count() == 1` and a persisted version counter, so a stale `HowToPlayVersion` in the tester's settings will suppress it and look identical to "it never shipped"._
_**Unchanged and still owed:** a Grad or TOS salvo, the 27 smoke trails at `MovingInterval: 2`, and the turret lock at 50%._

**Perceived:** nothing new. This is the cost of the fact that **everything the art/audio manager shipped is code-read or statically checked and never observed** — no sprite was rendered, no sound was played, no game was launched.
_Source: [`closeout/art-6cde8456.md`](../../closeout/art-6cde8456.md) §1 and §4. **Bundled deliberately so it costs one launch, not three.** Best done once the first real cameo exists._
_**One launch confirming:** (a) **the credits screen opens** — duplicate widget keys throw at **runtime** and `make test` does not validate chrome layouts, so a crash there points at `mods/ww3mod/chrome/credits.yaml` first; (b) **a real 64×48 cameo reads correctly in the 62×46 slot** (the 1px overhang is by design, not a clip); (c) **NVorbis actually decodes an `.ogg`**._
_**If one of the three is wrong, Ogg decoding is the bet** — NVorbis 0.10.5 on net6 has never executed in this build._
_Adjacent live checks owed from a different session and worth folding into the same play window ([`closeout/6361c2be.md`](../../closeout/6361c2be.md) §4 — **zero on-screen verification across that entire session**, four merges of static analysis reviewed twice), in its stated order of value: a **Grad or TOS salvo** (the only edits needing a new sequence *and* a new trait to both be right, neither lint-covered end to end); whether **27 new smoke trails at `MovingInterval: 2`** read as plumes or walls (one number in 27 places); whether a **soldier actually rearms at a Logistics Centre** (infantry have never walked that path); and whether the **turret lock at 50%** feels too early._
