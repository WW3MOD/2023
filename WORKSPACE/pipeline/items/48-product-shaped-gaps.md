### 48. Product-shaped gaps — the two things most likely to decide how the release lands **[PARTIAL — the onboarding half is DONE (`dd6171cd`); voices, screenshots and map previews are untouched]**

_**RECONCILED 2026-08-13 against `main @ dc899995`. The first bullet is CLOSED; the rest of the item is exactly as filed.**_
_**Onboarding — DONE at `e3dd236e`, merged `dd6171cd` (7 files, +390).** `mods/ww3mod/chrome/ingame-info-howtoplay.yaml` is a one-screen briefing titled **"There is no Construction Yard."** covering the four things this item asked for: no factories / no tech tree (budget allocation, not materials); units enter at the map edge nearest your Supply Route and march in real time; the Supply Route is your beachhead, **one per player, fixed, indestructible, not buildable**; win by contesting the enemy ring. Footer covers Evacuate. **Wiring traced and reachable, not merely defined**, via two entry points: (1) auto-open at `MenuButtonsChromeLogic.cs:106`, gated at `:126-131` on `!TestMode.IsActive && Game.Settings.Game.HowToPlayVersion < HowToPlayVersion && LocalPlayer != null && !IsReplay && !IsLoadingGameSave && NonBotClients.Count() == 1` — so **first-run, singleplayer, once**, versioned by a persisted setting (`Settings.cs:350`, default 0, written and saved on open); (2) a permanent tab in the game-info panel (`GameInfoLogic.cs:21,61,105,187`), added last so `AutoSelect` still lands on Objectives. `mod.yaml` swaps `common|chrome/ingame-info.yaml` → `ww3mod|` and adds the new file._
_**Deliberately NOT taught: Supply Route capture** — consistent with the CLAUDE.md correction landed the same day at `23802389`, which found the hard rule stating "more only by capturing neutral ones" flatly while both reference docs flag SR capture as designed-but-unimplemented. **That propagated exactly once and was caught:** a manager put the capture requirement into the onboarding brief from that line and the worker refused it after checking the rules. Teaching a new player to hunt for a mechanic that does not exist is precisely the failure this panel exists to prevent._
_**Verification caveat, stated honestly:** the commit claims "verified by screenshot in-match at 1600x900; build clean, NUnit 1402, test-screenshot-smoke passes". **No screenshot artifact is committed**, so the claim exists but is not independently confirmable from the tree. Worth folding into item 49's one launch._
_**Untouched and still open:** unit voices, store/release-page screenshots, map-preview curation. Path-scoped `git log 4d3c8f90..HEAD` on `mods/ww3mod/audio`, the voice/notification rules and `mods/ww3mod/maps` returns **nothing**._

**Perceived:** directly, and more than any art item. ~~A player with RTS muscle memory opens WW3MOD, **hunts for a construction yard, fails, and quits before finding the actual game**~~ — **onboarding shipped, see above.** And a **US GI says "Yes sir" for a Russian conscript**.
_Source: [`closeout/art-6cde8456.md`](../../closeout/art-6cde8456.md) §1, "Product-shaped gaps (raised with the user, never scheduled)". Recorded in full as a `track_note` on the `release-polish` track. **Raised, never scheduled — this is not a manager priority claim, it is that manager's stated judgement being preserved.**_
_- **No onboarding for the Supply Route economy.** No factories, no tech tree; units arrive as reinforcements from off-map reserves. That manager's read: **a one-screen "how this differs" panel or a short scripted first mission likely converts more players than any art**._
_- **Unit voices are still RA-era English.** Cameos are the visible half of the "still Red Alert" problem; the voices are the audible half. TTS makes Russian-language BRICS responses tractable._
_Also open, lower stakes: **store/release-page screenshots** (free to produce from the current build) and **map-preview / map-pack curation**._
