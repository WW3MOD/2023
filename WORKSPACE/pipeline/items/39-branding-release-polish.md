### 39. Branding and release polish — the product introduces itself as WW3MOD **[Phase C polish, NOT new v1 scope]**
**Perceived:** the game stops introducing itself as somebody else's. The main menu says WW3MOD, the install prompts talk about WW3MOD, there is a credits screen at all, and the jukebox can play music this project actually owns instead of looping the one Red Alert track a stock install happens to have. Nothing about the battlefield changes — this is the frame around it, and it is the first thing a new player reads.
_**Scope note:** this is **existing Phase C polish that finally has tooling**, not an addition to v1 scope — `RELEASE_V1.md` remains the locked source of truth and this item claims no new ground in it. What changed on 2026-08-11 is that the blockers came off: text overrides are wired, Ogg is on, and there is now a cameo converter._
_**Remaining, and why it is player-visible:** the **load-screen logo slot is empty**, the main menu has **no background asset**, and the installer still ships **stock icons** (recon `7eee21df`). Website/FAQ URLs and the version string are still the engine's, marked `TODO(release)` in `4836ceed`. Adjacent and genuinely felt in play: **a stock install plays exactly one music track on infinite loop** — `music.yaml` declares 88, but the `music` package is the only content package with no `Download:` key, so `scores.mix` is never fetched and unopenable tracks are silently dropped; victory and defeat stings are wired but dead for the same reason, and there is no menu theme and no briefing track. With Ogg enabled and loose files shadowing `scores.mix`, filling that slot is a **content task, not an engine one**._
_**Already shipped, in order:** `12db45a8` / `4836ceed` — the main menu header was rendering the engine's own "OpenRA" string, content-install prompts still talked about Red Alert, and the credits screen was commented out entirely; `ModCredits` enabled with an authored `credits.txt`, install wording rewritten without overclaiming. Non-obvious catch: `label-openra` is **shared** between the menu header and the credits screen's engine tab, so a bare override would have relabelled the OpenRA attribution tab — exactly where GPLv3 asks for it; a one-line `ww3mod|chrome/credits.yaml` points that tab at a separate key. Website/FAQ URLs and the version string left untouched, marked `TODO(release)`. · `aee3a02e` / `2f31404e` — `SoundFormats` listed only Aud, Wav, so the Ogg loader and NVorbis were present and simply never switched on (any `.ogg` would have shown in the jukebox as a silent 0:00 track); enabling it unblocks shipping the mod's own music. Same commit deletes `bits/sounds/arabs/` (32 files, unmounted **and** unreferenced) and promotes the audio recon into `DOCS/reference/architecture.md` § Audio pipeline. · `0eed8ba2` / `2c110a67` — `tools/cameo`: crop-to-fill to 64×48 RGBA, reproduce the mod's 1px white/black bevel, optional baked caption, `--install` writing PNGs under `.shp` filenames so no sequence edit is needed (`PngSheetLoader` sniffs magic bytes, not the extension). Touches nothing under `mods/` or `engine/`. · `54ea7210` — `credits.txt` claimed game content "are not distributed with WW3MOD", which is true of the mounted `.mix` content and false of the repo; replaced with an accurate acknowledgement._
_Split 2026-08-11: the asset-licensing half of this thread is **item 41**, at the very bottom of the queue. Same 08-11 work, deliberately different urgency._
