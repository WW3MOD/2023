### 44. AA and autotarget arithmetic — the overkill stand-down, and the preemption nobody has watched work **[(a) DONE `16eca8e8`. (b) still OPEN and its SHAPE CHANGED — the runs were performed and the control PASSED, so the test does not discriminate the fix.]**

> ❌ **VERDICT 2026-08-19 (`main @ 5890b053`) — (b) STILL OPEN, premise re-verified. (a) confirmed live. Three stale references corrected below.**
>
> **The answer to the flag's question: the header was rewritten; the deadline was NOT changed.** `test-autotarget-preempt-air.lua:37-43` now reads *"CONTROL ESTABLISHED 2026-08-12, AND IT DID NOT GO RED. THIS TEST DOES NOT CURRENTLY DISCRIMINATE THE FIX. Both runs PASSED at this deadline"*, with both seeds listed. **`DeadlineTicks` is still 110 (`:72`).** So the blocker is unchanged and the tick budget below is still the budget.
>
> **(b)'s engine premise is intact — nothing landed against it.** `git log -S 'PreemptScanInterval'` shows no engine change since this dossier; `PreemptScanInterval: 25` is still declared at four separate inheritance bases (`defaults.yaml:329, 574, 659, 756`), the C# default is still `0` (`AutoTarget.cs:212`), and the preempt read is still `AutoTarget.cs:1068`.
>
> **(a) is genuinely shipped, not merely tagged.** The per-shooter clamp is live at `AutoTarget.cs:1638` (`Math.Min(totalDamage * 100 / health.MaxHP, 100)`) with its reasoning as a PITFALL at `:1629-1637`, plus the `IsValidAgainst` repair at `:1607`. **`16eca8e8` touched (b)'s scenario directory but not (b)'s mechanism** — those two files got only test artefacts (a commented-out `strykershorad` pin, an inheritance PITFALL at `rules.yaml:21-30`, and the header rewrite recording the negative result), and its `defaults.yaml` delta is entirely `^HoldingFireMarker` / `Inherits@HoldFireMarker` with no `PreemptScanInterval` change.
>
> **⚠️ THREE STALE REFERENCES IN THIS DOSSIER, corrected here rather than silently left:**
> - **`:25` is wrong twice.** It quotes the header string above (gone), and it says `git log 68b627ce..HEAD` on `AutoTarget.cs` / `defaults.yaml` / the scenario dir *"returns empty"* — **it now returns `16eca8e8` and five later commits.**
> - **`:6` and `:19`** place the `BreakOffCondition` hard skip at `AutoTarget.cs:1371-1372`; it is now **`:1429-1430`**. (The `BreakOffCondition` declaration at `:232` has not moved.)
> - For orientation, (a)'s other refs — explicitly scoped "still open at `35876332`" and therefore historical, not wrong — now sit at `:1420-1424`, `:1500`, `:1570`, `:1596-1626`, `:1645`.
>
> **UNVERIFIED, and flagged rather than quietly inherited:** this dossier's named mechanism `opportunityTargetIsPersistentTarget` **does not grep anywhere in `AutoTarget.cs`** and `git log -S` on it returns nothing. Either the identifier is wrong or the mechanism lives elsewhere. **That specific causal claim has never been confirmed against the tree** — do not build a test design on it without re-deriving it.
>
> **CANNOT SETTLE WITHOUT A LAUNCH — and a launch today would be worthless.** The scenario's own header records that it cannot discriminate the fix at any seed, so running it would produce a meaningless green. **What would settle (b) is a REDESIGNED test, not a run of the existing one.**

_**RECONCILED 2026-08-13 against `main @ dc899995`.**_
_**(a) CLOSED at `afa18718`, merged `16eca8e8`.** All three of this item's "next concrete steps" landed in one branch: (1) the claim is clamped **per shooter** — `min(claim, 100)` — not on the shared accumulator, with the reasoning preserved as a `PITFALL` at the site; (2) the `ValidTargets` omission is repaired in the same warhead loop (`IsValidAgainst` now applied alongside `Penetration` and `Versus`); (3) the render-only "holding fire" marker shipped as `WithHoldingFireDecoration.cs` (+ `defaults.yaml`, `sequences-misc.yaml`). **Measured, not argued:** MANPAD vs Halo with every input verified — Damage 3000, Penetration 15 ≥ Thickness 10 so no reduction, no `Versus` table, inherited air-explosion template `CreateEffect`-only. Before: `3000*100/600` = 500 claim, three halvings, **172-tick stand-down**. After: `min(500,100)` = 100, one halving, **55-tick stand-down** — one decay period, which doubles as a check that the new `IsValidAgainst` filter did not wrongly zero the estimate. RED→GREEN on `test-aa-overkill-suppression` at seed 1829504673, with the RED reproducing the historical 172 exactly; the control lane and the plain left-click lane are byte-for-byte unchanged in both, so only the standing-down lane moved. **Composes with `68b627ce`'s soft penalty rather than replacing it.** Two `@stable` deltas disclosed — see item 43. **Not addressed and still as filed:** `BreakOffCondition = "critical-damage"` remains a hard skip at `:1371-1372`, bypassable only by force-attack._
_**(b) STILL OPEN, and the finding is NEGATIVE — read this before building anything on the preemption.** `f910ac7d` performed **both** required runs at the shipped 110-tick deadline. **BOTH PASSED.** RED control (`PreemptScanInterval` pinned to 0, seed 1298325022) → **pass**, when it was required to fail. GREEN confirm (shipped default 25, seed −1641486964) → pass. **So a green here is not evidence that preemption works, and `68b627ce` remains shipped on zero behavioural evidence.** The unaided break the test header describes — `CanAttack` going false during reload, dropping `IsAiming`, letting the opportunity-fire branch clear the persistent-target flag — beats 110 ticks on its own, which is exactly what the tick budget was constructed to rule out._
_**What this does and does not establish. "Preemption is inert" is NOT what was demonstrated.** The pin was verified **applied**, not assumed: a temporary trace in `AutoTarget.Created` printed `strykershorad PreemptScanInterval=0` on the control and `=25` on the confirm, with t90 reading 25 in both. That matters because a silently-ignored override is indistinguishable from an inert fix. It also has to be pinned **on the ACTOR** — `strykershorad` lists `Inherits@AutoTarget: ^AutoTargetAAIFV` after `Inherits@Combatant`, and that chain reaches `^AutoTargetGround`'s `PreemptScanInterval: 25`, so the obvious `^Combatant` override loses the merge. That trap is now a `PITFALL` in the scenario's `rules.yaml`._
_**Next concrete step, REPLACING the two runs above (they are spent):** the scenario needs a tick budget or a construction under which the unaided reload-driven break CANNOT beat the deadline — otherwise it can never discriminate. Until then this item is blocked on test design, not on engine work. `PreemptScanInterval: 0` is still **not** a clean revert (the same merge's `OnlyTargets` repair is not behind the flag — see item 43)._
_**Banked as a general rule at `97746a1d` / `dc899995`, in `DOCS/recipes/AUTOTEST.md`:** a green run is not evidence unless something could have made it RED. Ask not "did it pass?" but "what would have made this fail?"_

**Perceived:** an AA battery shoots at the helicopter in front of it. Today a MANPAD fires one missile and every other AA in the group **hard-skips that target for ~10 seconds**; four AA engaging one helicopter **serialise at ~185-tick spacing and take ~34 seconds** to all join, trickling in one at a time. The user spent an evening believing trees were the cause. And separately: a Stryker SHORAD is supposed to break off a low-priority target when a helicopter appears — that shipped, and **nothing has ever demonstrated in-game that it does.**
_Two sources, two angles on the same file, merged into one item because they touch the same arithmetic and want the same runs: [`closeout/f5b7d1a6.md`](../../closeout/f5b7d1a6.md) §1 (the stand-down defect, diagnosed and measured but **not fixed**) and [`closeout/fixes-cfcaa2ca.md`](../../closeout/fixes-cfcaa2ca.md) §1 (the preemption fix, **merged at `68b627ce` with zero behavioural evidence**). They do not contradict each other; `f5b7d1a6` explicitly checked `68b627ce` and found it does not close the stand-down._

_**(a) The overkill stand-down — `f5b7d1a6` track #2.** Foliage was a coincidence; reproduced with no trees and no fog. Cause: units write a shared "damage already inbound" tally so they do not all shoot one target, and MANPAD does 3000 damage against a 600-HP Halo, so a single missile records ~500 against a threshold of 100 — five kills' worth. Still open at `35876332`, each verified by reading the code:_
_- `EstimatePercentDamage` (`engine/OpenRA.Mods.Common/Traits/AutoTarget.cs:1505-1557`) still returns `totalDamage * 100 / health.MaxHP` with **no cap** — the 5× overshoot is intact._
_- The hard overkill skip is still a `continue` at `:1364-1365`, `OverkillThreshold = 100`._
_- The warhead loop at `:1530-1554` applies `Penetration` and `Versus` but **never checks `IsValidAgainst`**, so the latent `ValidTargets` over-count survives. Latent today — the templates that would trigger it, `^LargeExplosionEffectsAir` / `^HugeExplosionEffectsAir`, are inherited by nothing — but it is a strictly worse version of the same bug._
_- `BreakOffCondition = "critical-damage"` (`:232`) is still a hard skip at `:1371-1372`, still bypassable only by force-attack._
_**What DID land and does not close it:** `68b627ce` added a *soft* overkill penalty **before** the hard skip — `SoftOverkillThreshold = 50` / `SoftOverkillScale` at `:1439` — which de-prioritises a partially-committed target in the ranking. It reduces how often the hard skip is reached but **does not remove the stand-down and does not touch the arithmetic**._
_**Next concrete step, unchanged and composing with the soft penalty:** (1) clamp each shooter's contribution to one kill's worth in `MarkTargetForAttack` / `EstimatePercentDamage` (`:1505`, `:1565-1572`) — **per-shooter, not a total cap**; (2) fix the `ValidTargets` omission in the same pass (`:1530-1554`); (3) add the render-only "holding fire" marker so a unit deliberately declining to shoot is legible — no simulation change, so no benchmark impact._
_**Why per-shooter and not a total cap — the reasoning matters more than the conclusion.** That manager originally favoured capping the *total* accumulator, then the cadence measurement showed **ordinary firing does not re-apply the tally**: the lead shooter fired eight times at a strict 200-tick `BurstWait` and the other three AA still joined on schedule. The indefinite/runaway case therefore needs an external feeder that does not arise naturally, which removes the main argument for the blunt total-cap. The serialisation only exists where the claim exceeds damage actually landed (misses, or the `ValidTargets` over-count)._
_**The user's own framing, when the fix was re-explained:** the deeper complaint is that **a unit refusing to shoot a live enemy tells the player nothing** — which is what the "holding fire" marker addresses and what cost him the evening._

_**(b) The preemption has never been proven to work — `fixes-cfcaa2ca`.** `tools/autotest/scenarios/test-autotarget-preempt-air/` has **never been run at its shipped 110-tick deadline**; the test's own header still says `NOT YET ESTABLISHED at 110 ticks` (`test-autotarget-preempt-air.lua:38`). Checked against current main — `git log 68b627ce..HEAD` on `AutoTarget.cs`, `mods/ww3mod/rules/defaults.yaml` and the scenario directory returns **empty**._
_**Next concrete step — two runs, both required, and this is the user-gated half of this item:** (1) RED control: pin `PreemptScanInterval: 0`, run `test-autotarget-preempt-air`, must **FAIL**; (2) GREEN confirm: shipped default, must **PASS**._
_**Do not accept a lone GREEN as proof.** The persistent-target lock breaks on its own via the reload-driven decay of `opportunityTargetIsPersistentTarget`, so a passing run can measure nothing. The only control run ever performed was against an earlier 80-tick deadline and died on a since-removed scenario guard. **This branch produced three separate false-greens during development — treat any single result as suspect by default.** If the control does not go RED, the fix is not doing what the merge commit claims, and that needs chasing before anything is built on top of it._
_Existing test infrastructure to extend rather than duplicate: `09d44040` (`wt/aa-los-test`) landed four AA engagement scenarios — `test-aa-detection-fog`, `test-aa-overkill-cadence`, `test-aa-overkill-pump`, `test-aa-overkill-suppression`, 3027 lines, **scenarios and rules only, no engine change**._
_**Both halves move `@stable`** — see item 43, and note in particular that `PreemptScanInterval: 0` cannot restore it._
