### 54. Carried defects and hygiene — found, recorded, unowned **[RE-SCOPED 2026-08-19 — only TWO lines survive]**

> ✅ **VERDICT 2026-08-19 (`main @ 5890b053`) — every line re-checked, not just the flagged one. Four are dead or done; two remain.**
>
> **DEAD — `TEMPmt.txt`.** Not in the worktree root, and `git log --all --diff-filter=D` shows it was **never tracked** — it was an untracked stray. **The instruction below that it "is safe to delete only if the user says so" is discharged: there is nothing to delete and no decision to ask for.**
>
> **DEAD — the two desynced-game replays.** Confirmed unrecoverable: 1,398 replays under `~/Library/Application Support/OpenRA/Replays/ww3mod/release-20230225/`, 107 of them from 2026-08-11, and neither `…T170129Z.orarep` nor `…T170300Z.orarep` survives; no file from that date matches the recorded 108 KB / 76 KB sizes. Nearest neighbours are `T170009Z` and `T170229Z`. **The "preserve them" line is discharged by impossibility — the standing lesson (copy replays out IMMEDIATELY, do not file a note to do it later) is the part that survives, and it is why this matters.**
>
> **DONE — the `river-zeta` map edits.** Landed at `8343900b` *"river-zeta: fine-tuning pass on terrain and prop placement"*, plus `097738f4` re-cordon. `mods/ww3mod/maps/` is clean.
>
> **⚠️ STALE, BOTH CLAUSES — the `exp-terr-bias` line.** It says the branch is *"confirmed still the ONLY branch `--no-merged main`"* and *"still needing the `Passenger.cs:187` fix rebased in before any future run"*. **Neither holds.** `git branch --no-merged main` now returns **six** — `auto/preserved-wip-260520`, `auto/saved-game-load`, `mac-autoburn-260521-unpushed`, `wt/curation`, `wt/lint-clear`, `wt/passenger-row` — and `exp-terr-bias` **has no local branch at all**; it survives only as `origin/exp-terr-bias` (alongside `origin/wt/shellmap-session` and `origin/xavi`, also unmerged). **And the Passenger fix is now on `main`**: the dead/FrozenActor guard sits at `Passenger.cs:194`, landed `cb9d54c7`. Anyone branching off `main` today already has it.
>
> **STILL OPEN — the husk that may smoke indefinitely.** Untraced, and nothing has looked at it: `TrailWhileStationary: true` appears 17× in `mods/ww3mod/rules/husks/husks-aircraft.yaml` (`:52,60,82,90,112,120,146,172,261,315,370,405,481,532,587,626,663`); last touch is still `f2468743`; `FallsToEarth.cs` has no commit since the upstream merge `c5bb5ece`. No commit or WORKSPACE note addresses reachability.
>
> **STILL OPEN — the per-frame cost of sync reporting.** Still unmeasured. `PerfSample("sync_report")` exists at `engine/OpenRA.Game/Network/OrderManager.cs:348`; the only other mentions repo-wide are the two documents that filed it. **Needs a real game, so it stays user-side** — read it out of `perf.log` after any real match rather than spending a dedicated run.

_**RECONCILED 2026-08-13 against `main @ dc899995`.**_
_**DONE — `SpawnStartingUnits` `CPos.Zero` sentinel**, fixed at `e00b9f72` (merged `25396a33`). Switched to `List.FindIndex`, where −1 cannot collide with any legal cell. Worth keeping the reachability note: **8 of 9 ww3mod maps declare `Bounds` at 0,0**, so `Map.Contains(CPos.Zero)` is true and `FindTilesInAnnulus` can yield the origin — but **no current map triggers it** (the nearest spawn, `woodland-warfare-ww3` at 1,4, puts the origin at ceiling-Euclidean distance 5, just inside the `InnerSupportRadius+1` = 6 hole). Latent today; fires on the first map placing a spawn ~6 cells out. Deterministic either way, **so this was never a sync hazard** — do not cite it in item 42._
_**DONE — the three false file-header comments** in `BeliefStore.cs:29`, `ControlField.cs:31`, `DangerFieldLayer.cs:35`, fixed at `fd8007b7` (merged `d976a0c4`). Those staggers touch no RNG at all._
_**STILL OPEN, each verified in the tree at `dc899995`:** the husk that may smoke indefinitely (untraced); the per-frame cost of sync reporting (never measured — needs a running game, so it stays user-side; read `PerfSample("sync_report")` in `perf.log` after any real game); the two desynced-game replays (still worth preserving); `exp-terr-bias` — **confirmed still the ONLY branch `--no-merged main`**, and still needing the `Passenger.cs:187` fix rebased in before any future run; the user's `river-zeta` map edits._
_**`TEMPmt.txt` is still in the repo root** (the garbled-leading-byte filename), still untracked, still of unknown origin to every archived session. Nobody has claimed it; it is now named by five reports. **It is safe to delete only if the user says so** — do not remove it on a worker's judgement._

**Perceived:** individually small. Grouped so they are findable, not because they belong together.
_Each line names its source report._
_- **`SpawnStartingUnits.cs:145,148` use `CPos.Zero` as a "no cell found" sentinel**, but (0,0) is a legal cell. Deterministic, so **harmless for sync**, but wrong on any map whose search annulus reaches the origin. — [`54ab3880`](../../closeout/54ab3880.md) §4, "found and not chased"._
_- **Three false file-header comments.** `BeliefStore.cs:29`, `ControlField.cs:31`, `DangerFieldLayer.cs:35` claim "SharedRandom only staggers the first tick", which the code contradicts — **those staggers touch no RNG at all.** A doc fix in `engine/`, not a behaviour change. — [`54ab3880`](../../closeout/54ab3880.md) §4._
_- **A husk that comes to rest may smoke indefinitely — UNTRACED.** The 17 husk trail blocks migrated to `LeavesTrails` map to *unconditional* (because `^BasicHusk` never grants `critical-damage`) **and** carry `TrailWhileStationary: true`. **`FallsToEarth` was never traced to establish whether that state is reachable**, so this is a flagged risk, not a confirmed defect. — [`6361c2be`](../../closeout/6361c2be.md) §4._
_- **The per-frame cost of sync reporting has never been measured** — it needs a running game, which project rules forbid a worker to launch. Instrumented as `PerfSample("sync_report")`; **read it in `perf.log` after any real game.** If it costs more than roughly a millisecond per net frame in a large fight, the honest lever is `NumSyncReports` (raised 7 → 32 in `b1178706`), **not the feature**. — [`54ab3880`](../../closeout/54ab3880.md) §4._
_- ~~**Two desynced-game replays survive and are worth preserving before they age out**: `Replays/ww3mod/release-20230225/ww3mod-2026-08-11T170129Z.orarep` (108 KB) and `...T170300Z.orarep` (76 KB).~~ **FALSE as of 2026-08-19 — NEITHER FILE EXISTS.** The replay directory holds 1,397 `.orarep` files and 107 from 2026-08-11, but nothing matching either name. The nearest by timestamp are `T170009Z` (319 KB) and `T170229Z` (283 KB), so this is not a transcription slip in the names alone — the sizes do not match either, and no 2026-08-11 file is 108 KB or 76 KB. The warning to preserve them "before they age out" was correct and was not acted on; assume they aged out. **The only recorded artifact of the unresolved 2-human desync is gone.** The surrounding claims still hold and are worth keeping for whenever a new repro is captured: such replays hold **both clients' order streams** — the server-side recorder was invalidated on OOS (`Server.cs:791-798`) but the client-side files were not — and **`generateSyncReport` is explicitly false for a `ReplayConnection`, so replaying them cannot produce a sync report**; their value is the order streams alone. **If a desync is reproduced again, copy the replays out of `Application Support` immediately rather than filing a note to do it later.** — [`54ab3880`](../../closeout/54ab3880.md) §4, corrected 2026-08-19._
_- **Branch `exp-terr-bias` is the only branch `--no-merged main`.** Its tip message records that the TERR verify batch was **halted by the user with 20/20 already completed and no procs left**; not merged. **Needs the `Passenger.cs:187` fix rebased in before any future run.** — [`e929eb99`](../../closeout/e929eb99.md) §2, which recorded it as the lead's state rather than its own and deliberately left it untouched._
_- **The user's own `river-zeta` map edits** were uncommitted at that close-out and were **deliberately left untouched** (their work, unknown whether finished). `nav-guard` measured them as **strictly opening the map up** — wheeled passable 5,237 → 5,511, largest component 4,785 → 5,396, pocketed cells 452 → 115, components 38 → 30; **nothing sealed**. `nav_guard.py bless` re-records the baseline once the user is happy. **If those edits are still dirty in a later session, this is the evidence they are safe.** — [`bdedd544`](../../closeout/bdedd544.md) §2._
_- **Housekeeping already done, recorded so it is not re-attempted:** [`6361c2be`](../../closeout/6361c2be.md) §2 listed `wt/vehicle-occupants`, `wt/ammo-rearm` and `wt/fx-round1` as merged branch refs safe to `git branch -d`. **Verified 2026-08-12: all three are already gone from the branch list.** The untracked `TEMPmt.txt` in the repo root (garbled leading byte in the filename) is named by four reports as belonging to none of them; **its origin is unknown to every archived session.**_
