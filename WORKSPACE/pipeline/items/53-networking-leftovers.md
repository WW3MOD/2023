### 53. Networking leftovers — four items deliberately deferred, none solved upstream **[OPEN — unchanged, verified untouched 2026-08-13]**

_**RECONCILED 2026-08-13 against `main @ dc899995`: nothing in the 55 commits since `4d3c8f90` touches any of the four, and none was solved upstream.** All four still read as filed. **One adjacent landing worth knowing about, which does NOT close anything here:** `2502a8af` documented multiplayer **continuity** (disconnect handling, rejoin, claimable slots) as a plan at the user's explicit request not to implement it — that is item 55, not this item. Its load-bearing distinction is relevant to the dedicated-server bullet, though: continuity transfers nothing (every client already simulates the dropped player's units, budget and Supply Route, so only who issues orders changes), while **admission** must bring a new client to a bit-identical world state — and admission is gated on determinism **not yet demonstrated**, because restore is a replay from frame 0 rather than a snapshot. See item 42(iii): that determinism is currently RED._

**Perceived:** varies per item; the first one is the only one a player would feel.
_Source: [`closeout/multi-d1c4aecd.md`](../../closeout/multi-d1c4aecd.md) §1. Both of that manager's tracks are terminal (`mp-port-forward` shipped, `nat-traversal` shipped, merged at `bfd683c2`); **all four below were re-checked in the tree at `35876332` and none was solved upstream.**_
_- **Dedicated server** — the only option that structurally removes the NAT problem for *every* player. `engine/OpenRA.Server/Program.cs` still builds headless and unused. **Next step:** run the existing headless server on a VPS or always-on box, then add an "Official Server" button that Direct-Connects to a known hostname. The connect path already takes a hostname (`ConnectionTarget.cs:46`), **so joiners need no inbound port at all.** _(Note: the user **explicitly declined, for now**, the dedicated-server and relay options when asked about NAT scope — so this is deferred by user decision, not by oversight.)__
_- **~38 dead Fluent keys** in `mods/ww3mod/languages/en.ftl` — `no-port-forward` still at `:119`, `timeout-in` at `:84`, `chat-temp-disabled` at `:129`. **Next step:** delete or rename the block `:84`–`:129` to the live `notification-`-prefixed names. Logged `[low]` in `WORKSPACE/bugs/discovered.md`. **Deferred because a blind rename changes ~38 lobby strings at once and needs its own review.**_
_- **`ServerCreationLogic.BuildNotices()` snapshots `Nat.Status`** — still snapshotted at `ServerCreationLogic.cs:181-188`. **Only worth doing as a layout restructure** (the snapshot drives `Fonts[].Measure()` then repositions three sibling labels by `Bounds.X`/`Width`). **Low priority:** the sibling notice blocks at `:146-159` already re-read `Nat.Status` in live closures, and the shipped FIX 2 means a stale read no longer produces a false *assertion* anywhere._
_- **WW3MOD advertises on `master.openra.net`** — no `WebServices` override anywhere in `mods/ww3mod`. **This is a decision, not a task**: a total conversion listing itself on upstream's public infrastructure is both a dependency and a courtesy question. Pointing `WebServices.ServerList` / `ServerAdvertise` at our own master is a `mod.yaml` block, not code. Parked in [`AWAITING-USER.md`](../../AWAITING-USER.md)._
_**Two verification debts on shipped code.** (1) The **VPN-reject branch of `GetLocalAddress()` was never exercised** with a tunnel actually winning the route — confirmed only in the pass-through direction (it returns `192.168.0.35` on this machine). To close it: select a Tailscale exit node and re-run `LocalAddressIsPrivateOrNothing`; `GetLocalAddress()` should go null and the lobby should degrade to naming no address. (2) **Nobody has hosted a genuinely failing lobby with this build**, so the four-line lobby output is read off the enqueue order rather than seen on screen._
_**Residual user-side action, never completed: a DHCP reservation for `192.168.0.35`.** Without it a lease shuffle re-breaks the port-forward rule identically — that was the original root cause (the router forwarded external TCP 1234 to a stale LAN IP, `192.168.0.21`, while the machine was `192.168.0.35`). An end-to-end join by a second machine was also never performed. Diagnostic trap worth keeping: **the lobby prints "Master server communication established" even on failure** — it is *not* a success signal; the real signal is the **absence** of "Server port is not accessible from the internet". The only trustworthy check is external: `Invoke-RestMethod https://ifconfig.co/port/1234` with a lobby open._
