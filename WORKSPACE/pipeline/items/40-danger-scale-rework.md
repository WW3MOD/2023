### 40. Danger-scale rework — stop the bot treating ordinary ground as lethal **[stage (a) DONE `ddcc5d6c`; stage (c) OPEN. NO LONGER THE TOP OF THE QUEUE — item 56 outranks it as of 2026-08-13, on the user's direct instruction and with the blunt fix pre-authorised. This remains the principled version of the same problem and still gates the benchmark re-baseline.]**
**Perceived:** the bot stops flinching at nothing. Supply trucks actually deliver instead of driving part-way and turning back; units stop refusing to advance across terrain that is not in fact dangerous. Today the danger field reports numbers so far above every threshold that reads it that the safety gates are effectively always-on — the bot behaves as though the whole map is a kill zone, which is why "trucks won't deliver" has survived six merges aimed directly at it.
_Promoted from prose. This work had been living inside the SESSION STATE block, which is a weaker instrument than a numbered item, and **it is the gate in front of the benchmark re-baseline — which in turn gates items 22, 25, 31 and ambush gate (b).** Nothing downstream of a measurement can move until the measuring instrument is right._
_**The defect, in one line:** all **18 ground thresholds** (plus 1 air; 19 total, corrected up from the "13" the earlier tables said — `1107c98c`) sit **8× to 459× below the live median cell of the field they gate**. Only `ReserveDangerSafeUnits=0` is exempt, and only because zero converts losslessly. Evidence: first live read `084367b0`, arithmetic and sequencing in DISCOVERIES `f2a31035`._
_**Three stages, and they cannot be collapsed — each one moves the input to the next:**_
_**RECONCILED 2026-08-13 against `main @ dc899995` — stage (a) is DONE and this item's "IN FLIGHT" tag was already stale when the 08-12 intake was written.** `f09183e2`, merged `ddcc5d6c` on **2026-08-11**, which PREDATES the `4d3c8f90` intake baseline — so this is staleness caught, not a new landing. `HealthDivisor` 10 → 1000 and `CostDivisor` 50 → 5000 take the band from **1.20×–29.50× to 1.00×–1.28× (median 1.00×)**. **Both profiles moved** and it is disclosed: `DangerFieldLayer` is a World trait (`world.yaml:368`, bare key, no YAML override of these fields) — see item 43._
_**Stage (b) is partly PERFORMED and its instrument has landed.** The same commit added a `--danger-reference` utility command that re-derives the whole `[danger]` reference line from YAML through the production code; flipping the divisors back reproduces the 2026-08-10 play log **digit for digit** (`ground=3412 air=3627 ground-types=92/424 min=164 max=1271250`), which is what licenses trusting the post-change numbers: **ground 3412 → 2748 (−19.5%), air 3627 → 2034 (−43.9%)**. **The two channels rescale by DIFFERENT factors, so an air threshold can never be re-derived from a ground measurement.** Ground spread 21/2748/245000; air 514/2034/76890._
_**BOTH of this item's "two things to settle before (c)" are now settled, one by construction and one by refutation:**_
_- _(1) the percentile blocker is REMOVED — a `[danger] pct` line (deciles + p95/p99, both channels, same cell population as the existing median) now ships, so the successor percentile-based unit can be chosen against the distribution's real shape. Diagnostics only, on the same already-bounded episode gate as `[danger] dist`, which stays byte-identical.__
_- _(2) **the "phantom weapon" premise is REFUTED and no weapon is excluded.** `IskanderTargeter` and `HIMARSTargeter` were reported as harmless spotters padding the population and setting the `min=164` floor. They are not: in `DamageWarhead.DamageVersus` an **unlisted armor class matches nothing and takes the unmodified 100%, so omission is the OPPOSITE of a zero.** Their table zeroes a class this ruleset does not have (Brick) and omits three it does (Kevlar — every soldier — plus Unarmored and Indestructable), so both deal their full 50 to infantry. **Contributing population is unchanged at 92/424; `min` fell to 21 purely from the weight rescale.** A mod-data bug was filed for the tables themselves.__
_**Stage (c) — re-derive all 18 ground thresholds + 1 air on a percentile-of-live-field model — remains OPEN and is now the whole of this item.** Its strongest supporting argument is untouched: the two players' median danger cells differ by 3.4× in the SAME match on the SAME map (USA 818u, Russia 2755u), so no global constant can sit at the same percentile for both — **the constant form is wrong, not mistuned.**_

_**(a) Fix the RA-scaled durability weight. — DONE, see above.** The weight band was **1.20×–29.50×** against a documented ~1.0× intent. Counter-intuitively this moves the *reference* only **3,412 → 2,748 (−19%)**, because the reference is the median TYPE and that type is a 1.22× rifleman — but it divides heavy-armour contacts' stamps by up to **~20×**. So it barely moves the denominator while radically reshaping the distribution._
_**(b) Re-measure the field afterwards, not before.** This is exactly why (a) and (c) cannot be merged: thresholds derived before the weight fix would be fitted to a field about to shrink underneath them. The re-measurement is now **cheap and offline** — an offline MiniYaml parser reproduces the logged reference line exactly (`ground=3412 air=3627 92/424 min=164 max=1271250`), so re-deriving the unit **no longer needs a live game session**._
_**(c) Only then re-derive all 18, on a percentile-of-live-field model rather than the current median-of-types one.** The case for changing the FORM rather than retuning the constants is the strongest single finding here: **the two players' median danger cells differ by 3.4× in the SAME match on the SAME map** (USA 818u, Russia 2755u), so no global constant can sit at the same percentile for both — the constant form is wrong, not mistuned. Supporting: the weight fix **widens** the contributing-type spread (3.89 → 4.07 orders), so median-of-types is a weak denominator regardless._
_**Two things to settle before (c) can actually be executed** — flagged, not solved: (1) **no percentile can honestly be derived from the current logging**, which emits min/median/max only; a percentile model needs richer instrumentation added first, or it is a percentile in name only. (2) `WeaponThroughput` ignores `Versus`, so zero-damage targeter weapons currently register as ground threats and pollute the distribution the percentiles would be taken over._
_**Scope warning — this necessarily moves BOTH profiles.** `DangerFieldLayer` is a world trait with no per-profile gating, so there is no `@stable`/`@experimental` seam to hide behind here and no point building one (settled policy: `@stable` inherits improvements). Say so in the commit message so the next benchmark baseline is re-taken knowingly, and expect this to be the largest single contributor to that re-baseline's delta._

---
