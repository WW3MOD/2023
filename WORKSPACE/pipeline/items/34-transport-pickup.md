### 34. Transport pickup coordination — a tactical layer for humans AND bots
**Perceived:** you order soldiers into a transport and it just works — the vehicle drives to them, waits, collects everyone nearby, then carries on with whatever it was doing. Today the vehicle drives off without waiting and the player has to micro it.
_User 2026-08-08: "when soldiers are ordered to enter a vehicle, that vehicle directly routes towards the soldiers they should pick up, stopping a few cells away, and waiting until no more pickups are within those cells, then drives to the next pickup if there are more, and when all are done, it continues with its queue of orders." Explicitly wanted for HUMAN play too — "I find myself also often struggling with pickups." Prior art to reuse rather than reinvent: the heli-lift work (`3e59fed1`) already built passenger reservation, a shared `IsLiftCandidate` so demand and load agree, and `StandDownStragglers` to release passengers who did not make it — that last one exists precisely because a transport departing without its stragglers strands them and pins the `Cargo` lock. `MountedTransportBotModule` has the bot-side equivalent. The gap is the human/unit-level layer._
_**RECON LANDED, NOTHING IMPLEMENTED** (`f819d646`, `WORKSPACE/recon/260808-transport-census.md`, 273 lines) — and it reframes the item. **"One passenger at a time" is not the defect**: both real transport modules already batch (mounted 2–5, heli 4–8) and wait for a minimum load. What is missing is **demand** — no module publishes where a soldier needs to go, so each transport invents its own destination and the pickup pool is a 14-cell bubble around the own SR that a soldier leaves exactly once. Build the demand-publication layer first; the waiting behaviour the user described is downstream of it. Adjacent, already landed: `18838dd7` gave the unload side paced dismount and emergency bail, so the *drop-off* half of a pickup round-trip now reads correctly._
