# AI Benchmark — Status Board

**This is an information surface FOR you (the user), not a two-way channel.** The
loop writes here; you read. It is designed so that after a stretch of autonomous
work you can open this one file and, without reading any transcript, see: **is the
loop doing well, were there any larger pivots, and does it need you for anything.**

Read **top-down, most-urgent-first**:

1. **Needs attention** — the short list of things that want a human: open decisions
   running on a default, anything blocked, anything surprising. If this is empty,
   the loop is healthy and self-directed.
2. **Highlights** — the recent milestones/pivots/verdicts in plain language, so you
   can see the shape of progress at a glance.
3. **Current state** — the live ladder numbers, the `main` SHA, what's in flight.
4. **Activity log** — the full reverse-chronological history at the bottom.

> **To steer or answer a question, use the Maestro dashboard** — ask/answer there.
> REVIEW.md **no longer has an inbox**; the old write-back-in-this-file mechanism
> is retired. Anything the loop needs from you is surfaced both here (under *Needs
> attention*) and as a dashboard question.

> **Terminology — v2 → Experimental (2026-07-20, `ai-bench` rename commit).** The dev
> bot formerly keyed `@v2` / "V2 AI (experimental)" is now `ModularBot@experimental` /
> "Experimental AI"; a frozen **`ModularBot@stable`** ("Stable AI") holds the last
> validated snapshot (promotion policy: SPEC §13). Old references (`@v2`,
> `enable-ai-v2`, `[v2-*]` logs, `tournament-v2-vs-normal-*`) map to `@experimental` /
> `enable-ai-experimental` / `[exp-*]` / `tournament-experimental-vs-normal-*`.
> **Historical run records under `runs/` keep their original "v2" names.**

---

## Needs attention

Urgency-ordered. Few items, each one line + why it matters. Cleared when resolved.

1. **One declared re-baseline remains** (of two ratified 2026-07-21). (a) **Phase 0
   global cohesion cap — DONE** @ `1eb644de`: the `[cohesion-cap]` re-baseline moved
   only S2-base (engagement +37%, swing edge −350→−100), no pass/fail flip; bars now
   bind against `1eb644de` (`runs/260721_cohesion_cap_rebaseline.md`). (b) **Phase 4
   full fog migration** of the bot strategic grids (InfluenceMap + ThreatMapManager →
   per-player fog-respecting intel), which absorbs the old ladder cycle 5, is **still
   ahead**. *Why it matters:* expect a **bot-strength dip after the fog migration**
   (bots lose free omniscient intel); recovered via a new recon/scouting behavior
   cycle — a second re-baseline. Spec:
   `WORKSPACE/plans/260722_strategic_tactical_split_SPEC.md`.
2. **Research pair DELIVERED — roadmap-additions fork running on a default
   (2026-07-21).** Instead of the broad red-team the user floated, a targeted pair
   ran (dashboard question recorded that call). Both docs are in
   `WORKSPACE/plans/`: `260722_watchability_research.md` (the observer suite is
   already wired but unused; missing keystone = auto-director camera; the SHOW
   needs a separate config from the benchmark — same-faction ladder games are
   passive by design) and `260722_doctrine_realism_audit.md` (loudest realism gap:
   **no fires/artillery bot logic exists at all**; also caught the §4
   Defensive-stance inversion — **already fixed in the spec**, `5eb8755a` — and
   the Phase-4 blind-window risk — **scout link folded into Phase 4**). A second
   dashboard question is open: which additions join the roadmap (default on skip:
   observer/show packaging + fires cycle). *Why it matters:* these set the
   post-Phase-3 direction toward the watchable-bot-games goal.
3. **New PROPOSED bars await ratification** (the `[pre-regime]` bars are void).
   S1: paired win-rate ≥ 0.60 AND Exp capture-rate ≥ Stable +2/10. S2:
   median(Exp swing) ≥ median(Stable swing) + $1,000, sign-delta ≥7/10, both-spawn.
   *Why it matters:* these are the yardsticks the loop will run on by default; a
   "yes" locks them, or adjust. Detail in LADDER re-baseline banner. Still running
   on defaults.
4. **S2 combat signal is weak on the new regime — a scoping decision.** Same-faction
   Stable-vs-Stable fights only ~half the time (engagement −5–6× vs pre-regime; 3/10
   matches zero-combat). A **blocking batch-validity gate** (≥6/10 engaged, else
   re-scope) is proposed as the guard. *Why it matters:* if S2 keeps producing
   passive stalemates, the net-swing metric is low-signal — decide whether to move
   S2 to a **forced-contact map / `@rush` opponent** or keep the quiet regime.
   *Update:* the cycle-1 terr-bias verify measured engaged **7/10** (unchanged from
   baseline) — the quiet-regime + validity-gate stays the default.
5. **Sequencing now owned by the split plan (SUPERSEDES prior "which cycle runs
   next" items).** The old cycle-2 routing / post-S1 sequencing notes (RETHINK #2's
   5-cycle sequence) are superseded: the ratified split plan owns ordering now —
   **verification pass → Phase 0 cohesion → Phase 1 layers+overlay** (phases run
   0→5). Noted here rather than deleted silently; the RETHINK #2 highlight stays as
   history.

---

## Highlights

Most recent first. Milestones, pivots, and verdicts in plain language.

- **✅ `[cohesion-cap]` LADDER RE-BASELINE DONE (2026-07-21, `main` @ `1eb644de`).**
  Re-measured all rungs after the Phase 0 global cohesion cap (declared re-baseline;
  the cap ships to the frozen controls too). 43 matches, 5 phases, **0 crashes / 0
  no-verdict**. **The cap moved exactly one rung — S2 Exp-vs-Stable — in the predicted
  direction:** engagement **+37%** (Exp 1300→1775) and swing edge **−350 → −100**, as
  Experimental's SR-contestation assault groups get concentrated by the footprint cap.
  All three S1 rungs and the S2 Stable-vs-Stable calibration are **byte-identical** to
  `60b93501` (passive/small formations never reach the cap). **No pass/fail flipped:**
  Exp ≈ Stable still holds, all PROPOSED bars still not passing, S1 floor holds,
  validity gate 7/10 engaged. The cap is a human-play correctness fix that is benign on
  the benchmark. Bars now bind against `1eb644de`. Full doc:
  `runs/260721_cohesion_cap_rebaseline.md`.
- **🎯 STRATEGIC/TACTICAL SPLIT SPEC RATIFIED (2026-07-21).** The user ratified the
  3-layer vision: **L1 bot strategy** (where to fight) / **L2 squad ops** /
  **L3 SHARED stance-driven unit micro** (threat response, cover-seeking, treeline
  positioning — runs identically for bot- and human-owned units). New substrate:
  a per-player **fog-respecting sighting layer** (ThreatDirection/ThreatIntensity
  from own Shroud + FrozenActorLayer) + a static **cover-edge affordance layer**;
  a **hold-Space intel overlay** (balance-of-power color wash with computed
  grayzone + last-seen enemy **GPS dots reusing the in-repo OpenRA.Mods.Cnc GpsDot
  substrate**). The user's aggressive/defensive treeline case maps onto the
  existing **Engagement stance** (Hunt = cover-forward toward threat / Defensive =
  cover-back / HoldPosition = inert) — no new stance family. In-transit semantics:
  stances **detour** but **never cancel** an explicit order. **Forks answered by
  the user:** global cohesion fix + declared re-baseline; human default **ON**
  (per-type Ctrl-Alt-click defaults); **FULL** bot fog migration (absorbs ladder
  cycle 5). Phases run 0→5. Spec:
  `WORKSPACE/plans/260722_strategic_tactical_split_SPEC.md` (survey input:
  `WORKSPACE/plans/260722_stance_tactical_survey.md`, commit 9c94ce63).
- **🔧 LIVE-PLAY TRIAGE (2026-07-21, UNVERIFIED — verification pass now running).**
  Three fixes from live play, awaiting the verify batch: **cb9d54c7** Passenger NRE
  crash guard (Passenger.cs:187, dead/null transport); **d256efde** no-dismount
  root cause — the bot issued `"UnloadCargo"`, an *activity* name, not a resolvable
  order string; the correct `"Unload"` is gated behind @experimental-only
  `UnloadOnArrival`; **d4dc39cd** heli corner-idle diagnosed (SR-corner staging via
  the `ProductionFromMapEdge` fallback; fix deferred pending benchmark evidence).
- **📉 CYCLE-1 TERR-BIAS VERDICT: MISS.** The territorial offense bias (branch
  `exp-terr-bias`) came back a miss: S2 swing **−350 = baseline**, engaged count
  unchanged — the factor acted as a near-pure damper (the boost threshold is
  unreachable at offensive targets). Branch **parked UNMERGED @ ccd12c98**;
  retune-or-abandon is queued *behind* the split plan.
- **🧭 RETHINK #2 (2026-07-21) — next lever = the territorial map layer, started as a
  small slice.** The re-baseline said the benchmark caught up (Exp≈Stable, SR axis
  exhausted). The architecture review's verdict: the next gain is **structural**, and
  the data + the user's North Star agree on which structure — a **balance-of-power /
  territorial map layer**. The loudest signal (S2 collapsing into a passive economy
  race, 3/10 zero-combat) is a *behavior* gap: the bots have no drive to press a
  weakening sector. So the recommended cycle 1 is the smallest behavior-bearing slice —
  a "push where the enemy is comparatively weak, advance the front where safe" offense
  bias that reads the **existing `InfluenceMap`** (M effort, reuses the substrate).
  Sequence: (1) territorial bias + telemetry markers, (2) disable the negative-credit
  dispersion doctrine, (3) early-packet tuning, (4) add the Polar Disorder rung
  (anti-overfit), (5) the full fog-respecting classification layer. **Deferred:**
  mission abstraction (its decision rule isn't met — capture is at parity, not failing
  on retry). **Foundations:** seeded determinism already landed; structured telemetry
  ships *alongside* cycle 1. **S2 scoping:** keep the quiet regime + batch-validity
  gate and let cycle 1's engaged-count be the test of bot-passivity vs map-geometry —
  redesign to forced-contact only if the lever fails to lift engagement. Decision doc:
  `reports/260721_rethink2.md`.
- **✅ REGIME RE-BASELINE COMPLETE (2026-07-21) — Experimental ≈ Stable on both rungs.**
  43 matches, 5 phases, 0 crashes / 0 no-verdict, ~78 min. **S1:** Exp-v-Stable
  **5–5**, capture **6/10 vs 6/10** (both slots now capture — Motorized start + Stable
  carries the promoted capture floor). **S2:** Exp-v-Stable **5–5**, swing edge
  **−350** (no Exp force edge; the SR axis that beat Normal +$6,300 is neutral vs a
  competent Stable). Calibrations: S1 side-lean **7–3 Russia-slot** (mirror mandatory),
  S2 side **even** but **engagement collapsed ~5–6×** (3/10 zero-combat). Floor
  (Exp-v-Normal, N=3) weak **2–1**. New proposed bars logged (LADDER banner). **Takeaway:
  the benchmark caught up to the bot — next cycle needs a new lever.** Fast harness
  realized ~66–71 s/match (S1) and ~138–157 s/match (S2). Full doc:
  `runs/260721_regime_rebaseline.md`.
- **⚠ BENCHMARK REGIME CHANGED (2026-07-21, user-directed) — every S1/S2 bar now awaits
  re-baseline.** The instrument was changed in three ways at once: (1) **Motorized
  starting units** on both sides (was `none`), (2) **same faction both sides (US vs US)**
  so faction imbalance is out of the measurement, (3) **primary opponent = `@stable`**
  (the frozen PROMOTE snapshot), with Normal demoted to a single Exp-vs-Normal
  **sanity-floor** scenario. Scenario/harness files only — no bot behavior touched. **All
  numbers below and in LADDER are now `[pre-regime]`** (measured under none / mixed
  factions / vs Normal): not comparable, must not be reused as bars. Next: re-CALIBRATE
  (Stable-vs-Stable side-fairness + S2 min-engagement) then re-BASELINE S1/S2 on the new
  regime. Scenario names for the batch runner are in LADDER §Scenario registry.
- **SR-contestation cycle 1 MERGED** — the enemy Supply Route now wins an offensive
  axis. **S1 held** (capture 8/10, win 10–0, identical to reference) and **S2 got
  stronger**: relative edge **+$6,300** (vs +$4,850 before), win 10–0. Contesting
  the enemy SR is *positive* on the exact S2 metric where dispersion was negative.
- **S2 rung's first measurement: bar PASSED** — relative edge **+$4,850**, sign-delta
  **8/10**, win **10–0**. Big pivot in the same batch: the dispersion (cohesion)
  doctrine's **causal credit came back NEGATIVE** (ON−OFF median −$1,500) — cohesion
  buys *decisiveness*, not exchange efficiency. Combat economy is unharmed either way.
- **Capture-throughput design study** landed — recommends Option A (run the TECN
  floor every scan, floor 2) then Option B (escort-bundled reinforcement packaging),
  as separate cycles. Found a new root cause: requests die on a busy production queue.
- **PROMOTE: Stable ← Experimental** (post-S1 snapshot) — Stable now carries the
  TECN availability floor + dispersion doctrine; a known-good bot to play while the
  loop keeps moving Experimental. Controls (Normal/Rush/Turtle) byte-untouched.
- **Seeded reference baseline (N=10) PASSED** — S1 bar re-confirmed on the first
  fully reproducible sample: capture **8/10**, conditional gross median **$6,457**,
  win **10–0**. This per-seed table is now the paired anchor for future cycles.
- **Full replay determinism shipped** — same seed now → byte-identical verdict.
  Cross-cycle comparisons became *paired* (big variance win); enabled the clean
  dispersion on/off A/B.
- **S1 CYCLE 2 (TECN availability floor) MERGED** — in-window capture **4/10 → 8/10**,
  conditional gross median **$7,726**, win **10–0**. Causally confirmed (298 floor
  requests; matches fielding zero capturers all-match 5/10 → **0/10**).
- **S1 CYCLE 1: bar FAILED, diagnosis decisive** — merged anyway as behavior-neutral
  observability. Pinpointed the binding constraint: **TECN production/availability**,
  not coordinator logic — which set up cycle 2's win.
- **Dispersion cohesion MERGED** — spread-to-move, mass-to-assault. Non-regression
  exceeded on S1; its real combat signal was deferred to the (now-built) S2 rung.
- **S2 combat rung stood up + calibrated** — River Zeta combat scenario (720s clock),
  Normal-vs-Normal calibration passed the min-engagement check (Normal fights, no
  @rush fallback needed); mild russia-side lean neutralised by the mandatory mirror.

---

## Current state

Live ladder standing on `main`. Definitions in [`LADDER.md`](LADDER.md); full
per-scenario detail below and in `runs/`.

| | |
|---|---|
| **`main` @** | `1eb644de` (+ this doc commit) — Phase 0 global cohesion cap; `[cohesion-cap]` ladder re-baseline done |
| **Parked branch** | `exp-terr-bias` @ `ccd12c98` — **UNMERGED** (cycle-1 terr-bias MISS; retune-or-abandon queued behind the split plan) |
| **Active rung** | River Zeta WW3 (Scenarios 1–3) — **not yet cleared** (composite gate open) |
| **Run mode** | Mode B (minimized + framerate-uncapped, `SpeedMultiplier: 8`) — unlimited unattended runs |
| **Machine-hold** | **LIFTED** — builds/tests allowed, serialized (one build or one game-batch at a time) |
| **Autoburn** | **ACTIVE** — 8h grant |
| **In flight** | **`[cohesion-cap]` ladder re-baseline DONE** @ `1eb644de` (43 matches, 0 crashes) — cap moved only S2-base, no pass/fail flip; next is the Phase 1 layers + hold-Space overlay |
| **Next queued** | Per the ratified split plan (`WORKSPACE/plans/260722_strategic_tactical_split_SPEC.md`): **verification pass → Phase 0 global cohesion fix → Phase 1 layers + hold-Space overlay** (phases run 0→5). Supersedes the RETHINK #2 5-cycle sequence. |

> **Live standing — NEW REGIME + `[cohesion-cap]` (2026-07-21: Motorized /
> same-faction US-US / vs `@stable`, `main` @ `1eb644de`).** Numbers are Experimental
> vs the frozen **Stable** control. Re-measured after the Phase 0 global cohesion cap
> (declared re-baseline): S1×3 + S2-cal byte-identical to `60b93501`; **only S2-base
> moved** (swing edge −350 → −100, Exp eng 1300 → 1775 — cap binds on Exp's SR-pressure
> assault groups, no pass/fail flip). Bars PROPOSED, awaiting ratification. Full data:
> `runs/260721_cohesion_cap_rebaseline.md`.

| Scenario | Metric | Experimental | Stable (control) | Proposed bar | Verdict |
|---|---|---|---|---|---|
| **S1** Economy (5 min) | win-rate + capture-rate | win **5/10**; capture **6/10** | win 5/10; capture 6/10 | win ≥0.60 AND capture ≥ Stable +2/10 | **not passing** (tied; floor holds) |
| **S2** Force Eff. (12 min) | net swing (`kills_cost − deaths_cost`) | median **−100** (was −350); win 5/10 | median **0** | median(Exp) ≥ median(Stable) + $1,000, sign ≥7/10 | **not passing** (edge −100; batch valid 7/10 engaged) |
| **S1 floor** (vs Normal) | win-rate | **2/3**, capture 1/3 | Normal 1/3, capture 0/3 | Exp ≥ Normal (sanity) | weak **PASS** |
| **Composite gate** | all three, one commit | — | — | all pass together | **not cleared** |

Note: Exp's only behavioral delta from Stable is the SR-contestation axis; the
re-baseline shows it gives **no edge vs Stable** on either facet — the benchmark
has caught up, and the next cycle needs a new improvement lever.

---

## Activity log

Full reverse-chronological history, one line each:
`YYYY-MM-DD | <sha7> | CATEGORY | one-liner`.
Categories: `AI` `ENGINE` `HARNESS` `MERGE` `LADDER` `REVERT` `NOTE`.
Newest at top. This is the durable record; the sections above are the digest.

```
2026-07-21 | 1eb644de | LADDER | [cohesion-cap] LADDER RE-BASELINE DONE (main @ 1eb644de, Phase 0 global cohesion cap = bound ComputeBoxSlots footprint + regroup-on-arrival, a DECLARED re-baseline since the cap ships to the frozen @stable/@normal controls too -> the 60b93501 regime numbers are void). Rebuilt clean (0 errors); 43 matches / 5 phases hidden Mode-B (minimized+uncapped, 8x), 0 crashes / 0 no-verdict, all time_limit; git_dirty:false, verdict_v5, seeds 1017..10017. Same regime (Motorized / same-faction US-US / vs @stable); deltas vs 60b93501 isolate the COHESION CAP alone. RESULT: the cap moved EXACTLY ONE rung. PHASE 1 S1-CAL (Stable-v-Stable N=10): BYTE-IDENTICAL to 60b93501 (7-3 Russia-slot, USA gross med 6112.5 / Russia 2975.5, score 16376/12101). PHASE 2 S1-BASE (Exp-v-Stable N=10 5+5): BYTE-IDENTICAL (win 5-5, Exp capture 6/10, primary gross 6167, Stable 6124). PHASE 3 S1-FLOOR (Exp-v-Normal N=3): BYTE-IDENTICAL (win 2-1, Exp cap 1/3, Normal 0/3, weak PASS). PHASE 4 S2-CAL (Stable-v-Stable N=10): BYTE-IDENTICAL (even 5-5, swing 0/-725, eng 1200/1925, deaths 725/1275, score 37841/20967, same 3/10 zero-combat seeds 3017/7017/10017, MIN-ENGAGEMENT PASS). PHASE 5 S2-BASE (Exp-v-Stable N=10 5+5): THE CAP BINDS -- win 5-5 (same), median Exp swing -350 -> -100 (edge -350 -> -100, +250), Exp eng median 1300 -> 1775 (+37%), Stable eng 2800 -> 3175, sign-delta 3/10 (same), both-spawn 2/5+1/5 (same), engaged 7/10 VALID (same 3 zero-combat seeds); worst cell 8017 IDENTICAL (Exp -4950 k/d 4/10), 6017 shifted (-4200->-4500). MECHANISM: cap is inert on small/passive formations (all S1 + S2-cal replay identically) and binds ONLY on Experimental's SR-contestation assault groups large enough to exceed the MaxWidth/MaxDepth footprint cap -> tightens them -> match diverges; direction = predicted (tighter formations -> more concentrated fights, engagement up, Exp trades slightly less badly). NOTHING FLIPS: edge -100 still fails the PROPOSED +$1,000 S2 bar, win 5-5, sign 3/10, both-spawn 2/5+1/5, validity 7/10 -> Exp ~= Stable on both rungs still holds, S1 floor holds. Cap = human-play correctness fix (map-spanning scatter), benign on the bot-vs-bot benchmark. PROPOSED bars unchanged in value, now bind against 1eb644de (still unratified). Analysis: runs/260721_cohesion_cap_rebaseline.md; LADDER [cohesion-cap] banner + REVIEW Current-state/live-standing/Highlights updated.
2026-07-21 | 9c94ce63 | NOTE | STRATEGIC/TACTICAL SPLIT SPEC RATIFIED + doc persisted (this doc commit). 3-layer architecture ratified by the user: L1 bot strategy (where to fight) / L2 squad ops / L3 SHARED stance-driven unit micro that runs identically for bot- AND human-owned units (threat response, cover-seeking, treeline positioning, regroup). NEW substrate: (3a) per-player fog-respecting sighting/threat layer (ThreatDirection + ThreatIntensity from own Shroud + FrozenActorLayer, CellLayer<T> + staggered recompute); (3b) static cover-edge affordance layer (per-cell cover quality + outward edge orientation, precomputed at map load -> treeline lookup not search); (3d) hold-Space intel overlay (balance-of-power color wash w/ COMPUTED grayzone + last-seen enemy GPS dots REUSING the in-repo OpenRA.Mods.Cnc GpsDot/GpsWatcher/GpsDotEffect substrate; render-side only, reads viewing player's own layers so leaks nothing through fog). STANCE MAPPING reuses the existing Engagement family (NO new family): Hunt=cover-edge toward ThreatDirection (may creep forward within leash) / Defensive=cover-back hull-down + hold+return-fire / HoldPosition=inert. CONTRACT: upper layers command intent, L3 owns execution within a leash; L3 may DETOUR in transit but NEVER cancels an explicit order (stance-conditioned detour resumes the order); repositioning registers in the commitment ledger (PoiGoalGuard.Ledger pattern) to survive the 75-tick squad re-fire; a fresh explicit order aborts + re-arms L3. FORKS RESOLVED by user: (1) cohesion fix = GLOBAL bug fix + declared re-baseline; (2) human default autonomy = ON (per-type Ctrl-Alt-click defaults via UnitDefaultsManager, AutoTarget.cs:358-388); (3) bot fog policy = FULL migration now (Phase 4 rebuilds InfluenceMap+ThreatMapManager on per-player fog intel, absorbs ladder cycle 5 -> expect a bot-strength dip recovered by a follow-on recon/scouting cycle). PHASES 0->5 in order: 0 cohesion bug fix (bound ComputeBoxSlots footprint + regroup-on-arrival + remove stale debug log :679-695, ships to everyone incl. frozen controls, declared re-baseline) / 1 layers + overlay (pure data+render, overlay IS the verification tool) / 2 positioning executor (default-off) / 3 human enablement (default ON) / 4 bot consumption + full fog migration (declared re-baseline) / 5 extended micro (HP/threat flee AutoTarget.cs:472, bounding). DETERMINISM: LocalRandom NOT in sync hash (World.cs:543) -> SharedRandom / ActorID tie-breaks only; no RenderPlayer in sim. GOVERNANCE: shared L3 traits land default-off + priced before promotion; shipping-to-everyone is a deliberate declared re-baseline (dispersion episode = cautionary precedent). Two re-baselines ahead (Phase 0 + Phase 4). Also this turn: live-play triage (UNVERIFIED, verify pass running) cb9d54c7 Passenger NRE guard / d256efde no-dismount = "UnloadCargo" is an activity name not an order string (correct "Unload" @experimental-only) / d4dc39cd heli corner-idle = SR-corner staging via ProductionFromMapEdge fallback (fix deferred). Cycle-1 terr-bias VERDICT MISS (S2 swing -350 = baseline, engaged 7/10 unchanged, factor a near-pure damper), branch exp-terr-bias parked UNMERGED @ ccd12c98, retune-or-abandon queued behind the split plan. Machine-hold LIFTED (builds/tests allowed, serialized); autoburn active (8h grant). Spec: WORKSPACE/plans/260722_strategic_tactical_split_SPEC.md; survey input: WORKSPACE/plans/260722_stance_tactical_survey.md. Docs only this turn (no code / no build / no run).
2026-07-21 | 90a173c4 | NOTE | RETHINK #2 DECISION DOC (reports/260721_rethink2.md; read-only architecture review @ main 90a173c4, re-baseline was @ bfa8e876). TRIGGER: ~5 cycles elapsed + re-baseline shows Exp~=Stable on both rungs (S1 5-5/cap 6-6; S2 5-5/swing -350) -> incremental SR-multiplier tuning plateaued, next gain must be a NEW lever. VERDICT: next gain is STRUCTURAL and both the data and the user's North Star point at the same piece -- a TERRITORIAL / BALANCE-OF-POWER map layer. Loudest re-baseline signal (S2 engagement collapse ~5-6x, 3/10 zero-combat = passive economy race) is a BEHAVIOR gap: bots have no drive to press a weakening sector. RECOMMENDED CYCLE 1 = smallest behavior-bearing slice: a "push where enemy is comparatively weak / advance the front where safe" offense bias in PoiOffensiveBotModule that READS the EXISTING InfluenceMap (GetFriendlyInfluence/GetEnemyInfluence per perspective, InfluenceMap.cs:143-179) -> M effort not L (consumer of existing substrate: InfluenceMap/FrontlineOverlay/ThreatMapManager/PoiMap all present), @experimental-only default-frozen, ships with [exp-terr] telemetry markers. Targets S2 net-swing edge + S2 engagement floor (fixes the zero-combat stalemate at the bot level, not by re-scoping the map); S1 non-regression. RECOMMENDED 5-CYCLE SEQUENCE: (1) territorial bias + telemetry; (2) dispersion re-verify + DISABLE (it's in main+@stable, S2 causal credit NEGATIVE -$1500, predates evac/heli/TECN -> re-verify paired A/B on new regime then toggle CohesionSwitchEnabled:false on @experimental, flag @stable for corrective PROMOTE; do NOT retune a wrong-sign lever; unblocks cycle 3); (3) early-packet granularity (UnitsPerAxis 8->4-5 / MinAxisSize 3->2 / MaxAxes 4->5-6 @experimental, low-risk per-instance YAML, PoiOffenseTest-verifiable, North Star small-packets); (4) EXPAND Polar Disorder rung (anti-overfit gate on cycle 1 before deepening); (5) full fog-respecting territorial classification (safe/grayzone/enemy + own-half-safe prior + intel-driven, M-L, Woodland Warfare stands up here for the LOS/intel half). Q2 STRUCTURAL-vs-INCREMENTAL: recommendation IS structural (starts the North Star centerpiece), sliced for delivery; the one incremental cycle (packets) justified as a cheap complement. MISSION ABSTRACTION (the other structural option, fully costed plans/260720_mission_abstraction_costing.md step1 ~1-1.5d) DEFERRED: its decision rule not met (capture at parity 6/10, residual = production throughput not lost-TECN-no-retry) -> would buy retry/staging the current bars don't ask for while leaving the stuck S2 metric untouched; becomes the right vehicle when the territorial layer needs first-class retryable assault/garrison missions (cycle 6+). Q5 FOUNDATIONS: seeded LocalRandom determinism ALREADY LANDED (World.cs:217-228, verified) -> nothing to schedule; structured telemetry ships ALONGSIDE cycle 1 (behavior invisible in win/loss alone; capture-marker F-5-observability precedent). Q4 S2 SIGNAL: KEEP the quiet same-faction Stable regime + the blocking batch-validity gate (>=6/10 engaged), let cycle 1's engaged-count be the test of bot-passivity vs map-geometry; add a forced-contact/@rush variant ONLY if cycle 1 improves swing but fails to lift engagement (redesigning now would MASK the passivity cycle 1 is meant to cure). Q6 LADDER GROWTH: Polar Disorder AFTER cycle 1 shows signal (=cycle 4, anti-overfit gate before slice 2); Woodland Warfare held for cycle 5 (stresses the fog/LOS intel half). CAVEAT: TECN-ferry merge (90a173c4, @experimental-only) landed AFTER the re-baseline (bfa8e876) and is UNMEASURED on the new regime -> cycle 1's verify batch gives it its regime verdict for free; A/B the ferry toggle if cycle 1 reads worse than the re-baseline. No code changed / no build / no run.
2026-07-21 | 60b93501 | LADDER | REGIME RE-BASELINE COMPLETE (Motorized / same-faction US-US / vs @stable). 43 matches / 5 phases, hidden Mode-B (minimized+uncapped, 8x), 0 crashes / 0 no-verdict, ~78 min batch wall-clock; wall ~66-71s/match S1 (300s) and ~138-157s/match S2 (720s), ~4.3-5.2x realtime (init-dominated on short matches). PHASE 1 S1 CAL (Stable-v-Stable N=10): side lean 7-3 Russia-slot(80,35) -> mirror mandatory; BOTH slots now capture (USA-slot gross med 6113/6-10, Russia-slot 2976/~5-10) -- regime delta vs pre-regime Normal ~0-capture control. PHASE 2 S1 BASELINE (Exp-v-Stable N=10, 5+5): win 5-5, capture 6/10 vs 6/10 -> Exp HAS NO ECONOMY EDGE over Stable (Stable already carries the promoted TecnFloor+dispersion S1 rewards; S1 becomes a non-regression floor). PHASE 3 S1 FLOOR (Exp-v-Normal N=3): weak 2-1 (Exp cap 1/3, Normal 0/3) -- Motorized start equalizes early armies + single-spawn(USA) handicap compress the pre-regime 8-2/10-0 dominance; sanity PASS. PHASE 4 S2 CAL (Stable-v-Stable N=10): side EVEN 5-5 (swing 0/-725), BUT min-engagement PASS-but-FRAGILE -- engagement collapsed ~5-6x (vol med 1200/1925 vs pre-regime 7475/5950), 3/10 matches ZERO combat (seeds 3017/7017/10017), 5/10 negligible; same-faction frozen-v-frozen frequently stalemates into a passive economy race. PHASE 5 S2 BASELINE (Exp-v-Stable N=10, 5+5): win 5-5, median Exp swing -350 vs Stable 0 -> relative edge -350 (pre-regime +$1400 bar FAILS -1750), sign-delta 3/10, both-spawn 2/5+1/5 -> NO FORCE EDGE; Exp over-aggresses vs a competent defender (worst cells primary 6017/8017, Exp -4200/-4950 k/d 2-10/4-10). CORE FINDING: Experimental ~= Stable on BOTH rungs -- the SR-contestation axis (Exp's only delta from Stable, beat Normal +$6300 pre-regime) is neutral-to-negative vs Stable; the benchmark has caught up to the bot, next cycle needs a NEW lever. PROPOSED BARS (awaiting ratification, [pre-regime] bars void): S1 win-rate >=0.60 AND Exp capture >= Stable +2/10 (non-reg floor >=0.40 + capture parity); S2 median(Exp swing) >= median(Stable swing) + $1000 (offset gone -> $1400->$1000) + sign >=7/10 + both-spawn, PLUS a BLOCKING batch-validity gate >=6/10 engaged else re-scope S2 (forced-contact/@rush). OPEN USER DECISION: keep S2 on the quiet same-faction Stable regime or move to forced-contact/@rush for combat signal. HARNESS: patched parse-s1-batch.py / parse-s2-batch.py / parse-s2-bar.py for the regime (control auto-detected as the-other-bot = stable primary / normal floor; calibration slot disambiguated by player NAME USA-bot/Russia-bot not faction, since both sides america). Analysis: runs/260721_regime_rebaseline.md; LADDER re-baseline banner + REVIEW digest updated.
2026-07-21 | (regime) | HARNESS | BENCHMARK REGIME CHANGE (user-directed, consolidated instrument update; scenario/harness + docs only, NO bot behavior / controls / @stable YAML touched). THREE changes to what S1/S2 measure: (1) STARTING UNITS = MOTORIZED both sides -- StartingUnitsClass: motorized on each bot PlayerReference (was default none = SR-only); Motorized set world.yaml:404-419, faction-ASYMMETRIC (america abrams/bradley/humvee vs russia t90/bmp2/bmp2). (2) SAME FACTION both sides (US vs US) -- both bots Faction: america, so both field the identical @Motorized_america force; removes faction + Motorized-composition imbalance from the measurement (faction balancing is later work). (3) PRIMARY OPPONENT = @stable (frozen post-S1 PROMOTE snapshot) instead of @normal; the map.yaml Bot: line is authoritative (Matchup block is documentation, unread by engine/harness -- verified). Normal DEMOTED to one Exp-vs-Normal sanity-floor scenario. Files: S1 primary/mirror/cal-nn + S2 primary/mirror/cal-nn map.yaml (Faction+Bot+StartingUnitsClass, Titles, header regime notes) + their tournament-*.yaml Matchup docs; cal-nn scenarios flipped Normal-vs-Normal -> STABLE-vs-STABLE; NEW scenario tournament-s1-eco-floor-vs-normal (copy of S1 primary, P2=@normal) as the sanity floor. LADDER scenario tables + registry updated to the new regime; a top REGIME-CHANGE banner marks ALL prior S1/S2 baselines/bars [pre-regime] (none / mixed factions / vs Normal) -- not comparable, must not be reused as bars. Consequence: both mirrors are now pure SPAWN swaps (S2's no longer flips faction too). Pure-YAML/docs change (no compile) -> no build/NUnit needed. NEXT: re-CALIBRATE (Stable-vs-Stable side-fairness + S2 min-engagement) then re-BASELINE S1/S2 vs @stable on the Motorized regime; every bar awaits re-baseline. Batch runner: scenario names in LADDER §Scenario registry.
2026-07-20 | (srcontest) | MERGE | SR-CONTESTATION CYCLE 1 MERGED (exp-sr-contestation -> main, from 39821f93). Enemy Supply Route now wins an offensive axis: per-bot PoiOffensiveBotModule.SrPressureScoreMultiplier (x100, default 100 = INERT/frozen; guarded no-op so @stable + Normal/Rush/Turtle byte-identical) set 260 on @experimental ONLY, re-scaling the enemy-SR Pressure axis score after GetOffensiveTargets then re-sorting. 260 = (250*100)/(120*80) reproduces the plan's value=250/bias=100 uplift (addendum E supersedes the original world-level PoiMap edit — that trait is a singleton read by the frozen controls). DENY-ONLY preserved (SUPPLYROUTE has no CaptureManager -> AttackMove, never capture). Build green, NUnit 291/291. S1 NON-REGRESSION PASS (N=10 hidden Mode-B): capture 8/10, cond gross median $6,457, win 10-0 -- IDENTICAL to the seeded reference (same 2 america misses 2017/8017); the SR axis diverting combat units did NOT starve the TECN capture layer. S2 BAR PASS (N=10): median Exp swing +1125 >= Normal -5175 + $1,400 -> relative edge +$6,300 (margin +$4,900); sign-delta 8/10; both-spawn 4/5 + 4/5; min-engagement 6025 in-band; win 10-0 -- STRONGER than the dispersion-ON reference (edge +$4,850, median swing -200), lifting median Exp swing +$1,325 (Normal control stable) and rescuing the reference's worst cell (seed 3017 delta -$4,850 -> +$7,650). SR contestation is POSITIVE on the same S2 metric where dispersion's causal credit was NEGATIVE. SR Pressure axis proven live in-window: 8/10 both scenarios, first tick ~1600-2150 (mid-game, both spawns), 89-180 lines/match in S2 (sustained); 2 no-axis matches = the army-starved 2017/8017 cells. NOT promoted to @stable (separate user-accepted step, SPEC 13). Analysis: runs/260720_sr_contestation_cycle1_n10.md. NEXT: dispersion re-tune (S2 scores it negative) OR an SR-multiplier sweep (260 vs higher/lower, now that S2 gives SR contestation a positive causal score).
2026-07-20 | (s2meas) | LADDER | S2 FIRST MEASUREMENT: bar PASS + dispersion causal credit NEGATIVE. Experimental-vs-Normal N=10 (5 primary + 5 mirror, hidden Mode-B, all 18000t/time_limit, 0 crashes, v5) @ main 1594ffa1. PAIRED-RELATIVE BAR PASS: median Exp swing -200 >= median Normal -5050 + $1,400 -> relative edge +$4,850 (margin +$3,450); sign-delta 8/10 (Exp>Normal, bar >=7); both-spawn 5/5 primary + 3/5 mirror (>=3/5 each); min-engagement PASS (Exp eng median 5725 inside NN band 5950-7475 -> not winning by avoiding combat); win split 10-0. Watch cells: mirror seeds 1017(-500)/3017(-4850), both exp=russia (only negative deltas). DISPERSION ON/OFF A/B on identical seeds (the causal credit the dispersion VERIFY deferred): median paired delta ON-OFF = -$1,500 (positive only 5/10), median Exp swing ON -200 vs OFF +2325, engagement ON 5725 vs OFF 7125 (~20% less) -> cohesion does NOT improve combat economy, slightly DEGRADES it; win split ON 10-0 vs OFF 9-1, sole gain = flipping seed 8017 (+$7,300, loss->win) at high variance. Dispersion's payoff is decisiveness (S3 win-rate), not exchange efficiency (S2); advancement UNAFFECTED (S2 passes with dispersion ON, more strongly OFF). Toggle was @experimental-only + reverted (never committed); SHA drift A(1594ffa1)->B(777c51e8) is docs-only, A/B pairing valid. Added parse-s2-bar.py (paired-relative bar + A/B; parse-s2-batch.py untouched). Docs: runs/260720_s2_exp_vs_normal_n10.md + LADDER S2 row. NEXT (recommended): combat-focused dispersion RE-TUNE cycle (S2 now gives the doctrine a causal score to move, currently negative) OR the parked SR-contestation cycle for ladder breadth.
2026-07-20 | 67337e2 | NOTE | CAPTURE-THROUGHPUT DESIGN STUDY LANDED (plans/260720_capture_throughput_cycle.md, recon parallel to the S2 batch, researched @ e6d5627e). RECOMMENDATION: Option A then Option B, separate cycles. A (minimal, ~8 LOC LOW risk): run MaintainTecnFloor every scan instead of M-2-gated + TecnFloor: 2 on @experimental only — at @stable's promoted floor 1 the every-scan placement is provably BYTE-IDENTICAL (fires only when alive+pending<1 => M-2 already reached), so no new gate field and Stable stays frozen. B (structural, ~30-50 LOC MEDIUM): escort-bundled packaging via goal-guard ledger keys (escort:<targetId> commit/release in ReconcileGuardCommitments) — the ledger IS the package, no new shared trait; reservation-only MVV closes the filed escort-desync bug as a side effect; needs default-off ReserveEscorts bool. HONEST WATCH-CELL CALL: residual $0 seeds 2017/8017 are CONVERSION STALLS not availability gaps (runs already hold >=1 alive TECN at $0) -> A predicts PARTIAL flip + reliable america conditional-gross rise, and doubles as the diagnostic partitioning redundancy-flip vs queue-starved. NEW ROOT CAUSE FOUND (m7-class, 82 requests 0 produced): request dies at UnitBuilderBotModule.BuildUnit:155 — only starts production on a FREE queue; busy Infantry queue => popped request silently dropped (:90-91) = production starvation. Neither A nor B fixes it — a distinct capturer-queue-reservation cycle is the third candidate. STRUCTURAL FLAG: B touches escort/follow => re-triggers the parked mission-abstraction step-1 decision rule — queued for the upcoming RETHINK.
2026-07-20 | 3cbb489 | NOTE | SR-CONTESTATION PLAN RE-VERIFIED @ 1594ffa1 (read-only recon parallel to the S2 batch): IMPLEMENT-READY WITH AMENDMENTS. Scoring math fully reconfirmed (all constants unchanged; post-fix targets 17M mild / 42.5M safe / 4.25M hostile recompute exactly). LOAD-BEARING CATCH: the original plan put the tunables on the world-level PoiMap trait — a SINGLETON read by both @stable and @experimental, so implementing as written would have silently retuned the frozen benchmark control (violates the shared-trait-defaults rule, which postdates the plan). Fix: per-bot PoiOffensiveBotModule.SrPressureScoreMultiplier (default 100 = inert/frozen; 260 on @experimental only), reproducing the plan's (250*100)/(120*80) math; cost ~6 -> ~15-20 lines. Dispersion cohesion switch is action-agnostic (covers a Pressure axis automatically); deny-only invariant re-confirmed; TecnFloor orthogonal; mission-abstraction NOT triggered. Freshness addendum committed to plans/260720_sr_contestation_cycle1.md (3cbb4898); PoiMap-singleton constraint logged to DISCOVERIES. Implement dispatches when the S2 batch frees the run slot.
2026-07-20 | (s2cal) | HARNESS | S2 RUNG STOOD UP + CALIBRATE PASSED (River Zeta combat, plan 260720_s2_expand_design.md). Built tournament-s2-combat-river-zeta (+ -mirror bot-swap twin + -cal-nn), a byte-identical copy of the S1 River Zeta map (98x82, 12 OILB derricks) with tournament-combat-12min.yaml (720s clock, SpeedMultiplier 8); scorer/win-rule FROZEN identical to S1 -> NO S1 re-BASELINE. Metric = NET SWING (kills_cost - deaths_cost), read post-hoc. Reconciled the stale LADDER/SPEC S2/S3 rows flagged in DISCOVERIES: map refs -> River Zeta rung (66x34 stub RETIRED from the ladder), determinism-broken wording -> deterministic-since-2d3c8fe0 (LADDER Metric-extraction + SPEC 3.1/3.2). CALIBRATE Normal-vs-Normal N=10 hidden Mode-B @ 21510e05 (all 10 verdicts, all 18000t/time_limit, 0 crashes; the *_debug.log FileSystem-section exceptions are benign engine mod-probe noise). VERDICT: (1) MIN-ENGAGEMENT PASS -> GO for Normal (engagement-vol median 7475/5950, deaths_cost median 5725/4400, units dying both sides every match) -> @rush fallback NOT needed. (2) SIDE LEAN moderate russia/80,35 (win 7-3, score median 4525 vs 2400) -> mandatory 5+5 mirror + both-spawn symmetry in the bar. (3) BAR CAVEAT: both sides net-swing-NEGATIVE (attrition offset: deaths_cost counts all losses, kills_cost only enemy kills) so the absolute +$1,400 bar is biased against passing -> use the PAIRED-RELATIVE bar median(Exp swing) >= median(Normal swing) + $1,400 on the same seeds (cancels the offset) + >=7/10 sign-delta + both-spawn; flagged for ratification. (4) S3 watch: 7-3 lean is outside S3's 0.40-0.60 win-rate band. Wrote parse-s2-batch.py (combat aggregator, S1 tool untouched). Docs: runs/260720_s2_calibrate_nn.md + LADDER S2 row. NEXT: first Experimental-vs-Normal S2 batch (5 primary + 5 mirror).
2026-07-20 | 04622b7 | NOTE | CURATE pass #2 (trigger: 9 unpromoted entries + S1 arc closed): 6 promoted into reference — architecture.md gained a determinism section (one unseeded World.cs line was the whole benchmark-variance story; async pathfinding confirmed deterministic), a new AI-production section (UnitsToBuild weight = share CEILING never a priority; IBotRequestUnitProduction = the code-level floor seam), and a shared-trait-defaults rule (new fields default to frozen behavior so @stable/@controls can't be silently contaminated); conventions.md gained CVec.Length-is-Euclidean. 3 rejected: escort-ledger desync rerouted to bugs/discovered.md (live-reverified, line numbers refreshed), one run-specific stat, one already-resolved staleness note. All 9 verified against current source; no on-sight corrections needed. Commit 04622b7d (LADDER/SPEC deliberately left to the concurrent S2 standup worker).
2026-07-20 | (promote) | MERGE | PROMOTE: Stable <- Experimental post-S1 snapshot (SPEC §13, zero run cost). Copied the two @experimental-vs-@stable config deltas down onto the @stable blocks in ai.yaml: (1) CaptureCoordinatorBotModule TecnFloor: 1 (cycle-2 availability floor, c6a71c14, capture 4/10->8/10) and (2) PoiOffensiveBotModule dispersion doctrine (CohesionSwitchEnabled true / AssaultRadiusCells 15 / ApproachCohesion Spread / AssaultCohesion Tight; 56a57349, S1 non-regression win 9/10). Systematic field-by-field diff of all 8 @experimental/@stable module pairs confirms these were the ONLY two deltas — PoiGarrison, AdaptiveProduction x2, LayeredDefence, SquadManager x2 already field-identical. The two SHARED single-instance modules PoiGoalGuard@poi + MountedTransportBotModule@poi (gated experimental||stable, would throw if twinned) left untouched. Normal/Rush/Turtle byte-untouched (git diff = @stable blocks only). Evidence = seeded reference baseline (Experimental 8/10 capture / 10-0 vs pre-cycles Stable-tier 4/10 / 8-2, runs/260720_seeded_baseline_n10.md); §13 user-acceptance AUQ default-promote. Build green + NUnit 291/291. Stable is now a known-good post-dispersion+floor bot while Experimental keeps moving. NEXT: S2 combat rung standup + CALIBRATE.
2026-07-20 | 5aae65f | LADDER | SEEDED REFERENCE BASELINE (N=10) PASS — S1 bar RE-CONFIRMED on the first truly reproducible sample: in-window capture 8/10 (bar >=6/10) · conditional gross median $6,457 (bar >=$5,000) · win 10-0, all 10 verdicts v5 with seed stamped, git-clean @ e5a1c967. PER-SEED REFERENCE TABLE now the paired-comparison anchor for every future S1 cycle: mirror/russia 5/5 (seeds 1017..9017), primary/america 3/5 — misses 2017 + 8017, BOTH the cycle-2 residual (america floor-goes-quiet after first capture, M-2-gate throughput); those two seeds are the watch cells for any floor-extension cycle. PROMOTE PREP (§13, read-only): promotion = config copy-down @experimental -> @stable (minus shared singletons PoiGoalGuard/MountedTransport) + user acceptance; NO Experimental-vs-Stable head-to-head batch is required by SPEC — and no such scenario exists (building one + N=10 ~30-40 min if ever wanted). Ladder evidence read as sufficient: Stable's config measured 4/10 / 8-2 pre-cycles, Experimental now 8/10 / 10-0 on the same rung. Worker also fixed run-tournament.sh's stale non-determinism header comment. Doc: runs/260720_seeded_baseline_n10.md (commit 5aae65f8). NEXT: PROMOTE (assumption AUQ posted, default = promote by copy-down now) -> S2 standup + CALIBRATE.
2026-07-20 | 51a8afa | LADDER | S2 EXPAND DESIGN LANDED (plans/260720_s2_expand_design.md, recon parallel to the seeded BASELINE batch). S2 = the COMBAT-QUALITY rung S1 can't provide: primary metric NET SWING (kills_cost - deaths_cost), ALREADY emitted by BotVsBotMatchWatcher (:352-353) -> read post-hoc, NO scorer change, NO S1 re-BASELINE. Scenario: KEEP River Zeta (same map/12 derricks as S1, new 720s combat clock) — the draft LADDER's 66x34 combat stub REJECTED (zero capturables, fragments the rung; that LADDER/SPEC row also still says "determinism broken", stale since 2d3c8fe0 — logged to DISCOVERIES for the implementing cycle to reconcile). Opponent: Normal primary with Rush fallback if CALIBRATE shows Normal underfights (min-engagement check = the go/no-go). Bar TO RATIFY: median net swing >= +$1,400 (one IFV), N=10 paired seeds, >=7/10 sign robustness + both-spawn symmetry — exploits the just-shipped determinism. 4 optional additive observer stats costed (army-value integral ~25 LOC, losses-on-approach ~50, mid-control ~35, spacing ~30), all deferred from first cut; win rule FROZEN. STRUCTURAL: S2 is the measurement substrate for the operations/missions layer (BUILDUP->PROBE->OFFENSIVE shows as army-value-integral shape change); don't gate S2 on it, gate its CREDIT on S2. DETERMINISM DIVIDEND: clean dispersion on/off A/B on identical seeds = the causal credit dispersion's verify had to defer. Assumption AUQ posted (default = adopt recommended package); implement dispatches after BASELINE + PROMOTE routing.
2026-07-20 | 2d3c8fe | HARNESS | FULL REPLAY DETERMINISM SHIPPED (2d3c8fe0 engine + f3a61d9d docs, on main). LocalRandom now seeded from the lobby RandomSeed via a decorrelating PCG transform, guarded RandomSeed != 0 so normal gameplay still varies per launch; verdict stamps the authoritative "seed", verdict_version 4->5 (additive — both parsers read via .get(), no change needed). VERIFIED: same seed twice -> BYTE-IDENTICAL verdict incl. the 60-interval tick score log (async pathfinding leaked nothing); different-seed negative control -> 25 differing fields, different winner. NUnit 291/291. SIDE-FINDING that reframes the statistics: in-window capture is a NEAR-BINARY PER-SEED outcome (seed 1017 -> 0 captures both sides; 9017 -> exp gross $10,917) — the 4/10-vs-9/10 wobble IS seed draw, now reproducible. CONSEQUENCE: run-tournament.sh already passes fixed per-index seeds, so all future N=10s use the SAME seed set -> cross-cycle comparisons become PAIRED (same worlds, big power win); caveat to watch: don't overfit behaviors to the fixed seed set (rotate/expand seeds at BASELINE if a behavior only wins on the standard 10). Self-tuning on-ramp is now open. Next per assumed route: seeded N=10 BASELINE on merged main (S1 confirm + PROMOTE evidence).
2026-07-20 | c6a71c1 | MERGE | CYCLE 2 RESULT: bar PASSED -> exp-tecn-floor MERGED to main @ c6a71c14 (branch 6f01ed6c on a4257d54; clean 3-way merge over the dispersion advance, NUnit 287/287 branch, merged-main build green). TECN AVAILABILITY FLOOR: default-off TecnFloor field on CaptureCoordinatorBotModule — at the M-2 no-idle-capturers branch, when alive+pending < floor AND a derrick is still capturable, request one capturer via the shared UnitBuilder's IBotRequestUnitProduction queue (that path is popped first each build cycle and BYPASSES the UnitsToBuild share-ceiling AND UnitLimits, out-competing the blind lottery that starved the pool). TecnFloor: 1 set ONLY on @experimental.tecn (@stable.tecn default 0 -> byte-identical; controls use CaptureManagerBotModule, untouched). N=5 gate 4/5 -> N=10 (5 primary + 5 mirror): in-window capture 4/10 -> 8/10 (bar >=6/10 PASSED) · conditional gross median $7,726 (bar >=$5000 PASSED) · win split 8-2 -> 10-0 (no collapse — captures feed the gross-income scorer axis). CAUSAL PROOF (not confounded like dispersion, which doesn't touch the pipeline): tecn-floor-request fired 298x with faction-correct type resolve, M-2 total-tecns=0 scan share 88% -> 76%, matches fielding ZERO TECNs all-match 5/10 -> 0/10. RESIDUAL 2/10 misses are upstream production throughput, not floor logic: m2 america fires the floor once then goes quiet (M-2-only gate + a retained idle lottery-TECN means M-2 stops being reached); m7 russia requested 82x but never converted (queue contention/walk latency). NEXT: extend floor past the M-2 gate (check alive+pending<floor every scan) + escort-bundled reinforcement packaging (roadmap item 3, reuses this request plumbing). Analysis: runs/260720_tecn_floor_cycle2_n10.md; DISCOVERIES: live-verified IBotRequestUnitProduction floor seam + M-2-gate gotcha.
2026-07-20 | aa0dc7e | HARNESS | SEEDED-DETERMINISM RECON LANDED (plans/260720_seeded_determinism.md) — promoted from backlog because two N=10s of the same TECN pipeline scored 4/10 vs 9/10 capture (variance eats the bar's power). ROOT CAUSE, single line: World.cs:214 builds LocalRandom UNSEEDED (falls back to Environment.TickCount wall clock); ~40 bot-decision sites read it (scan countdowns, call-in picks, squad splits, rally cells) -> bots differ every launch. KEY SURPRISE: the deterministic seed is ALREADY PLUMBED end-to-end — run-tournament.sh already passes Test.RandomSeed per match -> GlobalSettings.RandomSeed -> seeds SharedRandom (combat inaccuracy/burst rides it, already deterministic). ONLY LocalRandom is missed. FIX ~5 LOC total: seed LocalRandom from the lobby seed via a decorrelating transform (World.cs:214, ~2 LOC) + actually write the "seed" field the verdict header claims but never emits (BotVsBotMatchWatcher, ~3 LOC, verdict_version->5). No env-var/YAML work needed; OPENRA_SEED rejected as a redundant second path. Validation: same-seed-twice -> byte-identical verdict + different-seed negative control. Risks: wall-clock watchdog kills, async pathfinding divergence (first suspect if same-seed still diverges). STRUCTURAL PAYOFF: (params, seed) becomes a pure cacheable function -> parameter-sweep harness = the self-tuning on-ramp. Queued as a harness cycle right after cycle-2 concludes (build forbidden while its batch holds the run slot).
2026-07-20 | 56a5734 | MERGE | DISPERSION VERIFY PASSED both bars -> exp-dispersion MERGED to main @ 56a57349 (branch verify ran at ad71ed54; NUnit 287->291 with 4 new PoiOffenseTest). ACTIVATION clean: 130 [exp-offense] orders carry cohesion= (52 Spread / 78 Tight), AssaultRadiusCells=15 gate fired with 0 violations (every Spread at dist 16-61, every Tight at 3-15). NON-REGRESSION exceeded: win 9/10 (baseline 8-2) · in-window capture 9/10 (baseline 4/10) · conditional gross median $11,191 (baseline $6,047). HONEST CAVEAT (in runs doc + LADDER): the 4/10->9/10 capture jump is CONFOUNDED with cycle-1's merge (TTL 600 + instrumentation) and unseeded variance — NOT credited to dispersion (it doesn't touch the TECN pipeline); S1 shows non-regression only, dispersion's real combat signal awaits an S2-class rung. CohesionSwitchEnabled true ONLY on @experimental — controls untouched. Analysis: runs/260720_dispersion_verify_n10.md. NOTE: the 9/10 capture rate makes cycle-2's TecnFloor N=10 (running now) doubly interesting — either cycle-1's TTL fix helped more than its own N=10 showed, or run variance is larger than assumed.
2026-07-20 | 6ac51f4 | AI | CYCLE 2 RECON LANDED (TECN availability) — ROOT CAUSE FOUND: the tecn call-in weight 500 is a NO-OP for priority. UnitBuilderBotModule.ChooseUnitToBuild shuffles and takes the first entry under a share CEILING (weight/100 = 500%, never binds) -> TECN is merely "always eligible", rolled ~uniformly ~1/11 per infantry opportunity, one-at-a-time queue gate -> tiny-count variance explains 5/10 zero-TECN matches exactly. ConsumedByCapture drains on success and CaptureCoordinator NEVER calls RequestUnitProduction — zero demand feedback. No YAML fix exists (module has ceilings/delays only, no floor). ALSO: the TECN comes from the SHARED UnitBuilder@normal (enable-ai-player, granted to normal+experimental+stable) — retuning ai-*.yaml weights would contaminate the frozen Normal control; ruled out. PLAN (plans/260720_tecn_availability_cycle2.md): default-off TecnFloor on CaptureCoordinatorBotModule, lazy faction-TECN + IBotRequestUnitProduction resolution, re-request while alive+pending < floor; TecnFloor: 1 set ONLY on @experimental.tecn (~25-40 LOC + 1 YAML line; request path bypasses share test AND UnitLimits). Structural step toward reinforcement packaging: demand-gate the request at the M-2 branch (produce TECN only when a capture target exists). Implement dispatches when the run slot frees (dispersion verify batch running).
2026-07-20 | 4dc3939 | MERGE | CYCLE 1 RESULT: bar FAILED, diagnosis DECISIVE, branch merged anyway as behavior-neutral + observability. N=5 gate 3/5 passed -> N=10: capture 4/10 (bar >=6/10 FAILED, unchanged from baseline) · conditional gross median $6,377 (passed) · win 8-2 (unchanged). MARKER VERDICT (F-5 closed): pooled over 994 no-idle scans, 88% had total-tecns=0; 5/10 matches fielded ZERO TECNs the whole match; tecn-killed fired only twice, neither on a committed capturer; the 6 capture orders that did fire were prompt (t680-1477). BINDING CONSTRAINT = TECN PRODUCTION/AVAILABILITY (call-in cadence + ConsumedByCapture pool drain vs the tecn:3 ceiling-not-floor), NOT survival/coordinator logic. F-2 minor (2 expiries, 25-cell outliers); escort desync not observed in capture context. RETHINK RULE APPLIED (decision doc 06): failures are supply-shaped not coordinator-shaped -> CaptureMission DEFERRED (trigger now: coordinator-shaped failures or S2); CYCLE 2 = TECN availability (assumption AUQ posted, default running). Merged 4dc3939d (TTL 600 measured neutral; M-1/M-2/M-3 + per-match debug.log now on main), LADDER updated, main @ 0c0f2d11. Analysis: runs/260720_capture_reliability_cycle1_n10.md.
2026-07-20 | e0ca877 | NOTE | RETHINK PREP: mission-abstraction FULL COSTING landed (WORKSPACE/plans/260720_mission_abstraction_costing.md). Problem cost from code: NO retry/abort lifecycle anywhere (re-attempts are accidental scan-loop side effects); ~90% duplication offense<->garrison (504+445 LOC, line-referenced); NEW FIND — ESCORT DESYNC: capture escorts are dispatched but never committed to the goal-guard ledger, so the offense module re-steals them ~100 ticks later (direct F-4 evidence, feeds cycle-2 routing). Design: one typed Mission class + MissionBoard layered ON the shared ledger (never forks @poi); step 1 = CaptureMission only, kill-switch-gated, ~1-1.5 days; step 2 offense/garrison unification deferred to S2. DECISION RULE: build step 1 next capture cycle IF cycle-1 patches miss 6/10 AND markers show lost-TECN-no-retry or escort desync; else defer. RETHINK checkpoint will apply this rule to the N=10 result now running.
2026-07-20 | 741fdd0 | NOTE | CURATE pass: 27 DISCOVERIES entries verified against code — 8 promoted into DOCS/reference (conventions/architecture/game-model/supply-route), 19 rejected. On-sight doc fix: supply-route.md + game-model.md wrongly claimed the SR is capturable — it is NOT (no Capturable/CaptureManager on SUPPLYROUTE; neutral-on-defeat only). Deny-only invariant for SR-contestation confirmed structural; "SR capture wiring" backlog item confirmed as engine work.
2026-07-20 | e5b5421 | AI | CYCLE 1 RECON LANDED (capture-reliability) + IMPLEMENT DISPATCHED. Plan WORKSPACE/plans/260720_capture_reliability_cycle1.md traces 5 failure paths in the lone-TECN flow: F-1 TECN killed + slow replacement (call-in + edge walk 300-600+ ticks; ConsumedByCapture depletes pool even on success); F-2 DefaultCommitmentTicks 300 < the ~330-tick 8-cell walk -> commitment thrash; F-3 75-tick scan dead zone after TECN loss; F-4 escort attacks the derrick instead of screening the TECN (200 HP, dies in 1-2 hits); F-5 failure points unobservable (debug.log overwritten per match). PRIMARY FIX (implementing now, ~1.5h): ai.yaml DefaultCommitmentTicks 300->600 (also applies to Stable via shared @poi guard — acceptable per SPEC) + INotifyKilled on CaptureCoordinator resetting the scan countdown + M-1/M-2/M-3 log markers + per-match log preservation. Dual-TECN dispatch needs NO code (coordinator already assigns per idle TECN — availability is the constraint); escort rework deferred to cycle 2 pending F-4 marker evidence. STRUCTURAL OPTION costed per doctrine: CaptureTask mission abstraction judged PREMATURE this cycle (3 small patches close the S1 gap; revisit at the RETHINK checkpoint / Phase-3 offense unification — flagged as post-S1 design goal). Validation: N=5 diagnostic (>=3/5) gate before full N=10 vs the reliability bar.
2026-07-20 | 4344150 | NOTE | AIM-HIGH MANDATE (user) + LOOP DOCTRINE CODIFIED. User directive: explore radical structural changes, not just botmodule patching — every RECON now includes a structural-option section; RETHINK checkpoints every ~5 cycles (or 2 failed bars) where radical options get costed for real. Codified the full operating routine in DOCTRINE.md (9 action types: RECON/IMPLEMENT+VERIFY/BASELINE/CALIBRATE/RETHINK/CURATE/PROMOTE/EXPAND/REPORT, with triggers + cadence rules). Full system report for the user: reports/260720_system_report.md (mission, bot stack, benchmark substrate, orchestration loop, baseline numbers, structural roadmap: mission abstraction > operations/phase layer > reinforcement packaging > strategic blackboard > self-tuning > telemetry diagnosis > ladder growth). Host memory incident froze 2 workers mid-turn (both resumed); throttle adopted: max 2 concurrent workers, build and game-batch never overlap.
2026-07-20 | e51e1c3 | AI | DISPERSION COHESION IMPLEMENTED (spread-to-move, mass-to-assault) — PARKED on branch exp-dispersion, NOT merged, awaiting its own verify cycle (cycle order: capture-reliability first per baseline routing). CommitAndOrder() gates per-unit SetCohesion by axis-centroid Chebyshev distance to target: >AssaultRadiusCells(15) -> Spread (~12x9 formation), <= -> Tight (~4x3 assault cluster); re-issued only on mode change, mode change forces repath. CohesionSwitchEnabled kill-switch default OFF, flipped on ONLY for @experimental (deviation from plan, correct: code defaults would have silently contaminated the frozen @stable control). Caught plan bug: CVec.Length is Euclidean, not Chebyshev -> new unit-tested PoiOffenseMath helpers. clumpRadius+distToTarget telemetry on [exp-offense] order line (computed for all bots, cohesion= token only when active). Build green, NUnit 287->291. Design recon that fed it: WORKSPACE/plans/260720_dispersion_cycle_design.md (ee0e05b7) + architecture.md CohesionMoveModifier correction (cee85888 — the doc's old description was wrong).
2026-07-20 | 4344150 | MERGE | ai-bench -> main (S1 baseline daea12a9 + calibration scenario + parse-s1-batch.py); ai-bench worktree ff'd to main. RELIABILITY BAR provisionally ADOPTED (AUQ posted, default=adopt): capture >=6/10 AND conditional gross median >=$5000, collapsing to plain gross median >=$3000 once capture >50%. ROUTING: behavior cycle 1 = CAPTURE-RELIABILITY (recon worker dispatched: failure trace of the lone-TECN flow, minimal mechanism candidates reissue-on-loss/backup-TECN/escort, structural mission-abstraction option, capture-attempt log markers); SR-contestation plan (WORKSPACE/plans/260720_sr_contestation_cycle1.md) parked implement-ready.
2026-07-20 | f8052ec | HARNESS | S1 STATISTICAL BASELINE (2x N=10 hidden Mode-B, sequential). Batch 1 Experimental-vs-Normal (5 primary + 5 mirror via --mirror): 10/10 verdicts v4, all time_limit@7500t, no crash. In-window CAPTURE RATE 4/10; gross-when-captured {5882,6020,6074,11355} median 6047 (11355=2 derricks); gross MEDIAN 0 (6/10 zero -> median in zero mass), symmetric 2/5 per side. Normal captured 0/10. WIN SPLIT experimental 8 / normal 2, SYMMETRIC by spawn (4/5 america + 4/5 russia) -> real skill, mirror cancelled spawn luck. Batch 2 Normal-vs-Normal calibration (new scenario tournament-s1-eco-cal-nn, both bots @normal; identical bots make mirror a no-op so N=10 on one scenario is the correct side-fairness probe): win split russia(80,35)=6 america(14,45)=4 (within noise), score median america 2875 vs russia 3675 (~28% mild russia lean); Normal captured once (incidental ~1/20). SIDE-FAIRNESS: mostly fair, mild russia/80,35 lean, neutralised by the mandatory mirror. BAR VERDICT: median>=control*1.15 DEGENERATE (0>=0) because control~0 -> recommend absolute reliability bar (capture rate>=6/10 AND conditional gross>=$5000); flagged, NOT applied to LADDER. Capture<6/10 -> characterized failures via persistent per-match watcher.log: failing curves FLAT (no income ramp) = lone TECN never holds a derrick to term; pipeline fires fine (2 diag N=1 both captured 11236/6131, 1-3 issues, no retire). Root cause = single-TECN availability/survival VARIANCE (reliability), not scoring/map. NEXT CYCLE = capture-reliability, not SR-contestation. Added tools/autotest/parse-s1-batch.py. Deliverable runs/260720_s1_baseline_n10.md; cards runs/260720_0216__tournament-s1-eco-river-zeta__f8052ec.json + __tournament-s1-eco-cal-nn__f8052ec.json. NOT merged (measurement only).
2026-07-20 | cd06358 | HARNESS | PART B — built tournament-s1-eco-river-zeta-mirror (S1 bias twin, SPEC 9.4). Byte-identical copy of tournament-s1-eco-river-zeta with ONLY the two bots' spawn assignments swapped in map.yaml Players (USA/14,45 america -> Bot: normal; Russia/80,35 russia -> Bot: experimental); terrain/spawns/factions/12 OILB derricks/tournament-eco-5min.yaml all git-verified byte-identical (map.bin + shadows.bin cmp-identical). This is a BOT-assignment swap, not the faction swap the S2/S3 combat-stub mirror uses — S1's bias is derrick DISTANCE per spawn, so each bot must play each spawn once; averaging primary+mirror cancels spawn-side derrick-distance luck. NON-OBVIOUS: tournament.yaml's Matchup{P1Bot,P2Bot} is informational only (loaded into config.Matchup, never read by the engine) — real assignment is the Bot: key on each map.yaml PlayerReference (DISCOVERIES 2026-07-20). Hidden N=1 smoke: boots + full 7500t, verdict_version 4, no crash. Cycle card: runs/260720_0117__tournament-s1-eco-river-zeta-mirror__cd06358.json.
2026-07-20 | cd06358 | HARNESS | PART A — repointed the SCORER economy term net -> gross (LADDER S1 follow-up 1a, user-approved). WeightedComponentMatchScorer.capture_income (which feeds TimeOrSrCaptureWinRule, i.e. match OUTCOMES) now reads the GROSS integral state.GrossCaptureIncomeFor (the same per-player value emitted as capture_income_gross) instead of net PlayerResources.Earned. Net Earned is blind to a held derrick whose gross income doesn't overcome upkeep in the SR-budget economy, so outcomes ignored captured income (same defect the S1 metric fixed at v3); the win rule now counts it. Emitted JSON fields UNCHANGED (capture_income_gross + resources_earned both stay); verdict_version bumped 3->4 to flag the changed MEANING of the emitted score_components.capture_income (bump on emitted-field-meaning change, not only add/remove). Done NOW because S2/S3 have no recorded baselines -> redefinition is free. Weighting math factored to pure WeightedComponentScoring.Compute + WeightedComponentScoringTest (5 cases, pins capture_income == gross x weight); NUnit 282->287, build green. 2 hidden N=1 smokes (primary + mirror) confirm capture_income == gross*2 in every player-case (gross 0 both draws — known in-window capture variance, orthogonal to this change; nonzero path pinned by the unit test). Committed cd06358a, NOT merged. Cycle card: runs/260720_0120__tournament-s1-eco-river-zeta__cd06358.json.
2026-07-20 | (rename) | NOTE | Terminology retire: v2 -> Experimental across all LIVE code/config/scenarios; added a frozen Stable bot. ai.yaml: ModularBot@v2->@experimental (name "Experimental AI"), enable-ai-v2->enable-ai-experimental, all module @v2 suffixes renamed; NEW ModularBot@stable ("Stable AI") + enable-ai-stable + byte-for-byte @stable copies of the 10 Experimental-gated modules (frozen snapshot; promotion policy in SPEC §13). Engine log markers [v2-*]->[exp-*] (capture/garrison/offense/transport/poi/layered-defence) + C# doc comments. Scenario dirs renamed tournament-v2-vs-normal-{,mirror-}2p / test-v2-poi-* / demo-v2-capture-coordinator -> *-experimental-*; every `Bot: v2`/`P1Bot: v2` -> experimental. Control AIs Normal/Rush/Turtle UNTOUCHED. Historical runs/ names left as written. verdict schema versions (BotVsBotMatchWatcher v2/v3) are unrelated and unchanged.
2026-07-19 | (n/a)   | NOTE | AUTOBURN STARTED — 8h grant from the user; THIS manager now runs the loop directly (supersedes the pending root-spawned dedicated manager; recommendation faabb3ec stays parked). Standing assumptions posted as AUQ cards (user overrides any time): hidden Mode-B batches fully authorized under the grant; baseline-before-behaviors cycle order; win-rule capture_income repointed to gross while S2/S3 are still unmeasured. Plan: (1) land in-flight v2->Experimental+Stable rename worker, regular-merge ai-bench<->main (branches diverged: realism doc f16db81e on main); (2) repoint win rule to gross; (3) S1 N=10 baseline + mirror scenario + Normal-vs-Normal calibration (SPEC 9.4); (4) behavior cycle 1 = enemy-SR contestation scoring (realism dossier #1), then dispersion cohesion (#2). Realism north star doc landed: DOCS/design/ai-realism.md (9 doctrine patterns ranked).
2026-07-19 | 2bb65d6 | HARNESS | S1 METRIC BLOCKER RESOLVED (harness-side, observer-only, no AI change, no re-roll). Added GrossIncomeIntegrator + new verdict stats field capture_income_gross (verdict_version 2->3): a read-only per-player accumulator that integrates PlayerResources.TotalBuildingIncome (gross building income, pre-upkeep) each tick in BotVsBotMatchWatcher. Side-effect-free — it only READS TotalBuildingIncome and writes to the watcher's own state dict, never mutating actor/player/trait/resource state; robust to mid-match ownership changes because CashTrickler re-registers under the new owner on capture, so the integral follows current ownership. ADDITIVE: every prior verdict field byte-compatible, resources_earned kept as net context, and NO scorer/win-rule input changed (capture_income score component still reads Earned -> S2/S3 outcomes untouched). One hidden Mode-B smoke: v2 capture_income_gross=6093, control=0 (matches ~$5,900 predicted from a $50/50-tick derrick held ~t1550->7500); resources_earned still 0/0 as documented. v2 also won this draw on combat (8650 vs 600) — independent RNG, NOT an effect of the observer change. Added GrossIncomeIntegratorTest (7 cases); unit suite 275->282, build green. LADDER S1 metric repointed to capture_income_gross + follow-up note that the win-rule economy term (still net Earned) deserves loop-manager review before moving to gross (would redefine S2/S3). Committed on ai-bench (2bb65d6c), NOT merged. Cycle card: runs/260719_2017__tournament-s1-eco-river-zeta__2bb65d6.json; finding: runs/260719_s1_gross_metric_verified.md.
2026-07-19 | 2d5433a | AI | FIRST AI-SIDE CHANGE of the benchmark era. Applied the recommended S1 fix: delisted the $0 logisticscenter from v2's two income tables (PoiMap.IncomeWeights world.yaml + CaptureCoordinatorBotModule@v2.tecn ai.yaml) + PITFALL comments; control/legacy configs, unit stats, LC traits, map, engine untouched. Build green, NUnit 275/275. Hidden N=1 smoke CONFIRMS THE FIX WORKS: v2's top capture target flipped logisticscenter@31,52 (score 104M) -> oilb@17,44 (score 45M, targets 15->13), v2 issued the capture (t1351), and CAPTURED + still OWNS the nearest derrick at match end (buildings_killed/dead=0 -> not destroyed; USA assets_value 1450 = army 1250 + 200 building; garrison held 0->1). BUT resources_earned is STILL 0 for a NEW, metric-side reason: it reports net PlayerResources.Earned, which in the SR-budget economy only rises on a net-positive periodic tick (PassiveIncome+TotalBuildingIncome-Upkeep, PlayerResources.cs:199-211) and never via the unused harvester path -> a lone $50 derrick nets 0 Earned (both bots read 0 before AND after the capture). This is a HARNESS/METRIC defect, not a v2 gap (CashTrickler re-registers on capture, bots are Playable -> both ruled out). Fix recorded as a bug against the approved POI plan (income POIs are cash-valued; a $0 depot outranking derricks violated it). NEXT CYCLE = metric fix (gross capture-income accumulator in BotVsBotMatchWatcher), no re-roll. Committed on ai-bench (2d5433aa), NOT merged. Cycle card: runs/260719_1934__tournament-s1-eco-river-zeta__2d5433a.json; finding: runs/260719_s1_earned_metric_finding.md.
2026-07-19 | 0eb71d49 | NOTE | S1 no-capture ROOT-CAUSED (static + surviving debug log; AI-code-side, no code changed). v2 DID build a TECN and DID issue a capture — but PoiMap ranked a Logistics Center @31,52 (value 200, score 104M) above every OILB, so the sole TECN was committed cross-map to it (t762) and died ~t1737 without ever taking the derrick 3 cells from its SR. The LC has NO CashTrickler (structures.yaml:345-403) → capturing it earns $0, yet PoiMap.IncomeWeights (world.yaml:303) + CaptureCoordinator@v2 (ai.yaml:126) weight it logisticscenter:200 (highest of all). resources_earned=0 by MIS-SCORING, not map/reachability/dead-pipeline — the earlier "capture layer didn't fire" note is corrected. Recommended one-line AI fix (loop's first cycle): delist logisticscenter from both IncomeWeights tables (a no-income depot must not be an income POI; PoiMap.cs:91-92 contract). Diagnosis: runs/260719_s1_no_capture_diagnosis.md. No harness/map/AI change this cycle.
2026-07-19 | 86aa2db | HARNESS | S1 rescope: new scenario tournament-s1-eco-river-zeta (River Zeta terrain 98x82 + all 12 neutral OILB derricks + 2 SR/spawn overlay; SRs at 14,45 v2 / 80,35 normal, each ~3-4 cells from a derrick). Fixes the diagnosis root cause — the old tournament-v2-vs-normal-2p stub had 0 capturable POIs so resources_earned was 0/0 by construction. Hidden N=1 smoke GREEN: map boots + runs full 7500t, no crash. BUT resources_earned still 0/0 for a NEW reason — neither bot captured a derrick in 5 game-min (score curve pure combat, capture_income 0 throughout). Normal=$0 is correct (no capture logic); v2=$0 is the live finding (capture layer didn't fire in-window). No clock bump (300s isn't the blocker). S1 is now a live/discriminating test. LADDER S1 repointed + false "runs on River Zeta" premise fixed. Cycle card: runs/260719_1844__tournament-s1-eco-river-zeta__86aa2db.json. Committed on ai-bench, NOT merged to main. Follow-ups: first AI cycle = make v2 capture; build S1 mirror + Normal-vs-Normal calibration.
2026-07-19 | 6d7c561 | HARNESS | Bootstrap smoke run GREEN in Mode B (hidden, OPENRA_WINDOW_HIDDEN=1) from the ai-bench worktree: 1 verdict/1, USA-bot(v2) beat Russia-bot(normal) on time_limit @7500t (~58s wall, ~5x). All 5 Windows-port items resolved — (a) clean single Windows-path passthrough (no /c/ double-mangle), (b) ResultPath/TournamentConfig read+written at cygpath paths, (c) image=dotnet.exe so the CIM kill-filter holds, (d) watchdog kill NOT exercised (match completed normally), (e) settings.yaml round-trips byte-identical (mute didn't leak). S1 baseline data point #1: resources_earned 0/0 (v2/normal), capture_income 0/0 — neither AI captured income in 5 min, so the S1 metric is null-vs-null on this match; flagged for scenario-design review. Cycle card: runs/260719_1816__tournament-v2-vs-normal-2p__6d7c561.json.
2026-07-19 | (pending) | HARNESS | Harness is Windows-native as of 4dec6a74 (cygpath arg conversion, PowerShell CIM process-kill, %APPDATA%\OpenRA settings resolution, CRLF hardening). SPEC §3 updated; 5 portability items flagged for confirmation in the §3.3 bootstrap smoke run.
2026-07-19 | (pending) | HARNESS | Amendment: hidden-window substrate RESOLVED. OPENRA_WINDOW_HIDDEN=1 landed+verified (d716eade/fda8370c) -> Mode B active from bootstrap (unlimited unsupervised runs); Mode A now fallback-only. Replaced the impossible same-seed identity gate (per-seed replay is broken: bots use unseeded LocalRandom) with the structural sim/render-decoupling guarantee; seeds are run labels, N-run stats unaffected. Updated SPEC §3 + LADDER seeds wording.
2026-07-19 | 06afb643 | NOTE | System bootstrapped: SPEC/LADDER/REVIEW/README authored under WORKSPACE/ai-bench/. Loop not yet started.
```
