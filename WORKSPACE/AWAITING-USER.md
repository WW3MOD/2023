# AWAITING-USER — decisions and reviews parked on the user

> **Purpose:** the single place where everything **dependent on the user's decision, review, or grant** is parked so nothing is lost or forgotten. The manager adds items (dated, with context + exactly what is needed); the user resolves them in chat or by editing this file. Resolved items move to the bottom with the decision recorded. Complements `PIPELINE.md` (the work queue) — this file is the *user-gate* queue.

> **Reconciled 2026-08-11 against `main` @ `54ea7210`.** Every OPEN item below was re-checked against the current tree. **Nothing could be proven resolved, so nothing moved to RESOLVED** — see the per-item verification notes. Five items were added.

## OPEN — needs a user decision

### New 2026-08-12 — from the nine archived manager close-outs

> Nine manager sessions were archived on 2026-08-12; their reports are in [`WORKSPACE/closeout/`](closeout/) (index: [`closeout/README.md`](closeout/README.md)), committed at `9d11d72f`. **Each validated its own claims against `main` @ `35876332`.** Everything below is a question a manager actually put to the user and never got an answer to. The corresponding work is queued as PIPELINE items 42–54.
>
> **Two of the nine had no unanswered questions at all** and are noted here so nobody goes looking: [`multi-d1c4aecd`](closeout/multi-d1c4aecd.md) §3 (its one question — NAT scope — **was** answered: *"Diagnostics + flip the UPnP default"*, which is what shipped) and [`e929eb99`](closeout/e929eb99.md) §3 (never asked the user anything; never ran any work).
>
> **Questions that were PROCEEDED PAST are NOT in this section** — they are records, not gates, and live under [RECORDS](#records--proceeded-past-do-not-re-ask) below. Do not promote them back up here.

#### AUTOTEST / LIVE-RUN GRANTS — grouped, because one grant unlocks several items

_The repo's **no-autonomous-multi-test** rule puts every one of these in the user's hands. They are listed together deliberately: **at least two reports name a specific run they could not perform**, and a single scoped grant covers the batch._

- **`test-autotarget-preempt-air` — the RED+GREEN pair** *([`fixes-cfcaa2ca`](closeout/fixes-cfcaa2ca.md) §3, §1)* — an autotest run grant was posted early in that track, **never answered, and the merge proceeded without it. That is the direct cause of PIPELINE item 44(b) being open**: everything else on the branch was closed and the merge was held on this alone until the lead landed it. **Needed: two runs.** (1) RED control — pin `PreemptScanInterval: 0`, must FAIL. (2) GREEN confirm — shipped default, must PASS. **A lone GREEN proves nothing** (the persistent-target lock breaks on its own via reload-driven decay of `opportunityTargetIsPersistentTarget`); that branch produced **three separate false-greens** during development. **If unanswered:** a merged fix (`68b627ce`) keeps claiming a behaviour nothing has ever observed, and further work builds on it unverified.
- **Missile Phases 3–5 — one goahead runs the whole repro batch with tracing on** *([`missiles-e2475f8d`](closeout/missiles-e2475f8d.md) §1)* — **the batching is deliberate**: autotest runs are user-gated and the harness is single-instance, so all non-gated work is front-loaded. **Note this grant is not what unblocks the programme** — Phase 0 (instrumentation) and Phase 1 (four read-only auditors) need **no** run and can start the moment the three design questions below are answered.
- **`@stable` benchmark re-baseline ladder** — multi-test grant. **Already open in this file** (see "the debt is now bigger" under *New 2026-08-11*, and *Post-merge benchmark goahead* under *Balance / measurement*). **What changed on 2026-08-12: four separate close-outs raise it independently and none of their counts agree** — see PIPELINE item 43, which records the disagreement rather than picking a number. **Sequencing per [`bdedd544`](closeout/bdedd544.md) §1a is unchanged: take it ONCE and LAST, after the danger-unit question.** One recon step comes first and needs no grant: the `ai-bench` worktree was **638 commits behind `main`**, so settle whether the ladder runs from `main` or from that worktree before spending an hour of matches on stale code.
- **Eight failing autotests — triage pass** — **already open in this file** (*New 2026-08-11*). Listed here only so the grant group is complete; no new claim.
- **A live play window — not an autotest grant, the user actually playing** *([`art-6cde8456`](closeout/art-6cde8456.md) §1, [`6361c2be`](closeout/6361c2be.md) §4)* — PIPELINE item 49. **One launch** settles three unobserved claims (credits screen opens; a 64×48 cameo reads in the 62×46 slot; NVorbis decodes an `.ogg`), and one play session settles four more from a session with **zero on-screen verification across four merges** (Grad/TOS salvo, 27 smoke trails at `MovingInterval: 2`, a soldier rearming at a Logistics Centre, the turret lock at 50%). Also cheap to fold in: the **2-human game with the user hosting** that confirms the PIPELINE item 42 desync fix.

#### Missile system — three design questions that gate the whole programme

*([`missiles-e2475f8d`](closeout/missiles-e2475f8d.md) §3. Manager's own confidence in brackets, as stated.)* **If unanswered:** Phase 0 and Phase 1 cannot be dispatched, and PIPELINE item 45 stays exactly where it is. Nothing else in the programme moves.

1. **Audit scope** — every `Projectile: Missile` weapon **[85]** / just ATGM and AA **[45]** / everything that flies and guides **[38]**.
2. **Where should a missing missile detonate?** — at the point of closest approach **[72]** / fly on and detonate on terrain or at fuel-out, but **never be silently removed** **[66]** / a rule per weapon class **[58]**. *(The user has **already ruled** that AA missiles are exempt and may fly on until fuel-out — this question is only about **ground** missiles.)*
3. **Close-range AA at 2–4 cells** — the missiles should hit, treat as a bug **[80]** / **both**: arm faster AND make launchers refuse hopeless shots **[68]** / the launcher should hold fire below a floor **[47]**. *(Load-bearing: the two live hypotheses — arming delay vs fire control taking a shot it cannot make — **need opposite fixes**. Do not assume which.)*

#### AA stand-down — three questions, all on track #2

*([`f5b7d1a6`](closeout/f5b7d1a6.md) §3. PIPELINE item 44(a).)* **Distinction worth preserving: two of these were partially engaged, not ignored** — the user answered *"explain plainly"*, the agent re-explained in plain language, and **no pick was made**.

1. **Which aircraft type was involved** (drone / helicopter / fixed-wing). **Never answered.**
2. **Which cap to apply.** Answered *"explain plainly"*; re-explained; no pick. **Note the manager's own recommendation CHANGED mid-investigation** — from a total-accumulator cap to a **per-shooter clamp** — because the cadence measurement showed ordinary firing does not re-apply the tally (lead shooter fired eight times at a strict 200-tick `BurstWait` and the other three AA still joined on schedule). So the option set the user was first shown is not the one the report now recommends.
3. **Whether the second silent filter** (ignore targets below 25% health) should get the same visibility marker. Same shape — *"explain plainly"*, no pick.

**The user's own framing when it was re-explained, and it is the most useful sentence in the whole item:** the deeper complaint is that **a unit refusing to shoot a live enemy tells the player nothing** — which is what cost him an evening suspecting trees. **If unanswered:** the report states a concrete next step (per-shooter clamp + `ValidTargets` fix + a render-only "holding fire" marker) that could proceed on the manager's recommendation. Nobody has.

#### Art direction and the release version string

*([`art-6cde8456`](closeout/art-6cde8456.md) §3. Both are one-line changes once decided; neither blocks anything else.)*

- **Splash / menu art direction** — full-bleed image behind the load screen, keep the live-map menu **[78]** / logo only, no code change anywhere **[62]** / static full-screen background on the main menu too, losing the shellmap **[34]**. `default_on_skip` was the **first**. **Context that makes the question real: there is no code path for a full-screen image anywhere in the mod** — `LogoStripeLoadScreen` draws only a 256×256 logo, a solid gray bar and text, and the main menu renders a **live playing map** (`River Zeta WW3`) with the stock background/logo widgets commented out. So a full-bleed splash is a **code/chrome change plus art**, not an asset drop. The agent's view: the live shellmap is a genuine asset that looks better in motion, so replacing it tends to read as *more* amateur. **The logo fix (PIPELINE item 46) is needed regardless of this answer.**
- **Release version string** — `0.9 / Beta` **[72]** / `1.0` **[66]** / defer and leave "Pre-Alpha" **[45]**. **The branding worker was explicitly told to leave both version strings untouched pending this. If unanswered:** `mod.yaml:3` stays `Version: release-20230225` (the *engine* version, and the string the launcher registers) and `MainMenuLogic.cs:277` stays hardcoded `"WW3MOD — Pre-Alpha"`.

#### Gameplay verdicts held rather than assumed

- ~~**FX audit items 10 and 12 — two one-line edits**~~ — **ANSWERED AND CLOSED 2026-08-19: DECLINED.** Asked directly whether the verdict pass had missed them or declined them, the user chose *"Declined — retire both from the queue"*. `VehicleCookoffTiny` keeps `Explosions: piff` / `ImpactSounds: gun27.aud` and `HandGrenade` keeps `explosion_medium`; **do not re-propose either without new art or a fresh instruction**, exactly as with item 17. The long-standing read that the silence was an oversight rather than a rejection was WRONG — recorded because that inference looked strong (17 had been declined explicitly and in the user's own words, while these drew no comment at all) and was still wrong. Retired from PIPELINE item 50.
- **Bail order — should passengers wait for the crew?** *([`6361c2be`](closeout/6361c2be.md) §3)* — passengers now bail out roughly **45 ticks before** the crew. The user asked for parity **believing the crew ejects instantly; it does not** — `VehicleCrew` waits for the hull to stop (`StopTimeout: 25`) then waits again (`PostStopDelay: 20`), while the new passenger path has neither, because "regardless if the vehicle is still moving" is incompatible with a stop-wait. The fix that needs **no `VehicleCrew.cs` edit** is `EmergencyBailDelay: 45` — verified still unresolved, `Cargo.cs:110` has `EmergencyBailDelay = 0` and **no YAML anywhere sets it**. **CAVEAT THAT MUST TRAVEL WITH THIS DECISION: `EmergencyBailDelay > 0` is the only uncovered code path on that branch, and the fix round *added* logic to it** (the re-check on arrival plus the `bailPending` interlock). **If the user sets 45, that is first-run-in-anger code.** There is a defensible **do-nothing**: infantry bail as fast as they physically can while the crew spends a moment trying to stop the vehicle.

#### Infrastructure decisions

- **Should WW3MOD keep advertising on `master.openra.net`?** *([`multi-d1c4aecd`](closeout/multi-d1c4aecd.md) §1; PIPELINE item 53)* — there is no `WebServices` override anywhere in `mods/ww3mod`, so a total conversion is currently listing itself on upstream's public infrastructure. **This is a decision, not a task**: both a dependency and a courtesy question. Pointing `WebServices.ServerList` / `ServerAdvertise` at our own master is a `mod.yaml` block, not code. **If unanswered:** status quo — it keeps advertising there.
- **Caveat to carry into the existing asset-licensing item above** *([`art-6cde8456`](closeout/art-6cde8456.md) §4)* — `WORKSPACE/ASSET-LICENSING.md`'s counts, reachability and breakage columns were **mechanically verified and are solid**, but its **origin column is inference** from filenames matched against Westwood naming conventions: **no file was listened to and no sprite was viewed.** `chem/`, `robot/` and `informan/` match no Westwood convention at all and are parked as "unknown, high-risk" **on suspicion alone — they may be entirely clean.** One cheap unexplored lead that would resolve much of the uncertainty: cross-check those files against other OpenRA community mods, where several likely circulate with a known origin and a stated licence. **This changes nothing about the ruling — ship as-is stands.**

### New 2026-08-11

- **Post-measurement decisions — five, with a stated order** *(2026-08-10, `6cddfed8`, `WORKSPACE/plan-260810-post-measurement.html`)* — The first live `[danger] reference` read (`084367b0`) settled that **all thirteen danger thresholds sit BELOW the median cell of the field they gate** (highest is 100 units = 3,412 against live medians of 818u and 2,755u), so this is a **scale error in the unit, not thirteen independent tuning errors**. The plan records that finding, the 68-test regression tally (60 pass / 8 fail, none traceable to the two engine merges), four harness/repo fixes already committed, and five open decisions. **Its binding constraint: the durability-weight question must be settled before any threshold is re-derived** — otherwise you re-derive twice off two unverified inputs. Needed: read the plan and pick the order.
- **`@stable` benchmark re-baseline — the debt is now bigger than the 08-09 note described** *(2026-08-11)* — supersedes nothing, but sharpens the "post-merge benchmark goahead" item below. **Four separate merges now declare a knowing `@stable` behaviour change in their own commit messages**: `5642d931` (danger cadence, unconditional for both profiles), `dd3430a8` (PathFinder's adjacent-cell shortcut stopped skipping the height-discontinuity rule), `175a4784` (`ReturnWhenEmpty` on `^Soldier` + unconditional mid-resupply withhold), `18838dd7` (firepower gated at Heavy not Critical, passenger evacuation, bot target-selection skipping paused armaments). This is per settled policy — `@stable` inherits improvements — but it means the old baseline is **non-comparable on four counts**, not one. Take it ONCE, last, and **after** the danger-unit question is settled. Multi-test grant required.
- **Where aircraft rearm and repair** *(carried from PIPELINE SESSION STATE, still unanswered)* — posted with four options. `HPAD`/`AFLD` carry an unsatisfiable *build* prerequisite and are pre-placed on zero of ten maps, while `logisticscenter` (a working rearm host) is named by every infantry template and ground vehicle — **aircraft alone name only the two impossible actors**. The agent's own highest-rated option is one no worker proposed: put rearm on the **Supply Route** itself. **Caveat for whoever implements the answer:** all three hosted arms of `AirframeReadiness` are structurally unreachable today, pinned by unit test and **never executed** — wiring any host makes them all go live in the same instant.
- **Asset licensing — acknowledge and park, or schedule the free tier?** *(2026-08-11, `17e3ce4c`)* — [`WORKSPACE/ASSET-LICENSING.md`](ASSET-LICENSING.md) inventories the **1,246** binary files this repo redistributes with per-item origin confidence and a removal plan. **It states its own answer: the decision to ship as-is has been taken, it is not a release blocker, and none of it should be executed now.** No decision is being requested. It is listed here only so the user knows the artifact exists and can say if they want the free tier scheduled anyway — steps 1–6 remove ~549 files (~44% of everything redistributed) at **zero gameplay cost and no replacement work**, the largest single piece being the 124 voice files in seven directories that no actor attaches. `credits.txt` has already been corrected (`54ea7210`) since it asserted the opposite. **Do not act on the removal plan without an explicit instruction.**
- **Eight failing autotests** *(2026-08-10, `6cddfed8`)* — of the 68-test tally, 8 fail and none are traceable to the two engine merges. One is already attributed: `test-offense-ammo-guard` is **pre-existing and its premise is stale** (`418e9c60`). The remaining seven have not been triaged. Needed: whether to spend a triage pass on them now or let them ride — triage means autotest runs, which is user-gated.

### Balance / measurement
- **Balance proposals 001–003 — per-proposal sign-off** *(2026-08-02, audit merged @ 660a0ee2)* — Audit complete (`WORKSPACE/balance/260802-parity-audit.md`): 3 CLEAR asymmetries, each with a proposal doc awaiting individual approval. **NO stat change applied without explicit approval.**
  - `001-tunguska-duplicate-health.md` — Tunguska has TWO `Health:` keys (14000 and 8000) in the same actor; delete the 8000 block.
  - `002-himars-iskander-parity.md` — Iskander strictly dominates HIMARS at equal cost 6000; Option A (recommended): Iskander → 8000.
  - `003-mi28-secondary-air.md` — Mi-28 advertised AA is non-functional (`secondary-air` armament undefined); define it, or remove refs + fix description.
  - Also flagged: 8 SUSPICIOUS stat ratios + 2 bot-config skews (A1: RU 7 attack helis vs US 4; A2: offense ceilings US 35 vs RU 45) that can masquerade as faction imbalance in bot-vs-bot data.
  - _Verified 2026-08-11 against `main` @ `54ea7210`: **none of the three has been applied.** Iskander is still `Cost: 6000` (`vehicles-russia.yaml`), and `secondary-air` is still referenced by `aircraft-russia.yaml:319/329/374` with no definition. Item stays OPEN, unchanged._
- **Cross-faction + RU-mirror test batches** *(2026-08-02)* — Configs AUTHORED and merged (`tools/autotest/scenarios/tournament-parity-{mirror-us,mirror-ru,cross-usru,cross-usru-swapped}`); **runs need a user grant**. Commands: `run-tournament.sh tournament-parity-mirror-us --seeds 20` / `…-mirror-ru --seeds 20` / `…-cross-usru --seeds 20 --mirror tournament-parity-cross-usru-swapped`. Metrics: mirror winner_name deviation from 50/50 = side bias; cross pair faction_winrate_pct. NOTE: run only AFTER the burn-lane merges settle, and bot-config skews A1/A2 mean cross results partly measure ai.yaml asymmetry, not just unit stats.
- **Item-31 aggressiveness sweep — grant + gate flip** *(2026-08-05, merged @ `af8bca1f`)* — The slider and the opportunistic advance are implemented, but the advance ships **OFF in `@experimental` too**, so nothing is measurable until you grant the sweep. Needed: (1) a multi-test grant, (2) agreement to flip `OpportunisticAdvanceEnabled: true` for the sweep arms only. **Spec:** seed-matched S1/S2 ladder over `Aggressiveness ∈ {20,35,50,65,80}` on the `@experimental` block, `@stable` untouched as control; read the win-rate/territory curve and pick the baseline. A flat curve is itself a result — it says the advance is not earning its cost. **Grid/slope correction (review round 1, FIX 3):** the knob shift is integer division truncating toward zero, so the first cut's slopes (depth 4 / force 6) left three of the five grid points identical to neutral on two of three dials — that sweep would have measured the danger ceiling alone. Slopes widened to 7; the grid now moves every dial at every point: depth `{1,2,3,4,5}`, ceiling `{8,14,20,26,32}`, force `{3,4,5,6,7}` (pinned in `OpportunisticAdvanceMathTest.Dials_MoveAtEveryPointOfTheParkedSweepGrid`). If you would rather sweep `{0,25,50,75,100}`, the endpoints are depth `0..6` / ceiling `0..40` / force `2..8` — but note depth 0 at knob 0 disables the advance outright, which is a legitimate arm rather than a dial reading. **Known bounds on what the sweep can show:** (a) the advance re-seeds from the staging anchor every re-eval, so it is a standing offset ahead of the muster point, not a ratchet that accumulates ground — the depth dial widens that offset; (b) **force cap and screen FRONTAGE decouple** — at `AdvanceSpreadRings: 1` the grant-test fallback collapses a failing spread slot onto the anchor, and whenever the anchor sits at frontier distance 1 (the common case for a depth-maximising walk) the forward ring-1 octants all fail and stack on one cell, so effective frontage is materially narrower than the nominal 9 cells. Read a force-cap effect as "how much mass is committed", not "how wide the screen is".
- **Fires P2+P3 + brain 1c enablement sweep — grant + gate flip** *(merged 2026-08-04 @ `ea0d0f89`; parked here 2026-08-05)* — Three levers shipped code-complete but **OFF in `@experimental`** (the `@stable` twin omits the fields — byte-identical either way): `AllocationScoreQuantizeBandPct: 0` (Phase-1c axis-sizing quantize band, sweep start 25), `PreparatoryFires: false` + `PrepFireMaxTicks: 150` (Phase 2 prep barrage hold), `SuppressionCoordinatedAdvance: false` + `PrepSuppressionThreshold: 60` / `PrepSuppressionRadius: 5c0` (Phase 3 fog-legal release; needs P2 on). Flipping the values is the whole enablement; full rationale sits in comments at the knobs (`ai.yaml`). Needed: (1) multi-test grant, (2) sweep-arm agreement — @experimental's top live regression is axes that hold and never close, so no new HOLD discipline goes live unpriced (manager decision 2026-08-04). Suggested arms: baseline OFF / P2 on / P2+P3 on / quantize 25, seed-matched vs `@stable` control.
- **Post-merge benchmark goahead** *(2026-08-02; scope widened 2026-08-11)* — Once the burn lanes merge, a fresh benchmark (new Experimental vs Stable **0802**) re-baselines the improvement campaign. Needs goahead (multi-test rule). **See "the debt is now bigger" above — four merges have since changed `@stable` knowingly, so this is one run answering four questions, and it should be taken after the danger-unit decision rather than before.**
- **Case-01 bar ratification** *(2026-07-28)* — The 1:3 cost-ratio bar is ill-posed (÷0 when defender losses hit zero). Proposed reframe: "def casualties ≤ X AND att casualties ≥ Y over N seeds." Awaiting ratified numbers before iterating case-01 to GREEN.
- **Ambush gate (b) pricing** *(2026-07-29)* — Benchmark pricing of default-on ambush was lean-OFF but inconclusive at noise scale. Decision: price again with more seeds, or keep default-off and close.
- **Item-24 repoint gates disposition** *(2026-07-29; sharpened 2026-08-04)* — A/B (40 matches) showed byte-identical arms → KEEP OFF recommendation, but at HEAD the gates are committed **ON in BOTH profiles**: @experimental (`ai.yaml:159`/`:498`) **and @stable** (`:1216`/`:1294`, since stable-0802) — so the shipping baseline bot runs them and @stable is no longer byte-identical to its pre-item-24 form. Decide: flip to OFF (both profiles) or accept ON. Doc gap line updated in curation `a313b306`. _Re-verified 2026-08-11 at `54ea7210`: **still ON in both profiles** — `StrategicCaptureRepointEnabled: true` at `ai.yaml:195` and `:1895`, `DefendRepointEnabled: true` at `:741` and `:1983`. Unchanged since the item was written; stays OPEN._

### Posted questions (in the dashboard, unanswered)
- **Tactical-layer default for humans** *(2026-08-04, proceeding on default)* — auto supply-seek / OOA evac ON by default for human units, stance-disableable (default, conf 85) vs OFF until tried in-game. The default-ON flip ships as its own one-line commit; redirect reverses it trivially.
- **OOA fallback** *(2026-08-04, proceeding on default)* — vehicle out of ammo with NO reachable rearm source: terminal evac + sell (default, conf 80) vs hold safe + periodic re-check.
- **Streak protocol** *(2026-07-31)* — what counts as one game in the 10-win streak.
- **Non-wins** *(2026-07-31)* — do draws / timeouts / crashes break the streak.
- **Queue handling** *(2026-07-31)* — do the streak campaign + fixes supersede the parked pipeline items.

### Housekeeping
- **LANDED auto-branch disposition** *(2026-07-29)* — merged branches `auto/may-salvage`, `auto/spread-prefix`, `auto/b1-walkback` left intact on origin; delete or keep.
- **Standing sims/tests state** *(2026-08-02)* — deferred until weekly budget is burned; conditional grant for leftover 5h-window time; full max-burn resumes at 5h reset.

## RECORDS — proceeded past; do NOT re-ask

> **These are not open gates.** Each was asked, went unanswered, and **the agent proceeded**. They stand as **overridable records**: the user can still redirect any of them, but re-asking spends the user's attention on a decision that has already been made in code. **Do not promote these into the OPEN section, and do not reword them into a fresh question.** Added 2026-08-12 from the archived close-outs.

- **How a supply truck decides a delivery is dangerous enough to drop short rather than drive in** *([`bdedd544`](closeout/bdedd544.md) §3, which states explicitly: **"Do not re-ask or reword."**)* — **Resolved in code** as a **two-limb classifier**: a cell stands out against the player's own live median **OR** exceeds an absolute figure derived from the danger unit's own definition (100 units = one believed contact at point-blank), **plus a floor so a quiet field reads quiet**. **Both limbs are required, and the reasoning is why re-opening this is expensive:** purely relative was tried and classified a cell reading **462,272 as *safe***, because a saturated field has an enormous median; purely absolute is how the original thresholds broke.
- **Spending one autotest run on the instrumented order log** (`Test.Mode=true Test.UnitLifecycleLog=<path>`) *([`bdedd544`](closeout/bdedd544.md) §3; also listed under "do not re-ask" in PIPELINE's SESSION STATE 2026-08-11)* — posted two days before that close-out, never answered, **agent proceeded**. Still the cheapest way to rank churn sources empirically, if the user ever volunteers the run.
- **Whether to chase the content-divergence desync theory** *([`54ab3880`](closeout/54ab3880.md) §3)* — the options put were: get the friend's `debug.log` and diff its missing-sprite lines / have the friend copy `Content/ra/v2/` wholesale / skip it and replay on the shipped build. Never answered, and **now largely MOOT** — superseded in importance by the `Detectable` condition-token finding (PIPELINE item 42). **The `debug.log` diff survives as a cheap independent check if the token fix does not resolve the desync**, and is recorded as such on that item.
- **An autotest run at the real 3840 helicopter altitude** *([`missiles-e2475f8d`](closeout/missiles-e2475f8d.md) §3)* — asked earlier in that session, unanswered, and **now MOOT**: superseded when the user rescoped the work. The three live missile questions in the OPEN section above replace it.

## RESOLVED
_Move items here with the decision + date._

> **2026-08-11 reconciliation note — nothing was moved here.** Every OPEN item above was checked against `main` @ `54ea7210`. The balance proposals are verifiably unapplied, the item-24 gates are verifiably still ON, and for the remainder (test-batch grants, sweeps, posted questions, branch disposition) **no commit, config change or recorded decision exists that would demonstrate resolution.** Two items *look* resolvable and deliberately were not moved: the tactical-layer default and the OOA fallback both shipped on their stated default and the agent proceeded, but **proceeding on a default is not the user deciding** — they stay open until the user says so. No decision has been invented here.

_Two rulings added here 2026-08-12 from the archived close-outs. Neither was ever parked in OPEN — both were asked and answered inside a session — but both are the kind of thing a later session re-diagnoses as a bug, so they are recorded where the gate queue will be read._

- **Burning ejected crew is INTENDED — do not "fix" it** *(asked as an approval question and **DENIED**; recorded in `DISCOVERIES.md` at `d53779d9`; source [`closeout/bdedd544.md`](closeout/bdedd544.md) §4)* — the user, verbatim: *"The crew is supposed to burn sometimes, when the vehicle is heavily damaged. I see no need to change any of that, sometimes it just looks cool (in a dark way) to see your enemies crawling out of the vehicle only to burn and die."* **The mechanism looks exactly like a defect and will be re-diagnosed as one:** `VehicleCrew.cs:358-362` grants `onfire` with **no duration**, unlike `VehicleCookoff`'s `Duration: 100`, against `ChangesHealth@BurnDamage_3` at −1% MaxHP every 8 ticks. A fix was built and **reverted at `36ad9865`**, with `CrewFireDurationTicks` removed rather than left dormant. **Binding consequence for the test suite: every phase of `test-evac-suite` must assert *who got out*, never *who is still alive*** — post-ejection survival is not something the game guarantees, so a survivor-count assertion is a coin flip no threshold can stabilise (the 12 → 8 → 6 threshold walk of 2026-05-09 was three attempts at exactly that). _Noted without reopening: the user said "burn **sometimes**" while the mechanism kills every ejected crewman eventually — the variance is only in **when**._
- **FX audit item 17 (un-flatten the middle of the explosion ladder) — DECLINED** *(source [`closeout/6361c2be.md`](closeout/6361c2be.md) §1; recorded in `DISCOVERIES.md`)* — user: *"we need more effects, but I don't think it is worth it now."* **Do not re-propose without new art.**

- **SR flow shape** *(posted 2026-08-04, DECIDED 2026-08-05)* — user picked the non-default arm: **"Advance immediately, singly — zero assembly anywhere; maximally responsive but arrives piecemeal into contact."** Implemented on `auto/spawn-flow` as `ImmediateReinforcementCommit` (`SpawnFlowMath.SuppressMassingHold`), which suppresses damper arm (b) — the fill-completion massing hold at the forward muster — and nothing else. Post-retreat dwell, `SectorPostureHold`, the free-pool forward stager and transport-fill waits stay live (the last per the fork record's own "legitimate either way"). @experimental-only; `@stable` byte-identical. Revert path: drop the single `ImmediateReinforcementCommit: true` line from `ai.yaml`.
